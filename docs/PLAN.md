# JurisCalendar — Yol Haritası ve Döngü Durumu

> **Bu dosya döngünün hafızasıdır.** Her uyanışta önce burayı oku, `[ ]` olan ilk
> maddeden devam et, işi bitir, commit'le, kutuyu `[x]` yap ve "Oturum günlüğü"ne
> tek satır yaz. Konteyner sıfırlansa bile buradan devam edilebilir.

- **Dal:** `claude/juriscalendar-legal-platform-y8f5lr`
- **Görsel referans:** `docs/DESIGN-REFERENCE.md` (+ `docs/design-reference/*.jpg`)
- **Hedef:** GitHub Pages'ten kurulabilen PWA → ana ekrana ekle → test → sonra
  Capacitor ile App Store paketi.

---

## Mimari kararlar

| Konu | Karar | Gerekçe |
|---|---|---|
| Çatı | React 18 + TypeScript + Vite | Hızlı, PWA ve Capacitor ile uyumlu |
| Yönlendirme | Elle yazılmış hash router (`src/router/`) | GitHub Pages'te 404 hilesi gerektirmez; react-router'ın açık RSC zafiyeti ve ~15 kB yükü taşınmıyor |
| Depolama | IndexedDB (Dexie) — **local-first** | Belge/dekont ekleri, çevrimdışı çalışma, sunucu bağımlılığı yok |
| Stil | Elle yazılmış CSS + tasarım token'ları | Editoryal görünüm birebir tutturulacak; framework varsayımı istemiyoruz |
| Tarih | `date-fns` + `tr` yerel ayarı | Türkçe ay/gün adları, süre aritmetiği |
| Grafik | Elle yazılmış SVG | Küçük paket, tasarıma birebir uyum |
| Asistan | Önce **kural motoru**, üstüne opsiyonel LLM | Şartname örnekleri ("Bilirkişi ücreti yatırılmadı") veriden deterministik çıkar; çevrimdışı ve doğru |
| Dağıtım | GitHub Actions → GitHub Pages, base `/MYLOVE/` | Kullanıcı "github üzerinden ana ekrana ekleyip test edeceğim" dedi |

**Fontlar** (self-host, Türkçe karakter tam): serif `Newsreader`, gövde `Inter`,
mono `IBM Plex Mono`. `src/assets/fonts/` altında yalnızca `latin` + `latin-ext`
altkümeleri tutuluyor (toplam 296 kB); Kiril/Yunan/Vietnam altkümeleri service
worker'ın çevrimdışı önbelleğini şişirmesin diye dışarıda bırakıldı.

**Sunucu gerektiren özellikler** (SMS, e-posta, push, LLM, takvim senkronu) bir
`services/` adaptör katmanı arkasına alınacak: varsayılan uygulama cihaz içi
(yerel bildirim, ICS dosyası, kural motoru), sunucu geldiğinde adaptör değişecek.

---

## Aşamalar

### F0 — Temel
- [x] Ekran kaydını çöz, görsel referansı çıkar ve repoya kalıcı kaydet
- [x] Yol haritası ve döngü durumu dosyası
- [x] Vite + React + TS iskeleti, tasarım token'ları, gömülü fontlar
- [x] Uygulama kabuğu: üst çubuk, yan menü (drawer), yönlendirme, boş sayfalar
- [x] PWA: manifest, ikonlar, service worker, iOS ana ekran meta etiketleri
- [x] GitHub Actions → GitHub Pages dağıtımı

### F1 — Veri katmanı
- [x] Alan modeli: Dosya, Müvekkil, Olay, Görev, Süre, Finans kaydı, Belge, Kişi, Not, Hareket
- [x] Dexie şeması, sürüm/göç altyapısı
- [x] Videodaki örnek verinin birebir tohumlanması (Yılmaz/Arslan, Demir İnşaat, Kaya/Nova…)
- [x] Yedekleme: tüm veriyi JSON+ek olarak dışa/içe aktarma
- [x] Repository katmanı (`src/data/sorgular.ts` — canlı Dexie sorguları)

### F2 — Genel bakış (Dashboard)
- [x] Hero kartı + selamlama + tarih
- [x] 2×2 istatistik ızgarası (canlı hesaplanan)
- [x] Yaklaşan son tarihler
- [x] Bugünün programı — Duruşmalar
- [x] Bugün yapılacaklar (satır içi tamamlama)
- [x] Hazırlık durumu (dosya sağlığı)
- [x] Son hareketler + alt güvenlik kartı
- [x] Panelden satır içi hızlı görev ekleme (Enter ile kaydeder; "Detaylı" forma bağlantı)

### F3 — Takvim (şartname md. 1)
- [x] Ay / hafta / ajanda görünümleri
- [x] Olay türleri ve renkleri: duruşma, icra takibi, müvekkil görüşmesi, dilekçe teslimi,
      arabuluculuk, keşif, son tarih, diğer
- [x] Gün detayı, dosya bağlantısı
- [x] Hukuki süreler de takvimde (olaylarla tek zaman çizelgesinde)
- [x] Olay oluştur/düzenle/sil formu
- [x] Tekrar eden olaylar (seri olarak maddeleştirilir; "yalnızca bu / tüm seri" silme)

### F4 — Dosyalar (md. 4)
- [x] Dosya listesi: durum, esas no, mahkeme, müvekkil, hazırlık yüzdesi ile filtre/arama
- [x] Dosya detayı sekmeleri: Genel, Duruşmalar, Süreler, Görevler, Belgeler, Finans, Notlar
- [x] Hazırlık kontrol listesi (Genel sekmesinde, eksiklerin eylem metniyle)
- [x] Belgeyi cihaza indirme
- [x] Dosya oluştur/düzenle/sil formu (yeni müvekkil satır içi eklenebiliyor)
- [x] İlgili kişi ve not ekleme/silme (Notlar sekmesinde satır içi form)

### F5 — Hukuki süre hesaplama (md. 5)
- [x] Resmî tatil takvimi (sabit + dinî bayramlar, 2024–2030 Diyanet tablosu)
- [x] Adli tatil kuralı (20 Temmuz – 31 Ağustos) ve sürenin 7 Eylül'e uzaması (HMK m. 104)
- [x] Süre kataloğu: 17 kural (istinaf, temyiz, cevap, bilirkişi itirazı, tedbire itiraz,
      ödeme emrine itiraz, ihtiyati hacze itiraz, itirazın iptali, ceza istinaf/temyiz/itiraz,
      idari iptal/vergi/cevap, işe iade, arabuluculuk) — her biri kanun maddesiyle
- [x] Tebligat tarihinden son güne hesaplama + hafta sonu/tatile denk gelirse ilk iş günü (HMK m. 92–93)
- [x] Hesaplanan süreyi dosyaya ve takvime otomatik işleme (`kaynak: sure-hesabi`)
- [x] Görünür uyarı: bilgilendirme amaçlıdır, sorumluluk kullanıcıdadır (her ekranda)
- [x] Süre kataloğu genişletildi (16 → 31 kural): replik/düplik, istinaf dilekçesine cevap, yargılamanın iadesi, hakem iptal; icra şikayet/gecikmiş itiraz/kambiyo/ihalenin feshi/istirdat; ceza eski hâle getirme/tazminat; idari üst makam/YD itiraz/tam yargı — karar düzeltme HMK ve İYUK'ta kaldırıldığı için bilinçli olarak eklenmedi

### F6 — Müvekkiller (md. 8)
- [x] Müvekkil profili: iletişim, TCKN/vergi no, adres, etiketler
- [x] Açık/kapalı dosyalar, ödeme durumu (tüm dosyalardan toplanan bekleyen), görüşme geçmişi
- [x] Hızlı iletişim (tel: ara / mailto: e-posta / görüşme kaydet)
- [x] Müvekkil oluştur/düzenle; bağlı dosya varsa silme yerine arşivleme

### F7 — Görevler (md. 9)
- [x] Genel görev ekranı: zaman gruplu liste (gecikmiş/bugün/yarın/yaklaşan/vadesiz/tamamlanan)
- [x] Süzgeçler: açık / bana atanan / öncelikli / tamamlanan
- [x] Dosya bazlı görev (dosya detayında zaten vardı; formda dosya seçimiyle bağlanıyor)
- [x] Kullanıcıya atama (3 kişilik tohum büro: Ayşe/Mert/Elif)
- [x] Hazır görev şablonları (8 şablon: harç, dilekçe, müvekkil arama…)
- [x] Görev oluştur/düzenle/sil formu
- [x] Tekrar eden görevler (vade taşıyan seri; "yalnızca bu / tüm seri" silme)

### F8 — Finans (md. 6, 7)
- [x] Gider/gelir kalemleri: 11 kategori (harç, gider avansı, bilirkişi, keşif, tebligat,
      arabuluculuk, noter, icra masrafı, müvekkil avansı, vekâlet ücreti, diğer)
- [x] Ödeme durumu (bekliyor/kısmi/ödendi), vade, yaklaşan ödeme (özet: 7 gün içi sayısı)
- [x] Genel finans ekranı: büro özeti + süzgeçler (tümü/tahsilat/gider/bekleyen)
- [x] Dosya bazlı gelir–gider özeti ve bakiye (dosya detayı Finans sekmesi)
- [x] Makbuz/dekont arşivi: PDF/görsel yükleme (Blob, IndexedDB), indirme, kalemle ilişkilendirme
- [x] Türkçe tutar girişi ("7.500,50" → doğru kuruş) ve kısmi ödeme

### F9 — Belge yönetimi (md. 12)
- [x] PDF / Word / Excel / görsel / ses ekleme, IndexedDB'de saklama (dosya detayı Belgeler sekmesi)
- [x] İndirme ve silme; tür otomatik tahmini (ad/MIME'den)
- [x] Etiketleme (belge satırında ekle/sil) ve global belge arama ekranı (`/ara` — son placeholder kapandı)
- [x] Önizleme (görsel/PDF/ses gömülü — `BelgeOnizleme`, hem aramada hem dosya detayında)
- [x] Depolama kullanımı göstergesi ve kota uyarısı (arama ekranında; `navigator.storage.estimate`)

### F10 — Hazırlık durumu (md. 11)
- [x] Dosya türüne göre kontrol listesi şablonları (icra/ceza/arabuluculuk/tüketici/idari türe özel; uygulanmayan madde yüzdeye katılmaz)
- [x] Otomatik tespit (duruşma girildi mi, vekâletname yüklendi mi, harç yatırıldı mı…) — her madde veriden okunur, elle işaretleme yok
- [x] Yüzde hesabı, eksikler listesi, "bir sonraki adım" önerisi (ağırlıklı; en ağır eksik sıradaki adım)

### F11 — Hatırlatmalar ve bildirimler (md. 2, 3)
- [x] Çoklu ofset motoru: 30/15/7/3/1 gün, aynı gün, 1 saat önce — Ayarlar'da düzenlenebilir
- [x] Olay türüne göre varsayılan hatırlatma profilleri (duruşma / hukuki süre)
- [x] Cihaz içi bildirim (Notification API adaptörü) + uygulama içi bildirim kutusu (Bildirimler ekranı)
- [x] Kanal adaptörü (`services/bildirim.ts`): uygulama/push/SMS/eposta + net "yapılandırılmadı" hâli
- [x] Sessiz saatler
- [x] **Ayarlar ekranı** (son placeholder): profil, hatırlatma profilleri, kanallar, sessiz saatler, LLM anahtarı, yedekleme (dışa/geri), veri sıfırlama
- [x] Üst çubuk zil noktası gerçek hatırlatma sayısına bağlandı
- [x] Snooze (tekrar erteleme): 1 saat / 3 saat / yarın / 1 hafta; ertelemeyi geri al — durum Ayarlar'da id→an haritasında, süresi geçen kayıtlar temizlenir

### F12 — Asistan (md. 10)
- [x] Kural motoru: yaklaşan süreler, ödenmemiş kalemler, yaklaşan duruşma, geciken görev, eksik vekâletname, düşük hazırlık
- [x] Büro geneli "Gündem" — bulgular önceliğe göre (kritik/uyarı/bilgi)
- [x] Dosya özeti üretimi ("durumunu özetle")
- [x] Soru–cevap arayüzü (niyet çıkarımı + önerilen sorular); şartname örneklerini birebir veriyor
- [x] Opsiyonel LLM katmanı (`services/llm.ts`): varsayılan kapalı, kullanıcının kendi OpenAI-uyumlu uç noktası + anahtarı; açıkken yalnızca soru + dosya özeti gider; kural motoru varsayılan kalır; hata hâlleri nazik

### F13 — Raporlar (md. 14)
- [x] Aylık duruşma / görüşme / tamamlanan görev sayıları
- [x] Son 6 ay gelir-gider sütun grafiği (elle SVG)
- [x] Açık süreler aciliyet dağılımı (elle SVG halka)
- [x] Kategori bazlı gider dağılımı (yatay çubuk)
- [x] Dosya bazlı gelir–gider analizi
- [x] CSV dışa aktarma (UTF-8 BOM + ; ayraç, Excel-TR uyumlu)
- [x] Serbest tarih aralığı seçimi: hazır aralıklar (bu ay / son 3-6-12 ay / bu yıl) + serbest başlangıç-bitiş; sütun/aktivite/gider/bakiye/toplam aralığa göre; açık süre halkası anlık durum

### F14 — Takvim senkronizasyonu (md. 13)
- [x] ICS dışa aktarma — tüm takvim (olaylar + hukuki süreler) ve tek olay
- [x] Google / Apple / Outlook için içe aktarma rehberi ekranı
- [x] Canlı abonelik (webcal) için dürüst açıklama — sunucu gerektirir, sonraki katmanda
- [x] `services/ics.ts`: RFC 5545 uyumlu üretim (kaçış, satır katlama, UTC/tüm-gün) — 8 test

### F15 — Güvenlik (md. 15)
- [x] Uygulama kilidi: PIN (PBKDF2 özeti, ham PIN saklanmaz) + WebAuthn/biyometri (Face ID/Touch ID)
- [x] Parola türevli şifreleme (WebCrypto PBKDF2/AES-GCM): şifreli yedek + PIN özeti — cihaz içi düz veri için OS disk şifrelemesine dayanıldı (dürüst kapsam, bkz. günlük)
- [x] Oturum zaman aşımı (0/1/5/15 dk seçilebilir), arka planda ekran maskeleme
- [x] Şifreli yedek dosyası dışa/içe aktarma (.jcenc)
- [x] KVKK notu, veri saklama/silme (Ayarlar'da veri sıfırlama)

### F16 — Bitiş
- [!] **Geçici sürüm yenileme düğmesini kaldır** — `src/components/SurumYenile.tsx`,
      `AppShell` içindeki kullanımı, `shell.css` sonundaki blok ve `vite.config.ts`
      içindeki `define` bloğu birlikte silinecek. **Şimdilik test için DURUYOR;
      kaldırma App Store paketlemesinden hemen öncesine ertelendi (bkz. BEKLEYEN).**
- [x] Çevrimdışı davranış, boş/yükleniyor/hata hâlleri — çevrimdışı bilgi şeridi (`CevrimdisiUyari` + `useCevrimici`, local-first: alarm değil sakin ton), React hata sınırı (`HataSiniri`, beyaz ekran yerine kurtarma ekranı, redaksiyonlu günlük); boş/yükleniyor hâlleri zaten 18 sayfada (`section-empty`/`SatirIskeleti`)
- [x] Erişilebilirlik: odak sırası, kontrast, ekran okuyucu etiketleri — F23'te tamamlandı: kontrast AA (nötr griler koyulaştırıldı), aria/odak/skip-link/canlı-bölge geçişi, `:focus-visible` halkası, yazı boyutu ölçeği
- [x] Vitest ile birim testleri (süre motoru, hazırlık skoru, hatırlatma ofsetleri) — süre motoru `sureHesabi.test.ts`, hazırlık skoru `hazirlik.test.ts`, hatırlatma ofsetleri yeni `domain/hatirlatma.ts` (saf çekirdek) + `hatirlatma.test.ts` (kanca aritmetiği DB'siz test edilebilir hâle getirildi)
- [x] README: kurulum, GitHub Pages yayını, ana ekrana ekleme, Capacitor ile App Store yolu
- [x] Sürüm etiketi ve son elden geçirme — **sürüm etiketi:** `package.json` sürümü `__APP_VERSION__` ile gömülüp Ayarlar altbilgisinde gösteriliyor ("sürüm 0.1.0 (derleme-kimliği)"). **Son elden geçirme:** tüm ekranlar (20+ rota) tarayıcıda tarandı — yatay taşma/konsol hatası yok, kontrast AA, aria/odak tam. *Not: ürün öncesi kalan temizlik (geçici "sürüm yenile" düğmesini kaldırma, gerçek OAuth, hukuki metin içerikleri) `[!]` BEKLEYEN'de; bunlar App Store paketlemesiyle birlikte, kullanıcı adımlarıyla yapılacak.*

> **Not (madde 8):** F16'nın açık maddeleri (çevrimdışı/boş/hata hâlleri,
> erişilebilirlik, kalan testler, sürüm etiketi) planda zaten duruyor.
> Erişilebilirlik ayrıca F23'te ayrıntılandırıldı. "Sürüm yenile" düğmesi
> `[!]` işaretlendi ve şimdilik silinmeyecek.

---

### F17 — Uygulama içi rehberli tur (in-app guided tour)
> **Yön değişikliği (kullanıcı isteği):** Eski F17 animasyonlu bir tanıtım
> slaytıydı (sahte ekranlar; S0–S7). Kullanıcı bunu istemedi: "gerçekten
> uygulamanın içinde tutorial yapıp senaryoya göre eğitim… dilekçe kısmını
> menüden AI asistana tıklayıp sen götürüp gerçek panelden göstereceksin."
> Slayt gösterisi kaldırıldı (`Onboarding.tsx`, `script.ts`, `script.test.ts`,
> `efektler.ts` silindi) ve yerine **gerçek panelleri gezen** bir koç-işaretli
> tur kondu. Sahte ekran yok; her durak gerçek öğeyi spotlight ile işaret eder.
- [x] `src/tur/` altında tur motoru: SVG maske spotlight (delik) + halka + konumlanan balon; çalışan uygulamanın ÜZERİNE binen katman (`Tur.tsx`)
- [x] Adımlar `src/tur/turAdimlari.ts` içinde **veri** olarak durur (rota + gerçek öğe seçicisi + başlık + metin + gerekiyorsa `drawer`); test korur (`turAdimlari.test.ts`)
- [x] Tur gerçek rotalara **kendisi gider** (`useNavigate`) ve gerçek öğeleri ölçer; öğe bulunamazsa balon ortada gösterilir, tur takılmaz
- [x] Yan menüyü açıp kapatmak için `juris-drawer` CustomEvent'i yayınlar; `AppShell` dinler (tur menüden Asistan'a geçişi gerçekten yapar)
- [x] Kayıt tamamlanınca (F18+F19 sonrası) ilk girişte otomatik başlar; bir kez görülünce/kapatılınca tekrar açılmaz (`ayarlar.turGoruldu` kalıcı bayrak; `App.tsx` içinde RouterProvider altında mount)
- [x] Her adımda "Turu kapat", adım sayacı (n/8) ve "İleri/Bitir"; dokunma hedefleri ≥ 44pt; `prefers-reduced-motion` parıltıyı kapatır
- [x] Menü/sayfa geçişi (~320ms) boyunca birkaç kez yeniden ölçüm → spotlight öğeyi kayarken de takip eder, son konumda oturur
- [x] Ayarlar → "Yardım" altında **"Uygulama turunu yeniden başlat"** düğmesi (`turGoruldu:false`)
- [x] Duraklar (kullanıcı seçimi): **Genel bakış + menü**, **AI Asistan + maskeleme**, **Dosya ekleme**, **Takvim + süre hesabı** — tarayıcıda uçtan uca doğrulandı (Playwright: 8 durak, hepsi gerçek panelde spotlight; tur bitince kapanıyor, reload'da açılmıyor)
- [x] AI Asistan durağı gerçek `/asistan` panelinde "Dosyaya danışın" alanını işaret eder; maskeleme açıklaması balonda: "kimlik bilgileri maskelenir; sağlayıcıya yalnızca maskeli metin gider"
- [x] Dil denetimi: yasak ifadeler ("hiçbir veri paylaşılmıyor"/"hacklenemez"/"%100 güvenli") tur metinlerinde **yok**
- [x] **Cihaz videosu düzeltmeleri (kullanıcı geri bildirimi):** (1) balon artık güvenli alan içinde konumlanır (çentik/ana çubuk altını `env(safe-area-inset-*)` ölçüp kırpar) — eskiden aşağıdaki hedeflerde balon durum çubuğunun/saatin arkasına kaçıyordu; (2) metinler **senaryo temelli** yeniden yazıldı ("bir cevap dilekçesi hazırlamanız gerekiyor → menüden Asistan'a → gerçek panel") — kullanıcının istediği "senaryoya göre eğitim".
- [x] **Tepki düzeltmesi (2. geri bildirim: "laglı, eski hâli daha güzeldi"):** İlk denemede geçiş sürerken balonu gizleyen `hazir` durumu **gecikme hissi** yaratıyordu → kaldırıldı. Artık "İleri"ye basınca balon metni ANINDA değişir (tarayıcıda ölçüldü: 25 ms içinde başlık değişiyor, balon hiç gizlenmiyor); yeni hedef ölçülene dek spotlight önceki konumunda kalır (ortada balon sıçraması YOK), ölçülünce kısa/snappy (140 ms) kayışla oturur. reduced-motion'da kayış kapalı. 8 durakta: `instantTextChange=true`, balon ekran içinde, gerçek paneller spotlight.

### F18 — Hukuki onay akışı (onboarding S8)
- [x] Onay ekranında 5 kutucuk; hepsi başta işaretsiz; "Kabul et ve başla" yalnızca 5'i de işaretliyken aktif olur
- [x] Kutucuklar, uzun metin **sonuna kaydırılmadan** aktifleşmez *(varsayım: tek birleşik ToS+Aydınlatma metni sona kaydırılınca 5 kutu birden açılır — bkz. günlük)*
- [x] 5 madde birebir: (Kullanım Şartları + Aydınlatma) / (şifre unutulursa kurtarılamaz) / (maskeleme tam anonimleştirme değil, onay kontrolü kullanıcıda) / (AI çıktısı taslak, doğruluk + meslek sırrı kullanıcıda) / (cihaz güvenliği kullanıcıda)
- [x] Onay kaydı cihaza yazılır: zaman (ISO tarih+saat), metin sürümü ve metin **hash'i** (SHA-256) — tarayıcıda doğrulandı
- [x] Metin sürümü değişince yeniden onay istenir; değişmediyse tekrar istenmez (test + tarayıcı: reload'da onay çıkmıyor)
- [x] Hukuki metinler **yer tutucu**; içerik [!] avukatta (bkz. BEKLEYEN). Uygulama yer tutucuyu gösterir, sürüm/hash'i kaydeder

### F19 — Kayıt / giriş ve profil
- [x] Kayıt formunda ad, soyad, ünvan alınır ve yerelde saklanır (`KayitEkrani`; App gating `profilKuruldu`)
- [x] Ad/soyad/ünvan yan menü (drawer) altında görünür — artık dinamik (avatar baş harfleri de); ana ekran selamlaması F2'de zaten adı gösteriyordu
- [x] E-posta ile kayıt/giriş **arayüzü** (yerel profil oluşturur); gerçek e-posta doğrulama sunucusu [!]
- [x] Google ve Apple ile giriş butonları arayüzde; tıklanınca açıkça "henüz yapılandırılmadı" der; OAuth entegrasyonu [!]

### F20 — Cihazlar arası iş bölümü (devir teslim)
- [x] Devir teslim penceresinde "Telefonda yap" seçeneği **her zaman** var ve **varsayılan vurgulu** buton odur (`DevirTeslim`)
- [x] Süre tahminleri gerçek ölçümden (`performance.now`) hesaplanır; ölçüm yoksa hiç gösterilmez (uydurma yok — test)
- [x] Bilgisayar erişilemezse devir **hiç** gösterilmez; iş sessizce telefonda yapılır (varsayılan; tarayıcıda 0 doğrulandı)
- [x] Bir kez "telefonda yap" → aynı oturumda aynı iş tipi için tekrar sorulmaz (oturum içi tercih; test + tarayıcı)
- [x] Masaüstü algılama + gerçek transfer adaptör arkasında (`services/devir.ts`), varsayılan "masaüstü yok → telefonda"; gerçek taşıma [!] (BEKLEYEN)

### F21 — hukuk-ai arayüz sözleşmesi
- [x] `packages/hukuk-ai`'nin sağlayacağı arayüz **tüketici tarafta** tanımlandı (`src/services/hukukAi.ts`); `packages/hukuk-ai/` klasörüne **dokunulmadı**
- [x] Arayüz: `maskele` (metin → maskeli metin + eşleme), `demaskele`, `sor`, `hazirMi`; `MaskeSonucu`/`MaskeEslesmesi`/`MaskeTuru` tipleri; eşleme cihazda kalır sözleşmesi
- [x] `bagliDegilHukukAi` varsayılanı (paket yokken derlenir/çalışır) + `npm run build` geçer; sözleşme sahte uygulamayla test edildi
- [!] **Paketi app'e bağla** (paket tüketilebilir olunca) — `packages/hukuk-ai` motoru dala merge edildi (PR #1, 271 test yeşil) ama app hâlâ `bagliDegilHukukAi` passthrough kullanıyor. Engel: pakette `package.json`/dışa aktarım yok (ayrı session tamamlayacak), o yüzden import edilemez. Hazır olunca: LLM'e gitmeden önce `maskele` çağrılacak, yanıt `demaskele` ile geri konacak; tur "AI Asistan" durağındaki **maskeleme cümlesi geri eklenecek** (o zaman gerçek olur).

### F22 — Gizli günlükleme (redacting logger)
- [x] Merkezi logger (`services/gunluk.ts`): redaksiyon çıktıdan **önce** yapılır (yaz-sonra-sil değil); tarayıcıda disk günlüğü yok, tek yazan yer bu dosya
- [x] Doğrudan `console.*` çağrısı **kaynak-tarama kapısıyla** engellenir (proje ESLint kullanmıyor → lint yerine test kapısı, `gunluk.test.ts`); tek konsol kullanan yer `gunluk.ts`, `main.tsx` de logger'a geçirildi
- [x] Test: kimlik desenli hata → çıktıda TCKN/IBAN/e-posta/GSM maskeli *(pattern bazlı; serbest metin isim/adres logger'a geçirilmez — mesaj geliştirici metnidir, ham kayıt log'lanmaz — bkz. günlük)*

### F23 — Erişilebilirlik (ileri yaş kitle; F16'yı genişletir)
- [x] Büyük taban punto; kullanıcı yazı boyutunu artırabilir veya sistem büyütmesine uyar — Ayarlar → **Yazı boyutu** (Normal/Büyük/Çok büyük), kök `font-size` %100/112.5/125; tüm ölçüler `rem` olduğu için arayüz orantılı büyür; `%` taban sistem büyütmesini korur; ayar kalıcı (`ayarlar.yaziOlcegi`, App'te köke uygulanır). Tarayıcıda doğrulandı: 16→20px, reload'da kalıcı, düzen bozulmuyor.
- [x] Metin/arka plan kontrastı WCAG AA (≥ 4.5:1) — denetlenebilir — nötr metin grileri koyulaştırıldı: `--text-secondary` #6b7472→**#5b6462** (kırık beyaz 4.45→5.64), `--text-muted` #9aa0a0→**#6b7170** (2.46→4.60); aynı gri-yeşil aile korundu, tarayıcıda görsel olarak doğrulandı (eyebrow'lar hâlâ sade, başlıkla yarışmıyor). *Varsayım: marka (teal link) ve kategori rozet renkleri (kırmızı/amber/yeşil…) marka kimliği olduğundan ve büyük/dekoratif kullanıldığından bu geçişte değiştirilmedi — kimliği bozmamak için kullanıcı kararına bırakıldı.*
- [x] **Kategori renklerinin METİN olarak kullanıldığı yerlerde AA'yı karşıla.** 6 kategori için hue korunarak koyulaştırılmış `--cat-X-text` token'ları eklendi (red #a2564a, amber #866638, purple #71639c, blue #526e8a, green #3e7566, slate #656d6b); beyaz/kırık-beyaz/tinted zemin/`surface-sunken` üzerinde ≥4.55:1 (script ile hesaplandı). Aksan sınıflarına `--acc-text` eklenip TÜM `color: var(--acc-fg)` ön plan kullanımları buna bağlandı; doğrudan `--cat-X-fg` metin kullanımları da düzeltildi: `.field-error`, `.button-danger`, `.sonuc-uyari`, `.check-action`, avatar baş harfleri; satır içi (Finans/Raporlar tutar rengi, MüvekkilDetay bekleyen ödeme, Bildirimler son-tarih, Ayarlar "Açık" rozeti, Görevler/Müvekkiller rozetleri) ve önemli durum ikonları (`.check-mark` ✓, `.bildirim-oneri-icon`, `.cevrimdisi-ikon`). **Dekoratif kullanımlar (nokta/kenarlık/rozet zemini/sol aksan) DEĞİŞMEDİ** — pastel marka kimliği korundu. **Runtime doğrulama (Playwright):** para/istatistik/rozet metinleri 2.0–2.3:1 → **4.93–5.33:1 (AA PASS)**; görsel: tutarlar okunur, tasarım dili aynı. *Varsayım: bilerek soluk hafta sonu başlıkları (`.cal-weekday` opacity 0.75) ve onboarding illüstrasyon grafikleri (metin değil) değiştirilmedi.*
- [x] Tüm dokunma hedefleri min 44×44 pt — `styles/dokunmatik.css`: görünür boyutu DEĞİŞTİRMEDEN, görünmez `::after` "dokunma alanı" ile tıklama hedefi 44px'e çıkarıldı (görsel dil korundu). Yatay komşu çakışmasını önlemek için kural: geniş/satır kontrolleri yalnızca dikeyde (44px yükseklik), yalıtık kare ikon düğmeleri (üst çubuk, drawer-close) iki eksende 44×44, görev onay kutusu sağdaki gezinme bağlantısına taşmadan sola+dikey. *Varsayım: yatayda bitişik birkaç kontrol (takvim ‹›, görev onay kutusu genişliği) çakışmayı önlemek için ≥24px (WCAG 2.5.8 AA) bırakıldı; liste satırları/başlık bağlantıları zaten AA. Tarayıcıda doğrulandı: görünür boyutlar aynı, uzatılmış alandan tıklama çalışıyor, komşu kontroller (onay kutusu↔satır bağlantısı, takvim ileri/geri) doğru yönleniyor, görsel regresyon yok.*
- [x] `prefers-reduced-motion` desteklenir; animasyonlar sadeleşir/kapanır — token'larda `--duration-*` sıfırlanır; tur animasyonları (`tur-belir`, halka/balon geçişi) reduced-motion'da kapalı; eski animasyonlu onboarding slaytı kaldırıldı (yerine sade koç-işaretli tur)
- [x] ~~Ses ve titreşim Ayarlar'dan kapatılabilir; onboarding "tık" sesleri~~ **KONU DIŞI** — sesler/titreşim animasyonlu slaytla (`efektler.ts`) birlikte kaldırıldı; uygulama içi turda ses/titreşim yok
- [x] Görünür odak halkası + mantıklı odak sırası + tüm etkileşimli öğelerde ekran okuyucu (aria) etiketleri — odak halkası `:focus-visible` (base.css); işaret alanları `<header>/<nav>/<main>`; drawer/tur/kilit `role="dialog"` (drawer odak tuzağı var); gezinme `aria-current="page"`, açık/kapalı düğmeler `aria-pressed`, dekoratif ikonlar `aria-hidden`. **Playwright denetimi:** 15 ekran + drawer'da adsız etkileşimli öğe YOK. Eklenenler: **"İçeriğe geç" skip link** (klavye; hash router'ı tetiklemeden `<main id="ana-icerik">`e odak taşır), Ayarlar durum mesajı `role="status"` canlı bölge, zil düğmesine sayıyı içeren erişilebilir ad ("Bildirimler, N aktif hatırlatma"). Tarayıcıda doğrulandı: ilk Tab → skip link, Enter → içerik odağı (hash değişmiyor).

---

## BEKLEYEN — KULLANICI
Bu maddeler kod tarafında hazırlanır ama tamamlanması **senin** elle yapacağın
adımları gerektirir; otonom döngü bunları `[!]` sayıp atlar.

- [!] **Hukuki metin içerikleri** (Kullanım Şartları, Aydınlatma Metni, sorumluluk maddeleri) — avukat hazırlayacak; uygulama yer tutucu + sürüm/hash yönetir (F18)
- [!] **Google/Apple OAuth** client ID'leri, "Sign in with Apple" sertifikaları ve e-posta doğrulama sunucusu (F19)
- [!] **Cihazlar arası gerçek devir/senkron altyapısı** — masaüstü varlığı algılama + iş transferi bir sunucu/eşleme ister (F20)
- [!] **Geçici "sürüm yenile" düğmesinin kaldırılması** — şimdilik test için DURUYOR (F16)
- [!] **App Store yayını** — Apple Developer hesabı, imzalama profili, Capacitor paketi yükleme (F16 devamı)

---

## Oturum günlüğü

| # | Tarih (UTC) | Yapılan |
|---|---|---|
| 263 | 2026-08-19 20:00 | **PIN kilidi akışı + ICS dışa aktarma doğrulandı; kilit bildirimlerindeki yanıltıcı fiiller düzeltildi.** **(1) ICS geçerliliği:** kaynak + testler denetlendi — her VEVENT'te RFC 5545 zorunlu alanları TAM (UID, DTSTAMP, DTSTART, SUMMARY) + VCALENDAR (VERSION/PRODID/CALSCALE); kaçış, 75-oktet katlama, CRLF, UTC/tüm-gün tarihleri test kapsamında; `disaAktar` doğrudan test edilen `icsUret`'i çağırıyor → sağlam. **(2) PIN kilidi (kritik güvenlik, runtime, ilk kez):** PIN `pinOzetiUret` ile HASH'leniyor (düz metin değil); akış — PIN kur → oturum anahtarı temizle + reload → kilit ekranı ("JurisCalendar kilitli") → yanlış PIN "9999" reddedildi ("PIN yanlış, tekrar deneyin.", role=alert) → doğru PIN "1234" → açıldı, uygulama göründü. Kilitlenme riski yok, ekran tasarımı temiz (koyu lacivert + teal J). **(3) Bulunan hata + DÜZELTME:** kilit bildirimleri düğme fiilleriyle çelişiyordu — "Kilidi **kur**" düğmesi → "kilidi **açıldı**" mesajı (bir kilit için "açmak = çözmek" çağrışımı yanıltıcı); "Kilidi **kaldır**" → "kilidi **kapatıldı**" ("kapatmak = kilitlemek" ile ters okunuyor). Mesajlar düğmelerle hizalandı: "Uygulama kilidi **kuruldu**." / "**kaldırıldı**." (durum rozeti "Açık" olduğu gibi kaldı — o durumu, bu eylemi anlatıyor). Runtime doğrulandı. **376 test** yeşil, derleme (tsc+vite) geçti. |
| 262 | 2026-08-19 18:30 | **Durum/geri-bildirim mesajlarının erişilebilirlik tutarlılığı — 2 sessiz mesaj sese kavuştu.** Tur-261'deki ".ayar-mesaj yanıltıcı renk" bulgusunun izinden tüm geri-bildirim mesajları tarandı. **Bulgu 1 — Takvim dışa aktarma:** `TakvimSenkron` başarı mesajı ("N olay ve N süre dışa aktarıldı") `.ayar-mesaj` sınıfını Ayarlar'la paylaşıyor ama `role`/`aria-live` YOKTU → ekran okuyucu kullanıcısı dışa aktarmanın tamamlandığını (ve kaç öğe) duymuyordu. `role="status" aria-live="polite"` eklendi (Ayarlar kalıbıyla aynı). **Bulgu 2 — Belge yükleme HATASI (daha kritik):** `DosyaDetay` belge yükleme hata mesajı (`setHata` → "Belge yüklenemedi.") `.field-error` kullanıyordu ama `role="alert"` YOKTU → görme engelli avukatın belge yüklemesi başarısız olunca (çok büyük/desteklenmeyen tür) SESSİZ kalıyordu; kullanıcı belge eklendi sanıp kritik evrak (ör. vekâletname) olmadan devam edebilirdi. `role="alert"` eklendi. **Denetim:** artık 7 `.field-error`'ın HEPSİ `role="alert"` (form doğrulama + belge yükleme); FinansForm dekont yükleme hatası zaten form hata `<p>`'sini (role=alert) tekrar kullanıyor. **376 test** yeşil, derleme (tsc+vite) geçti. |
| 261 | 2026-08-19 17:00 | **Yedek gidiş-dönüş (kritik veri güvenliği) doğrulandı + TASARIM HATASI: Ayarlar hata bildirimi başarı-yeşiliyle gösteriliyordu — düzeltildi.** **(1) Yedekleme runtime gidiş-dönüş (uçtan uca, ilk kez):** DÜZ yedek — 24 müvekkil/27 dosya/40 finans/**28 belge** dahil dışa aktar → tümünü temizle → içe aktar → HEPSİ birebir geri geldi ✓ (belge blob'ları dahil; dosya adı yerel tarihli `…2026-08-19.json`, tur-236 düzeltmesi geçerli). ŞİFRELİ yedek — parolayla `.jcenc` → temizle → içe aktarınca parola sordu → **yanlış parola: veri 0'da kaldı** (kısmi/bozuk geri yükleme yok, istem retry için duruyor) → doğru parola: birebir geri geldi ✓. **(2) Bulunan hata:** `.ayar-mesaj` HEM başarı HEM hata için tek yeşil (sage) stil kullanıyordu → "Yedek okunamadı", "Parola yanlış", "izin verilmedi", "biyometrik kurulamadı" gibi HATALAR başarı yeşiliyle çıkıp yanıltıyordu (yeşil = evrensel "iyi"). **Düzeltme:** `mesaj` durumu `{text, hata?}` oldu; `bildir`/`bildirHata` yardımcıları; 4 hata çağrısı `bildirHata`, 5 başarı `bildir`; render `data-hata`; CSS `.ayar-mesaj[data-hata='true']` → `cat-red-bg`+`cat-red-text` (AA 4.58:1). **Runtime doğrulandı:** yanlış parola → kırmızı kutu "Parola yanlış ya da dosya bozuk." (bg #f8ecea, metin #a2564a); başarı hâlâ yeşil. **376 test** yeşil, derleme (tsc+vite) geçti. |
| 260 | 2026-08-19 15:30 | **Odak-görünürlüğü, HTML-yapısı, bağlantı ve masaüstü-düzen denetimi — SAĞLAM (kaynak değişmedi).** **(1) Odak görünürlüğü (WCAG 2.4.7):** `outline:none` yalnız iki yerde — `:focus:not(:focus-visible)` (fare için bastır, klavye korunur) ve `.shell-content:focus` (skip-link ile odaklanan kap); global `:focus-visible` 2px halka çiziyor + skip-link var. **Runtime (Tab ile gezme):** panel/ayarlar/form/takvimde klavye-odaklı HER öğe görünür gösterge veriyor → TEMİZ. **(2) Geçersiz iç içe yerleşim (16 ekran):** iç içe interaktif (button-in-button, a-in-a) YOK, `<p>` içinde blok YOK, formda `type`siz buton YOK → TEMİZ. **(3) Kırık bağlantı:** tüm statik `to="/…"` + drawer menüsü (`navigation.ts` 9 yol) + dinamik `/:id` hedefleri kayıtlı rotalara çözülüyor; görev/finans düzenleme `/:id`'ye gidiyor (basit kayıtlar detay sayfasını atlıyor — anlamlı). **(4) Masaüstü/tablet düzeni (İLK KEZ, 768/1280/1440px):** içerik 640px sütuna sınırlı + ORTALANMIŞ, yatay taşma yok, üst çubuk sütun kenarlarına hizalı — referansın "mobil öncelikli tek kolon, masaüstünde de çalışır" niyetiyle uyumlu (ekran görüntüsüyle onaylandı). Kaynak değişmedi (denetim); son geçen derleme (tsc+vite) + **376 test** geçerli. |
| 259 | 2026-08-19 14:00 | **Erişilebilir-ad / etiket denetimi + baskı çıktısı — SAĞLAM (kaynak değişmedi).** Sistematik ölçülebilir denetim (kontrast/tap-hedefi kalıbının devamı). **Runtime tarayıcı, 17 ekran+form:** (a) etiketsiz input/select/textarea — YOK (formlar input'u `<label class="field">` ile SARIYOR, örtük ilişki; tarayıcı `closest('label')` ile doğruladı); (b) erişilebilir-adı olmayan buton/bağlantı — YOK; (c) yinelenen `id` — YOK (`form-hata-metni` dahil, aynı anda tek form mount); (d) sarkan `aria-describedby`/`aria-labelledby` referansı — YOK. Hepsi TEMİZ. **Kopya/fiil tutarlılığı:** gönder düğmeleri — düzenleme "Değişikliği kaydet" (tutarlı); oluşturma "Dosya **oluştur**" vs "Müvekkil/Görev/Kayıt/Takvime **ekle**" — HATA DEĞİL: Türk hukuk dilinde dosya *oluşturulur/açılır*, müvekkil/görev *eklenir* (anlamca doğru). **Baskı çıktısı (`emulateMedia:print`):** Süre hesapla + Raporlar → "bilgilendirme amaçlıdır" uyarısı GÖRÜNÜR ✓, üst çubuk + FAB GİZLİ ✓ (CLAUDE.md "hesap çıktısında uyarı" şartı karşılanıyor). Kaynak değişmedi (denetim); son geçen derleme (tsc+vite) + **376 test** geçerli. |
| 258 | 2026-08-19 12:30 | **Sayaç-tutarlılık, tarih-mantığı ve tepkisellik denetimi — SAĞLAM (kaynak değişmedi).** Tur-257'deki Asistan sayaç hatasından sonra benzer "gösterilen sayı ≠ liste" kalıpları tarandı. **Kesme (`slice`) kalıpları:** Takvim gün-hücresi noktaları `slice(0,3)` ama dekoratif (aria-hidden), aria-label GERÇEK sayıyı söylüyor ("N kayıt") ve gün-detayı (`GunListesi`) TÜM olayları gösteriyor (kırpma yok) → sağlam. **Arama:** `toplam` = 4 kategori uzunluğunun toplamı, hiçbir kategori kırpılmıyor (`.map` tam dizi) → tutarlı. **Panel bölüm önizlemeleri:** sorgu düzeyinde limit (top-3/5) + `hepsiYolu` "Hepsini gör" bağlantısı; "Son hareketler"de tam-geçmiş sayfası olmadığı için bilinçli olarak bağlantı yok (`BolumKarti` koşullu render ediyor) → sağlam. **Tarih/zaman:** `goreliZaman` (az önce/N dk/N saat/Dün/tam tarih) ve `selamlama`/`hitap` doğru; Türkiye DST yok, yerel-gün duyarlı. **Tepkisellik (runtime):** panelde bekleyen görev tamamlandı → "Bekleyen görev" istatistiği 17→16 anında güncellendi (useLiveQuery), hata yok. **Müvekkil detay** (ilk kez incelendi): tüzel-kişi başlığı, boş alanlar "—", "1 AÇIK · 1 TOPLAM" dosya sayısı listeyle tutarlı, görüşme-geçmişi — temiz. **Not:** `PlaceholderPage.tsx` artık hiç kullanılmıyor (ölü kod; tüm sayfalar gerçek) — kullanıcıya görünmediği için tasarım hatası değil, dokunulmadı. Kaynak değişmedi (denetim); son geçen derleme (tsc+vite) + **376 test** geçerli. |
| 257 | 2026-08-19 11:00 | **TASARIM/İÇERİK HATASI düzeltildi — Asistan başlık sayacı listeyle uyumsuzdu + bulgular gizleniyordu.** Onboarding akışı (onay/kayıt/tur) ve tarih-mantığı (`gunFarki`/`kalanSureMetni` yerel-gün duyarlı, doğru) denetlendi — sağlam. Ama **Asistan** sayfasında iki gerçek hata: (1) Hero "Dikkat gerektiren **4** konu var. Aşağıda önceliğe göre sıraladım." diyordu ama sayaç `kritikSayi` (yalnız kritik) idi; alttaki Gündem listesi TÜM bulguları (kritik+uyarı) gösteriyor → "4 konu" ama 12+ satır (tutarsız). (2) Kritik yokbut uyarı varsa hero jenerik "Dosyalarınızı tarayıp… önünüze koyarım" (hiçbir şey bulunmamış gibi) diyordu — oysa uyarılar altta listeliydi. **Düzeltme:** sayaç `bulgular.length` (toplam) oldu; kritik kırılımı korundu ("14 konu var; 4 tanesi kritik"); `bulguSayi===0` → jenerik, aksi hâlde sayılı mesaj. **Ek (asıl neden):** liste `bulgular.slice(0, 12)` ile kırpılıyordu → 14 bulgunun 2'si GİZLİYDİ. Asistan tam-gözden-geçirme ekranı; eksik hukuki iş kaçmasın diye kırpma kaldırıldı (bulgular önceliğe göre sıralı, `oncelikSira` ile doğrulandı). **Runtime:** hero(14)=liste(14 satır)=nokta(14: 4 kritik+10 uyarı), tam tutarlı. **376 test** yeşil, derleme (tsc+vite) geçti. |
| 256 | 2026-08-19 09:30 | **Yapısal + dokunma-hedefi + kayan-düğme denetimi — SAĞLAM (4 yeni açı, kaynak değişmedi).** Directive #7. **(1) Başlık hiyerarşisi + landmark'lar (runtime, 11 ekran):** her sayfada TAM BİR `h1`, ardışık `h2` bölümleri (atlama yok), `header`+`nav`+`main` landmark'ları — kusursuz; ekran-okuyucu yapısal gezinme sağlam. **(2) Takvim Hafta + Ajanda görünümleri** (ilk kez incelendi): hafta şeridi + gün ajandası, gün-grupları + "son gün" işaretleri; temiz, yatay taşma yok. **(3) FAB (kayan küme) yasal uyarının üstünü kapatıyor mu?** İlk ölçüm kapatıyor gibiydi ama YANLIŞ kaydırma kabı (test yapaylığı) idi; doğru kap (HTML) gerçek dibe kaydırılınca uyarı (alt=640) FAB'ın (üst=706) 66px ÜSTÜNDE — `.fab-spacer-cluster: 140px` doğru kalibre; Ay+Hafta ikisinde de temiz. **(4) Dokunma hedefleri (WCAG 2.5.8, min 24×24):** tarayıcı `.task-check` (21×21) ve `.ertele-chip` (y=23) işaretledi ama İKİSİ DE yanlış-pozitif — `dokunmatik.css` görünmez `::after` ile tıklama alanını **44px**'e genişletiyor (2.5.5 AAA'yı bile geçiyor, görünür ölçü referansa sadık kalıyor); `elementFromPoint` ile ±18px'te isabet doğrulandı (task-check ilk testte ekran-dışıydı, o yüzden null'dı). Kaynak değişmedi (denetim); son geçen derleme (tsc+vite) + **376 test** geçerli. |
| 255 | 2026-08-19 08:00 | **Sistematik renk kontrastı denetimi (WCAG 1.4.3 AA) — 2 gerçek hata bulundu ve düzeltildi.** Directive #7 yeni açı: ölçülebilir kontrast. Önce tüm token çiftleri hesaplandı (çoğu geçiyor; token'lar zaten `cat-*-fg` canlı-dolgu vs `cat-*-text` AA-metin ayrımını yapmış). Sonra **runtime tarayıcı** yazıldı: 11 ekranda gerçekten yan yana gelen her metin düğümünün rengi ile efektif arka planı ölçülüp AA eşiğiyle (normal 4.5:1) karşılaştırıldı — teorik değil, ekranda GERÇEKTEN oluşan çiftler. **Bulunan 2 hata:** (1) **Takvim hafta-sonu başlıkları** ("CMT"/"PAZ") canlı dolgu `cat-red-fg` (#c96a5b) + `opacity:0.75` kullanıyordu → beyazda ~3.7:1 (opacity ile daha da düşük) → AA altı. Düzeltme: AA-güvenli `cat-red-text` (#a2564a, ~5.3:1), opacity kaldırıldı; kırmızı ayrım korundu (ekran görüntüsüyle onaylandı). (2) **Ayarlar bildirim-kanalı rozeti** ("Yapılandırılmadı") `text-muted` sunken zeminde 4.37:1 → AA altı. Düzeltme: `text-secondary` (~5.36:1), rozet hâlâ sessiz gri. **Kararlı bırakılan:** `cat-*-fg` küçük dekoratif noktalar/kenarlıklar 3:1 non-text eşiğinin altında ama tasarım referansı "hepsi soluk pastel" diyor + bilgi metinle de veriliyor → koyulaştırmak referanstan sapardı. Runtime tarama tekrar: **11 ekranın hepsi temiz.** **376 test** yeşil, derleme (tsc+vite) geçti. |
| 254 | 2026-08-18 16:30 | **Form doğrulama hatası artık alanı metnine `aria-describedby` ile bağlıyor (ARIA1) + yıkıcı-eylem güvenliği denetlendi.** Directive #7 yeni açı: etkileşimli durumlar. **(a) Yıkıcı-eylem denetimi — SAĞLAM:** ana varlıklar (dosya/müvekkil/görev/finans) iki adımlı "Emin misiniz?" onayı; alt kayıtlar (süre/belge/kişi/not) paylaşılan `SilDugmesi` ile iki-dokunuş onayı (× → "Sil?" → sil, 3sn zaman aşımı) — yorum: geri alınamaz, belge çoğu zaman tek kopya; verileri sıfırla da onaylı; belge-etiketi kaldırma tek dokunuş (kolay geri alınır, uygun). **(b) Form doğrulama runtime — çalışıyor:** boş gönderimde kayıt engelleniyor, `aria-invalid` + kırmızı kenarlık (`--danger` #C96A5B) + odak hatalı alana + `<p role="alert">` mesajı. **Bulunan boşluk + DÜZELTME:** hatalı alan ile hata metni programatik olarak BAĞLI DEĞİLDİ — ekran okuyucu kullanıcısı alana yeniden odaklanınca yalnız "geçersiz" duyuyordu, gerekçeyi değil (role="alert" yalnız bir kez, belirdiğinde okur). `useFormHata` tek noktadan düzeltildi: `FORM_HATA_ID` sabiti eklendi; `alanHatasi()` hatalı alana `aria-describedby='form-hata-metni'` de veriyor; 5 formun (Dosya/Finans/Görev/Müvekkil/Olay) hata `<p>`'sine `id` eklendi. **Runtime doğrulandı:** 5 formda da hatalı alanın `aria-describedby`'si `role="alert"` mesajına çözülüyor (ör. alan-baslik → "Dosya başlığı girilmeli.", alan-gun → "Tarih girilmeli."). Görsel değişiklik yok. **376 test** yeşil, derleme (tsc+vite) geçti. |
| 253 | 2026-08-18 15:00 | **Erişilebilirlik + boş-durum + dayanıklılık denetimi — SAĞLAM (yeni açılar; kaynak değişmedi).** Directive #7 için önceki turlarda bakılmayan alanlar sınandı. **(1) Boş durumlar:** tüm veri tabloları temizlenip tam yeniden yükleme yapıldı — Genel Bakış her bölümde bağlama uygun boş mesaj gösteriyor ("Şu an açık bir hukuki süre yok.", "Henüz bir hareket kaydedilmedi." vb.), istatistikler doğru 0/0/0/₺0, tahsilat başlığı akıllıca "karşılaştırma yok"a düşüyor (sıfıra bölme yok); **SVG grafikler boş veriyle KIRILMIYOR** — Raporlar "toplam tahsilat ₺0" + boş çubuk grafik + "0 AÇIK SÜRE" halkası, JS hatası yok. (İlk turda görülen 2/1/4 ham-IndexedDB temizleme + hash-gezinme'nin Dexie canlı-sorgu tepkiselliğini atlaması kaynaklı test yapaylığıydı; tam reload ile 0/0/0 doğrulandı.) **(2) Klavye/odak yönetimi:** drawer kapalıyken panel `inert` (ekran-dışı çocuklar odaklanamaz), açıkken odak panele giriyor + Tab tuzağı + Esc kapatıyor + odak tetikleyiciye ("Menüyü aç") dönüyor — runtime doğrulandı; modal'lar (BelgeOnizleme, DevirTeslim) `role="dialog"`+`aria-modal` tam semantikle. **(3) `prefers-reduced-motion`:** kapsamlı — global `tokens.css` süre token'larını 0ms yapıyor + 4 animasyonlu dosyada (dashboard/shell/tur/onboarding) hedefli kurallar. **(4) Uzun içerik dayanıklılığı:** 60 karakterlik boşluksuz firma adı + uzun e-posta/konu/not 320px'e enjekte edildi — HİÇBİR ekranda yatay taşma yok; müvekkil/dosya kartları temiz kısaltıyor (ellipsis, "İstanbul U..."). Kaynak değişmedi (denetim); son geçen derleme (tsc+vite) + **376 test** geçerli. |
| 252 | 2026-08-18 13:30 | **Kapsamlı görsel + yapısal tasarım denetimi; küçük bir erişilebilirlik tutarlılığı düzeltildi.** Directive #7 için tüm ana yüzeyler `DESIGN-REFERENCE.md`'ye karşı gözden geçirildi (390px ekran görüntüleriyle): Genel Bakış (2×2 istatistik + 5 bölüm + zorunlu süre uyarısı + koyu güvenlik kartı), drawer (iki grup menü, "Sistem hazır"), Takvim (ay ızgarası + gün ajandası + FAB), Süre hesapla (**hesap doğru:** İstinaf HMK m.345, tebliğ 18 Ağu → 1 Eylül 2026, 14 gün; adli tatil sonrası, hafta içi; "bilgilendirme amaçlıdır" uyarısı var), Finans (özet + işlem listesi), Raporlar (SVG grafikler + uyarı + CSV), Ayarlar (**LLM varsayılan kapalı** ✓, footer grameri kaynakta doğru), Dosya Detay (Genel/Süreler[uyarı var]/Belgeler sekmeleri), Bildirimler (ertele hapları), Ara sayfası (global arama çalışıyor). **Doğrulanan sağlamlıklar:** güvenli alan (safe-area) girintileri kapsamlı (üst çubuk/drawer/FAB/toast/arama/kilit `env(safe-area-inset-*)` tüketiyor); karanlık mod bilinçli olarak yok (`color-scheme:light`); **tüm para ızgaraları taşma-güvenli** — `.money-grid` (`min-width:0` + `@media(max-width:360px)` tek sütun + `.money-value{overflow-wrap:anywhere}`, ölçümle: 7 haneye kadar tek satır, yalnız 8+ hane son çare sarma), `.info-grid` (`min-width:0`), `.stat-grid` (tur-251 düzeltmesi); formlar cokBuyuk 320px'te tek sütun temiz. **Düzeltme:** üst çubuktaki arama düğmesi ikon-SVG `<title>`'a dayanıyordu; zil düğmesi ise açık `aria-label` kullanıyor. Arama düğmesine `aria-label="Ara"` eklendi + Icon `title` kaldırıldı (ikon `aria-hidden` oldu) — iki ikon-düğme artık aynı, daha sağlam kalıpta; runtime doğrulandı (erişilebilir ad "Ara", `/ara`'ya gidiyor). **376 test** yeşil, derleme (tsc+vite) geçti. |
| 251 | 2026-08-18 12:00 | **TASARIM HATASI düzeltildi — Genel Bakış istatistik kartları dar ekranda + büyük yazıda sayfayı yatay taşırıyordu.** Yeni açı: en dar telefon eni **320px** ve en büyük yazı ölçeği (**cokBuyuk 125%**) — daha önce hep 390px'te test edilmişti. **Runtime tarama (Playwright, 320px):** cokBuyuk'ta `.stat-grid` (`repeat(2, 1fr)`) sayfayı 2px taşırıyordu (`scrollW 322 > 320`). **Kök neden:** grid izleri varsayılan `min-width:auto` yüzünden içeriğin içsel genişliğinin altına inemiyor; `.stat-value` (mono 1.75rem, `word-break:keep-all` — para sayıları rakam ortasından bölünmesin diye kırılamaz) cokBuyuk'ta iki izi 320px'in dışına itiyor. **Ölçümle bulunan ikinci (daha ciddi) hata:** normal yazıda bile **yedi haneli tutar** ("₺1.250.000", genişlik 162px) 320/360px'te kart kutusunu (138–158px) aşıp **sayfayı yatay kaydırıyordu** — ₺1M+ aylık tahsilatı olan bir avukatta gerçekçi; 360px en yaygın Android eni. `.card`'ta `overflow:hidden` yok, yani sayı kırpılmıyor (okunur kalıyor) ama sayfayı genişletiyor. **Düzeltme (`dashboard.css`):** (1) `repeat(2, minmax(0,1fr))` — izler içerik altına inebilsin; (2) `:root[data-yazi-olcegi='cokBuyuk'] .stat-grid` ve (3) `@media (max-width:360px) .stat-grid` → tek sütun (form `.field-row`/`.type-grid-3` erişilebilirlik kalıbının aynısı). **Doğrulama:** 320/360px (her iki ölçek) + 375/390px cokBuyuk → yedi/sekiz haneli tutarların hepsi taşmadan sığıyor; 390px normal **2×2 tasarım-referans düzeni korundu** (ekran görüntüsüyle onaylandı). **Kabul edilen sınır:** yalnızca **sekiz haneli** (₺12.5M/ay) 375/390px'te 7–14px taşıyor — bu tek-avukat yerel uygulamada aşırı uç; düzeltmek tüm kullanıcılarda sayıyı küçültüp referans düzeni bozardı, sayı yine de kırpılmadan okunur. **376 test** yeşil, derleme geçti. |
| 250 | 2026-08-18 11:15 | **İlgili kişi (dosya kişileri) ekleme akışı denetlendi — SAĞLAM; CRUD kapsamı tamamlandı.** Son denenmemiş CRUD özelliği: DosyaDetay Notlar sekmesindeki "İlgili kişiler" bölümü (kişiler notlarla aynı sekmede, sayaç = notlar+kişiler). **Runtime:** Notlar sekmesi → "İlgili kişiler → Ekle" → satır-içi form açıldı → ad "Av. Test Bilirkişi" + rol "bilirkisi" → "Kişiyi ekle" → `kisiler` 0→1 (kayıt ✓), arayüzde göründü ✓, rol etiketi "Bilirkişi" doğru (`kisiRolEtiketleri`) ✓, hata yok. **Kapsam kilometre taşı:** artık HER varlığın oluşturma akışı runtime doğrulandı — dosya, müvekkil, görev, olay, finans, not, kişi, süre, tekrar-serisi. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 249 | 2026-08-18 10:30 | **PWA manifesti + iOS ana-ekran varlıkları denetlendi — SAĞLAM (çekirdek hedef).** "iOS ana ekranına eklenebilir" hedefi için manifest ve simgeler doğrulandı. **Manifest (`dist/manifest.webmanifest`):** ad/açıklama (tr), `display:standalone`, `orientation:portrait`, tema/arka renkleri, 192/512/maskable simgeler; kritik olarak `start_url:'.'` ve `scope:'.'` **göreli** — `/MYLOVE/` alt-yoluna doğru çözülür (mutlak `/` alt-yol dağıtımını bozardı; en yaygın hata doğru yapılmış). **Simge dosyaları dist'te mevcut:** apple-touch-icon.png, favicon.svg, icons/icon-192/512/maskable-512.png. **index.html referansları doğru `/MYLOVE/` yolunda:** apple-touch-icon (180x180, iOS), manifest, theme-color #16262E. **Workbox:** autoUpdate + skipWaiting + clientsClaim (ana ekranda eski sürümde takılma önlenir), app-shell precache. Yani GitHub Pages `/MYLOVE/`'ten ana ekrana ekleme: doğru simge + standalone + uygulamaya (köke değil) açılan start_url. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 248 | 2026-08-18 09:45 | **Not (görüşme geçmişi) ekleme akışı denetlendi — SAĞLAM.** Müvekkil detayında "Görüşmeyi kaydet" ile not ekleme (CRUD + etkinlik günlüğü). **Runtime:** not eklendi → `notlar` 0→1 (kayıt ✓), `not-eklendi` hareketi 1→2 (etkinlik günlüğüne yazıldı ✓), not arayüzde görüşme geçmişinde göründü ✓, hata yok. **Ek gözlem:** not oluşturma etkinlik günlüğüne YAZILIYOR — bu, tur-245'teki "görev-oluşturma neden loglanmıyor" analizini destekliyor: notlar (anlamlı konuşma kayıtları) loglanır, görevler (yüksek-frekanslı yapılacaklar) loglanmaz — asimetri bilinçli, gözden kaçma değil. Ayrıca ayrı ESLint yok; tek kapı `tsc` typecheck (derlemede geçiyor). Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 247 | 2026-08-18 09:00 | **Hukuki süre KURAL listesi (en kritik veri) denetlendi — SAĞLAM.** Motor mantığı önce doğrulanmıştı; bu tur bireysel kurallar. **Yapı:** 31 kural (HMK/İİK/CMK/İYUK/iş 7036), **yinelenen id yok**, tüm süreler pozitif ve makul birimde (hafta/gün/ay/yıl). **Süre doğruluğu (bilinen sürelerle):** istinaf HMK 345 (2 hafta), temyiz HMK 361 (2 hafta), cevap HMK 127 (2 hafta), ihtiyati tedbire itiraz HMK 394 (1 hafta), yargılamanın iadesi HMK 377 (3 ay), ödeme emrine itiraz İİK 62 (7 gün), gecikmiş itiraz İİK 65 (3 gün), kambiyo itiraz İİK 168 (5 gün), itirazın iptali İİK 67 (1 yıl), şikayet İİK 16 (7 gün), istinaf CMK 273 (7 gün), temyiz CMK 291 (15 gün), iptal davası İYUK 7 (60 gün) — hepsi doğru. **`adliTatileTabi` bayrakları** yargı koluna göre doğru: İİK/CMK süreleri adli tatilde işler (false), HMK/İYUK durur (true). Kanun-madde referansları id'lerde kodlu ve tutarlı. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 246 | 2026-08-18 08:15 | **Süre kaydet → takvimde görünme uçtan uca denetlendi — SAĞLAM.** SureHesapla'dan bir süreyi dosyaya kaydetme akışı (veri bütünlüğü). **Runtime:** tebligat 2026-09-10 + İstinaf (2 hafta) → `Sure` kaydı oluştu, `sonTarih` **2026-09-24** (doğru — Perşembe, kaydırma yok); sureler 3→4. **Bağlı olay:** kaydetmede `sure-hesabi` kaynaklı olay OLUŞMADI (tohum verisinde de 0) — bu DOĞRU güncel tasarım: `takvimSorgulari` hem `olaylar` hem `db.sureler`'i okuyup süreyi doğrudan çiziyor (satır 51-96); `kaynak !== 'sure-hesabi'` süzgeçleri yalnız ESKİ kayıtlar için savunma amaçlı (yorumla teyitli). **Takvim görünümü doğrulandı:** Eylül 2026'ya gidip 24. güne tıklandı → süre görünüyor ("Perşembe 24 Eylül 2026", 1 kayıt). Yani hesap→kayıt→takvim zinciri doğru. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 245 | 2026-08-18 07:30 | **Etkinlik günlüğü (hareketler) kapsamı incelendi — asimetri bilinçli varsayıldı, değiştirilmedi.** `HareketTuru` 9 değer: 8'i oluşturma/olay (dosya-olusturuldu, olay-eklendi, belge-yuklendi, muvekkil-eklendi, odeme-kaydedildi, sure-hesaplandi, not-eklendi, hatirlatma-gonderildi), 1'i tamamlama (**gorev-tamamlandi**). Yani her varlık OLUŞTURMAYI günlüğe yazıyor AMA görev yalnızca TAMAMLAMAYI yazıyor — `gorevEkle`'de hareket kaydı yok (öteki tüm `*Ekle` fonksiyonları var). **Karar:** düzeltme (görev-eklendi ekleme) tutarlılık sağlar ama gerçek bir sakıncası var — görevler YÜKSEK FREKANSLI eylem maddeleridir (avukat günde çok küçük görev ekler); her oluşturmayı yazmak kendini-sınırlayan "Son hareketler" akışını doldurup daha yüksek-sinyalli olayları (duruşma/süre/ödeme) ve görev-tamamlama kilometre taşlarını dışarı iterdi. Görev-oluşturmayı hariç tutup görev-tamamlamayı tutmak tutarlı bir "yüksek-sinyal akış" gerekçesi. Tasarımcı onayı olmadan savunulabilir mevcut davranışı değiştirmek muhafazakâr yargıya aykırı → **olduğu gibi bırakıldı, varsayım günlüğe yazıldı.** Kaynak değişmedi; son geçen derleme (**376 test** yeşil) geçerli. |
| 244 | 2026-08-18 06:45 | **Tekrar eden seri sil akışı ("yalnızca bu / tüm seri") uçtan uca denetlendi — SAĞLAM (regresyon kontrolü).** Bu oturumun en karmaşık denenmemiş etkileşimi; `seriesId` mantığı. **Runtime:** haftalık×4 olay oluşturuldu → 4 kayıt, **tek** ortak seriesId ✓. Silme onayı hem "Yalnızca bu" hem "**Tüm seri (4)**" seçeneklerini sunuyor (sayaç doğru, `useOlaySeriSayisi`) ✓. "Yalnızca bu" → tam 1 sildi (3 kaldı) ✓. Kalan birinden "Tüm seri" → hepsi silindi (0 kaldı) ✓. Hata yok. Tur boyunca yaptığım değişikliklerden sonra bu karmaşık etkileşim hâlâ doğru çalışıyor. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 243 | 2026-08-18 06:00 | **Form DÜZENLEME yolları denetlendi (veri bütünlüğü) — SAĞLAM.** Mevcut kaydı düzenleme formuna yükleme yolu: bir alan yüklenmezse kaydetmede sessizce silinirdi (gerçek veri kaybı). **DosyaForm (runtime):** Demir İnşaat düzenlemesinde 8 alanın hepsi DB'den doğru dolduruldu — dosya adı, müvekkil (ID), tür (ticaret), durum (derdest), konu, mahkeme, esas no, karşı taraf. **FinansForm (runtime):** para gidiş-dönüşü tam — DB `4200000` kuruş → düzenlenebilir "42.000,00" (Türkçe biçim: binlik nokta, ondalık virgül, kayıpsız); kategori, dosya, işlem tarihi, ödeme durumu çipi ("Ödendi") doğru yüklendi. Formlar aynı yükleme desenini (useEffect → durum) paylaştığından ikisinin geçmesi tümü için güçlü kanıt. Düzenlemede veri kaybı riski yok. Hata yok. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 242 | 2026-08-18 05:15 | **Sayfa göz-kaşı etiketleri + süzgeç-boş durumları denetlendi — SAĞLAM.** Referans-dışı ekranların tasarım tutarlılığı. **Göz-kaşı (eyebrow) etiketleri:** tutarlı şema — "Çalışma alanı" (kayıt/referans: Dosyalar, Müvekkiller, Raporlar, Ayarlar, Ara) vs "Yürütme" (eylem: Görevler, Finans); hepsi aynı mono-büyük-harf stili, anlamlı kategorizasyon (keyfi değil). **Süzgeç-boş durumları (runtime):** Görevler "Bana atanan" süzgeci 0 satır → temiz bağlamsal `.section-empty` kartı ("Size atanmış açık görev yok."); tasarım diline uygun (kart stili, sessiz metin, FAB duruyor), geçici süzgeç durumu için uygun sadelik. Finans süzgeçlerinin hepsinde veri var (Tümü 40 / Tahsilat 9 / Gider 31 / Bekleyen 4). Hata yok. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 241 | 2026-08-18 04:30 | **Gösterge tasarım sadakati referans görsellere karşı yeniden denetlendi — SAĞLAM.** Directive #7 (tasarım) odağı: 9 referans JPG'nin kapsadığı ana ekran (Gösterge) ölçüldü. **Hero** (01): adaçayı degrade + dekoratif halkalar, mono tarih "SALI, 18 AĞUSTOS 2026", serif selam "Günaydın, Av. Ece", gövde metni, beyaz hap "Güncelle" düğmesi ✓. **İstatistik ızgarası 2×2** (02): dört `stat-dot` **sağ üstte** kategori renkleriyle (mavi/amber/teal/mor) doğru konumlu ✓; değerler (24/+3, 8/2 bugün, 17/4 öncelikli, ₺184.500/+%12) ✓. **Bölüm başlıkları** (5.4): mono etiket→serif başlık→"Hepsini gör ↗" hepsi eşleşiyor. **Son tarihler** (03): aciliyet rengi doğru — "2 gün kaldı" kırmızı (162,86,74), "5 gün" amber (134,102,56), "9 gün" nötr-mavi (82,110,138). **Duruşmalar** tarih rozeti, **görevler** tamamlananlarda üstü-çizili+soluk+yeşil-dolu kutu + kesikli "Yeni görev ekle" satırı, **hazırlık** yüzde+çubuk, **hareketler**, alt güvenlik kartı — hepsi referansa sadık. Süre uyarısı gösterge son-tarihler altında da mevcut. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 240 | 2026-08-18 03:45 | **Dexie şema/göç + finans muhasebesi (kısmi ödeme) denetlendi — SAĞLAM.** **Şema göçü (`db.ts`):** v2 yalnızca `olaylar`/`gorevler`'e `seriesId` indeksi **ekliyor** (yıkıcı değil — Dexie yeniden indeksler, birincil anahtar `id` değişmiyor, öteki tablolar devrediyor); yükseltmede veri kaybı riski yok. **Kısmi ödeme muhasebesi (`finansIslemleri.ts`):** `odenenKirp = max(0, min(odenen, tutar))` ödeneni [0,tutar]'a kırpıyor (bekleyen = tutar−odenen hep ≥0); `durumNormalize` durumu doğru türetiyor (0→bekliyor, ≥tutar→odendi, arası→kismi). **Gösterge + rapor toplamları:** tahsilat/gelir/gider HER yerde `odenenTutar` (fiilen ödenen, nakit-esaslı) topluyor ve `yon`'a göre süzülüyor — gösterge `finans` sorgusu zaten `yon='gelir'` filtreli (gider tahsilata karışmıyor); rapor per-dosya `dosyaBakiye` (tur-208 eklemem) gelir/gider'i doğru ayırıyor; `FinansYonu='gider'|'gelir'` ikili olduğundan `else→gider` dalı güvenli. Kısmi ödemeler ödenen kısmıyla sayılıyor. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 239 | 2026-08-18 03:00 | **Gösterge sorguları + CSV para biçimi + yedek kripto denetlendi — SAĞLAM.** Servis/veri katmanı taraması sürüyor (tur-236 dosya-adı UTC hatasının izinden). **Gösterge tarih aralıkları:** `gunBaslangici` `setHours(0,0,0,0)` ile **yerel** gece yarısı, `aralik(7)` ondan türüyor; `.between(basIso, sonIso)` mutlak UTC-ISO anları karşılaştırıyor → hafta/gün sınırları İstanbul-doğru. Ay sınırı yerel `new Date(y,m,1)` + `finans.tarih` (salt tarih) önek eşleşmesi. Yani çekirdek tarih mantığı tutarlı yerel-farkında; dosya-adı UTC hatası izole bir gözden kaçmaydı (düzeltildi). **CSV para (`csvTutar`):** `(kuruş/100).toFixed(2).replace('.',',')` → "1250,50" (virgül ondalık, binlik ayracı YOK) — `;` ayracı + BOM ile Türkçe Excel doğru okur (binlik nokta Excel'i şaşırtırdı). **Yedek kripto (`kripto.ts`):** PBKDF2 210k/SHA-256 (güçlü); `sifrele` HER şifrelemede **taze rastgele tuz VE taze rastgele IV** üretiyor (`getRandomValues`, nonce tekrarı yok); AES-256-GCM (kimlik doğrulamalı/kurcalama-belirgin) — ders-kitabı doğru. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 238 | 2026-08-18 02:15 | **Hatırlatma + tekrar + hazırlık + yedek doğrulama alt sistemleri denetlendi — SAĞLAM, kaynak değişmedi.** Servis/domain katmanı taraması sürüyor. **Hatırlatma zamanlaması:** `tetikAni = hedef − ofset·dk` doğru; hedef zaman olayda mutlak `baslangic`, sürede tz'siz `${sonTarih}T09:00:00` = **yerel 09:00** çıpası (kasıtlı, yorumla uyumlu); ofset profilleri türe göre doğru anahtarlı (`profiller[olay.tur]` olaylara, `profiller['son-tarih']` sürelere — takas yok). **Tekrar üreteci (`tekrar.ts`):** ay-sonu/artık-yıl taşması `date-fns` `addMonths`/`addYears` ile doğru kırpılıyor (31 Oca+1ay→28/29 Şub; 29 Şub+1yıl→28 Şub); adet [1,60] sınırlı (sonsuz döngü yok); "hafta-ici" hafta sonlarını atlıyor. **Hazırlık motoru:** yüzde `toplamAgirlik === 0 ? 100 : …` ile **sıfıra bölme korumalı** (uygulanabilir madde yoksa %100, NaN değil). **Yedek doğrulama (`dogrula`):** biçim/sürüm/her-tablo-dizi yapısı DB'ye dokunmadan ÖNCE denetleniyor (clear-sonra-patlama veri kaybını önler — belgeli kasıtlı seçim); derin kayıt şeması denetlenmiyor ama artık risk (elle düzenli düz .json) hata sınırıyla yakalanıyor, şifreli .jcenc kimlik-doğrulamalı. Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 237 | 2026-08-18 01:30 | **Hukuki süre motoru + tatil verisi denetlendi — SAĞLAM (en kritik bileşen).** Yasal doğruluk taraması. **Resmî tatiller:** yedi ulusal tam-gün tatil eksiksiz (1 Oca, 23 Nis, 1/19 May, 15 Tem, 30 Ağu, 29 Eki); yarım günler (28 Eki öğleden sonra, arefe) bilinçle sayılmıyor. **Dinî bayramlar:** Ramazan/Kurban 2024–2030 elle girili (lunar, hesaplanamaz). **Zarif bozulma:** `DINI_BAYRAM_KAPSAMI={2024,2030}` export'u SureHesapla'da CANLI kullanılıyor — kapsam dışı tarihte "bayram kaydırması eksik olabilir" uyarısı çıkıyor (dead-code değil); runtime doğrulandı: 2031 tebligat → uyarı gösterildi + ham pazar günü Pazartesi'ye kaydı, 2026 → uyarı yok. **İşlem sırası (yasal olarak doğru):** ham son gün → adli tatile denk gelirse (kural tabiyse) 7 Eylül'e uzar (HMK m.104) → sonra hafta sonu/tatilse ilk iş gününe kayar (HMK m.93); yani 7 Eylül hafta sonuna denk gelirse Pazartesi'ye kayar. Her ayarlama gerekçesi ilgili HMK maddesini yazıyor. Adli tatil doğru tanımlı (20 Tem–31 Ağu). Kaynak değişmedi (doğrulama); son geçen derleme (**376 test** yeşil) geçerli. |
| 236 | 2026-08-18 00:45 | **GERÇEK DÜZELTME — dışa aktarma dosya adları UTC güne damgalanıyordu (yerel gün olmalı).** Servis/veri katmanı denetimi sürüyor. Önce CSV doğrulandı: UTF-8 BOM + `;` ayracı ile Türkçe Excel'de doğru açılıyor (sağlam); para ayrıştırma (`metindenKurus`) Türkçe biçim sezgisi sağlam. **Bulgu:** yedek (`.jcenc`/`.json`), CSV rapor ve ICS dışa aktarım dosya adları tarihi `new Date().toISOString().slice(0,10)` = **UTC gün** ile damgalıyordu. İstanbul UTC+3; yerel gece yarısı–03:00 arası UTC hâlâ önceki gün → 01:00'de alınan yedek "…-dün" adıyla iniyordu. Uygulamanın zaten yerel-bugün yardımcısı `bugunIso()` (yerel getter'lar) var; üç dosya-adı damgası buna çevrildi. Yalnız dosya adları değişti — ICS `DTSTAMP` UTC kalıyor (`utcDamga`), yedeğin `olusturmaZamani`'ı etkilenmedi. **Doğrulama:** 2026-08-17T22:30Z (İstanbul'da 01:30, 18 Ağu) → yerel "2026-08-18", UTC "2026-08-17" veriyordu. `npm run build` geçti, **376 test** yeşil. |
| 235 | 2026-08-18 00:00 | **GERÇEK DÜZELTME — ICS dışa aktarımı satırları oktet yerine kod-birimiyle katlıyordu (RFC 5545).** Takvim dışa aktarma (Google/Apple/Outlook) denetlendi: yapı sağlam (CRLF, VEVENT/VCALENDAR, UID, tüm-gün özel `yerelGun` ile doğru, kaçış test edilmiş) ama `satirKatla` uzunluğu `.length`/`.slice` (UTF-16 kod-birimi) ile ölçüyordu. RFC 5545 içerik satırlarını **75 oktet** ile sınırlar; Türkçe harfler UTF-8'de 2 bayt (tire — 3 bayt) olduğundan Türkçe-yoğun SUMMARY satırı 75 kod-biriminin altında kalıp 75 okteti aşıyor, **katlanmadan** uzun satır olarak çıkıyordu (Türkçe-öncelikli uygulamada sık); kod-birimi indeksinde dilimleme çok baytlı karakteri de bölebilirdi. **Düzeltme:** katlama `TextEncoder` ile okteti sayacak ve **kod noktası** bazında ilerleyecek şekilde yeniden yazıldı — çok baytlı karakter asla bölünmez, her fiziksel satır (devam satırındaki baştaki boşluk dâhil) ≤75 oktet. Uzun Türkçe dosya adıyla test eklendi: her satır ≤75 oktet, katlama gerçekleşti, açılınca özgün ad bozulmadan geri geliyor. `npm run build` geçti, **376 test** (yeni katlama testiyle +1) yeşil. |
| 234 | 2026-08-17 23:15 | **GERÇEK DÜZELTME — büyük yazıda "Bekliyor" çipi sözcük-içi sarması giderildi (tur-230'da kabul edilmişti, temiz çözüm bulundu).** En büyük ölçekte (%125) üç eşit-genişlikli ödeme-durumu çipi (Bekliyor / Kısmi / Ödendi) "Bekliyor"u iki satıra ("Bekliyo/r") sıkıştırıyordu — genel `overflow-wrap` tek sözcüğü kırıyordu. Daha önce elenen yollar (keep-all/break-word ızgara min-content'i şişirir → sayfa taşması; etiket küçültme → a11y amacına ters) yerine **kapsamlı-ama-dar** çözüm: satır-içi `1fr 1fr 1fr` stili `.type-grid-3` sınıfına taşındı ve YALNIZ en büyük ölçekte (mevcut kök `data-yazi-olcegi` özniteliğiyle) tek tam-genişlik sütuna iniyor; her çip etiketi tek satıra sığar. **Doğrulama (satır-kutusu sayımı, `Range.getClientRects`):** "Bekliyor" normalde (3 sütun), %112,5'te (3 sütun, zaten sığıyor) ve %125'te (yığılmış) **tek satır**. Normal-font düzeni birebir korundu (sınıf, eski satır-içi stili yeniden üretiyor); diğer 2-sütunlu type-grid'ler (Müvekkil/Olay) dokunulmadı. Ekran görüntüsüyle onaylandı. `npm run build` geçti, **375 test** yeşil. |
| 233 | 2026-08-17 22:30 | **Türkçe arama + takvim gezinmesi denetlendi — SAĞLAM, kaynak değişmedi.** **(1) Türkçe-duyarlı arama (Ara):** kod `toLocaleLowerCase('tr')` kullanıyor; runtime'da doğrulandı — "inşaat"/"İNŞAAT" ikisi de "Demir İnşaat"ı buluyor (İ→i katlaması), "Demir"/"demir"/"DEMİR" eşit, "istanbul"/"İstanbul" 38 sonuç, "yılmaz"/"YILMAZ" "Yılmaz"ı buluyor (I/ı doğru); "ıcra" (noktasız ı) 0 sonuç, "icra" (noktalı i) 7 sonuç — **doğru Türkçe davranışı** (ı≠i), naif `toLowerCase` değil. **(2) Takvim gezinmesi:** Ağustos→Eylül→Ekim (Sonraki), geri (Önceki), +12 ay → Eylül 2027 çökme yok/boş aylar render oluyor, "Bugün" bugüne dönüyor (bugün hücresi "18" aria-selected=true), Hafta görünümü gezinmesi de çalışıyor — hata yok. Not: prev/next düğmelerinin erişilebilir adı Icon SVG `<title>` üzerinden geliyor (ad denetimi tur-231'de zaten temiz çıkmıştı). Bilinen kabul: %125 "Bekliyor" çip sarması (tur-230). Tur boyunca kaynak değişmedi; son geçen derleme (**375 test** yeşil) geçerli. |
| 232 | 2026-08-17 21:45 | **Asistan ekranı + yoğun-veri DosyaDetay + sıralama mantığı denetlendi — hepsi SAĞLAM, kaynak değişmedi.** **(1) Asistan:** kural tabanlı **cihaz-içi** ekran — "Gündem" öncelikli uyarı listesi (duruşma/süre/ödeme yaklaşımları, kırmızı=acil/sarı=yakın) cihaz verisinden hesaplanıyor; "Dosyaya danışın" bölümü dosya seç + hızlı-soru çipleri + serbest metin. Runtime: "Aksoy / Merkez Sigorta" + "Yaklaşan süre var mı?" → cihaz-içi yanıt ("Bu dosyada yaklaşan açık bir süre yok."), LLM gerekmiyor. LLM katmanı isteğe bağlı/varsayılan kapalı, gizlilik uyarısı net. **(2) Yoğun-veri DosyaDetay:** bir dosyaya 15 olay + 12 görev enjekte edildi (geçerli şema: `baslangic` IsoDateTime + `kaynak`). Sekme sayaçları doğru (Duruşmalar 16, Görevler 13), taşma yok (docW=390; yalnız niyetli tab-row kaydırması), sıralama makul, hata yok. **(3) Hata sınırı (error boundary):** ilk denemede bozuk-şema kayıtlar (eksik `baslangic`) render'ı çökertti ama uygulama **zarifçe** "Bir şeyler ters gitti" kartıyla (Ana sayfa/Yeniden dene) yakaladı — sağlam dayanıklılık. Not: yedek şifreli+kimlik-doğrulamalı (.jcenc AES-GCM) olduğundan bozuk kayıt gerçek kullanımda içeri giremez. **(4) Sıralama incelemesi:** Duruşmalar sekmesi olayları azalan (ters-kronolojik, `baslangic` desc) sıralıyor — Süreler/Görevler'in artan (aciliyet) sıralamasından farklı ama Finans ile tutarlı (geçmiş-günlük deseni; karışık dosyada yaklaşanı-üstte tutar). Bilinçli tasarım, hata değil — değiştirilmedi. Tur boyunca kaynak değişmedi; son geçen derleme (**375 test** yeşil) geçerli. |
| 231 | 2026-08-17 21:00 | **Denetlenmemiş dört alan tarandı — hepsi SAĞLAM, kaynak değişmedi.** **(1) Onboarding akışı** (onay + kayıt): 320px'te ve %125 yazıda üç adımda (onay/yöntem/kayıt) taşma yok, hata yok; görsel düzen temiz, okunur. **(2) Kilit ekranı (PIN):** kilitEtkin+pinOzeti ile tetiklenip 390/320/%125'te ölçüldü — tam opak koyu katman (arkadaki içerik/müvekkil verisi sızmıyor, gizlilik korunur), PIN noktaları + sayısal tuş takımı düzgün; `role="dialog"`, hatalı PIN uyarısı `role="alert"` ("PIN yanlış, tekrar deneyin.") ile ekran okuyucuya duyuruluyor; taşma yok. **(3) Ayarlar:** tam sayfa görsel — profil, yazı boyutu, hatırlatma ofsetleri, bildirim kanalları, sessiz saatler, AI katmanı, yedekleme, güvenlik, sıfırlama; hepsi düzenli. Alt bilgi kaynak metni "veriler cihazınızda saklanır" (dilbilgisi doğru; düşük çözünürlük yanıltmıştı — kaynaktan doğrulandı). **(4) Çift-gönderim koruması:** MuvekkilForm'da hızlı çift-tık runtime'da **tek kayıt** üretti; beş kayıt formu + SureHesapla'nın hepsinde `disabled={kaydediliyor}` guard'ı doğrulandı — kopya kayıt yok. Bilinen kabul: %125'te "Bekliyor" çipi sözcük-içi sarıyor (tur-230, temiz düzeltmesi yok). Tur boyunca kaynak değişmedi; son geçen derleme (**375 test** yeşil) geçerli. |
| 230 | 2026-08-17 20:15 | **Büyük yazı iç-kırpma denetimi (temiz) + yazdırma çıktısı cilalandı.** İki eksen. **(1) %125 yazıda iç kırpma:** 11 ekranda çip/rozet/düğme/hücre metinleri `overflow:hidden` ile dikey kırpılıyor mu tarandı — **hiçbiri kırpılmıyor** (öğeler büyüyor/sarıyor, içerik gizlenmiyor). Tek gözlem: FinansForm "Ödeme durumu" çipinde "Bekliyor" %125'te sözcük-içi sarıyor ("Bekliyo/r"); genel `overflow-wrap:anywhere`'den. Temiz bir düzeltmesi yok (keep-all/break-word ızgara min-content'i şişirip sayfayı taşırır — tarih-alanı tuzağı; en büyük fontta çip yazısını küçültmek erişilebilirlik amacına ters). Tam okunur, opsiyonel max-fontta kabul edilir kozmetik → **varsayım günlüğe yazıldı, düzeltilmedi.** **(2) GERÇEK DÜZELTME — yazdırma:** `@media print` çıktısı SureHesapla'da "Dosya / Kaydetmeden hesapla" bölümünü ve pasif "Kaydetmek için dosya seçin" düğmesini basıyordu — kâğıtta anlamsız etkileşim arayüzü, dosyaya konan bir hesapta bitmemiş görünüyor. Yeniden kullanılabilir `.no-print` yardımcı sınıfı (yalnız `@media print`) eklendi ve o bölüme uygulandı. **Runtime (print emülasyonu):** çıktı artık yalnız hesap bağlamı (süre türü, tebliğ tarihi) + hesaplanan son gün + uyarı; kaydet bölümü kâğıtta yok, ekranda duruyor. Diğer print doğrulaması: topbar/FAB/geri-bağlantısı gizli, içerik tam genişlik (794px A4), uyarı korunuyor. `npm run build` geçti, **375 test** yeşil. |
| 229 | 2026-08-17 19:30 | **GERÇEK DÜZELTME — büyük yazı ölçeğinde Finans formu sayfayı yatay taşırıyordu (WCAG 1.4.4).** Yazı-boyutu (metin yeniden boyutlandırma) ekseni: uygulamanın kendi en büyük ayarı (Çok büyük = kök %125) seçilip 17 ekran 390px'te tarandı. **Bulgu:** FinansForm'daki iki-sütunlu `.field-row` (İşlem tarihi + Vade tarih girişleri) sayfayı **453px**'e itiyordu — tüm form yatay kayıyordu. Sebep: ızgara ögelerinin öntanımlı `min-width:auto`'su, yerel tarih girişlerinin içerik-asgari genişliğini her `1fr` sütuna dayatıp taşırıyordu; `≤360px` yığma kesme noktası ise medya sorgusu kök font ölçeğini göremediğinden devreye girmiyordu. **Düzeltme (2):** (1) `.field-row`'a `minmax(0,1fr)` sütunlar — ögeler içeriğin altına küçülebilir (genel emniyet; %112,5 durumunu da çözer); (2) en büyük ölçekte `.field-row` tek sütuna iniyor — App kökte yeni `data-yazi-olcegi` özniteliği yazıyor, CSS ona bakıyor; böylece eşli tarih alanları tam genişlik olur ve yıl kırpılmaz. **Runtime doğrulama:** FinansEkle belge genişliği %125'te **453→390** (ve %112,5'te 390); yığılınca tarih alanları tam/okunur; %100 düzeni değişmedi (hâlâ iki sütun). Diğer taşma bayrakları niyetli `overflow-x:auto` satırları (chip/tab, hepsi docW=390). `npm run build` geçti, **375 test** yeşil. |
| 228 | 2026-08-17 18:45 | **Erişilebilirlik geniş denetimi — çok eksen SAĞLAM, kaynak değişmedi.** Yeni eksenler runtime'da tarandı, gerçek hata çıkmadı: **(1) Erişilebilir ad (WCAG 4.1.2)** — 16 ekranda tüm button/link/input; ikon-düğmeleri dâhil hepsinin aria-label/metin adı var, adsız öğe yok. **(2) Başlık hiyerarşisi + landmark (1.3.1/2.4.6)** — 13 sayfada her birinde tam 1 `h1`, atlama yok, `main` mevcut. **(3) Süre uyarısı (proje kuralı)** — hesap çıktısında görünür: SureHesapla ("Bu hesap bilgilendirme amaçlıdır… teyit sizde"), Takvim ("Süre kayıtları bilgilendirme amaçlıdır…") ve 8 sayfada mevcut. **(4) Renk-tek-başına (1.4.1)** — Takvim'de olay türü METİNLE ("Duruşma", "Müvekkil görüşmesi", süreler "son gün" etiketi) veriliyor; ay-ızgarası noktaları yalnız yardımcı (dokununca metin liste). **(5) Takvim Hafta/Ajanda görünümleri** — taşma yok, hata yok, temiz. **(6) FAB ↔ süre uyarısı** — gerçek viewport'ta çakışma yok (uyarı tam görünür, FAB altındaki boşlukta). **(7) Bildirimler ertele-çipleri** — görünür 23px ama `::after` ile etkin dokunma hedefi 56×45px (2.5.8 geçer). Tur boyunca kaynak değişmedi; son geçen derleme (**375 test** yeşil) geçerli. |
| 227 | 2026-08-17 18:00 | **GERÇEK DÜZELTME — form doğrulama hatasında hatalı alan belirlenmiyordu (WCAG 3.3.1, 5 form).** Doğrulama denetimi: her ekleme/düzenleme formu submit'te `role=alert` mesajı gösteriyordu ama mesaj altta (kaydet düğmesinin yanında), hatalı alan çoğu kez üstte; hangi alanın hatalı olduğu **işaretlenmiyordu** — `aria-invalid` hiçbir yerde bağlı değildi, dolayısıyla mevcut `.input[aria-invalid='true']` kırmızı-kenarlık stili ÖLÜ koddu ve başarısız submit sonrası odak kaydet düğmesinde kalıyordu (kullanıcı hatalı alanı elle arıyordu). **Düzeltme:** küçük paylaşılan `useFormHata` hook'u — başarısızlıkta mesajı kurar, hatalı alanı `aria-invalid` ile işaretler ve odağı ona taşır (`id="alan-<alan>"`), böylece kullanıcı (fare+klavye) doğrudan düzeltilecek alana gider (alan görünüme de kaydırılır). Kırmızı-kenarlık stili `.select` ve `.textarea`'yı da kapsayacak şekilde genişletildi. Beş kayıt formuna bağlandı (Dosya/Müvekkil/Görev/Olay/Finans); Finans, alan-dışı makbuz-yükleme hatası için `setHata`'yı koruyor. **Runtime doğrulama:** her formda boş/geçersiz submit doğru alanı odaklıyor + kırmızı kenarlık + mesaj; çok-alanlı formlar sıradaki hataya ilerliyor (ör. Dosya başlık→müvekkil); OlayForm'da bitiş<başlangıç → bitiş saati alanı odaklanıyor. `npm run build` geçti, **375 test** yeşil. |
| 226 | 2026-08-17 17:05 | **Devingen-hareket (reduced-motion) doğrulandı sağlam + büyük tutar taşması düzeltildi.** İki eksen. **(1) `prefers-reduced-motion` — SAĞLAM:** token'lar (`--duration-*: 0ms`) tüm token-süreli geçiş/animasyonları sıfırlıyor; sonsuz keyframe animasyonları (spinner `donen`, iskelet `kayan`, onboarding `ob-parla/ob-imlec/ob-titre(.ob-kasa)/ob-akis(.ob-hat-metin)`, tur) per-dosya `animation:none` bloklarıyla kapatılmış; sabit-süreli `transition:`ların hepsi `var(--duration-*)` kullanıyor → kapsam tam, düzeltme gerekmedi. **(2) GERÇEK DÜZELTME — büyük tutar özet kutusunu taşırıyor:** iki sütunlu `money-grid`'de her hücre 390px'te ~126px; tek satırda + `word-break:keep-all` olan `.money-value` büyük tutarda hücreyi taşırıyordu (ölçüm: 126px kutuda 194px içerik → komşu KPI kutusunun üstüne binme riski). Gerçekçi solo değerler (≤~11 hane) sığıyor ama kuruşlu büyük ticari dava tutarı ("₺15.000.000,00", 14 hane) veya büyük bekleyen toplamı taşıyordu. Tasarımcı ≤360px'te zaten tek sütuna iniyor ("tutarlar kırpılmadan sığsın"); aynı niyet 390px'e taşındı: `.money-value`'ya `min-width:0; overflow-wrap:anywhere`. Sığan değerler değişmez (tek satır); taşan değer artık **kendi hücresi içinde** satır kırar — tamamı görünür, komşuya binmez. **Runtime doğrulama:** Finans + DosyaDetay mali kutularında gerçekçi/patolojik tutarlarda hiçbir `.money-cell` taşmıyor; küçük değerler birebir aynı. `npm run build` geçti, **375 test** yeşil. |
| 225 | 2026-08-17 16:20 | **GERÇEK DÜZELTME — klavye odak halkası WCAG 1.4.11'i geçmiyordu (sistemik).** Odak-görünürlüğü denetimi: her ekranda Tab ile gezilip odak halkasının rengi ve zeminine göre kontrastı ölçüldü. **Bulgu:** global `:focus-visible` halkası `--brand-teal` (#5fb49c) kullanıyordu — token dosyasının kendi notuyla açık zeminde AA'yı geçmez (~2.5:1). Odak göstergesi metin-dışı bir bileşen ve komşu renklere karşı **≥3:1** ister (WCAG 1.4.11); ölçüm: beyaz kartta **2.47**, kırık-beyaz yüzeyde 2.29, sage çipte 2.10 — her ekranda eşiğin altında. Metin kontrastı zaten `--brand-teal-text` ile çözülmüştü ama odak halkası açık teal'de bırakılmış (gözden kaçmış). **Düzeltme:** yeni anlamsal `--focus-ring` token'ı (#35806b: beyazda ~4.7:1, kırık-beyaz/sage zeminlerde ≥3:1) eklendi; üç `:focus-visible` outline kuralı (global, onay-metin, onay-kutu) buna bağlandı. `--brand-teal` dolgu/kenarlık/ikon kullanımları için (açık teal doğru) dokunulmadı. **Runtime doğrulama:** Tab turu yeniden koşuldu — tüm form alanları, çipler, satırlar, bağlantılar ve düğmeler artık ≥3:1; tek sub-3 okuma tamamlanmış task-check (halkayı kutunun kendi dolgusuyla kıyaslayan ölçüm artefaktı; halka `outline-offset:2px` ile beyaz kartın üzerinde, 4.7:1 — ekran görüntüsüyle onaylandı). `npm run build` geçti, **375 test** yeşil. |
| 224 | 2026-08-17 15:25 | **GERÇEK DÜZELTME — uzun etiket sayfayı yatay taşırıyordu.** Uzun-metin dayanıklılık taraması: kayıtlara aşırı uzun ad/başlık/etiket enjekte edilip liste ve detay ekranları 390px'te tarandı. **Bulgu:** MüvekkilDetay'da tek bir ~76 karakterlik boşluksuz etiket (`.tag`) sayfayı **646px** genişletiyordu (390 viewport'ta belge genişliği 646 → tüm sayfa yatay kaydırma). Sebep: `.tag` `white-space:nowrap` ama genişlik sınırı yok, `flex-wrap` kabında etiket sınırsız büyüyüp kabı zorluyordu. Diğer alanlar sağlamdı (dosya/müvekkil liste satırları `.truncate` ile kısaltıyor, uzun dosya başlığı DosyaDetay'da düzgün sarıyor). **Düzeltme (`dosya.css`, paylaşılan `.tag` kuralı → tüm etiket kullanımlarını kapsar):** `max-width:100%; overflow:hidden; text-overflow:ellipsis`. Kısa etiketler değişmez; patolojik etiket kap içinde "…" ile kırpılır. **Runtime doğrulama:** MüvekkilDetay belge genişliği **646→390** (yatay kaydırma yok), uzun ad temiz sarıyor, uzun etiket "…" gösteriyor; ekran görüntüsüyle onaylandı. Kalan Görevler/DosyaDetay taşma bayrakları niyetli `overflow-x:auto` kaydırma satırları (chip-row/tab-row, hepsi `docW=390`). `npm run build` geçti, **375 test** yeşil. |
| 223 | 2026-08-17 14:35 | **Tasarım denetimi: dar ekran (320px) + tüm-ekran dokunma hedefi taraması; 404 bağlantısı düzeltildi.** İki eksen sistematik tarandı (gerçek runtime). **(1) 320px taşma (iPhone SE):** 16 rota — hiçbirinde sayfa düzeyi yatay kaydırma yok (`docW=320` her yerde); taşan tek öğeler niyetli kaydırma kaplarında (`.tab-row`/`.chip-row` `overflow-x:auto`, kaydırılabilir/erişilebilir) veya bilinçli dekoratif kırpmada (`.hero` `overflow-x:hidden` hero-ring). Dar ekran düzeni sağlam. **(2) Dokunma hedefi (WCAG 2.5.8, ≥24px):** görünür kutuyu ölçen tarama iki aday buldu. `.task-check` (görünür 21px, gösterge+görevlerde onay kutusu) YANLIŞ POZİTİF çıktı — `dokunmatik.css` `::after` ile hedefi genişletiyor; `elementFromPoint` ile etkin hedef **36×45px** ölçüldü (geçer). Gerçek bulgu: NotFound (404) sayfasındaki "Genel bakışa git" bağlantısı (`.placeholder-link`) küçük yazı satır-yüksekliğiyle **20px** — herhangi bir eskimiş/yanlış URL'de erişilebilir. **Düzeltme:** `min-height:24px` (inline-flex, metin dikey ortalı). Runtime: bağlantı **24px** ✓, kontrast 5.33 (AA) ✓, 404 düzeni temiz. `npm run build` geçti, **375 test** yeşil. |
| 222 | 2026-08-17 13:50 | **Tur-221 yeni arayüzünün erişilebilirlik geçişi + dokunma hedefi düzeltmesi.** Arşiv özelliğiyle eklenen üç yeni öğe WCAG ölçümüne alındı (390×844, gerçek runtime). **Bulgu:** DosyaDetay künyesindeki yeni "Müvekkil" köprüsü (`.info-value-link`) `display:block` + satır-yüksekliğiyle yalnız **20px** yükseklikteydi — WCAG 2.5.8 (24px dokunma hedefi) altında. **Düzeltme (`dosya.css`):** `.info-value-link` → `display:flex; align-items:center; min-height:24px` (hücre genişliğinde, dikey ortalı, 24px tıklanır alan; metin/renk değişmedi). Runtime yeniden ölçüm: köprü **24px** ✓, kontrast 5.33 (AA≥4.5) ✓; arşiv şeridi metni kontrast 4.76 (AA) ✓, "Arşivden çıkar" düğmesi 57×158px (≥24px) ✓, arşivli detayda taşma 0 ✓. Ekran görüntüsüyle info-grid hizası doğrulandı — flex değişikliği grid düzenini bozmadı (Müvekkil köprüsü "Karşı taraf" ile aynı satırda hizalı). `npm run build` geçti, **375 test** yeşil. |
| 221 | 2026-08-17 08:30 | **GERÇEK DÜZELTME — "yaz-bir-kez" arşiv: gizlenen müvekkile geri dönüş yolu yoktu.** Silme/arşiv denetimi sürerken çıktı: müvekkil silme akışı, dosyası olan müvekkili **arşivliyor** (veri korunur) ama arşiv TEK YÖNLÜ idi — arşivli müvekkil listeden gizleniyor, DosyaDetay'da adı düz metin (bağlantı değil), arşivden çıkarma işlevi/arayüzü YOK. Sonuç: müvekkil kalıcı gizli, kendi detay sayfasında bile "arşivli" demiyordu; kurtarılamaz bir durum (arşiv yalnız "sil" niyetiyle tetiklense de meşru geri-alma ihtiyacı var). Çekirdek davranış sağlamdı (runtime: veri korunur, URL ile erişilebilir, dosyalar adı gösterir) ama geri-alma yolu yoktu. **Düzeltme (3 dosya):** (1) `muvekkilArsivdenCikar(id)` — arsivlendi:false; (2) MuvekkilDetay: arşivliyken sakin şeftali "Bu müvekkil arşivli…" şeridi + "Arşivden çıkar" düğmesi (durum görünür + geri alınabilir); (3) DosyaDetay künyesinde "Müvekkil" adı artık müvekkile **köprü** — arşivli müvekkil (arşiv yalnız dosyası olanda olur) dosyaları üzerinden erişilebilir. **Runtime doğrulama:** arşivle → dosyadan müvekkil bağlantısıyla eriş → şerit göründü → "Arşivden çıkar" → arsivlendi:false, şerit kayboldu, müvekkil **listeye geri döndü**; konsol temiz, tasarım dili tutarlı (şerit `--cat-amber` bilgi rengi). `npm run build` geçti, **375 test** yeşil. |
| 220 | 2026-08-17 07:40 | **Tüm silme çağlayanları denetlendi + müvekkil-silme düzeltmesi runtime doğrulandı — sağlam.** Geçen turun yetim-kayıt bulgusunu genelleştirmek için HER silme işlemi incelendi. **muvekkilSilVeyaArsivle (runtime):** dosyasız yeni müvekkil + görüşme notu → notlar:1, hareketler:1; müvekkil silinince **hepsi 0, yetim yok** (tur-219 düzeltmesi çalışıyor). **Diğer silmeler (kod):** `finansSil` finans + makbuzları (`belgeler.finansKaydiId`) temizliyor ✓; **makbuz ince bağı GÜVENLİ** — FinansForm makbuzu hem `finansKaydiId` hem `dosyaId` ile yüklüyor (finans zaten dosya-zorunlu), yani `dosyaSil` makbuzları `dosyaId` ile yakalıyor, yetim kalmıyor; dosyasız müvekkilin finansı olamayacağından (finans dosya gerektirir) müvekkil-silmede de makbuz yetimi yok; `olaySeriSil`/`gorevSeriSil` seriyi `seriesId` ile toplu siliyor ✓; `sureSil` süre + bağlı olayı (`olayId`) siliyor ✓; yaprak silmeler (belge/not/kişi/görev/olay) temiz ✓. Sonuç: tur-219 boşlukları kapattı, kalan silmeler zaten tamdı — silme alt sistemi tümüyle yetim-güvenli. Bu tur kaynak değişmedi (doğrulama). `npm run build` geçti, **375 test** yeşil. |
| 219 | 2026-08-17 06:50 | **GERÇEK DÜZELTME — silme çağlayanı yetim `hareketler` (etkinlik günlüğü) bırakıyordu.** Dosya silme veri bütünlüğü uçtan uca sınandı (Playwright, her deponun `dosyaId` referansı sayıldı). **Bulgu:** `dosyaSil` "yetim kayıt bırakma" diyen kendi yorumuna rağmen dosyaya bağlı TÜM depoları temizliyor ama **`hareketler`'i atlıyordu** — silmeden sonra ölçüm: olaylar/sureler/gorevler/finans/belgeler → 0, ama **hareketler → 1 yetim** (silinen dosyanın adı `ayrinti`'de gömülü olarak "Son hareketler" akışında kalıyor; hukuk uygulamasında silinen dosyanın izi = gizlilik/tutarlılık sorunu). **Aynı sınıf ikinci bulgu:** `muvekkilSilVeyaArsivle` silme yolu (dosyasız müvekkil) yalnız müvekkil+notlar temizliyordu; müvekkil-düzeyi hareketler/olaylar/gorevler/finans yetim kalıyordu. **Düzeltme:** `dosyaSil` çağlayanına `hareketler` eklendi; `muvekkilSilVeyaArsivle` silme yoluna `muvekkilId` ile hareketler+olaylar+gorevler+finans eklendi (arşivleme yolu değişmedi). **Runtime doğrulama:** dosya silindikten sonra **hiçbir depoda yetim yok** ("NONE (clean cascade)"); hareketler 1→0. Not: db işlemleri birim testlerde örtülü değil (fake-indexeddb kurulu değil, mevcut testler saf fonksiyon); çağlayan Playwright ile doğrulandı. `npm run build` geçti, **375 test** yeşil. |
| 218 | 2026-08-17 06:00 | **PWA kurulumu + çevrimdışı yeteneği doğrulandı — sağlam (çekirdek gereksinim: iOS ana ekranı + local-first).** Uygulamanın kurulabilirliği ve çevrimdışı çalışması ilk kez uçtan uca sınandı. **Manifest:** geçerli JSON; `start_url:"."` + `scope:"."` (alt-yol/`/MYLOVE/` dağıtımı için göreli), standalone, portrait, tema #16262E, zemin #F7F6F3, lang:tr, 3 ikon (192/512/maskable-512). **Varlıklar:** public/ ve dist/'te ikonlar + apple-touch-icon + favicon mevcut. **iOS meta:** apple-mobile-web-app-capable/title/status-bar-style, theme-color, viewport-fit=cover, apple-touch-icon, manifest bağlantısı — hepsi var. **Dağıtım tabanı:** `BASE_PATH=/MYLOVE/` derlemesinde index.html bağlantıları `/MYLOVE/` önekini alıyor, manifest göreli yollar kullanıyor → GitHub Pages'te 404 yok. **Çevrimdışı (Playwright, SW ile):** service worker **etkin + kontrol ediyor**; ağ kapatılıp yeniden yüklenince uygulama **tümüyle önbellekten açılıyor** (pano render, selam görünür), 25 dosya yerel IndexedDB'den erişilebilir, konsol temiz. **Çevrimdışı şeridi (CevrimdisiUyari):** doğru görünüyor — cloud-off ikonu + sakin şeftali bilgi şeridi (alarm veren kırmızı değil) + "Uygulama cihazınızda çalışmaya devam ediyor" mesajı, içeriğin başında. Yan bulgu (araç, hata değil): `vite preview`'i BASE_PATH'siz çalıştırınca taban uyuşmazlığı 404 verdi — eşleştirilince düzeldi; gerçek dağıtımda sunucu zaten /MYLOVE/. Düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil. |
| 217 | 2026-08-17 05:15 | **Derin dev-mode React uyarı taraması (11 etkileşim fazı) — tamamen temiz.** Geçen tur bulunan takvim uyarısının ardından, ilk taramanın tetiklemediği durumlar dev sunucusunda kapsamlıca sürüldü (Playwright; her faz try/catch ile yalıtık, konsol yakalayıcı sürekli): çekmece aç/kapa, tekrar serisi formu (TekrarSecici sıklık + adet + tüm-gün toggle), finans koşullu alan + ödeme-durumu geçişleri (Kısmi/Ödendi/Bekliyor), silme-onayı geçişleri (Sil→Vazgeç), arama (sorgu + temizle), süre hesap sonucu, görev süzgeçleri (Aktif/Bana/Öncelikli/Tamamlanan), müvekkil görüşme metin alanı, bildirim erteleme çipi. **Sonuç: 0 console.error, 0 console.warn, 0 pageerror** — hiçbir etkileşim (kontrollü/kontrolsüz giriş, dinamik liste anahtarı, koşullu alan, durum toggle) uyarı üretmiyor. Tur-216'daki camelCase düzeltmesinden sonra uygulama dev modunda da baştan sona temiz. Bu tur kaynak değişmedi (doğrulama). `npm run build` geçti, **375 test** yeşil. |
| 216 | 2026-08-17 04:30 | **GERÇEK DÜZELTME — takvimdeki camelCase data-özniteliği React uyarısı (`data-baskaAy`).** Bugüne dek hiç yapılmamış bir tarama: **dev sunucusunda** (üretim derlemesi React dev uyarılarını siler; hep onu test ediyordum) tüm ekranlar + onboarding + tur boyunca `console.error/warn` + `pageerror` + başarısız istek yakalandı. Tek uyarı çıktı: "React does not recognize the `data-baskaAy` prop on a DOM element" — Takvim ay ızgarasında (`AyGorunumu`, Takvim.tsx:148) komşu-ay günlerini işaretleyen öznitelik camelCase (`data-baskaAy`) yazılmıştı; React yalnız küçük harf `data-*`'ı tanır. **Etki:** her takvim render'ında konsol uyarısı + kırılganlık (DOM özniteliği küçük harfe indirdiği ve CSS öznitelik adı büyük/küçük harf duyarsız olduğu için gri stil *bugün çalışıyordu*, ama JS `getAttribute('data-baskaAy')` null döndürürdü). **Düzeltme:** hem TSX (Takvim.tsx:148) hem CSS (takvim.css:134) `data-baskaAy` → `data-baska-ay` (kardeşleri `data-bugun`/`data-secili` ile tutarlı kebab-case). **Doğrulama (dev):** tanınmayan-öznitelik uyarısı artık **0**; komşu-ay günü grileme korundu (27 → `#6b7170`, opaklık 0.55). Uygulamanın tek konsol uyarısıydı; artık dev'de tamamen temiz. `npm run build` geçti, **375 test** yeşil. |
| 215 | 2026-08-17 03:45 | **Tasarım referansı sadakat denetimi tamamlandı — görev listesi + dosya sağlığı bölümleri (sağlam, yönerge #3).** Geçen turun hero+çekmece karşılaştırmasına ek olarak iki gerçek pano bölümü referansla eşleştirildi. **04-task-list:** "İŞ LİSTESİ / Bugün yapılacaklar / Hepsini gör" başlığı; onay kutusu + başlık + dosya alt-satırı + sağda tarih rozeti (BUGÜN/YARIN/tarih) satırları; tamamlanan görevler işaretli + üstü çizili; kesikli "+ Yeni görev ekle" — birebir uyumlu. **05-file-health:** "DOSYA SAĞLIĞI / Hazırlık durumu"; dosya başına satır: ad + yüzde (sağ) + müvekkil·mahkeme alt-satırı + renkli ilerleme çubuğu + sonraki-adım/durum etiketleri ("İyi ilerliyor"/"Dikkat gerekiyor") — yapı birebir; yüzdeler (ref 82/64/47 → şimdi 73/73/77) yalnızca tohum verisi evrimi yüzünden farklı, görsel dil aynı. **Sonuç:** 9 referanstan 4'ü (hero, çekmece, görev listesi, dosya sağlığı — tüm ayrık bölüm tiplerini kapsıyor) doğrudan karşılaştırıldı; hepsi sadık. Kalan referanslar ya doğrulananların alt-parçası (02/03 istatistik+süreler, 06 aktivite alt bilgisi) ya da uygulamanın artık tam kurduğu prototip yer-tutucuları (08 raporlar, 09 takvim). İstenmeyen sapma yok; düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil (yalnızca günlük değişti). |
| 214 | 2026-08-17 03:05 | **Tasarım referansına (docs/design-reference/*.jpg) sadakat denetimi — sağlam (yönerge #3).** Render edilen arayüz, kaynak görsel dille (Replit prototip kareleri) karşılaştırıldı. **01-dashboard-hero:** birebir uyum — yeşil degrade hero, serif "Av. Ece" başlığı, mono tarih eyebrow, "Güncelle" düğmesi ve dört istatistik kutusu (Aktif dosya 24 / Duruşma 8 / Bekleyen görev 17 / Tahsilat ₺184.500) aynı renkli noktalarla ve değerlerle. Tek fark: statik "Günaydın" yerine **saate duyarlı** selam (İyi günler/akşamlar) — geliştirme, sapma değil. **07-sidebar-drawer:** koyu inverse çekmece, "Juris CALENDAR" başlığı, "ÇALIŞMA ALANI" grubu, aktif "Genel bakış" (aksan çubuğu + yeşil nokta), "Sistem hazır" kartı, Ayarlar, profil alt bilgisi — uyumlu. İki bilinçli evrim: (a) uygulama 5 → 8 gezinme öğesine büyüyünce **"YÜRÜTME" grubu** eklendi (Görevler/Finans/Asistan/Raporlar); (b) "Sistem hazır" metni yerel-öncelikli gerçeğe göre düzeltildi (referanstaki "senkronize" yerine "cihazda saklanıyor, bağlantı gerekmiyor"). Prototipin "Made with Replit"/"Build for free" rozetleri uygulamada doğru şekilde yok. Görsel dil sadık korunmuş; istenmeyen sapma yok, yalnız kasıtlı iyileştirmeler. Düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil (yalnızca günlük değişti). |
| 213 | 2026-08-17 02:20 | **Görev tamamlama/geri-alma döngüsü doğrulandı — sağlam (en sık etkileşim).** Uygulamanın en çok kullanılan etkileşimi uçtan uca sınandı (Playwright, IndexedDB sayımıyla). **Tamamla:** görev onay kutusuna tıklandı → tamamlanan 4→5, bekleyen 17→16, `gorev-tamamlandi` hareketi 1→2 (üçü de doğru). **Geri al:** "Tamamlanan" süzgecinden aynı görev geri açıldı → bekleyen 17'ye geri döndü, hareket 2'de kaldı — **geri alma çift hareket eklemiyor** (yalnız tamamlamada loglanıyor, `gorevDurumunuDegistir` `if (!tamamlandi)` koşulu doğru). `tamamlanmaTarihi` tamamlamada set, geri almada temizleniyor. Tamamlanan görev üstü-çizili gösterim önceki ekran görüntülerinde görülmüştü; pano "Bekleyen görev" sayacı aynı `useLiveQuery` deposundan tepkisel okuyor. Konsol temiz, düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil (yalnızca günlük değişti). |
| 212 | 2026-08-17 01:35 | **Para ayrıştırma (`metindenKurus`) + FinansForm bağlantısı doğrulandı — sağlam (finansal doğruluk).** Hata-eğilimli para girişi hem kod hem runtime'da denetlendi. **Saf fonksiyon:** ~15 uç durum implementasyona karşı izlendi — Türkçe (1.250,50), İngilizce (1,234.56), binlik-vs-ondalık ayrımı (ayraç sonrası ≤2 rakam = ondalık; "1.250"=1250, "1.05"=1,05), negatif (-1250,50), para simgesi/boşluk, geçersiz (''/'abc'/'.'/'-'→null); hepsi doğru ve **kapsamlı birim testli** (para.test.ts). **Runtime bağlantı (Playwright):** FinansForm'a "1.250,50" tutar + "500,25" ödenen (Kısmi) girildi → depoda `tutar:125050`, `odenenTutar:50025`, `odemeDurumu:'kismi'` — **birebir** (Türkçe binlik-nokta + ondalık-virgül kayıpsız ayrışıyor). Yan bulgu (tasarım kararı, hata değil): FinansForm dosya zorunlu kılıyor ("Dosya seçilmeli." — finans dosya-bazlı). Düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil (yalnızca günlük değişti). |
| 211 | 2026-08-17 00:50 | **Süre hesapla → dosyaya kaydet → yayılım (çapraz-özellik entegrasyonu) doğrulandı — sağlam.** Hesaplayıcının kalanla entegrasyonu uçtan uca sınandı (Playwright). Demir İnşaat dosyasında `?dosya=` ön-seçili SureHesapla'da İstinaf (HMK m. 345, 2 hafta), tebliğ 2026-10-05 → ekranda "19 Ekim 2026 · 63 gün kaldı". "Dosyaya kaydet" → `/dosyalar/demir-insaat`'a gidildi. **Doğrulama:** (1) süre sayısı dosya için 1→2, toplam 3→4; (2) kaydedilen süre `baslangic 2026-10-05 / ham 2026-10-19 / son 2026-10-19` — ekrandaki değerle **birebir**, HMK m. 92/2 doğru (Pazartesi+2hafta=Pazartesi, adli tatil/hafta sonu kayması yok); (3) Süreler sekmesi artık 2 kayıt, yenisi "İstinaf başvuru süresi · HMK m. 345 · 19 Eki · 63 gün kaldı" (tamamla kutusu + sil ×); (4) ajandada görünüyor; (5) `sure-hesaplandi` hareketi loglandı; (6) uyarı metni mevcut. `sureKaydet` süre+hareketi atomik yazıyor; süre ayrı olay üretmeden takvim/ajanda/ICS/hatırlatma tarafından okunuyor (tek kayıt, çok görünüm). Ekran↔kayıt↔sekme tutarlı; düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil (yalnızca günlük değişti). |
| 210 | 2026-08-17 00:05 | **Bildirim erteleme (snooze) + geri alma akışı doğrulandı — sağlam (tarih aritmetiği).** Tarih matematiği içerdiği için hata-eğilimli olan erteleme akışı runtime'da sınandı (Playwright). Seçenekler: 1 saat (now+1h), 3 saat (now+3h), Yarın (ertesi gün 09:00 yerel), 1 hafta (now+7g). **Test:** "Şimdi" bölümündeki 16 hatırlatma; birine "3 saat" tıklandı → `hatirlatmaErtelemeleri`'ne hedef yazıldı, hesaplanan fark **tam 180 dakika** (doğru); ardından "Ertelemeyi geri al" → kayıt temizlendi ({}'e döndü). **Sağlamlık:** `hatirlatmaErtele` yeni hedefi saklarken süresi geçmiş ertelemeleri de çöp-topluyor (harita sınırsız büyümez). Konsol temiz, hata yok. Erteleme mekanizması ve geri alma doğru; düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil (yalnızca günlük değişti). |
| 209 | 2026-08-16 23:20 | **ICS dışa aktarımın metin kaçışı + tüm-gün saat dilimi işleyişi doğrulandı — sağlam.** Geçen turun CSV bulgusunun ICS'teki eşdeğeri (yapılandırılmış çıktıya kullanıcı metni) denetlendi. **Metin kaçışı (metinKacis, RFC 5545):** `\`→`\\` (önce), `;`→`\;`, `,`→`\,`, satır sonu→`\n`; SUMMARY/LOCATION/DESCRIPTION üçüne de uygulanıyor. Özel karakterli olay (başlık "Duruşma, acil; C:\dosya", yer "Kat: 3, Oda; 12") oluşturulup ICS indirildi: `SUMMARY:Duruşma\, acil\; C:\\dosya`, `LOCATION:Kat: 3\, Oda\; 12` — virgül/noktalı virgül/ters eğik çizgi doğru kaçışlı, iki nokta (`:`) doğru şekilde kaçışsız; içe aktarmayı bozacak ham ayraç yok. **Tüm-gün saat dilimi:** tüm-gün olayı `yerelGun` ile yerel güne yazılıyor — 15 Eyl (İstanbul) olayı `DTSTART;VALUE=DATE:20260915` (bir gün erken 0914 DEĞİL), `DTEND;VALUE=DATE:20260916` (RFC 5545 dışlayıcı ertesi gün); klasik "tüm-gün bir gün erken görünür" hatası önlenmiş. Zamanlı olaylar UTC `Z` (geçen tur). Konsol temiz. ICS içe-aktarım yolu (uygulamanın çekirdek değeri) tümüyle sağlam; düzeltme gereken bulgu yok. `npm run build` geçti, **375 test** yeşil (yalnızca günlük değişti). |
| 208 | 2026-08-16 22:40 | **Dışa aktarım (ICS + CSV) doğrulandı; GERÇEK DÜZELTME — CSV'ye eksik "dosya bazlı" bölüm + formül-enjeksiyonu koruması eklendi.** İki dosya-çıktı akışı gerçek indirmeyle yakalanıp içerikleri denetlendi (Playwright). **ICS (sağlam):** `juriscalendar-takvim-2026-08-16.ics` — CRLF satır sonları, dengeli 16 VEVENT, VERSION:2.0 + PRODID, doğru alanlar (UID/DTSTAMP/SUMMARY/DTSTART/DTEND/LOCATION/CATEGORIES), zamanlı olaylar UTC `Z` damgalı (07:30Z = 10:30 İstanbul, UTC+3 doğru); RFC 5545 uyumlu. **CSV:** UTF-8 BOM + `;` ayraç + virgül ondalık (Türkçe Excel) doğru. **Bulunan hata:** `raporCsv` ekranda görünen **"Dosya bazlı gelir-gider"** bölümünü dışa aktarmıyordu (yalnız aylık + kategori) — muhasebe için en işe yarar kırılım eksikti. **Düzeltme:** üçüncü bölüm "Dosya;Gelir (TL);Gider (TL)" eklendi (ekranla tutarlı). Dosya başlığı SERBEST METİN olduğundan yeni `csvHucre` kaçışlayıcısı: `;`/tırnak/satır sonu için RFC 4180 tırnaklama **ve** `=`,`+`,`-`,`@` ile başlayanlar için baştaki tek tırnakla **CSV formül-enjeksiyonu** etkisizleme (elektronik tabloda kod çalışmasını önler — kullanıcı verisi ihracatı için gerçek güvenlik sertleştirmesi). **+3 test** (bölüm var, `;`/tırnak kaçışı, formül enjeksiyonu). Runtime doğrulandı: CSV artık 3 bölüm içeriyor, dosya gelirleri ekranla eşleşiyor. `npm run build` geçti, **375 test** yeşil. |
| 207 | 2026-08-16 21:00 | **Şifreli yedek → temizle → geri yükle turu uçtan uca doğrulandı — sağlam (kritik veri güvenliği).** Yerel-öncelikli/sunucusuz bir hukuk uygulamasında en kritik akış (veri kaybı riski) ilk kez tam sınandı (Playwright, gerçek indirme/yükleme). **Adımlar:** (1) tohum verisi (27 dosya / 24 müvekkil / 13 olay / 21 görev / 40 finans); (2) Ayarlar→Yedekleme, parola girip "Şifreli yedek indir" → tarih damgalı `juriscalendar-yedek-2026-08-16.jcenc` (AES-GCM); (3) tüm veri tabloları IndexedDB ile silindi (hepsi 0); (4) "Geri yükle" ile dosya seçildi → "Bu yedek şifreli. Parolayı girin:" istemi (temiz satır-içi form); (5) **yanlış parola → "Parola yanlış ya da dosya bozuk."** (net hata); (6) **doğru parola → tüm 5 tablo birebir geri geldi** (27/24/13/21/40), tur-başarı reload'u değerleri kontrolsüz alanlara yansıtıyor. Konsol temiz. Şifreleme birim testleriyle (kripto.test) zaten örtülüydü; bu tur UI round-trip'ini kanıtladı. Düzeltme gereken bulgu yok. `npm run build` geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 206 | 2026-08-16 20:15 | **Rehberli turun 8 adımı uçtan uca yürütüldü — sağlam.** Her yeni kullanıcının gördüğü ama şimdiye dek yalnız 1. adımı doğrulanan akış tümüyle sürüldü (Playwright, turu atlamadan İleri→…→Bitir). **8 adım:** 1 "Gününüz burada başlar" (pano), 2 "Her yere buradan" (menü), 3 "Menüden Asistan'a" (çekmece), 4 "Gerçek panel: Dosyaya danışın" (→/asistan), 5 "Yeni dava geldiğinde" (→/dosyalar, FAB), 6 "Duruşma ve süreler" (→/takvim), 7 "Süreyi hesaplayın" (→/sure), 8 "Hazırsınız" (ortalı, Bitir). **Doğrulama:** her adımda balon **görünüş alanı içinde** (ekran dışına taşan yok), tur sayfalar arası doğru geziniyor, 3. adımda çekmeceyi otomatik açıp "Asistan" öğesini teal halkayla spotlight'lıyor, son adım ortalı ve "Ayarlar → Yardım'dan yeniden başlatın" ipucu gerçekle uyumlu, Bitir'de tur temiz kapanıyor (balon kalmıyor), konsol temiz (0 hata). Bu turda düzeltme gereken bulgu yok. `npm run build` geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 205 | 2026-08-16 19:30 | **PIN kurulum + kilit ekranı akışı denetlendi; GERÇEK DÜZELTME — yanlış "Kilidi aç" etiketi.** İlk kez uçtan uca sınanan güvenlik yüzeyi (Playwright): Ayarlar→Güvenlik→PIN belirle→1234/1234→kaydet→yeniden yükle→kilit ekranı→yanlış PIN→doğru PIN. **Güvenlik doğrulandı:** kilit ekranı (`.kilit-ekran`) `position:fixed; inset:0`, 390×844, **opak** `--surface-inverse` zemin, z-index 70; arka plan içeriği (`.kilit-icerik`) `inert` — yani hem görsel olarak kapalı hem etkileşimsiz (müvekkil verisi korunur). fullPage ekran görüntüsündeki pano yalnızca dikiş artefaktıydı; gerçek görünümde sadece PIN paneli var. Yanlış PIN → "PIN yanlış, tekrar deneyin." (tur-193'te token'a alınan `--danger-on-inverse` rengiyle, gerçek yolda doğrulandı); doğru PIN → panoya açılıyor. **Bulunan hata (içerik/açıklık):** PIN KURULUM düğmesi "Kilidi aç" yazıyordu — oysa eylem kilidi *etkinleştiriyor* (`kilitEtkin:true`); "aç" ise uygulamada (biyometri "…ile aç", kilit ekranı) *kilit açma* anlamında kullanılıyor → çelişki. **Düzeltme:** "Kilidi aç" → **"Kilidi kur"** (eylemle uyumlu, "aç"=kilit-açma tutarlılığı korunur). Runtime doğrulandı: düğme artık "Kilidi kur", "Kilidi aç" kalmadı. `npm run build` geçti, **372 test** yeşil. |
| 204 | 2026-08-16 18:05 | **DosyaDetay sekmeleri + satır-içi ekleme durumları denetlendi — sağlam.** Geçen turun bulduğu hatanın (çok-düğmeli satır) benzerini aramak için DosyaDetay'ın 7 sekmesi ve satır-içi formları test edildi (Playwright, 390px). **Sekmeler:** Genel, Duruşmalar, Süreler, Görevler, Belgeler, Finans, Notlar — hepsinde `docW==390` (sayfa taşması yok; görünürdeki taşanlar yalnızca bilinçli kaydırılabilir `.tab-row` düğmeleri). **Satır-içi formlar:** (a) İlgili kişiler — "Ekle"/"Vazgeç ×" ile açılan tek-kolon form (kişi adı + rol açılırı + tam-genişlik "Kişiyi ekle"), düğme çakışması yok; (b) Notlar — metin alanı + tam-genişlik "Not ekle"; (c) Belge etiketleri — `.belge-etiket-giris` girişi (buton değil input; placeholder "+ etiket"), yazıp Enter'a basınca etiket çip olarak ekleniyor (kaldır × ile), giriş açık kalıyor — çalışıyor, çip+giriş yerleşimi temiz. Konsol temiz, hata yok. Bu turda düzeltme gereken bulgu yok; DosyaDetay (sekme-yoğun, satır-içi form zengini) sağlam kurulmuş. `npm run build` geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 203 | 2026-08-16 17:20 | **Etkileşimli durum denetimi (çekmece / satır-içi yeni müvekkil / finans ödeme durumları) — sağlam.** Geçen turun hatası bir durum geçişinde (silme onayı) gizliydi; aynı mantıkla üç yeni durum test edildi (Playwright, 390px). **(1) Gezinme çekmecesi (ilk kez):** gruplu (Çalışma alanı / Yürütme), aktif öğe aksan çubuğu + yeşil noktayla vurgulu, ikonlar, "Sistem hazır" çevrimdışı kartı, alt bilgide kullanıcı profili — koyu inverse yüzey, temiz. **(2) DosyaForm satır-içi "+ Yeni müvekkil ekle":** seçilince koşullu "Yeni müvekkil adı" alanı açılıyor, doğru konumda, taşma yok. **(3) FinansForm ödeme durumu:** Bekliyor/Kısmi/Ödendi bölümlü kontrol; "Kısmi" seçilince koşullu **"Ödenen tutar"** alanı beliriyor (kalan = tutar − ödenen türetiliyor, `metindenKurus`), "Vade (bekleyen)" kalan bakiye için gösteriliyor. Üç durumda da `docW==390` (0 taşma), konsol temiz. Bu turda düzeltme gereken bulgu yok. `npm run build` geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 202 | 2026-08-16 16:35 | **GERÇEK DÜZELTME — tekrar serisi silme onayında bozuk düğme yerleşimi.** Tekrar eden olay/görev akışı uçtan uca test edilirken (haftalık, 4 kez → tam +4 kayıt, 1 seri; düzenleme "Bu kayıt 4 kayıtlık… serinin parçası" + "Tüm seri (4)"; tüm seri silme → 4'ü de gitti, 17→13; hepsi doğru) **görsel bir hata yakalandı:** seri kaydının silme onayında `.form-actions` (flex satır) üç düğme taşıyordu — birincil "Değişikliği kaydet" (`flex:1`) + "Yalnızca bu" + "Tüm seri (N)" (`flex:none`) — 390px'e sığmayınca birincil düğme okunmaz bir dar sütuna çöküp etiketi **harf harf** sarıyordu ("De ğişi kliğ i ka yd et"). **Kök neden:** silme onayında kaydet düğmesinin gizlenmemesi (OlayForm + GorevForm; seri olmayan DosyaForm/MuvekkilForm 2 düğmeyle sığıyordu ama aynı desen). **Düzeltme:** `silmeOnayi` açıkken birincil kaydet düğmesi gizlenir (dört formda da tutarlı; kaydet-vs-sil çakışan niyeti de kaldırır, "Vazgeç" ile geri gelir). **Runtime doğrulama (Playwright ekran görüntüsü):** onay artık yalnız "Yalnızca bu" + "Tüm seri (4)"yi temiz, yan yana, okunur gösteriyor; seri oluşturma/silme hâlâ çalışıyor, konsol temiz. `npm run build` geçti, **372 test** yeşil. |
| 201 | 2026-08-16 14:30 | **Klavye-yalnız gezinme denetimi (WCAG 2.1.1/2.4.3) — görünürde bir sıra anomalisi araştırıldı, tasarımın doğru olduğu doğrulandı.** Otomatik DOM taramalarının yakalamadığı bir alan: Tab sırası, erişilebilirlik, görünür odak, tuzak. **İlk gözlem (yanlış alarm):** ilk 12 Tab durağı içerikle başlıyor; üst çubuk (menü/ara/bildirim) ve atlama bağlantısı görünmüyordu → "üst çubuk erişilemez mi?" şüphesi. **Derin araştırma:** AppShell DOM sırası aslında doğru (`.skip-link` → `.topbar` → `<main id="ana-icerik">`); sapma, gezinmede odağı `#ana-icerik`'e taşıyan bilinçli efektten (AppShell.tsx:42–61) kaynaklanıyor — yükleme/gezinme sonrası odak ana içerikte başlıyor (atlama bağlantısının yapacağı şeyin aynısı). **Kanıt (Playwright):** yüklü durumdan tek **Shift+Tab** sırayla Bildirimler → Ara → sürüm → "Menüyü aç" → "İçeriğe geç"e ulaşıyor; ileri Tab içeriğe giriyor, ileri-sarma da üst çubuğa varıyor. Tüm duraklar `:focus-visible` çerçeveli (görünür odak), Tab/Shift+Tab çift yönlü çalışıyor (tuzak yok), form alan sırası da mantıklı (geri→ad→tür çipleri→telefon→…→ekle). Sonuç: örnek nitelikte klavye erişilebilirliği; düzeltme gereken bulgu yok (ilk şüphe, odağın nerede *başladığının* yanlış okunmasıydı, DOM sırası değil). `npm run build` geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 200 | 2026-08-16 13:45 | **Takvim hafta görünümü (ilk kez görsel denetim) + süre hesaplama motoru hukuki doğruluğu/test kapsamı — sağlam.** **(1) Hafta görünümü (Playwright):** daha önce hiç bakılmamış yüzey; mobil-uygun tasarım — yatay hafta şeridi (Pazartesi-başlangıçlı PZT 10 → PAZ 16, bugün koyu pille + olay noktalarıyla vurgulu, hafta sonu başlıkları aksan renginde) + ay görünümüyle aynı seçili-gün detay kartı. Telefona sığmayan saat-ızgarası zorlanmamış; etkileşim modeli tutarlı. `docW==390`, 0 taşma; Ajanda da yeniden doğrulandı (temiz). **(2) Süre motoru:** `sureHesabi.ts` kuralları Türk usul hukukuna sadık — doğru HMK madde atıfları (m. 345 istinaf, m. 361 temyiz, m. 136 cevap/cevaba cevap, m. 281 bilirkişi, m. 92/93 sayım, m. 104 adli tatil) ve açıklamalar. **17 kapsamlı birim test** tam da kritik uç durumları örtüyor: tebliğ günü sayılmaması, son günün hafta sonu/resmî tatile kayması, adli tatil→7 Eylül uzaması (ve 7 Eylül'ün kendisi hafta sonuna denk gelirse tekrar kayması), adli tatile tabi olmayan icra süresinin uzamaması, ayda olmayan güne denk gelince ay sonuna sabitlenmesi, yıl süresi, katalog bütünlüğü (31 kural, benzersiz kimlik) ve "kaydırılmış son tarih her zaman çalışma günü" değişmezi. Çekirdek hukuki hesap titizlikle doğrulanmış; düzeltme gereken bulgu yok. `npm run build` geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 199 | 2026-08-16 13:00 | **Boş durumlar (empty states) görsel denetimi + tohum-koruması runtime doğrulaması — sağlam.** Bugüne dek hiç görsel denetlenmemiş bir sınıf: tohum verisi olmayan hesap. Onboarding sonrası veri tabloları (muvekkiller/dosyalar/olaylar/sureler/gorevler/finans/belgeler/kisiler/notlar/hatirlatmalar/hareketler) ham IndexedDB ile temizlenip `ayarlar`+`kullanicilar` korundu, sonra **tam yeniden yükleme** yapıldı. **Tohum koruması doğrulandı:** yeniden yüklemede ne onboarding döndü ne demo dirildi (`tohumlaGerekiyorsa`: dosya=0 ama `profilKuruldu` true → yeniden tohumlamaz) — "son dosyasını silen kullanıcıya 24 sahte dosya geri gelmez" güvencesi runtime'da kanıtlandı. **Boş durumlar (Playwright ekran görüntüsü, hepsi temiz):** **Pano** — istatistik kutuları bağlamlı 0 ("bu ay yeni yok", "bugün yok", "öncelikli yok", "karşılaştırma yok"), her bölüm insani boş mesaj ("Şu an açık bir hukuki süre yok.", "Önümüzdeki üç günde planlanmış bir işlem yok.", "Bekleyen görev yok.", "Henüz açık dosya yok.", "Henüz bir hareket kaydedilmedi."); **Raporlar** — grafikler zarifçe boşalıyor: çubuk grafiği eksen/açıklama ile boş ızgara (NaN/kırık SVG yok), halka gri boş halka + merkez "0 AÇIK SÜRE"; **Dosyalar** — süzgeç-farkında "Bu süzgeçte dosya yok." + FAB; **Takvim** — gün başlığında "boş" rozeti + "Bu gün için kayıt yok."; **Asistan** — güven veren "Dosyalarınız düzenli.", Sor düğmesi dosya seçilene dek pasif. Düzeltme gereken bulgu yok. `npm run build` geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 198 | 2026-08-16 12:15 | **Onboarding akışının görsel denetimi (4 adım) + Google/Apple butonlarının çıkmaz-yol olmadığı doğrulandı — sağlam.** İlk-izlenim yüzeyi bugüne dek yalnızca programatik sürülmüş, hiç görsel denetlenmemişti; dört adım ekran görüntüsüyle incelendi (Playwright, 390px, hepsi `docW==390` / 0 taşma / konsol temiz): **(1) Hukuki onay** — kaydırılabilir metin (F18 gereği bilinçli "[YER TUTUCU — nihai metin avukat tarafından hazırlanacaktır]"), kutular metin sonuna kaydırılmadan etkin olmuyor, 5 açık onay maddesi (KVKK/şifre/maskeleme/AI/cihaz güvenliği), tümü işaretlenmeden "Kabul et ve başla" pasif — bilgilendirilmiş onay deseni doğru. **(2) Yöntem seçimi** — E-posta (birincil) + Google + Apple + "yalnızca bu cihazda saklanır" notu. **(3) Kayıt formu** — avatar, "Sizi tanıyalım.", Ad/Soyad, Ünvan (Avukat ön-dolu), e-posta (isteğe bağlı), Devam/Geri. **(4) Tur** — spotlight deseni: 1. adımda hero tam parlak, gerisi karartılmış; balon 1/8 sayaç + "Turu kapat" + İleri. **Çıkmaz-yol kontrolü (F19 kapılı OAuth):** Google/Apple tıklanınca sessiz kalmıyor — çalışma zamanında "Google/Apple ile giriş henüz yapılandırılmadı (kurulum gerekiyor)." notu (`.kayit-not`) beliriyor (KayitEkrani.tsx). Düzeltme gereken bulgu yok. `npm run build` bu turda geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 197 | 2026-08-16 11:30 | **Form doğrulama akışı (runtime) + takvim ay ızgarası nokta-sınırı denetlendi — sağlam.** Daha önce çalışma zamanında sınanmamış iki alan: **(1) Form doğrulama (Playwright):** dört form boş gönderildiğinde beklenen `role="alert"` hataları çıkıyor ve gönderim engelleniyor (URL formda kalıyor) — Müvekkil "Ad girilmeli.", Dosya "Dosya başlığı girilmeli.", Görev "Görev başlığı girilmeli.", Olay tarihsiz "Tarih girilmeli."; OlayForm bitiş<başlangıç → "Bitiş saati başlangıçtan önce olamaz." (HH:MM sıfır dolgulu olduğundan dizgi karşılaştırması doğru; aynı saat/tek saat/tüm gün doğru şekilde muaf). Hata metni gönder düğmesinin hemen üstünde, kırmızı, okunur; odaklanınca "İçeriğe geç" atlama bağlantısı beliriyor (WCAG). **(2) Takvim ay görünümü:** gün hücrelerindeki olay noktaları `ogeler.slice(0, 3)` ile **en fazla 3** ile sınırlı — yoğun gün hücreyi taşırmıyor; buna karşın hücrenin `aria-label`'ı **tam** sayıyı duyuruyor (`${ogeler.length} kayıt`), ekran okuyucu 3'ten fazlasını da biliyor. Pazartesi-başlangıçlı ızgara (TR doğru), bugün vurgusu, hafta sonu başlık ayrımı, seçili gün detay listesi, FAB kümesi ve dışa-aktarım hepsi düzgün; `docW==390`, konsol temiz. Düzeltme gereken bulgu yok. `npm run build` bu turda geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 196 | 2026-08-16 10:30 | **Görsel denetim (Ayarlar / SureHesapla / Bildirimler / Ara) + daha önce atlanan arama sayfası a11y kapsamı kapatıldı.** Karmaşık, yakın zamanda görsel denetlenmemiş yüzeyler ekran görüntüsüyle incelendi (Playwright, 390px): **Ayarlar** (profil, yazı boyutu, iki hatırlatma-zamanı grubu, bildirim kanalları + YAPILANDIRILMADI rozetleri, sessiz saatler, LLM, yedekleme, güvenlik/KVKK, sıfırlama) — hiyerarşi ve çipler tutarlı; **SureHesapla** sonuç kartı — istinaf (HMK m. 345) tebliğ 1 Ağu → ham son gün 15 Ağu → **adli tatile denk geldiği için** 7 Eylül'e uzatıldı (HMK m. 104), `accent-purple` doğru, uyarı mevcut; **Bildirimler** — "Şimdi/Yaklaşan" grupları, erteleme çipleri, izin kartı; **Ara** — depolama kartı + kapsam ipucu, "Kaya" → 2 sonuç (Dosyalar·1 + Müvekkiller·1) türe göre gruplu. Dört yüzeyde de `docW==390` (0 taşma), konsol temiz. **Araç düzeltmesi + kapsam:** önceki turların a11y/dokunma taramaları arama için yanlışlıkla `/arama` (404) kullanmış — gerçek rota `/ara` (routes.tsx; topbar araması AppShell.tsx:107 `navigate('/ara')`). Böylece arama sayfası o taramalarda hiç denetlenmemişti. **Kapatıldı:** `/ara` sonuçlu hâlde denetlendi — erişilebilir adı olmayan **0** kontrol, **0** sub-24px hedef, **0** başlık atlaması, **0** yinelenen id. Uygulama hatası yok; yalnızca test yolu düzeltmesi. `npm run build` bu turda geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 195 | 2026-08-16 08:35 | **Patolojik uzun içerik + grafik + para gösterimi denetimi (Playwright, ekran görüntüsüyle) — hepsi sağlam.** Varsayılan tohum verisinin gizlediği hatalar için aşırı uzun içerik üretildi: ~125 karakterlik kurumsal unvan ("Abdülkerimoğulları Hacımehmetzade Konstantinopolisoğlu … Anonim Şirketi"), boşluksuz 60 harflik stres kelimesi ve çok uzun e-posta ile müvekkil+dosya oluşturuldu. **(1) Taşma:** müvekkil listesi, dosya listesi ve iki detay sayfasında `docW==390` — **sayfa düzeyinde 0 taşma** (yalnızca beklenen kaydırılabilir `tab-row`). Listede başlık/alt satır elipsle kısalıyor; detay hero'su ve iki kolonlu bilgi ızgarası (müvekkil/karşı taraf/mahkeme/konu) boşluksuz kelimeyi bile `word-break` ile kırıp sarıyor, kırpma/taşma yok; uzun e-posta kendi kolonunda sarıyor. **(2) Raporlar grafikleri:** elle çizilen SVG çubuk grafiği (Gelir/gider — doğru oranlar, açıklama, ay etiketleri okunur), halka grafiği (Açık süreler — kritik/yakın/ileri dilimleri + merkez sayaç + açıklama) ve yatay Gider-dağılımı çubukları oransal; hepsi düzgün, uyarı ve CSV dışa-aktarım mevcut. **(3) Finans para gösterimi:** binlik ayraç, ondalık virgül, ₺ simgesi, +/- işaret ve yeşil/kırmızı (tahsilat/gider) kodlaması tutarlı; uzun başlıklar elipsle kısalıyor. Konsol tüm sayfalarda temiz (0 hata). Düzeltme gereken bulgu yok. `npm run build` bu turda geçti, **372 test** yeşil (yalnızca günlük değişti). |
| 194 | 2026-08-16 08:05 | **Dürüst doğrulama turu — yasal uyarı kapsamı, erişilebilir ad, yapısal a11y, yönlendirici, aksan-değişken sistemi ve FAB boşluğu: hepsi sağlam.** Somut hata aranan altı sınıf denetlendi, düzeltme gerektiren bulgu çıkmadı: **(1) Yasal uyarı (CLAUDE.md sert kuralı "her ekranda/hesap çıktısında görünür"):** hesaplanmış süre gösteren 8 ekranın hepsinde mevcut — GenelBakis, Takvim, Dosyalar, DosyaDetay, SureHesapla, Bildirimler, Raporlar **ve** Asistan (ilk grep `teyit` ararken Türkçe yumuşamayı — teyit→teyidi — kaçırmıştı; Asistan alt bilgisinde "süre teyidi kullanıcının sorumluluğundadır" var). OlayForm muaf: elle giriş, hesap değil; ürettiği olaylar zaten uyarılı ekranlarda görünür. **(2) Erişilebilir ad (Playwright):** 17 ekranda erişilebilir adı olmayan **0** etkileşimli kontrol. **(3) Yapısal a11y (Playwright):** `html lang="tr"`; 17 ekranda **0** başlık seviyesi atlaması, **0** yinelenen id, **0** etiketsiz form girdisi. **(4) Yönlendirici:** her yol değişiminde başa sarma, rota başlıkları (WCAG 2.4.2), yola göre anahtarlı taze mount (bayat form verisini önler). **(5) `--acc-*` aksan değişkenleri:** tüm tüketiciler (`.type-chip`/`.type-chip-icon`, `.row`, `.money-cell`, `.file-row`, `.sonuc-*`) bir `.accent-*` kapsamı altında — fallback'siz `var(--acc-text)` sessizce kırılmıyor; SureHesapla sonuç kartı `accent-purple/blue` (ikisi de tanımlı) ile geliyor. **(6) FAB boşluğu:** FAB'li 5 sayfanın hepsinde `.fab-spacer` var; Takvim iki düğmeli `fab-cluster` için daha yüksek `.fab-spacer-cluster`'ı (140px) doğru kullanıyor — son satır kapanmıyor. `npm run build` bu turda geçti, **372 test** yeşil (kaynak değişmedi, yalnızca günlük). |
| 193 | 2026-08-16 07:35 | **320px yatay taşma taraması (temiz) + kilit ekranı hata renginin token'a alınması.** Mobil öncelikli PWA için en dar desteklenen genişlikte (320px, iPhone SE) yatay taşma sınıfı tarandı (Playwright): TÜM ekranlarda `document.scrollWidth == 320` (sayfa düzeyinde **0px taşma**). Görünürde taşan öğelerin hepsi ya bilinçli yatay kaydırılan kaplara ait (Raporlar çip satırı `chip-row` overflow-x:auto sw525>cw246; DosyaDetay sekme şeridi `tab-row` sw615>cw320 — "Serbest"/"Notlar" erişilebilir, kırpılmıyor) ya da dekoratif hero halkası (`.card.hero` overflow:hidden ile kırpılıyor) — hiçbiri gerçek düzen hatası değil. Ayrıca ham renk taraması: `.tsx` tamamen tokenli; tek istisna **`kilit.css .kilit-hata` = ham `#e7a79c`** (koyu kilit ekranındaki PIN hata satırı). Kontrast sorunu değil (`--surface-inverse` üstünde ~8.7:1, AA rahat geçer) ama token sisteminin "bileşende ham renk yazılmaz" kuralını çiğniyor; sistemin `--cat-red-fg` (#c96a5b) bu koyu zeminde yalnızca ~4.8:1 kaldığından bu açık kırmızı bilinçli ve doğru bir seçim. **Düzeltme:** `--danger-on-inverse: #e7a79c` token'ı (açıklamalı) eklendi, `.kilit-hata` ona bağlandı — son ham renk literali de bileşen stilinden kalktı. `npm run build` geçti, **372 test** yeşil. |
| 192 | 2026-08-16 04:45 | **Dokunma hedefi (tap target) boyut taraması — 2 sub-24px bağlantı düzeltildi (WCAG 2.5.8 AA).** Kontrast taramasının eşi olarak, tüm ekranlarda her etkileşimli öğenin ETKİN dokunma alanı (dokunmatik.css `::after` genişleticileri dahil) ölçüldü (Playwright). Sonuç çok temiz: yalnızca **2 öğe** 24px AA altında — ikisi de panodaki bağımsız bağlantı: `.section-link` ("Hepsini gör", 23px) ve `.safety-link` ("Çalışma alanını yönet", 20px). Üstelik `dokunmatik.css` `.section-link`'i "zaten ≥24px" varsayımıyla kapsam dışı bırakmıştı — 1px yanlış. **Düzeltme:** ikisine `min-height: 24px` (inline-flex olduklarından içerik ortalı kalır) — `::after` taşma riski yok (zaten bu yüzden genişletici listesinde değillerdi). `dokunmatik.css` notu gerçeğe göre güncellendi. **Doğrulama (Playwright):** tarama artık tüm ekranlarda **0 sub-24px** etkileşimli hedef bildiriyor, hata yok. `npm run build` geçti, **372 test** yeşil. |
| 191 | 2026-08-16 04:00 | **Sistematik runtime kontrast taraması — brand-teal metin + hero eyebrow AA hataları düzeltildi.** Geçen turdaki başlık bulgusunu genelleştirmek için TÜM ekranlarda her metin öğesinin gerçek zeminine karşı hesaplanan kontrastı (büyük/normal eşikle) tarandı (Playwright). Tur-163 yalnızca `--cat-*` kategorisini ele almış; `--brand-teal` ve hero eyebrow atlanmıştı. **Bulgular + düzeltmeler:** **(1) `--brand-teal` (#5fb49c, açık zeminde ~2.3:1) METİN olarak** kullanılıyordu: takvim dışa-aktar bağlantısı (2.29:1 doğrulandı), bugünün gün numarası (seçili değilken), pano "Detaylı" bağlantısı, 404 yer-tutucu bağlantısı, onboarding eyebrow ve üç "Vazgeç" iptal bağlantısı. `--brand-teal-text` (#3e7566) token'ı eklendi, bunlar ona bağlandı. **Kalan brand-teal metin doğru bırakıldı:** KOYU safety kartı/qa-answer üstünde, dekoratif ikonlar, tüm border-color. **(2) Hero tarih eyebrow'u** `--hero-fg-muted` (#5b7a68) yeşil degrade üstünde ~3.5:1'di → #46614f'e koyulaştırıldı (degrade boyunca ~4.7–5.6:1); token yalnızca hero'da. **Doğrulama (tarama + ekran görüntüsü):** cal-export-link ve hero-date artık geçiyor (Pano 0 hata); hero eyebrow okunur, hiyerarşi korundu. **Kalan (kabul, günlüğe):** soluk hafta sonu başlıkları (tur-163 belgeli istisna) ve 9px kanal rozeti 4.37:1 (standart `--text-muted`, 0.13 altında — token'ı uygulama genelinde değiştirmemek için bırakıldı). `npm run build` geçti, **372 test** yeşil. |
| 190 | 2026-08-16 03:15 | **Görevler listesi denetlendi — grup başlığı kontrast hatası düzeltildi (tur-163'ün atladığı nokta).** Görevler sayfası (süzgeçler, satır-içi tamamlama, gruplama, öncelik rozetleri) incelendi. **Bulgu (DÜŞÜK — WCAG AA):** grup başlıkları (Bugün/Yarın/Yaklaşan/Gecikmiş/Vadesiz/Tamamlanan) 24px `.t-title`'ı pastel `--cat-*-fg` ile boyuyordu. Tur-163 metin için koyu `--cat-*-text` varyantlarını üretip geniş uygulamıştı ama BU başlık atlanmış: amber (#d9a45b ~2.2:1) ve yeşil (#5fb49c ~2.2:1) büyük-metin 3:1 eşiğini bile geçmiyor, mavi sınırda. **Düzeltme:** başlık rengi `--cat-*-text`'e alındı. **Runtime doğrulama (Playwright, hesaplanan kontrast):** render edilen başlıklar artık ~**5.3:1** (Bugün/Yarın/Yaklaşan) — AA'nın çok üstünde, hata yok. **Kalan `-fg`-metin kullanımları incelendi, hepsi kabul:** soluk hafta sonu başlıkları + onboarding illüstrasyonları (tur-163'te bilinçli bırakılmış, belgeli) ve `.row-remove:active` (anlık basış geri bildirimi) — dokunulmadı. `npm run build` geçti, **372 test** yeşil. |
| 189 | 2026-08-16 02:30 | **DosyaForm denetlendi — arşivli müvekkil seçicide boş görünme hatası düzeltildi.** Dosya oluştur/düzenle formu incelendi (doğrulama, düzenleme ön-doldurma, çift-gönderim bekçisi, satır-içi yeni-müvekkil — hepsi sağlam). **Bulgu (DÜŞÜK–ORTA — UI hatası):** `useMuvekkiller()` arşivli müvekkilleri elediğinden, müvekkili ARŞİVLENMİŞ bir dosyayı düzenlerken (ki bu, dosyası olan bir müvekkil "silinince" olur) `<select value>` hiçbir seçenekle eşleşmiyor → müvekkil alanı **BOŞ** görünüyordu: dosyanın kime ait olduğu kayboluyor ve kullanıcı yanlışlıkla başka müvekkile atayabiliyordu. **Düzeltme:** DosyaForm artık mevcut müvekkili ayrıca çekiyor (`useMuvekkil`) ve seçili olup aktif listede yoksa "(arşivli)" etiketli bir seçenek olarak ekliyor. **Runtime doğrulama (Playwright):** dosyası olan bir müvekkil arşivlendi (arsivlendi=true), o dosya düzenlenince seçici artık **"Demir İnşaat A.Ş. (arşivli)"** gösteriyor (doğru değer, boş değil), hata yok. `npm run build` geçti, **372 test** yeşil. |
| 188 | 2026-08-16 01:45 | **Müvekkil alt sistemi (form + yazma işlemleri) denetlendi — sağlam, kod değişikliği yok.** Tur-161 dosya tarafını (`dosyaGuncelle` denormalizasyon) düzeltmişti; bu tur müvekkil CRUD'u incelendi. **`MuvekkilForm`:** ad zorunlu doğrulaması, çift-gönderim bekçisi (`kaydediliyor`+disabled), düzenlemede tek-seferlik ön-doldurma (`yuklendi`), tüzel/gerçek tür seçimi kimlik/vergi-no etiketini doğru değiştiriyor, silme "Sil→Emin misiniz?" + "Vazgeç" iptaliyle, `mevcut===null`'da "bulunamadı" yer tutucusu. **`muvekkilEkle`/`muvekkilGuncelle`:** alanlar trim/temizleniyor, boş etiketler eleniyor, ekleme hareket günlüğü yazıyor. **Kritik doğrulama — denormalizasyon YOK:** müvekkil `ad`'ı hiçbir yere kopyalanmıyor (dosya/olay/finans/görev `muvekkilId` referans veriyor, gösterimler canlı `.ad` okuyor) → ad değişince her yere otomatik yansır, bayat kopya yok (tur-161'de `dosyaGuncelle`'nin `muvekkilId` denormalizasyonu gibi bir sorun burada YOK). `muvekkilGuncelle` `arsivlendi`'yi koruyor (Dexie kısmi güncelleme). **Not/varsayım:** arşivlenen müvekkili UI'dan geri-al (un-archive) yolu yok — ama arşiv kasıtlı gizlemedir ve veri korunur; geri-alma bir özellik olur (madde 7 "hata düzelt" kapsamı dışı, dokunulmadı). `npm run build` geçti, **372 test** yeşil. |
| 187 | 2026-08-16 01:00 | **Silme kaskadları / referans bütünlüğü denetimi — sağlam, kod değişikliği yok (runtime doğrulandı).** Bir kayıt silinince bağlı kayıtların yetim kalıp kalmadığı incelendi (yetim = boşa depolama + listeler/arama/raporlarda dangling `dosyaId`). **Statik denetim:** `dosyaSil` tek transaction'da dosya + olaylar/sureler/gorevler/finans/belgeler/kisiler/notlar'ı `.where('dosyaId')` ile siliyor (dekont belgeleri de `dosyaId` taşıdığından yakalanıyor) — yetim yok. `muvekkilSilVeyaArsivle`: bağlı dosya VARSA arşivliyor (dosyaları yetim bırakmaz), YOKSA müvekkil + müvekkil-düzeyi notlar'ı siliyor; olay/finans/görev `muvekkilId`'yi dosyadan türettiğinden dosyasız müvekkilin bunlardan hiçbiri olmaz, `belgeYukle` da hiç bare-`muvekkilId` ile çağrılmıyor (MüvekkilDetay'da belge yükleme UI'ı yok) → yetim yok. `finansSil` bağlı dekont/makbuzları da siliyor. **Runtime doğrulama (Playwright):** en çok bağlı kayda sahip seed dosyası (`dosya-demir-insaat`, **8 bağlı kayıt**: 1 olay + 1 süre + 1 görev + 3 finans + 2 belge) DosyaForm düzenleme modundan "Sil → Emin misiniz?" ile silindi → dosya gitti, **7 tabloda da 0 yetim**, hata yok (`.where('dosyaId')` indeksleri de çalışıyor). `npm run build` geçti, **372 test** yeşil. |
| 186 | 2026-08-16 00:15 | **Boş durum (sıfır veri) denetimi — 10 ekranın hepsi sağlam, kod değişikliği yok.** Seed her zaman veri tohumladığından sıfır-veri render'ı nadiren görülür; sistematik test edilmemişti (çökme, NaN/undefined sızıntısı, bozuk düzen riski). **Runtime denetim (Playwright):** onboarding sonrası TÜM veri tabloları (muvekkiller/dosyalar/olaylar/sureler/gorevler/finans/belgeler/kisiler/notlar/hatirlatmalar/hareketler/kullanicilar) IndexedDB'den temizlendi (ayarlar korundu → uygulama ana ekranda kaldı), reload edildi ve 10 ekran gezildi. **Sonuç:** hiçbirinde pageerror YOK, hepsinde içerik + kart render oldu, HİÇBİRİNDE `NaN`/`undefined`/`[object Object]` sızıntısı yok. **Görsel onay:** **Pano** her bölümde özel, iyi yazılmış boş-durum mesajı gösteriyor ("Şu an açık bir hukuki süre yok", "Önümüzdeki üç günde planlanmış bir işlem yok", "Bekleyen görev yok", "Henüz açık dosya yok", "Henüz bir hareket kaydedilmedi"; istatistikler "0 / bu ay yeni yok" vb.) — jenerik değil, bölüme özgü. **Raporlar** sıfır veriyle temiz: istatistikler "0" (NaN değil), sütun grafiği boş çizim alanı + ay ekseni + efsane, halka "0 AÇIK SÜRE" + boş yatak halkası, "toplam tahsilat ₺0"; `Math.max(1, ...)` ve `toplam>0` korumaları sıfır bölme/bozuk grafik önlüyor. Böylece boş durum tüm uygulamada özenle karşılanmış. `npm run build` geçti, **372 test** yeşil. |
| 185 | 2026-08-15 23:30 | **Satır içi hızlı-ekleme formlarına çift-gönderim koruması eklendi (tur-161'in atladığı 3 nokta).** Tur-161 beş ana forma çift-gönderim bekçisi eklemişti ve pano hızlı-görev-ekleme'de zaten var; ama üç satır içi detay-sayfası hızlı-eklemesi ATLANMIŞTI: `gorusmeKaydet` (MüvekkilDetay), `notKaydet` ve `kisiKaydet` (DosyaDetay). Üçünde de giriş yalnızca await'ten SONRA temizleniyordu → kaydet düğmesi asenkron ekleme sırasında etkin kalıyor → hızlı çift dokunuş (mobil hedefte yaygın) **çift not/kişi/görüşme** oluşturuyordu. **Düzeltme:** her birine `kaydediliyor` bekçisi eklendi (pano deseniyle aynı): kaydederken atla, try/finally ile kilitle, kaydederken düğmeyi devre dışı bırak. **Runtime doğrulama (Playwright):** "Görüşmeyi kaydet"e hızlı çift tıklama artık **tam 1** not oluşturuyor (önceden 2 olurdu), kasıtlı ikinci kayıt hâlâ bir tane daha ekliyor (bekçi meşru kullanımı engellemiyor), hata yok. Böylece uygulamadaki TÜM yazma yolları (5 ana form + pano + 3 satır içi) çift-gönderime karşı korunmuş oldu. `npm run build` geçti, **372 test** yeşil. |
| 184 | 2026-08-15 22:30 | **Ayarlar güvenlik yüzeyi denetlendi + "tüm veriyi sil" onayına otomatik geri alma eklendi.** Ayarlar sayfası (PIN, biyometri, LLM, yıkıcı eylemler) incelendi. **Bulgu (DÜŞÜK–ORTA — güvenlik/tutarlılık):** "Verileri sıfırla" iki adımlı onaydı ama bir kez KURULUNCA (`sifirlamaOnayi=true`) ne kendini geri alıyor ne de iptali vardı — uygulamanın diğer yıkıcı onaylarının (SilDugmesi 3 sn geri alma; form silmelerinde "Vazgeç") aksine. Yani EN yıkıcı eylem (tüm veriyi sil, geri alınamaz) EN zayıf onaya sahipti: kullanıcı kurup uzaklaşınca sonradan kazara ikinci kez tıklayıp her şeyi kaybedebilirdi. **Düzeltme:** 4 sn otomatik geri alma eklendi (SilDugmesi güvenlik deseniyle aynı) — onaylanmazsa kurulu durum kendini temizler. **Runtime doğrulama (Playwright):** ilk tık uyarı etiketini gösteriyor ("Emin misiniz? Tüm veriyi sil"), 4,6 sn sonra "Verileri sıfırla"ya dönüyor, veri sağlam, hata yok. **Ayrıca denetlendi (sağlam, değişmedi):** PIN girişleri rakam-filtreli (`replace(/\D/g,'').slice(0,8)`) → numerik kilit ekranının kabul edemeyeceği harfli PIN KURULAMAZ (kilitlenme yok); `pinKur` uzunluk+eşleşme doğruluyor, yalnızca PBKDF2 özetini saklıyor; kilidi kaldırınca biyometri kimliği de siliniyor; biyometri seçenekleri yalnızca kilit açıkken görünüyor; şifreli/düz geri yükleme sonrası reload doğru. `npm run build` geçti, **372 test** yeşil. |
| 183 | 2026-08-15 21:15 | **Güvenlik + temel katman kod denetimi (kripto, PIN, biyometri, router, devir, yazı ölçeği) — hepsi sağlam, kod değişikliği yok.** Hassas müvekkil verisini koruyan ve navigasyonu taşıyan çekirdek incelendi. **(1) `kripto` (şifreli yedek + PIN — güvenlik-kritik):** PBKDF2-SHA256 **210.000 iterasyon** (güncel OWASP), **her işlemde taze rastgele 16-bayt tuz** (yeniden kullanım yok), **her şifrelemede taze rastgele 12-bayt IV** (GCM için kritik — tekrar yok), **AES-256-GCM** (kimliği doğrulanmış şifreleme → kurcalama tespiti), ham PIN/parola HİÇBİR yerde saklanmıyor, yanlış parola → GCM doğrulaması patlar → temiz `SifreCozmeHatasi`. En iyi uygulamalara uygun. **(2) `biyometri` (WebAuthn):** dürüst kapsam (erişim kapısı, şifreleme değil), `userVerification:'required'` + platform authenticator + kaynak-bağlı rp, rastgele challenge, try/catch → cancel/hata'da null/false; platform doğrulayıcısının izin verdiğinden fazla baypas yok. **(3) `router`:** `matchPath` exact/wildcard/`:param` + güvenli decode (bozuk param çökmez, 404'e düşer), `useIsActive` `/` sınırıyla yanlış önek eşleşmesini önlüyor, `Link` modifier-tuş yeni-sekme niyetine saygılı, yol-anahtarlı remount (tur-162 düzenleme-formu veri bozulmasını önler). **(4) `devir`:** localStorage private-mode korumalı, ölçümler doğrulanıp 20 örnekle pencereleniyor, tahmin yalnızca gerçek veriden (uydurma yok). **(5) `yaziOlcegi`:** rem tabanlı 3 ölçek (%100/112.5/125), bilinmeyen→%100 (125% tur-168'de taşmasız test edilmişti). `npm run build` geçti, **372 test** yeşil. |
| 182 | 2026-08-15 20:30 | **Opsiyonel LLM isteğine zaman aşımı eklendi — askıda kalan uç nokta arayüzü kilitlemesin (+4 test).** `services/llm.ts` denetlendi (uygulamanın tek `fetch`'i; opt-in, varsayılan kapalı). Hata yolları sağlamdı (ağ/OK-değil/JSON/boş yanıt hepsi `LlmHatasi`) ama **`fetch`'te zaman aşımı YOKTU.** Kullanıcı AI katmanını açıp kendi uç noktasını girdiğinde, uç nokta yanıt vermezse istek asla çözülmüyor: Asistan'ın `finally`'si çalışmıyor → "Yapay zekâya sor" düğmesi sonsuza dek **"Yanıtlanıyor…"da kilitli** kalıyor, iptal/yeniden-deneme yok. **Düzeltme:** istek `AbortController` + 60 sn zaman aşımına alındı (uzun tamamlamalar için cömert ama sınırlı); abort mı gerçek ağ hatası mı ayrımı `signal.aborted` ile (ortamdan bağımsız güvenilir), zamanlayıcı her hâlde `finally`'de temizlenir. Zaman aşımında `llmSor` net mesajlı `LlmHatasi` fırlatıyor; Asistan bunu zaten gösteriyor ve `finally` yükleme durumunu sıfırlıyor → düğme kurtuluyor. **Testler:** başarı (içerik kırpma), OK-değil (durum kodu), boş içerik ve **zaman aşımı yolu** (20ms, askıda uç nokta mock'u) eklendi. `npm run build` geçti, **372 test** yeşil (+4). |
| 181 | 2026-08-15 19:00 | **Üç alan denetlendi (hareket-azaltma, hukuki terim yazımı, cihaz bildirim hattı) — hepsi sağlam, kod değişikliği yok.** **(1) `prefers-reduced-motion` (F23 ileri yaş kitle):** `tokens.css` `--duration-fast/base/slow`'u 0ms'ye indiriyor → tüm `var(--duration)` geçişleri anında; sabit süreli 8 `@keyframes` animasyonunun HER BİRİ kendi dosyasının reduced-motion bloğunda `animation:none` ile susturuluyor — seçici seçici doğrulandı: `.hero-button svg`/`.skeleton` (dashboard), `.surum-icon` (shell), `.ob-telefon/parla/imlec/kasa/hat-metin` (onboarding), `.tur-halka/balon` (tur). Kapsam tam. **(2) Türk hukuk terminolojisi yazımı (CLAUDE.md sert kısıt):** arayüz metni tutarlı ve TDK'ye sadık — "Vekâlet/Vekâletname" ve "Hâkim" düzeltme işaretli (â), "tebliğ" ğ'li; KOD tanımlayıcıları düz ASCII (`vekalet-ucreti`, `'teblig'`, `hakim` anahtarı → `'Hâkim'` değeri). "adli" doğru biçimde şapkasız (TDK), "hâkim/vekâlet" doğru biçimde şapkalı — ortografi seçici uygulanmış, rastgele değil. **(3) Cihaz bildirim hattı (`bildirimTetikleyici` + `services/bildirim`):** ilk-tur tohumlama (açılış patlaması yok), push-kanalı kapısı, sessiz-saat ERTELEME (bastırma değil — işaretlemeden atlıyor), oturum-içi tekilleştirme; `cihazBildirimiGoster` izni içeride denetliyor (`permission!=='granted'`→false) + `new Notification` try/catch — izin sonradan iptal edilse bile güvenli no-op; SMS/e-posta "yapılandırılmadı" diye dürüstçe bildiriliyor (sessiz başarısızlık yok). `npm run build` geçti, **368 test** yeşil. |
| 180 | 2026-08-15 18:15 | **Geniş ekran / masaüstü render denetimi (1280px) — sağlam, kod değişikliği yok.** Uygulama telefon-öncelikli ama GitHub Pages URL'i kullanıcının önce MASAÜSTÜ tarayıcıda açacağı bir sayfa (ana ekrana eklemek için); bu boyut daha önce denetlenmemişti (tur-168 320px + %125'ti). **Runtime denetim (Playwright @1280px):** içerik sütunu 640px ORTALI (sol 320 / sağ 960); `.topbar-inner` sağ kenarı içerik sağ kenarıyla birebir (960=960) hizalı — kontroller viewport kenarına kaçmıyor; `.fab-cluster` sağ kenarı içerik sütununun sağına (940 ≈ 960−space5) hizalı, viewport kenarında DEĞİL; **yatay taşma yok** (scrollWidth 1280 = viewport); konsol hatası yok. **Görsel onay (ekran görüntüleri):** pano (hero + 2×2 ızgara), takvim (ay ızgarası + içerik sütununa hizalı FAB kümesi), dosyalar, raporlar (SVG grafikleri viewBox ile sütunu dolduruyor) — hepsi temiz, kasıtlı "ortalanmış mobil sütun" görünümü. **CSS ile doğrulanan sınırlar:** modal pencereler bağlı (`onizleme-pencere` `min(720px,100%)`, `devir-pencere` `min(420px,100%)`, ortalı), kilit ekranı tuş takımı sabit `repeat(3,72px)` ızgara (esnemiyor). Not/varsayım: masaüstüne özel düzen EKLENMEDİ — tasarım referansı (docs/DESIGN-REFERENCE.md) telefon-öncelikli; ortalanmış sütun kasıtlı ve doğru, masaüstü için yeniden tasarlamak referanstan sapardı (madde 3). `npm run build` geçti, **368 test** yeşil. |
| 179 | 2026-08-15 17:30 | **Yazdırma/PDF çıktısı eklendi — hukukçu süre hesabını/dosya kaydını temiz basabilsin.** **Bulgu:** uygulamada HİÇ `@media print` kuralı yoktu; herhangi bir ekranı (en çok da bir avukatın dosyaya koyacağı Süre Hesapla sonucunu) yazdırınca ya da "PDF olarak kaydet" deyince üst çubuk, ekran dışı menü, FAB'lar ve mürekkep yiyen degrade/gölgeler çıktıya giriyordu — hukuk bürosu aracı için gerçek bir boşluk. **Düzeltme:** `src/styles/print.css` (en son import edilir, tamamı `@media print` — ekranı ETKİLEMEZ): kabuğu gizler (üst çubuk, menü, FAB, tur, çevrimdışı şeridi, geri/dışa-aktar bağlantıları), içeriği tam genişliğe yayar, kartları beyaz zemin + ince çerçeve + gölgesiz/degradesiz basar, kartları sayfa arasında bölmez (`break-inside: avoid`), `@page` kenar boşluğu 1.6cm. Bilgilendirme uyarısı içerik olduğundan basılmaya devam eder. **Runtime doğrulama (Playwright print-media emülasyonu + üretilen PDF):** ekranda kabuk görünür; print'te topbar/skip-link/FAB `display:none`, `.sonuc-card` ve "bilgilendirme amaçlıdır" uyarısı KALIYOR, içerik tam genişlik (maxWidth=none); üretilen A4 PDF temiz ve profesyonel (başlık + hesaplanan son gün 7 Eylül + adli tatil gerekçesi + uyarı, kabuk yok). `npm run build` geçti, **368 test** yeşil. |
| 178 | 2026-08-15 16:30 | **Erişilebilirlik: rehberli tur klavye/ekran-okuyucu ile kullanılabilir hâle getirildi (ilk-açılış deneyimi).** Tur (`Tur.tsx`) `role="dialog"` olmasına rağmen —uygulamadaki diğer TÜM modallerin (Drawer/BelgeOnizleme/DevirTeslim/KilitKapisi) aksine— odak yönetmiyor ve Esc'i yoktu: klavye kullanıcısı odak arkadaki sayfada kalıyor, "İleri"ye ulaşmak için tüm kabuğu Tab'lamak zorunda kalıyor, Esc hiçbir şey yapmıyordu. **Düzeltme:** standart modal deseni eklendi — her adımda birincil eyleme ("İleri") odaklan, Tab'ı balon içinde döndür, Esc ile kapat, tur bitince odağı `#ana-icerik`'e iade et (balonla birlikte `<body>`'ye düşmesin). **Çakışma çözümü:** tur gerçek gezinme yapıp menüyü açtığından iki kabuk mekanizması odağı çalıyordu — AppShell'in gezinmede-ana-içeriğe-odak'ı (SPA a11y, tur-164) ve Drawer'ın açılışta-odak'ı. İkisi de `turAktif` bayrağı alıp tur açıkken odak/Esc/kaydırma-kilidini TURA bırakıyor; bayrak false iken kod yolları öncekiyle AYNI (normal gezinme ve menü davranışı değişmedi — kanıtlanabilir: `if (!open || turAktif)` → `if (!open)`). **Runtime doğrulama (Playwright):** tur odağı "İleri"de açılıyor, Tab tuzakta kalıyor, odak **menü-spotlight adımı ve rota değişimleri boyunca balonda kalıyor**, Esc kapatıp odağı ana içeriğe koyuyor; **normal menü** hâlâ açılışta kapat-düğmesine odaklanıp Esc'te odağı menü düğmesine iade ediyor; konsol hatası yok. `npm run build` geçti, **368 test** yeşil. |
| 177 | 2026-08-15 15:30 | **Erişilebilirlik denetimi (15 ekran) — hepsi sağlam; tek eksik ARIA deseni (takvim sekmeleri) tamamlandı.** İleri yaş kitle (F23) için sistematik a11y taraması yapıldı. **Playwright DOM denetimi (15 ekran):** adsız buton/link YOK, etiketsiz input YOK, her ekranda tam **1 h1** ve atlanmış başlık seviyesi YOK. **Tıklanabilir div taraması:** yalnızca modal/drawer scrim'leri (Drawer/DevirTeslim/BelgeOnizleme) — hepsi birincil kapat düğmesi + Esc ile klavye erişilebilir; klavyesiz tıklanabilir öğe yok. **Drawer:** etkin bağlantıda `aria-current="page"`, kapalıyken `inert`, Esc + odak tuzağı + odak iadesi, etiketli nav — örnek nitelikte. **Bulgu (DÜŞÜK — eksik ARIA):** Takvimdeki Ay/Hafta/Ajanda anahtarı `role="tablist"`/`role="tab"`+`aria-selected` kullanıyor ama altında değişen içerik `tabpanel` olarak ilişkilendirilmemişti — ARIA'nın vaat ettiği sekme/panel bağı DOM'da yoktu. **Düzeltme:** değişen görünümler tek `role="tabpanel"` (id=cal-panel, `aria-labelledby`=etkin sekme — sekme değişince güncelleniyor) içine alındı, her sekmeye `aria-controls="cal-panel"` eklendi; sarmalayıcı `.cal-panel` sınıfıyla shell-content'in sütun boşluğunu taşıyor ki ızgara–gün kartı aralığı bozulmasın. **Runtime doğrulama (Playwright):** panel role/label bağı doğru, etiket etkin sekmeyi izliyor (Hafta'ya tıklayınca `aria-labelledby`=cal-tab-hafta), kartlar arası boşluk **16px = --section-gap** (regresyon yok), konsol hatası yok, görsel bütünlük ekran görüntüsüyle onaylandı. `npm run build` geçti, **368 test** yeşil. |
| 176 | 2026-08-15 14:30 | **Tasarım sistemi tutarlılığı (CSS): paylaşılan yeşil hero paleti token'landı — sapma riski kapatıldı.** Tur-175'in devamı: CSS katmanında token'ı atlayan sabit renkler tarandı. **Bulgu:** Genel Bakış ve Asistan'daki yeşil hero kartları AYNI degrade zemini (`#dcebe1→#cfe3d7→#c3dbcc`) ve koyu-yeşil AA metin hiyerarşisini (`#16302a` başlık, `#3f5c4e` gövde, `#5b7a68` eyebrow, `#23453a` buton/ikon/çip) paylaşıyor ama her değer `dashboard.css`, `asistan.css` ve `ayarlar.css` içinde sabit hex olarak KOPYALANMIŞTI (7 renk, 3 dosya, 13 kullanım). Bu, tur-175'te rapor grafiği slate renginin sapmasına yol açan aynı anti-desen: iki hero'yu ve sage çip/mesajlarını zamanla ayrışmaktan alıkoyan hiçbir şey yoktu (değerler bugün tutarlı ama tek dokunuşla ayrışabilirdi). **Düzeltme:** `tokens.css`'e `--hero-surface-1/2/3`, `--hero-fg-strong/fg/fg-muted`, `--brand-sage-text` grubu eklendi; tüm çağrı yerleri buna bağlandı; hero degrade'i her dosyada token'lı ama kendi orta durağıyla korundu (dashboard %55, asistan %60 — kasıtlı). **Runtime doğrulama (Playwright, hesaplanan değerler):** yedi metin rengi ve üç degrade durağı ORİJİNAL hex'e birebir çözülüyor (ör. hero-greeting `rgb(22,48,42)`=#16302a, buton `rgb(35,69,58)`=#23453a, degrade `rgb(220,235,225)→rgb(207,227,215)→rgb(195,219,204)`); render piksel-eş, hata yok. Palet artık tek kaynaktan. `npm run build` geçti, **368 test** yeşil. |
| 175 | 2026-08-15 13:30 | **Tasarım sistemi tutarlılığı: rapor grafiği renkleri sabit hex yerine token'lara bağlandı (bir renk kayması düzeltildi).** TSX'te sabit hex renk taraması yapıldı; tek kaynak `Raporlar.tsx` — üstelik dosya yorumu "renkler token'lardan gelir" DİYORDU ama `RENK` haritası hex sabitliyordu (yorum-kod çelişkisi). Altı değerin beşi `--cat-*-fg` token'larıyla eşleşiyordu; ama **`RENK.slate` kaymıştı**: eski `#9aa0a0`'ı tutuyordu, oysa token tur-163'teki WCAG-AA kontrast geçişinde (ileri yaş kitle için) `#6b7472`'ye koyulaştırılmıştı (üstelik bu girdi ölüydü — hiç okunmuyor). Taban çizgisi ve halka yatağı stroke'ları da sabit hex'ti. **Düzeltme:** satır içi SVG `var()`'ı çözdüğünden grafik dolgu/stroke'ları ve efsane noktaları artık doğrudan `--cat-*-fg`, `--border-hairline`, `--surface-sunken` kullanıyor; ölü/eski slate girdisi kaldırıldı. **Runtime doğrulama (Playwright, hesaplanan renkler):** yeşil çubuk `rgb(95,180,156)`=#5fb49c, taban `rgb(236,233,228)`=#ece9e4, halka yatağı `rgb(242,240,236)`=#f2f0ec, kritik dilim `rgb(201,106,91)`=#c96a5b, mor gider çubuğu `rgb(142,124,195)`=#8e7cc3 — hepsi token'la birebir; grafikler bugün piksel-eş render oluyor, hata yok. Böylece grafikler artık paleti izliyor (gelecekteki kontrast/tema değişimi otomatik yansır) ve dosya yorumu doğru hâle geldi. `npm run build` geçti, **368 test** yeşil. |
| 174 | 2026-08-15 12:30 | **Dokuz daha önce denetlenmemiş/az denetlenmiş alan adversarial incelendi — hepsi sağlam, kod değişikliği yok.** Son turlarda çekirdek (süre/hatırlatma/asistan/finans) düzeltildikten sonra bu tur kalan mantık katmanları tarandı. **(1) Global arama (`Arama`+`aramaSorgulari`):** düz `.includes` (regex enjeksiyonu yok — `.`/`*`/`(` düz eşleşir), `toLocaleLowerCase('tr')` ile İ/ı katlaması, 2 harf alt sınırı, çok alanlı; sağlam. **(2) Rotalar (kırık bağlantı taraması):** görev sonucu `/gorevler/:id`'ye gidiyor — rota var; tüm `:id` kalıpları literal segmentlerden (`yeni`/`duzenle`) SONRA sıralı; kırık link yok. **(3) Yedek/geri yükleme (`yedek.ts`):** `dogrula` bozuk/yeni-sürüm yedeği DB'ye DOKUNMADAN reddediyor (clear-sonra-patla veri kaybı önlenir), base64 kodlama parçalı (yığın taşması yok), blob yeniden kurulumu transaction'dan önce, temizle+geri-yükle tek atomik transaction. **(4) Belge önizleme (`BelgeOnizleme`):** object URL unmount'ta revoke (sızıntı yok), odak tuzağı+Esc+odak iadesi. **(5) Belge yönetimi (`belgeIslemleri`):** 25MB sınırı, Blob kopyası, yetim-güvenli silme, `navigator.storage.estimate` yoksa zarif düşüş. **(6) Olay formu + tarih/saat gidiş-dönüşü (`OlayForm`+`olayIslemleri.damgaKur`):** yerel→UTC saklama, tüm görüntüleme yolları `yerelGun`/yerel `Date` ile geri indirgeme; zamanlı ve tüm-gün olaylar gece yarısı sınırında bile doğru yerel güne oturuyor (Türkiye kalıcı UTC+3, DST yok); düzenleme gidiş-dönüşü temiz. **(7) Görev yazma (`gorevIslemleri`):** olay katmanının aynısı; tekrar yalnızca vade varsa, düzenleme durumu/seriesId'yi bozmuyor. **(8) Görev tamamlama (`gorevDurumunuDegistir`):** tamamlarken `tamamlanmaTarihi` set, geri alırken temizleniyor (rapor/pano buna dayanıyor). **(9) Dosya listesi süzme (`dosyaSorgulari`):** `durusmasi-yaklasan`/`sonrakiOlay` alt sınırı `bugun` = YEREL gece yarısı (ISO), tüm-gün-bugün olayının sakladığı UTC ile birebir eşit → dahil ediliyor (tur-165 hatasının aksine, `simdi` değil); ufuk matematiği doğru. **Not (varsayım, değiştirilmedi):** dosya/müvekkil araması arşivlenmişi eler ama görev/belge aramaları elemiyor — genel arama bir "her şeyi bul" aracı olduğundan ve arşiv silme değil saklama olduğundan bu kabul edilebilir/kasıtlı sayıldı; "doğru" davranış belirsiz, dokunmadım. `npm run build` geçti, **368 test** yeşil. |
| 173 | 2026-08-15 11:30 | **Finans/para alanı denetlendi — "Bekleyen ödeme" özetinde anlamsal tutarsızlık düzeltildi (+2 test).** Uygulamanın para katmanı uçtan uca incelendi. **Sağlam bulunanlar (değişmedi):** `para.ts` (`metindenKurus` TR/EN ayraç belirsizliğini doğru çözüyor — tek ayraç + >2 hane = binlik, negatif ve gidiş-dönüş sağlam; `tutarKisa` kuruş varsa tam biçime düşüyor), `finansIslemleri` (`odenenKirp` [0,tutar] kırpması + `durumNormalize` durumu ödenen/tutar oranından türetip formun geçtiği değeri geçersiz kılıyor → tutarlı; `finansGuncelle` dosyaId'yi değiştirmiyor, yetim kayıt yok), `FinansForm` (kısmi kaydı düzenlerken `odenenMetni` mevcut `odenenTutar`'dan dolduruluyor → veri kaybı yok), `raporSorgulari` (tüm para toplamları `odenenTutar` = gerçek nakit akışı; `yerelGun` ile aralık; `csvTutar` TR ondalık, binlik-ayraçsız → Excel doğru okur), `hazirlik` skorlaması (türe göre madde altkümesi, ağırlıklı, kaçmış-süre = elle işaretli VEYA açık+vadesi geçmiş; `etiketEksik` UI'da `!tamam` iken doğru gösteriliyor). **Bulgu (ORTA — anlamsal/ekranlar arası tutarsızlık):** `useFinansGenelOzet` "Bekleyen ödeme" (`toplamBekleyen`) ve "yaklaşan ödeme" (`yaklasanOdeme`) ödenmemiş TÜM kayıtları (gelir+gider) sayıyordu; oysa tahsil edilmemiş bir GELİR (müvekkilin borcu) bekleyen **tahsilat**tır, ödeme değil — büronun "ödeyeceği" tutarı yanlışça şişiriyor ve asistanın aynı adlı hesabıyla (`dosyaOzeti`, yalnızca gider) çelişiyordu. Seedde tüm gelir 'odendi' olduğu için gizli kalmış; kullanıcı ödenmemiş bir vekâlet ücreti girince ortaya çıkardı. **Düzeltme:** her iki alan yalnızca gider sayıyor (uygulamanın "ödeme=çıkış / tahsilat=giriş" sözlüğüyle ve asistanla tutarlı). Saf `finansGenelOzetHesapla(kayitlar, bugun)` fonksiyonuna çıkarıldı (IndexedDB'siz test edilebilir); bekleyen gelirin dışlandığını + kısmi giderin kalanının sayıldığını kanıtlayan 2 test eklendi. "Bekleyen" süzgeç çipi iki yönü de listelemeye devam ediyor ("ödenecek" değil "kapanmamış" demek). `npm run build` geçti, **368 test** yeşil (+2). |
| 172 | 2026-08-15 10:30 | **Asistan kural motoru denetlendi — kısmi ödeme raporlama hatası düzeltildi (davranışsal, +1 test).** Uygulamanın "akıllı" katmanı (`domain/asistan.ts` bulgu üretimi) incelendi. **Bulgu (ORTA — yanlış çıktı):** ödeme bulgusu döngüsü yalnızca `odendi` durumunu eliyordu; **kısmen ödenmiş** (`odemeDurumu==='kismi'`, `odenenTutar>0`) bir gider için "henüz yatırılmadı / vadesi geçti, henüz ödenmedi" diyor, ödenmiş kısmı tamamen yok sayıyordu. Bu, motorun temel vaadini ("veriden deterministik, **yanlış cevap üretmez**") çiğniyordu — üstelik `dosyaOzeti` aynı dosyada kalanı zaten doğru (`tutar−odenenTutar`) hesaplıyordu. **Düzeltme:** kısmi kalemler artık "kısmen ödendi; kalan ₺X" (kanonik para biçimi, vade/gecikme durumu korunarak; gecikmiş→kritik önceliği aynı) olarak bildiriliyor. Regresyon testi eklendi (kalan ₺7.500,00; "yatırılmadı/ödenmedi" ifadesi geçmiyor). **İkinci, aynı sınıf hata:** DosyaDetay finans sekmesi kısmi kaydı "· bekliyor" diye etiketliyordu → "· kısmi" (FinansForm etiketleriyle tutarlı). **Sağlam bulunanlar (değişmedi):** süre bulguları (geçti/kritik/yakın eşikleri, aciliyet), yaklaşan duruşma (≤7 gün, yerel gün), geciken görev, eksik vekâletname + hazırlık<%50 bulguları, niyet çıkarımı, `dosyaOzeti` para biçimi. Finans liste görünümleri (Finans.tsx) kısmi için tam tutarı amber ile gösteriyor — yanlış olgusal iddia içermediğinden kabul (liste basitleştirmesi). `npm run build` geçti, **366 test** yeşil (+1). |
| 171 | 2026-08-15 09:50 | **Süre/zaman/hatırlatma çekirdeği derin denetlendi + iki gerçek hata düzeltildi (biri davranışsal, runtime doğrulandı).** Uygulamanın en güvenlik-kritik katmanı (hukuki süre motoru, tarih yardımcıları, hatırlatma türetme, sessiz saat, uyarı uyumu) uçtan uca incelendi. **Sağlam bulunanlar (değişmedi):** `sureHesabi` (HMK m.92/93/104 — tebliğ günü sayılmaz, ay/yıl karşılık-gelen-gün + kısa aya sabitleme, adli tatil 20 Tem–31 Ağu → 7 Eylül uzaması, sonra tatil kaydırması), `tatil` (sabit+dinî tatiller, hafta sonu, ileri kaydırma), `tarih` (timezone-güvenli `isoDateToDate`/`yerelGun`), `sessizSaat` (gece yarısı aşan pencere doğru). **Uyarı uyumu (CLAUDE.md sert kısıt) tam:** "bilgilendirme amaçlıdır" uyarısı 8 ekranda (pano, takvim, dosya listesi/detayı, raporlar, bildirimler, süre hesapla, asistan) VE hesap çıktısı olan ICS dosyasının süre VEVENT açıklamasında mevcut. **Düzeltme 1 (DÜŞÜK — belge/yorum hatası):** `hatirlatmaSorgulari`'nda süre hatırlatma çıpasının yorumu "00:00" diyordu ama kod yerel **09:00** kullanıyor; iki kancada da yorum gerçek çıpaya göre düzeltildi + kasıtlı "çıpa geçince süre listeden düşer" ödünleşimi belgelendi (tek süre her ofset için ayrı 'kaçırıldı' satırı olarak çoğalmasın; son gün panoda/takvimde/dosyada ayrıca görünür). **Düzeltme 2 (DÜŞÜK–ORTA — davranışsal, adversarial denetimle bulundu):** Ay görünümünde ay değiştirme okları (`ayKaydir`) yalnızca ızgara ayını değiştirip **seçili günü önceki ayda bırakıyordu**; `useAyOgeleri` yalnızca çıpa ayının penceresini çektiğinden gün-detay kartı, dolu bir güne bile **yanlış "boş"** gösteriyordu (yükleniyor değil, kesin "kayıt yok" gibi). `ayKaydir` artık seçimi yeni aya taşıyor (gün numarasını koruyup kısa aya sabitleyerek). **Playwright doğrulaması:** Ağu→Eyl→Eki→geri gezildi; ızgara ayı ile seçili-gün kartı her adımda tutarlı, kart gerçek aya-ait günü yansıtıyor (Eyl 15 "boş", başa dönünce Ağu 15 "3 kayıt"), gün numarası (15) korunuyor, hata yok. `npm run build` geçti, **365 test** yeşil. |
| 170 | 2026-08-15 07:55 | **En kritik iki DAVRANIŞSAL akış uçtan uca runtime doğrulandı — sağlam, kod değişikliği yok.** Şimdiye dek statik/görsel denetlenmiş ama gerçek uçtan uca sürülmemiş iki karmaşık akış Playwright ile test edildi. **(1) Yedek dışa/içe aktarma turu (yerel-öncelikli tek veri kurtarma yolu — en kritik):** benzersiz bir dosya eklendi (28 dosya), `.json` yedek İNDİRİLDİ (58KB), "Verileri sıfırla" ile her şey silinip taze tohumlandı (27 dosya, benzersiz kayıt GİTTİ), yedek dosyası İÇE AKTARILDI → 28 dosya, benzersiz kayıt GERİ GELDİ, sayım eşleşti. Yani kullanıcı verisi yedekte eksiksiz yakalanıp aynen geri yükleniyor (tur-162'deki "geri yüklemeden sonra reload" düzeltmesi de çalışıyor: içe aktarımdan sonra onboarding'i atlayıp geri yüklenen veriyi gösterdi). **(2) Tekrar eden olay oluşturma (karmaşık maddeleştirme):** "Her hafta" × 4 seçildi → tam **4 olay**, günler **17/24/31 Ağu + 7 Eyl (tam 7 gün aralık)**, hepsi TEK `seriesId` paylaşıyor, hata yok. Böylece iki en karmaşık davranışsal akış da doğru çalışıyor. `npm run build` geçti, **365 test** yeşil. |
| 169 | 2026-08-15 07:10 | **Onboarding + rehberli tur görsel denetimi — sağlam, kod değişikliği yok.** Yeni kullanıcının ilk gördüğü, daha önce yalnızca mekanik sürülüp görsel eleştirisi yapılmamış akışlar render edilip incelendi. **Hukuki onay:** teal "SON ADIM" eyebrow + serif başlık, kaydırılabilir metin (avukat yer tutucusu — `[!]` F18), kaydırılana dek soluk/pasif 5 onay kutusu + pasif CTA — doğru. **Kayıt:** teal "J" markası, "Sizi tanıyalım.", Ad/Soyad/Ünvan(Avukat)/E-posta alanları, Devam/Geri — temiz. **Rehberli tur (8 adım) ilk kez render edildi:** koyu scrim + teal spotlight halkası hedefi doğru işaretliyor (adım 1 hero kartı, adım 2 menü düğmesi, adım 3 drawer AÇILIP içindeki "Asistan" bağlantısını spotlight'lıyor), balon güvenli alanda hedefin altına oturuyor, serif başlık + koyu "İleri" + "Turu kapat" + "N/8" sayacı — hepsi görsel dile uygun, konumlama kusursuz. Böylece uygulamanın TÜM ekran ve akışları (ana sayfalar, detaylar, formlar, boş durumlar, onboarding, onay, kayıt, tur) üç tur boyunca görsel denetimden geçti; tek sapma (tamamlanan görev rozeti) tur 167'de düzeltildi. `npm run build` geçti, **365 test** yeşil. |
| 168 | 2026-08-15 06:20 | **Görsel tasarım denetimi (kalan ekranlar) + büyük-yazı/dar-ekran stres testi — sağlam, kod değişikliği yok.** Geçen turdaki kritik görsel geçişi tamamlamak için kalan ekranlar tam-sayfa render edilip referans görsel diliyle eleştirel karşılaştırıldı: **müvekkil detayı** (boş alanlar "—", "ilk dosya" tarihi doğru [tur-162 düzeltmesi], devre dışı "Görüşmeyi kaydet"), **finans detayı** (nötr/yeşil çipler, "Sil" tehlike düğmesi artık okunur koyu kırmızı), **raporlar** (dönem istatistikleri + gider tutarları + dosya-net değerleri AA-okunur, SVG grafikler temiz), **boş arama durumu** ("… için sonuç yok", teal odak halkası) — hepsi görsel dile uyumlu, sapma yok. **A11y stres testi (Playwright):** yazı ölçeği **%125 (kök 20px)** — 8 ana ekran + dosya-detay finans sekmesi (para ızgarası dahil) yatay taşma YOK, kırpma yok, tutarlar okunur; **320px** (önceki tur) taşma yok. Böylece tüm ekranlar iki tur boyunca görsel olarak denetlendi (tek sapma — tamamlanan görev rozeti — tur 167'de düzeltildi) ve %125/320px'de dayanıklı. `npm run build` geçti, **365 test** yeşil. |
| 167 | 2026-08-15 05:30 | **Görsel tasarım denetimi (referansla karşılaştırmalı): panoda tamamlanan görev rozeti hatası düzeltildi.** Ekranlar tam-sayfa render edilip `docs/design-reference/*.jpg` ile eleştirel karşılaştırıldı. **Bulgu:** Panodaki "Bugün yapılacaklar"da geçen turda görünür olan bugün-tamamlanan (üstü çizili) görevler, kırmızı **"GECİKTİ"** aciliyet rozeti ve kırmızı sol aksanla gösteriliyordu — üstü çizili "bitti" durumuyla çelişen karışık sinyal. Referans (04-task-list.jpg) tamamlanan işi **nötr TARİH rozetiyle** ("6 AĞUSTOS", soluk) gösteriyor. **Düzeltme:** `GenelBakis.GorevListesi`'nde tamamlanan görevde rozet nötr tarihe (slate) ve satır aksanı slate'e alındı; bekleyen görevde aciliyet rozeti korunuyor. **Doğrulandı (Playwright):** tamamlananlar artık "3 Ağustos" vb. nötr gri (#656d6b) rozet + slate aksan; bekleyenler kırmızı "bugün" (#a2564a) aksanı sürüyor. **Diğer ekranlar (olay formu, görevler, asistan) referans görsel diliyle uyumlu, sapma yok:** tür-seçim ikonları/aciliyet başlıkları/öncelik rozetleri (yalnızca bekleyende) doğru; asistan öncelik noktaları dekoratif pastel kalıyor. `npm run build` geçti, **365 test** yeşil. |
| 166 | 2026-08-15 04:30 | **PWA/derleme/yapılandırma/hata-sınırı katmanı denetlendi + çevrimdışı ve duyarlılık runtime doğrulaması — sağlam, kod değişikliği yok.** **Runtime (Playwright):** **(1) Çevrimdışı çalışıyor** — SW `activated` + sayfayı kontrol ediyor; ağ kapalıyken reload'da uygulama tam açılıyor (başlık/kabuk/2240 karakter), `/dosyalar` IndexedDB'den 24 satır, `/takvim` çiziliyor, hata yok → "çevrimdışı çalışır" sözü tutuyor. **(2) 320px (iPhone SE)** tüm sayfalarda yatay taşma yok (para ızgarası ≤360px tek-sütun düzeltmesi dahil). **(3) Dağıtım yolları:** `BASE_PATH=/MYLOVE/` ile derleyince index.html varlık/manifest/registerSW/apple-touch-icon yolları `/MYLOVE/...`; manifest `start_url`/`scope`/ikon src'leri göreli → GitHub Pages'te doğru çözülüyor. **Statik denetim (kod değişmeden doğru bulundu):** SW tüm kabuğu (JS/CSS/8 font/3 ikon/manifest) precache ediyor (503KB < 2MiB limiti), `autoUpdate`+`skipWaiting`+`clientsClaim`+`cleanupOutdatedCaches`, hash-router için `navigateFallback: index.html` doğru; manifest+iOS meta tam (`viewport-fit=cover`+safe-area); `HataSiniri` gerçek kök sarmalayıcı, tam ekran stilli, hatayı yutmuyor; workflow tip-kontrol geçidi + `dist` gitignore. **İki düşük not (bilinçli/erişilemez, değiştirilmedi, varsayım günlüğe):** deploy.yml'nin özellik dalından yayınlaması kullanıcının "GitHub'dan telefonda test" akışı için KASITLI (kaldırmak testi bozardı); `App.tsx` yükleme-anı boşluğu splash rengiyle (#F7F6F3) kusursuz, IndexedDB-askıda-kalma kenarı tek-sekme iOS akışında erişilemez. Böylece uygulama uçtan uca (mantık/veri/servis/form/router/CSS/erişilebilirlik/PWA/çevrimdışı/dağıtım) adversarial denetimden geçmiş oldu. `npm run build` geçti, **365 test** yeşil. |
| 165 | 2026-08-15 03:45 | **Veri katmanı denetimi: üç gerçek hata düzeltildi (düşük şiddet); geri kalan sorgular/skorlama/seed doğru bulundu.** **(1) `useGunlukGorevler` bugün tamamlananları panelden atıyordu.** Sıralama önce duruma (`bekliyor` başa) sonra vadeye göreydi, ardından `slice(limit=5)`; bekleyenler limiti dolunca bugün tamamlananlar dışarı düşüyor, "üstü çizili kalır" sözü (satır 180-182) ihlal ediliyordu. **Yalnızca vadeye göre** sıralamaya geçildi → tamamlanan iş yerinde (üstü çizili) kalır. **E2E:** panelde 3 tamamlanan görünür (önceden 0), görünür bir işi işaretleyince kaybolmuyor, üstü çizili kalıyor (3→4). **(2) `useHazirlikDurumlari` acilIs alt sınırı yoktu.** `olaylar.some(planlandi && baslangic<=ufuk)` geçmişte kalıp işaretlenmemiş bir olayı da "acil" sayıp iyi hazırlanmış eski dosyayı geride kalanın üstüne çıkarıyordu. `yerelGun` ile **bugün ≤ gün ≤ +14 gün** alt/üst sınırı eklendi (ham UTC kıyası tüm-gün olaylarını sınırda yanlış eleyeceğinden yerel güne göre). Açık SÜRE'de alt sınır yok (geçmiş = kaçırılmış son tarih, hâlâ acil). **(3) İndirme object URL'i `click`'ten hemen sonra revoke ediliyordu** — Safari/iOS (hedef platform) ve Firefox'ta indirmeyi iptal edebiliyor. `belgeyiIndir` + yedek `indir` + `icsIndir`'de revoke `setTimeout(…,0)` ile ertelendi. **Doğru bulunan (kod değişmedi):** seed referans bütünlüğü, pano istatistik sayıları (24/8/2/17/4/₺184.500, timezone güvenli), hazırlık skoru (≤100, sıfır bölme yok, çelişkisiz), yazma işlemleri (eksik await yok, kaskadlar tam). `npm run build` geçti, **365 test** yeşil. |
| 164 | 2026-08-15 03:30 | **Erişilebilirlik: gezinmede odak yönetimi eklendi (SPA odak-kaybı hatası).** Denetim: rota değişince router içeriği başa sarıp `document.title`'ı güncelliyordu ama ODAĞI taşımıyordu. Runtime doğrulama (Playwright): satır içi bir bağlantıya (dosya satırı) tıklayıp detaya gidince `activeElement` **BODY**'ye düşüyor (odak kayboluyor); drawer'dan gidince menü düğmesinde kalıyor — her iki halde de yeni sayfaya inmiyor, klavye/ekran okuyucu kullanıcısı bağlamı kaybediyor (ileri yaş kitle için önemli, F23). **Düzeltme:** `AppShell`'te yol değişiminde odak `#ana-icerik` (zaten `tabIndex=-1` olan `<main>`) öğesine taşınıyor; ilk yüklemede taşınmaz; `requestAnimationFrame` ile ertelenir ki drawer'ın kendi odak-iadesinden sonra çalışıp odağı yeni sayfaya oturtsun; `preventScroll` ile router'ın başa-sarmasıyla çakışmaz. **Doğrulandı:** satır içi bağlantı → MAIN#ana-icerik; drawer nav → MAIN#ana-icerik; drawer'ı Esc ile (gezinmeden) kapatınca odak yine tetikleyici düğmeye dönüyor (mevcut davranış korundu). `npm run build` geçti, **365 test** yeşil. |
| 163 | 2026-08-15 02:40 | **F23 açık maddesi tamamlandı: kategori renkleri METİN olarak WCAG AA'yı karşılıyor.** Önceki denetim, kategori `-fg` pastellerinin küçük SAYISAL/uyarı metni olarak (para tutarları, rapor istatistikleri, süre uyarısı, eksik-iş ipucu, rozetler, avatar baş harfleri, son-tarih sayacı) ~2.0–2.3:1 ile AA altında kaldığını göstermişti — finans uygulaması + ileri yaş kitle için kritik. **Çözüm (kimliği bozmadan):** hue korunarak koyulaştırılmış `--cat-X-text` token'ları (bir kontrast script'iyle ≥4.55:1 hedefine göre hesaplandı) eklendi; aksan sistemine `--acc-text` girdisi kondu ve TÜM ön plan (metin/ikon) kullanımları — sınıf tabanlı (`money-value/amount`, `rapor-stat-value`, `deadline-kalan`, `task-badge`, `health-level`, `file-percent`, `tag`, `sonuc-eyebrow/kalan`, `field-error`, `button-danger`, `check-action`, `sonuc-uyari`, avatarlar, durum ikonları) ve satır içi (Finans/Raporlar tutarları, MüvekkilDetay/Bildirimler/Ayarlar/Görevler/Müvekkiller) — buna bağlandı. **Dekoratif kullanımlar (nokta, kenarlık, rozet zemini, sol aksan) `-fg`'de bırakıldı; pastel görünüm korundu.** **Runtime doğrulama (Playwright, hesaplanan renk × gerçek zemin):** money-amount/stat/badge/percent 2.0–2.3:1 → **4.93–5.33:1, hepsi AA PASS**; ekran görüntüsünde tutarlar net okunur, görsel dil aynı. Varsayım günlüğe yazıldı (soluk hafta sonu başlıkları ve onboarding illüstrasyonları dokunulmadı). `npm run build` geçti, **365 test** yeşil. |
| 162 | 2026-08-15 01:50 | **Altı gerçek hata düzeltildi (sayfa/router + CSS denetimi); bir AA maddesi `[ ]` olarak açıldı.** **(1) ORTA — yedek geri yüklemeden sonra bayat ayar alanları + sessiz üzerine yazma.** Profil/sessiz saat/LLM alanları kontrolsüz (`defaultValue`+`onBlur`); `yedektenGeriYukle` sayfayı yenilemediği için geri yüklenen değerler GÖRÜNMÜYOR ve o alana dokunulunca eski değer geri yazılıyordu. Sıfırlama akışıyla aynı şekilde başarıdan sonra `window.location.reload()` eklendi. **(2) DÜŞÜK — müvekkil "ilk dosya açılış tarihi" yanlış.** `dosyalar` açık-önce/yeni→eski sıralı; `.at(-1)` en erken dosya DEĞİL (kapalı grubun en eskisi). Gerçek en erken `acilisTarihi` `reduce` ile hesaplanıyor. **(3) ORTA — tek dokunuşla geri alınamaz silme (belge/süre/not/kişi).** Satır "×" onaysız/geri-alınamaz siliyordu; belge çoğu zaman dosyanın tek kopyası. Paylaşılan `SilDugmesi` bileşeni: ilk dokunuş "Sil?" onayı, ikincisi siler, 3 sn sonra geri alınır (uygulamanın "Emin misiniz?" desenine uyumlu); 5 silme noktasına uygulandı. **E2E:** ilk dokunuş silmiyor ("Sil?" gösteriyor), ikinci dokunuş siliyor. **(4) ORTA — Takvim iki-düğmeli FAB kümesi son satırı kapatıyordu** (`.fab-spacer` 76px < küme ~130px); kümeye özel `.fab-spacer-cluster` (140px). **(5) ORTA — büyük tutar dar ekranda yatay taşma:** `.money-grid` ≤360px'te tek sütun + hücrelere `min-width:0`. **(6) DÜŞÜK — drawer sol güvenli alanı yok sayıyordu** (yatay/çentik); sol dolgu `max(space-4, safe-left)`. **Ertelendi:** kategori renklerinin METİN olarak AA'yı karşılaması (F23'e `[ ]` madde eklendi — dekoratif kullanımı bozmadan `--cat-X-text` varyantı; renk seçimi kontrast doğrulaması ister, ayrı odak turu). Sekme değişiminde taslak kaybı (router-denetim bulgusu) sekmeler için tasarım gereği kabul edildi. `npm run build` geçti, **365 test** yeşil. |
| 161 | 2026-08-15 00:45 | **Beş gerçek hata düzeltildi (bileşen/servis/form denetimi + doğrulama).** **(1) YÜKSEK — dosya düzenlemede "+ Yeni müvekkil ekle" veriyi bozuyordu.** Seçenek düzenleme modunda da görünüyordu ama `dosyaGuncelle`, `yeniMuvekkilAdi`'nı yok sayıp `muvekkilId:''` yazıyor, üstelik bunu dosyaya bağlı TÜM finans/olay/görev kayıtlarına yayıyordu (müvekkil silinir, dosya yetim kalır). Düzeltme: `dosyaGuncelle` artık `dosyaEkle` gibi yeni müvekkili oluşturup id'sini kullanıyor; hedef müvekkil boşsa eskisi korunuyor (dosya asla müvekkilsiz kalmaz); müvekkil yazımı+dosya+kopya eşitleme tek transaction. **E2E doğrulandı:** yeni müvekkil oluştu (tüzel), dosya ve tüm bağlı kayıtlar ona işaret ediyor, boş muvekkilId=0. **(2) ORTA — formlar çift kayıt üretebiliyordu.** OlayForm/GorevForm/DosyaForm/FinansForm/MuvekkilForm kaydet düğmesi yazım uçarken kilitlenmiyordu; mobilde çift dokunuş iki kayıt (tekrarlıda iki seri = 16 olay) oluşturabilirdi. Beşine de `kaydediliyor` bekçisi + `disabled` eklendi (SureHesapla deseni). **(3) ORTA — kilit ekranı arka planı gizlemiyor + klavyeyle PIN girilemiyordu.** Kilitliyken uygulama arkada odaklanabilir kalıyordu (Tab ile gezinme/dosya verisine ulaşılıp kapı aşılabilir); ayrıca fiziksel klavyeyle PIN yazılamıyordu. Düzeltme: içerik sarmalayıcısına kilitliyken `inert` (erişim kapatılır), kilit ekranına odak + `keydown` ile rakam/Backspace girişi. **E2E:** arka plan inert=true, Tab arka plana geçmiyor, klavyeyle "1234" yazınca açılıyor. **(4) DÜŞÜK–ORTA — onay metni kaydırma gerektirmezse kilitlenme.** `okundu` yalnızca `onScroll`'da set ediliyordu; metin kısa/ekran uzunsa olay hiç tetiklenmez, kutular sonsuza dek kilitli kalır. Mount + ResizeObserver ile "zaten tümü görünür" kontrolü eklendi. **(5) DÜŞÜK — kapalı drawer bağlantıları Tab sırasında.** Kapalıyken panel `aria-hidden` ama çocukları odaklanır durumdaydı; panele kapalıyken `inert` verildi. `npm run build` geçti, **365 test** yeşil. |
| 160 | 2026-08-14 23:50 | **Üç gerçek hata düzeltildi (bağımsız denetim + doğrulama).** **(1) YÜKSEK — hesaplanan süre her yerde ÇİFT görünüyordu.** `sureKaydet`, "Takvime ekle" açıkken (varsayılan) hem `sure` kaydı hem ayrı bir `son-tarih` OLAYI (`kaynak:'sure-hesabi'`) yazıyordu; oysa takvim (`araliktakiOgeler`), hatırlatmalar (iki kanca) ve ICS (`icsUret`) süreleri zaten doğrudan `sureler` tablosundan çiziyor → aynı son gün: takvimde çift satır, iki bildirim (03:00 + 09:00), iki VEVENT. Üstelik "Takvime ekle" KAPALI iken de süre takvimde görünüyordu (süreler koşulsuz çiziliyor) → düğme yalnızca kopya üretiyordu. Seed bu olayı hiç yazmadığı için demo maskeliyordu; hata yalnızca kullanıcı bir süreyi kaydedince ortaya çıkıyordu. **Düzeltme:** son-tarih olayı artık üretilmiyor (süre tek kaynak); bozuk "Takvime ekle" düğmesi kaldırıldı; eski kayıtlar için okuma yollarında (`takvimSorgulari`, `hatirlatmaSorgulari`×2, `TakvimSenkron`/ICS, `dosyaSorgulari` Duruşmalar sekmesi) `kaynak==='sure-hesabi'` eleniyor. E2E doğrulandı: yeni süre takvimde tam **1** kez görünüyor (önceden 2), sure-hesabi olay=0, olayId=null. Varsayım: hukuki güvenlik gereği hesaplanan son gün takvimde her zaman görünür (gizlenemez). **(2) ORTA — Rapor CSV Türkçe Excel'de bozuk.** `raporCsv` `;` ayraç + BOM ile Türkçe Excel hedefliyor ama tutarları `toFixed(2)` ile NOKTA ondalıkla yazıyordu ("184500.50" → yanlış okunur). Ondalık virgüle çevrildi (`csvTutar`); 3 birim test eklendi. **(3) DÜŞÜK — `dosyaGuncelle` atomik değildi.** `db.dosyalar.update` denormalize `muvekkilId` eşitleme transaction'ının DIŞINDAydı; arada bir hata dosyayı yeni müvekkile taşırken kopyaları eskide bırakırdı. İkisi tek transaction'a alındı. `npm run build` geçti, **365 test** yeşil (+3). |
| 159 | 2026-08-13 15:10 | **Türkçe-arayüz uyumu doğrulandı (CLAUDE.md: "Arayüzün tamamı Türkçe") — sağlam, kod değişikliği yok.** İki sızıntı riski tarandı. **(1) Tarih yerelleştirmesi:** tüm `date-fns` `format()` çağrıları (tarih.ts, Takvim.tsx) ay/gün/haftagünü adı üretenlerin HEPSİ `{ locale: tr }` geçiyor (EEEE/MMMM/MMM/d MMMM yyyy → "Ağustos", "Pazartesi") — locale'siz tek çağrı `HH:mm` (saat, dile bağımsız). İngilizce ay/gün sızıntısı yok. **(2) Kullanıcı-yüzü öznitelikler:** `aria-label`/`placeholder`/`title`/`alt` içinde İngilizce UI sözcüğü yok (tek eşleşme "esas no" — Türkçe hukuk terimi, "no"=numara). Ekran okuyucu metni de dahil arayüz tam Türkçe. Sonuç: Türkçe hard-kısıtı karşılanıyor. `npm run build` geçti, 362 test yeşil. |
| 158 | 2026-08-13 14:40 | **Gizlilik/veri-akışı uyumu kapsamlı doğrulandı — CLAUDE.md sert kısıtları karşılanıyor, kod değişikliği yok.** "Müvekkil verisi cihazdan dışarı çıkmaz + üçüncü taraf analitik yok + LLM varsayılan kapalı" kısıtları uçtan uca tarandı. **(1) Ağ:** tüm kaynak kodda TEK `fetch()` var — `llm.ts:61`, yalnızca kullanıcının kendi LLM uç noktası (opt-in, varsayılan kapalı, kullanıcı girer). Başka fetch/XHR/WebSocket/sendBeacon/EventSource yok. **(2) Analitik/izleme:** gtag/google-analytics/sentry/mixpanel/segment/posthog/fbq/hotjar/amplitude — HİÇBİRİ yok; harici `<script src>` yok. **(3) Sabit dış URL:** yalnızca `Ayarlar.tsx:401`'de `api.openai.com` — input `placeholder` ipucu (varsayılan boş), çağrı değil. **(4) Yasak aşırı-vaat ifadeleri:** "hacklenemez/%100 güvenli/hiçbir veri paylaşılmıyor" vb. UI'da yok (tek "%100" eşleşmesi yaziOlcegi font ölçeği). **(5) Fontlar:** 8 `@font-face` tümü yerel `assets/fonts/*.woff2`; Google Fonts CDN/`@import`/`https://` yok → gizlilik sızıntısı yok, çevrimdışı çalışır. **(6) SW precache:** yalnızca yerel `{js,css,html,svg,png,woff2}`; harici önbellek yok. Sonuç: "veri cihazdan dışarı çıkmaz" gerçekten geçerli — hassas müvekkil verisi için kritik. `npm run build` geçti, 362 test yeşil. |
| 157 | 2026-08-13 14:00 | **UYUM hatası düzeltildi: Dosyalar listesi hesaplanmış hukuki süreyi "bilgilendirme amaçlıdır" uyarısı OLMADAN gösteriyordu (CLAUDE.md sert kısıtı).** CLAUDE.md: *"Hukuki süre hesabı bilgilendirme amaçlıdır. HER EKRANDA ve hesap çıktısında bu uyarı görünür olmalı."* Süre gösteren ekranlar denetlendi: pano/raporlar/bildirimler/süre-hesapla/dosya-detay/takvim uyarıyı taşıyor; **Dosyalar listesi taşımıyordu** — ama `dosyaSorgulari.sonrakiIs` en yakın iş bir SÜRE olduğunda `kuralAdi + sonTarih + aciliyet-rengi` (hesaplanmış hukuki son gün) gösteriyor (ör. "İstinaf başvuru süresi · 16 Ağustos · 2 gün kaldı"). Yani liste hesaplanmış süre gösterip uyarıyı atlıyordu. **Düzeltme:** Dosyalar listesinin altına diğer ekranlarla aynı sessiz altbilgi eklendi ("Satırlarda gösterilen hukuki süreler bilgilendirme amaçlıdır; son günün doğruluğunu teyit etmek kullanıcının sorumluluğundadır."). **TakvimSenkron denetlendi, uyarı gerekmiyor:** yalnızca SAYI ("X süre dışa aktarıldı") + özellik açıklaması gösteriyor, belirli hesaplanmış tarih göstermiyor; ICS'in kendisi her süre DESCRIPTION'ına uyarıyı gömüyor. **E2E (Playwright):** Dosyalar artık uyarıyı gösteriyor, taşma 0, 0 hata. Böylece hesaplanmış süre gösteren TÜM ekranlar uyarılı. `npm run build` geçti, **362 test** yeşil. |
| 156 | 2026-08-13 13:20 | **Kalan bileşen/yapılandırma denetimi (tema, gezinme, ikon, çevrimdışı) — hepsi sağlam, kod değişikliği yok.** Daha önce açıkça denetlenmemiş yüzeyler incelendi. **(1) Tema/PWA açılışı:** `index.html` `<meta name="color-scheme" content="light">` → OS koyu modunda form kontrolleri/kaydırma çubuğu ters dönmez; PWA manifest `background_color:#F7F6F3` (açık, uygulamayla aynı → açılış ekranı koyu-sonra-açık titremesi yok), `theme_color:#16262E` marka, `start_url/scope:'.'` göreli (base-uyumlu), 3 ikon mevcut. **(2) Drawer gezinme:** `navGroups` tüm ana rotaları kapsıyor (genel bakış[exact]/takvim/dosyalar/müvekkiller/görevler/finans/asistan/raporlar + ayarlar); `/ara` ve `/bildirimler` üst-çubuk ikonlarından, `/sure` bağlamsal → her rota erişilebilir; a11y güçlü (odak tuzağı, odak geri yükleme, gövde-kaydırma kilidi, Esc, scrim). **(3) Icon:** `IconName` katı birleşim + `paths: Record<IconName,…>` exhaustive → `tsc -b` eksik/bozuk ikonu imkânsız kılar. **(4) Çevrimdışı:** `useCevrimici` SSR-güvenli başlangıç + online/offline dinleyici + temizlik + mount'ta yeniden-eşitleme; bayat-kapanış/sızıntı yok. Sonuç: kalan bileşenler ve dağıtım/tema yapılandırması sağlam. Böylece uygulama uçtan uca (veri bütünlüğü/mantık/servisler/formlar/router/düzen: mobil-geniş-kısa-büyükyazı/PWA/tema/gezinme/erişilebilirlik) denetlenmiş oldu. `npm run build` geçti, 362 test yeşil. |
| 155 | 2026-08-13 12:40 | **Büyük-yazı erişilebilirlik modu (F23, %125) düzen bütünlüğü doğrulandı — sağlam, kod değişikliği yok.** `yaziOlcegi` "Çok büyük" (kök font 16→**20px**) mantığı vardı ama en büyük ölçekte DÜZEN görsel olarak hiç denetlenmemişti (kartlar/satırlar/haplar taşabilir/kırpılabilir). **E2E (Playwright, 390px, %125):** Ayarlar'dan "Çok büyük" seçildi (kök font-size **20px** doğrulandı) → **12 rota** (pano, takvim, raporlar, dosyalar, müvekkiller, görevler, finans, asistan, ayarlar, bildirimler, dosya-detay, süre) tarandı: **hepsinde yatay taşma 0**, konsol hatası 0. Ekran görüntüsü (pano): serif selam 2 satıra zarifçe sarıyor, istatistik-kart etiketleri ("Aktif dosya", "Bu haftaki duruşma") kart içinde sarıyor, sayılar belirgin, üst çubuk kontrolleri hizalı (row 151); kırpma/kırılma yok. rem-tabanlı sistem en büyük ölçeği temiz taşıyor. Böylece erişilebilirlik ölçekleme boyutu da (mobil 390 / geniş 1024 / kısa 320-360 / büyük-yazı %125) kapsandı. `npm run build` geçti, 362 test yeşil. |
| 154 | 2026-08-13 12:00 | **Kısa-ekran kardeş taraması (row 153 sonrası) — kalan sabit-yerleşim ekranları sağlam, kod değişikliği yok.** row 153 onay ekranı kısa-yükseklik hatasını açınca aynı desendeki (sabit tam-ekran + uzun içerik + footer) ekranlar denetlendi. **E2E (Playwright, 320×568 VE 360×640, tüm akış uçtan uca):** onay→kayıt→app→PIN-kilit→açılış hepsi geçiyor — kayıt ekranı `.kayit-govde` her şeyi ortalı kapsıyor (ayrı örtüşen footer yok, kısa ekranda sığıyor), Devam tıklanabilir + app'e giriyor; kilit ekranı ortalı + PIN-pad'in 4 hanesi tıklanabilir + açıyor; 0 hata. **Kod denetimi:** belge önizleme (`.onizleme-pencere` `max-height:100%`+`overflow:hidden`, başlık `flex:none` → kapat hep erişilir, `.onizleme-govde` `overflow:auto` uzun içerik kaydırır); DevirTeslim modalı aynı ortalı `.onizleme-katman`'ı kullanıyor (küçük içerik); `.skip-link` ortalı gizli. Sonuç: onay ekranı kısa-yükseklik hatasının TEK örneğiydi (ayrı flex-kardeş CTA + `min-height:140` metin); row 153 düzeltmesi bu sınıfı tamamen kapattı. `npm run build` geçti, 362 test yeşil. |
| 153 | 2026-08-13 11:10 | **CİDDİ hata düzeltildi: kısa/orta ekranda hukuki ONAY tamamlanamıyordu → uygulamaya hiç girilemiyordu.** 320/360px YÜKSEKLİK taraması (önceki turlar yalnızca 390×844 tall kullanıyordu) ortaya çıkardı. Onay ekranında `.onay-govde` `flex:1` + `overflow:visible`; metin kutusu (`.onay-metin` `min-height:140px`, `max-height:40vh`) küçülemeyince metin + 5 onay kutusu yüksekliğe sığmıyor, kutular taşıp normal-akıştaki "Kabul et ve başla" düğmesinin **ALTINA** düşüyor, düğme onları örtüp **tıklanamaz** kılıyordu. **@360×640 (yaygın Android portre): bir kutu tıklanamıyor → 5 kutu işaretlenemiyor → CTA hiç açılmıyor → onay tamamlanamıyor → uygulama kullanılamaz.** (Tall 390×844'te sığdığından önceki E2E'ler yakalayamadı.) **Düzeltme:** `.onay-govde`'ye `overflow-y:auto` — içerik sığmayınca govde İÇERİDE kaysın, kutular govde kutusuna kırpılıp CTA'nın altına taşmasın, kaydırılarak erişilebilsin. **E2E (Playwright, etiketle tıklama):** @320×568 ve @360×640 artık **5 kutu tıklanıyor + CTA açılıyor** (önce @360 `ok=false`); @390×844 **etkilenmedi** (`lastRowBelowCTA=false`, govde kaymıyor — içerik sığıyor, düzen aynı). Ekran görüntüsü: 360'ta kaydırınca 5 kutu + CTA erişilir, örtüşme yok. `npm run build` geçti, **362 test** yeşil. |
| 152 | 2026-08-13 10:20 | **TASARIM hatası düzeltildi (row 151 kardeşi): geniş ekranda kayan ekle-düğmesi (FAB) içerik sütunundan kopuyordu.** row 151'in geniş-ekran taramasını sürdürürken bulundu. `.fab` (ör. "Yeni dosya"/"Yeni olay") ve `.fab-cluster` `right: max(space-5, safe-right)` ile VIEWPORT sağ kenarına sabitlenmişti; 1024px'te FAB içerik sütununun (ortalı 640px) sağından **~172px** açıkta duruyordu. **Düzeltme:** `right`'a üçüncü terim eklendi — `calc(50% - var(--content-max) / 2 + var(--space-5))`; geniş ekranda bu terim baskın olup FAB'ı içerik sütununun sağ kenarına, mobilde ≤ space-5 olup eski davranışa bırakır. **Doğrulama (Playwright):** @1024 fab-sağ↔içerik-sağ boşluğu ~+172→**−20px**, @390 **−20px** (değişmedi) — ikisinde de içerik kenarından tam space-5 içeride, taşma 0. Ekran görüntüsü: FAB artık liste kartlarının üzerinde, viewport kenarında değil. Diğer sabit öğeler temiz: drawer `min(296px,86vw)` (makul), toast/onizleme/kilit/onboarding ortalı ya da tam ekran. `npm run build` geçti, 362 test yeşil. |
| 151 | 2026-08-13 09:40 | **TASARIM hatası düzeltildi: geniş ekranda üst çubuk kontrolleri içerik sütunundan kopuyordu.** İçerik `.shell-content` `max-width:640px; margin:0 auto` ile ortalıyken `.topbar` `position:fixed` tam genişlikti ve çocuklarını (hamburger solda, sürüm/ara/bildirim sağda) `space-between` ile viewport KENARLARINA yayıyordu. Ölçüm (@1024px): hamburger içerik sütununun **184px soluna**, aksiyonlar **176px sağına** düşüyordu → masaüstünde kontroller içerikten kopuk, cilasız. (Uygulama mobil-öncelikli ama GitHub Pages'te ana-ekrana-eklemeden ÖNCE masaüstü/tarayıcıda görülüyor.) **Düzeltme:** kontroller yeni `.topbar-inner` sarmalayıcıya alındı (`max-width:var(--content-max); margin:0 auto`, `.shell-content` ile aynı kenar dolgusu); `.topbar` tam-genişlik blur arka planını korur, iç sarmalayıcı içerikle hizalanır. **Doğrulama (Playwright):** @1024 hamburger↔içerik-sol boşluğu +184→**−8px** (hamburgerin bilinçli optik `-8px` marjı), aksiyonlar↔içerik-sağ +176→**−16px** = hizalı; @390 mobil **değişmedi** (birebir aynı), ikisinde de taşma 0. Ekran görüntüsü: masaüstü artık tek tutarlı sütun. `npm run build` geçti, **362 test** yeşil. |
| 150 | 2026-08-13 09:10 | **Denetim + tam-rota regresyon taraması — sağlam, kod değişikliği yok; son turların (bildirim kancası, PIN, transaction'lar) canlı temiz olduğu doğrulandı.** **Denetlendi, sağlam:** `GorevForm` (başlık zorunlu, tekrar yalnızca oluşturmada, `tekrarAdet` MAKS_TEKRAR ile aşağıda kırpılıyor); `Tur` rehberli tur (hedefi 2.5sn yoklar, bulamazsa önceki kutuyu korur → çökme yok); **JSON.parse/web-storage güvenliği** — tüm `JSON.parse` (yedek.ts) ya try/catch'li ya da kanıtlanabilir-güvenli, `devir.ts` localStorage try/catch'li; KilitKapisi sessionStorage guard'sız ama önemsiz (storage kapalıysa zaten IndexedDB=tüm veri katmanı çöker); `hukukAi.ts` — maskeleme katmanı (`maskele`/`demaskele`/`bagliDegilHukukAi`) kendi dosyası dışında HİÇ çağrılmıyor (grep boş) → maskesiz fallback zararsız, LLM yolu zaten `dosyaOzeti` (CLAUDE.md uyumlu) gönderiyor, gerçek maskeleme `[!]` pakete bağlı. **Regresyon taraması (Playwright, 390px, Notification stub, 12 rota):** yatay taşma **hepsinde 0**, konsol/page hatası **0**, saf gezinti sırasında **0 asılsız bildirim** (bildirim kancası rota değişiminde ateşlemiyor — tohumla+dedup çalışıyor, spam yok). Son turların değişiklikleri canlıda temiz, regresyon yok. `npm run build` geçti, 362 test yeşil. |
| 149 | 2026-08-13 08:10 | **Bütünlük sağlamlaştırması (row 148 kardeşi): `sureKaydet` atomik değildi — yarım kayıtta yetim olay ya da olaysız-ama-`olayId`'li süre riski.** row 148'de aynı deseni ararken bulundu. `sureKaydet` sırayla `db.sureler.add` → (takvimeEkle ise) `db.olaylar.add` → `db.sureler.update({olayId})` → `db.hareketler.add` yapıyordu, transaction'sız. Olay yazımı olup `update` başarısız olursa: süre `olayId`'siz kalır → `sureSil` bağlı olayı temizleyemez (yetim son-tarih olayı takvimde asılı kalır). Ya da süre yazılıp olay başarısız olursa: "takvime ekle" isteği sessizce yerine gelmez. **Düzeltme:** tüm yazımlar tek `db.transaction('rw', [sureler, olaylar, hareketler])` içine alındı; ayrıca `olayId` süreye **baştan** konarak ikinci `update` tamamen kaldırıldı (hem atomik hem sade). **E2E (Playwright):** süre hesaplanıp dosyaya "Takvime ekle" ile kaydedildi → sureler 3→**4**, olaylar 13→**14** (ikisi de +1); süre `olayId` taşıyor, bağlı olay `kaynak=sure-hesabi`, çift yönlü bağ `olay.sureId===sure.id` sağlam, 0 hata. Böylece uygulamadaki tüm çok-tablolu yazımlar artık transaction'lı (dosyaEkle row 148, dosyaSil/finansSil/muvekkilSil önceden, sureKaydet şimdi). `npm run build` geçti, **362 test** yeşil. |
| 148 | 2026-08-13 05:40 | **Bütünlük sağlamlaştırması: dosya + satır-içi yeni müvekkil oluşturma atomik değildi (yetim müvekkil riski).** `dosyaEkle` satır-içi "yeni müvekkil" akışında önce `db.muvekkiller.add` (kendi auto-commit'i), SONRA ayrı `db.dosyalar.add` yapıyordu; dosya yazımı başarısız olursa (ör. kota) müvekkil **yetim** kalırdı (formda try/catch yok → sessiz hata + kullanılmayan kayıt). Uygulamanın diğer çok-tablolu yazımları (`dosyaSil`/`finansSil`/`muvekkilSil`) transaction kullanıyor; bu oluşturma yolu kullanmıyordu. **Düzeltme:** müvekkil+dosya+hareket yazımları tek `db.transaction('rw', …)` içine alındı → ikisi de yazılır ya da hiçbiri (Dexie throw'da geri alır). Davranış aynı (müvekkil önce hazırlanıp id atanıyor, hareket ayrıntısı için tx içinde okunuyor). **E2E (Playwright):** satır-içi "İnline Test İnşaat A.Ş." müvekkiliyle dosya açıldı → müvekkil sayısı 24→**25** (tam +1, kopya/yetim yok), dosya yeni müvekkil id'sine **bağlı**, tür **tuzel** (A.Ş. doğru sezildi), 0 hata. Birim test yok (projede fake-indexeddb yok; yazımlar E2E ile doğrulanıyor). **Denetlendi, sağlam:** `MuvekkilForm` (ad zorunlu, ön-doldurma korumalı), `dosyaGuncelle` müvekkil re-sync tam (finans/olay/görev; not/kişi/belge muvekkilId taşımaz), önceki tur bildirim kancası (ilk-tanımlı-turda tohumla, dedup). `npm run build` geçti, **362 test** yeşil. |
| 147 | 2026-08-13 05:30 | **Ölü özellik tamamlandı: cihaz bildirimleri hiç ateşlenmiyordu + "sessiz saatler" ayarı hiç okunmuyordu (iki yanıltıcı UI vaadi).** İki UI açıkça söz veriyordu — Bildirimler: "Uygulama açıkken yaklaşan süre ve duruşmalar cihaz bildirimi olarak da düşsün"; Ayarlar: "Bu saatler arasında cihaz bildirimi gönderilmez" — ama `cihazBildirimiGoster` hiçbir yerden çağrılmıyordu (hiç bildirim düşmüyordu) ve `sessizSaatBaslangic/Bitis` yalnızca yazılıp hiç OKUNMUYORDU. İskele tamamdı (izin isteği, `cihazBildirimiGoster`, sessiz-saat UI); sunucusuz (ön plan Notification API) olduğundan `[!]` değil, eksik özellik. **Tamamlandı:** (1) saf `sessizSaatteMi`/`dakikaya` (gece yarısını aşan pencere doğru: [bas,bitis) veya sarma) + 5 birim test; (2) `useCihazBildirimTetikleyici` kancası AppShell'e bağlandı — tetik anı GELEN hatırlatmayı cihaz bildirimi yapar. **Muhafazakâr (yağmur yok):** açılışta aktif olanlar "bildirildi" tohumlanır (yalnızca oturumda AKTİFLEŞENLER düşer), id'ye göre oturumda tek kez, kapılar: push kanalı açık + izin verili + sessiz saatte değil (sessizde ertelenir, bastırılmaz). `pencere` alt sınırı (şimdi−3g) aktifleri olay başına ~1-2 ile sınırlar → patlama yok. **E2E (Playwright, Notification stub):** onboarding+gezinti sonrası **0 bildirim** (seed hatırlatmaları tohumlandı, spam yok); gelecek olay eklenince **tam 1** bildirim (`{başlık, gövde:"Duruşma"}`, patlama değil); 0 konsol/page hatası. `npm run build` geçti, **362 test** yeşil (+5). |
| 146 | 2026-08-13 04:30 | **HATA düzeltildi (ciddi — kullanıcı kilitlenmesi): 4 haneden uzun PIN girilemiyordu, uygulamaya erişim kalıcı engelleniyordu.** Kilit ekranı (`KilitKapisi`) denetlenirken bulundu. Ayarlar 4–8 haneli PIN'e izin veriyor ("PIN en az 4 rakam olmalı", `slice(0,8)`), ama kilit ekranı `rakamGir` her `length >= 4`'te otomatik doğrulayıp başarısızlıkta `setPin('')` yapıyordu. 6 haneli PIN'de: 4. hane girilince `dogrula("1234")` → yanlış (gerçek "123456") → giriş **sıfırlanıyor**; 5–6. hane hiç girilemiyor → kullanıcı kendi uygulamasına **kalıcı kilitleniyor** (özet uzunluğu vermediğinden ekran hedef uzunluğu bilemez). **Düzeltme:** PIN kurulurken `pinUzunlugu` ayarlara yazılıyor; kilit ekranı `hedefUzunluk = ayarlar.pinUzunlugu ?? 4` ile girişi TAM o uzunlukta doğruluyor, o uzunlukta kırpıyor ve nokta sayısını ona göre çiziyor (eski kayıtta yoksa 4 — üretim öncesi, 4 haneli yaygın durum korunur; >4 zaten kullanılamıyordu). **E2E (Playwright):** 6 haneli PIN kuruldu → uygulama anında kilitlendi, kilit ekranı **6 nokta** gösteriyor, "123456" girişi **açıyor** (düzeltmeden önce 4. hanede sıfırlanır, imkânsızdı), 0 pageerror. Bileşen+DOM olduğundan birim yerine E2E ile doğrulandı. `npm run build` geçti, **357 test** yeşil. |
| 145 | 2026-08-13 00:30 | **Sağlamlaştırma: `matchPath` bozuk yüzde-kodlu URL'de çöküyordu (URIError → tüm uygulama hata sınırına düşüyordu).** row 144 TZ-sınır kardeş-taraması sırasında router denetlendi. `matchPath` `:id` segmentini `decodeURIComponent(pathPart)` ile çözüyor; elle yazılmış geçersiz bir `%` dizisi (ör. `#/dosyalar/foo%`) **URIError** fırlatır, `Routes` useMemo'sundan yukarı yayılıp **tüm ekranı** HataSiniri'ye düşürürdü — oysa bozuk URL zarifçe "bulunamadı"ya düşmeli. **Düzeltme:** `guvenliCoz` yardımcısı `decodeURIComponent`'i try/catch'e alıp bozukta ham değeri döndürüyor → `matchPath` asla patlamıyor, bozuk parametre gerçek kayda eşleşmeyip yer tutucuya düşüyor. **Test:** matchPath saf+dışa açık olduğundan birim test eklendi (`foo%`/`%ZZ` → patlamaz, ham döner). **Kardeş taraması (row 144):** diğer zaman-damgası aralık sorguları güvenli — `aralik()` `gunBaslangici`+`toISOString()` (yerelden doğru UTC anı) kullanıyor, `sorgular.ts` `.between()` çağrıları doğru. **Denetlendi, sağlam (değişiklik yok):** `finansIslemleri` (`durumNormalize` ödeme durumunu tutardan türetir → tutarsız durum imkânsız; `odenenKirp` [0,tutar]'a kırpar → negatif bekleyen yok), `olay.ts` (tüm OlayTuru kapsanmış, exhaustive Record), `SureHesapla.tsx` (doğrulanmış motor + zorunlu "bilgilendirme amaçlıdır" uyarısı + kapsam-dışı uyarısı), router hash-query ayrıştırma (`#/x?a=1` doğru). `npm run build` geçti, **357 test** yeşil (+1). |
| 144 | 2026-08-12 23:30 | **HATA düzeltildi: Ajanda ve Hafta takvim görünümleri ilk günün TÜM-GÜN olaylarını düşürüyordu (saat dilimi sınır hatası).** `takvimSorgulari.araliktakiOgeler` olay sorgusunun aralık sınırlarını naif `${gun}T00:00:00.000Z` ile kuruyordu — yerel günü UTC sanıyor. Ama olaylar UTC saklanıp **yerel güne** kovalanıyor (`dateToIsoDate`). UTC+3'te ilk günün elle-eklenen tüm-gün olayı yerel gece yarısı = **önceki gün 21:00Z** saklanır; bu damga `basGunT00:00Z`'nin ÖNCESİNE düşüp sorgudan **dışlanıyordu** → olay takvimde kayboluyordu. **Ay görünümü** −7/+14 dolgu sayesinde korunuyordu (kayıp gün görünür ızgaranın dışında), ama **Ajanda** (`useAjanda`, ilk gün = bugün) ve **Hafta** (`useHaftaOgeleri`, ilk gün = Pazartesi) dolgusuz → veri kaybı. (Süre-kaynaklı tüm-gün olayları UTC-gece-yarısı saklandığından tesadüfen etkilenmiyordu; yalnızca `damgaKur` ile yerel-gece-yarısı saklanan elle olaylar.) **Düzeltme:** sınırlar yerel gün başlangıç/bitişinden türetiliyor — `new Date(`${gun}T00:00:00`).toISOString()` (Z'siz → yerel yorum → doğru UTC anı), `isoDateToDate`/`damgaKur` ile aynı desen. Süre olayları her iki sınırda da dahil kalıyor (doğrulandı). **E2E (Playwright, Europe/Istanbul):** tarayıcı "bugünü" yerel 13 Ağu; bugüne elle tüm-gün olay (`2026-08-12T21:00:00.000Z` — eski sınır `…13T00:00Z`'nin altında, dışlanırdı) seed edildi → düzeltmeyle **Ajanda VE Hafta'da görünüyor** (önce ikisinden de kaybolurdu), 0 pageerror. Birim testle yakalanamaz (UTC test ortamı maskeler; row 134/135 gibi yalnızca yerel TZ ortaya çıkarır). `npm run build` geçti, 356 test yeşil. |
| 143 | 2026-08-12 22:30 | **HATA düzeltildi: Asistan dosya özeti bekleyen ödemeyi tutarsız para biçimiyle yazıyordu (ondalık yutuluyor, ₺ yanlış yerde).** Asistan kural motoru denetlenirken bulundu. `dosyaOzeti` bekleyen tutarı ad-hoc `(bekleyen / 100).toLocaleString('tr-TR') + ' ₺'` ile biçimliyordu → küsuratlı tutarda **"10.500,5 ₺"** (tek ondalık, ₺ sonda) veriyordu; oysa uygulamanın kanonik biçimi (`tutarTam`, para.ts) **"₺10.500,50"** (₺ önde, iki ondalık) ve her yerde bu kullanılıyor. Kısmi ödeme (`odenenTutar`) küsurat bırakabildiğinden gerçekçi. **Düzeltme:** `tutarTam(bekleyen)` kullanılıyor → tüm uygulamayla tutarlı. **Test:** `dosyaOzeti` dışa aktarıldı + küsuratlı (1.050.050 kr) bekleyen ödeme için "₺10.500,50" doğrulayan yeni test. **Denetlendi, değiştirilmedi:** (a) hatırlatma türetme (`hatirlatmaSorgulari.ts`): liste ve rozet-sayacı karşılıklı tutarlı (ikisi de `planlandi` olay/`acik` süre süzüyor, geçmiş hedefi atlıyor; erteleme/pencere asimetrisi bilinçli+belgeli). (b) `DosyaDetay.tsx`'te yerel `kategoriEtiketleri` kanonikten kısa (Bilirkişi vs Bilirkişi ücreti) — ama `.row-sub truncate` dar bağlamda "· bekliyor" durumunu görünür tutmak için makul bir tercih; kanonik uzun etiketler 5 kategoride durum metnini kırpabilirdi → regresyon riski, dokunulmadı. `npm run build` geçti, **356 test** yeşil (+1). |
| 142 | 2026-08-12 21:30 | **HATA düzeltildi: son günü GEÇMİŞ açık süre dosya kartında "yaklaşıyor" (approaching) diyordu — geçmiş süreye yanlış ifade.** row 141 süre yaşam döngüsü çizgisini sürdürürken bulundu. `durumNotuUret` (panoda "Dosya sağlığı" kartlarındaki "şu an neyi bekliyor" cümlesi) en erken açık süreyi alıp **her zaman** `"${kuralAdi} yaklaşıyor"` diyordu — oysa o sürenin son günü geçmişse "yaklaşıyor" olgusal olarak YANLIŞ (geçmiştir). Hukuki-süre odaklı bir uygulamada geçmiş bir süreyi "yaklaşıyor" göstermek yanıltıcı. **Düzeltme:** `gunFarki(sonTarih, bugun) < 0` ise `"${kuralAdi} son günü geçti"`, değilse `"…yaklaşıyor"`. Aynı gün (fark 0) hâlâ son gün → "yaklaşıyor" doğru. `gunFarki` import edildi. Tutarlılık teyidi: pano "Yaklaşan son tarihler" (`useYaklasanSureler`, sonTarih artan sıra → geçmiş-açık en üstte kırmızı "geçti" rozetiyle) ve rapor "Açık süreler" halkası (`aciliyet==='gecti'` → kritik sayılıyor) zaten geçmiş-açık süreleri doğru işliyor; yalnızca bu cümle yanlıştı. **Test:** `durumNotuUret` saf fonksiyon dışa açıldı + **yeni `sorgular.test.ts`** (3 test, determinist tarihlerle: uzak-geçmiş açık → "geçti"; uzak-gelecek açık → "yaklaşıyor"; tamamlanmış → yok sayılır) — bu hukuki-doğruluk yolunun ilk birim testleri. `npm run build` geçti, **355 test** yeşil (+3, +1 dosya). |
| 141 | 2026-08-12 20:30 | **HATA düzeltildi: vadesi geçmiş AÇIK süre hazırlık kontrolünde "kaçırılmış" sayılmıyordu — en tehlikeli hâl gizli kalıyordu.** Süre yaşam döngüsü denetlenirken bulundu. `durum: 'kacirildi'` değeri kod tabanında **hiçbir yerde yazılmıyor** (seed dahil) — yalnızca tip tanımı + `hazirlik.ts`'te tek okuma. Dolayısıyla `kacmisSure = sureler.some((s) => s.durum === 'kacirildi')` pratikte **ölü kod**: hiçbir kod süreyi otomatik `kacirildi`'ya çevirmediğinden, son günü geçmiş ama hâlâ `acik` bir süre listelerde kırmızı ("gecti", `aciliyet` ile) görünse de hazırlık motoru **"Kaçırılmış süre yok ✓"** diyordu — avukat için **en tehlikeli durumu** (vadesi geçmiş, ele alınmamış süre) gizliyordu. **Düzeltme:** `kacmisSure` artık iki hâli kapsıyor — (a) elle `kacirildi`, (b) `acik` **ve** `gunFarki(sonTarih, bugun) < 0` (son gün geçmiş). Aynı gün (fark 0) henüz geçmemiş sayılır. Bu, uygulamanın zaten anlık hesapladığı yaklaşım (`aciliyet`/`kalanSureMetni`) ile tutarlı; durumsuz, migration gerektirmez; elle `kacirildi` işareti hâlâ onurlandırılıyor. Yanlış "dikkat" bir avukat için yanlış "temiz"den çok daha güvenli (uygulamanın risk-yüzeye-çıkarma ilkesi). **Test:** 2 yeni regresyon (uzak-geçmiş açık süre → madde eksik + "Kaçırılmış süre var"; uzak-gelecek açık süre → tamam), determinist tarihlerle. **Yan gözlem (değişiklik yok):** `sureIslemleri.ts` süre takvim olayını `${sonTarih}T00:00:00.000Z` (UTC gece yarısı) saklıyor; diğer tüm-gün olaylar `damgaKur` ile yerel-gece-yarısı — ama ikisi de `yerelGun` altında UTC+3'te doğru güne çözülüyor (row 135 kardeş-taraması: ham `.slice` kullanan tüketici yok), görünür hata yok → çalışan kod kurcalanmadı. `npm run build` geçti, **352 test** yeşil (+2). |
| 140 | 2026-08-12 19:30 | **Finans toplulaştırma denetimi + dosya detay (7 sekme) görsel taraması — sağlam, kod değişikliği yok; row 139 düzeltmesi canlı doğrulandı.** **(1) Finans denetimi:** panoda "Bu ay tahsilat" (`sorgular.ts` `tahsilatTopla`) `yon==='gelir'` süzgeci içermiyor gibi görünüp yanlış-pozitif verdi; ama üstteki sorgu `db.finans.where('yon').equals('gelir')` ile veriyi zaten geline süzüyor (satır 70) → tahsilat DOĞRU (gideri saymıyor). "Bekleyen" hesapları (`dosyaSorgulari.finansOzeti`, `muvekkilSorgulari`, `finansSorgulari`) tutarlı: hepsi ödenmemiş `tutar−odenenTutar`'ı iki yönde topluyor (uygulamanın "toplam açık bakiye" semantiği — bilinçli, tutarlı). Kısmi ödeme (`odenenTutar`) her yerde doğru; rapor sayfası gelir/gideri `f.yon` ile ayırıyor. **Spekülatif düzeltme yapılmadı** (yanlış "fix"ten kaçınıldı). **(2) Tarih çekirdeği:** `gunFarki`/`yerelGun`/`isoDateToDate` yerel-gece-yarısı kurgusuyla UTC bir-gün-kayması tuzağından kaçınıyor, `Math.round` DST'siz Türkiye'de güvenli. **(3) Görsel (390px, onboarding UI ile geçilerek, Demir İnşaat dosyası, 7 sekme):** yatay taşma 0, konsol hatası 0, tasarım dili tam; sekme şeridi 599px `overflow-x:auto` (kasıtlı kaydırma). **row 139 CANLI DOĞRULAMA:** Demir İnşaat hazırlık kontrolünde "Gider avansı tamamlandı" artık **✗** + turuncu "Gider avansını **kaydedin**" (gider-avansı kaydı yok) — düzeltmeden önce yanlış ✓ görünürdü; %73 dürüst yüzde. Belgeler sekmesi (belge kartları + "+etiket"), Notlar sekmesi (İlgili kişiler + Notlar, boş durumlar, textarea boşken "Not ekle" pasif) temiz. Yeni tasarım hatası yok. `npm run build` geçti, 350 test yeşil. |
| 139 | 2026-08-12 18:30 | **HATA düzeltildi: gider avansı kaydı HİÇ yokken "tamamlandı" sayılıyordu → hazırlık yüzdesi şişiyor + zorunlu adım gizleniyordu.** Hazırlık motoru (`hazirlik.ts`) denetlenirken bulundu. Harç maddesi bilinçli olarak `harclar.length > 0 && …every(odendi)` (yorum: "kayıt yoksa da eksik sayılır") kullanırken, **paralel** gider-avansı maddesi yalnızca `giderAvanslari.every((f) => odendi)` yapıyordu — boş liste `[].every()` **true** döndürdüğünden **kaydı hiç olmayan dosyada madde ✓ tamam** görünüyordu. Sonuç: (a) hazırlık yüzdesi haksız yükseliyor, (b) daha kötüsü avukata **yatırılmamış** gider avansını "tamam" diye gösteriyordu — oysa eksikliği davanın **açılmamış sayılmasına** yol açar (HMK m. 120/2). Asimetri açıkça gözden kaçmış (harç'ta guard+yorum var, gider-avansı'nda ikisi de düşmüş); iki yükümlülük hukuken paralel. **Düzeltme:** gider-avansı da `length > 0 &&` guard'ı aldı (harç'la birebir) ve eylem metni kayıt yokken "Gider avansını **kaydedin**", varken "…**tamamlayın**" olacak şekilde ayrıştı. **Etki:** seed'de 2 harç ama yalnızca 1 gider-avansı kaydı var → çoğu demo dosyası bu maddeyi yanlış ✓ gösteriyordu; panodaki "Hazırlık durumu" %'leri (73/77/82) şişkindi, artık dürüst. **Test:** helper `tamGirdi`'ye ödenmiş gider-avansı kaydı eklendi (gerçekten eksiksiz dosya → 100% testi anlamlı kaldı) + yeni regresyon testi (kayıt yoksa madde eksik, eylem "…kaydedin", yüzde <100). `icra>hukuk` boş-veri testi hâlâ geçiyor (icra=100%, hukuk=82%). `npm run build` geçti, **350 test** yeşil (+1). |
| 138 | 2026-08-12 17:35 | **İKİ düzeltme: (1) etkisiz dinamik import temizlendi, (2) iki gizlilik metni aşırı-vaat "hiçbir bilgi dışarı gönderilmez"den vetted ifadeye hizalandı.** **(1) Paketleme:** `npm run build` `INEFFECTIVE_DYNAMIC_IMPORT` uyarıyordu — `services/gunluk.ts` hem `main.tsx`'te dinamik (`import().then`) hem `HataSiniri.tsx`'te statik içe aktarılıyordu; statik import onu zaten ana pakete koyduğundan dinamik import ayrı parça oluşturmuyordu (sıfır fayda) ve hata-günlükleme yolunu ikinci bir async yüklemeye (kendi de hata verebilir) bağımlı kılıyordu. `main.tsx` artık `gunlukHata`'yı statik içe aktarıp doğrudan çağırıyor → uyarı gitti, hata yolu daha sağlam, paket ağırlığı değişmedi (zaten dahildi). **(2) Görsel tarama (390px, onboarding UI ile geçilerek 8 rota):** yatay taşma 0, konsol hatası 0, tasarım diline uyum tam; Raporlar/Takvim-senkron altbilgileri **"hiçbir bilgi dışarı gönderilmez"** (mutlak "hiç" çerçevesi) kullanıyordu — uygulamanın vetted ifadesi (`GenelBakis`, `Ayarlar`, CLAUDE.md'nin kendisi: *"veri cihazdan dışarı çıkmaz"*) daha kesin ve dürüst; opsiyonel LLM/takvim-senkron katmanları etkinken veri GÖNDERDİĞİNDEN blanket "hiçbir bilgi" tutarsız bir mutlak beklenti kuruyordu. İkisi de *"veri/dosya cihazınızdan dışarı çıkmaz"*a çevrildi. **Varsayım:** vetted ifadeyle hizalama içerik-tutarlılığı + dürüstlük düzeltmesidir (spekülatif değil); mutlak gizlilik-vaadi CLAUDE.md dürüstlük ilkesiyle çelişir. E2E doğrulandı: her iki rota yeni ifadeyi gösteriyor, yasak ifade DOM'da yok. `npm run build` temiz (dinamik-import uyarısı yok), 349 test yeşil. |
| 137 | 2026-08-12 16:30 | **Şifreli yedek (.jcenc) kripto parametreleri denetlendi + uçtan uca doğrulandı — sağlam, kod değişikliği yok.** row 107 gidiş-dönüş + yanlış-parola görmüştü; bu tur kripto PARAMETRELERİ denetlendi. **Kod:** AES-256-GCM; her şifrelemede taze `getRandomValues` IV (12 bayt = 96 bit, GCM için optimal) ve tuz (16 bayt); PBKDF2-SHA256; türetilen anahtar **dışa aktarılamaz** (`extractable=false`); IV reuse yok (her çağrıda taze). **E2E (Playwright):** aynı veriyle iki şifreli yedek alındı → zarf `{bicim,surum,tuz,iv,veri}`; **tuz, iv ve şifreli metin üçü de A≠B** (taze rastgelelik, IV tekrar yok, semantik güvenlik — determinist şifreleme yok); iv=12 bayt, tuz=16 bayt doğrulandı. 0 pageerror. **Gözlem (varsayım günlüğe):** PBKDF2 yineleme = 210k, güncel OWASP 2023 önerisi 600k'nın altında ama mobil-PWA UX dengesi için savunulabilir (her yedek/PIN-açılışta çalışır; 600k düşük telefonda algılanır gecikme yaratır). Güvenlik politikası kullanıcı yönlendirmesi olmadan otonom değiştirilmedi — bilinçli tercih sayıldı. `npm run build` geçti, 349 test yeşil. |
| 136 | 2026-08-12 15:30 | **Dosya silme çağlayan (cascade) bütünlüğü uçtan uca doğrulandı — sağlam, kod değişikliği yok.** Yetim kayıt = bozuk toplam (silinen dosyanın finansı toplamlarda görünmeye devam ederdi) riski nedeniyle denetlendi. `dosyaSil` bir transaction'da dosyayı + 7 çocuk tablosunu (olay/süre/görev/finans/belge/kişi/not) `dosyaId`'ye göre siliyor; `hareketler` (denetim izi) bilinçli korunuyor. **E2E (Playwright):** her tür çocuktan birer kayıtlı bir dosya (olay+süre+görev+finans+belge+kişi+not) + 1 hareket oluşturuldu; dosya düzenleme formundan "Sil → Emin misiniz?" ile silindi → **tüm çocuklar 0** (yetim yok), **hareket 1** (denetim izi korundu). Pageerror yok. Diğer silmeler de tutarlı: `finansSil` (finans+dekont), `muvekkilSilVeyaArsivle` (row 115), olay/görev/belge/kişi/not yaprak silmeler. Sonuç: silme veri-bütünlüğü güvenilir. `npm run build` geçti, 349 test yeşil. |
| 135 | 2026-08-12 14:30 | **Tüm-gün olay saat dilimi kardeş taraması + düzenleme gidiş-dönüşü doğrulandı — sağlam, kod değişikliği yok.** row 134'ün (ICS tüm-gün) kardeşleri arandı: kalan tek `datetime.slice(0,10)` yalnızca `yerelGun`'un kendi içinde (doğru saf-tarih dalı); tüm gösterim/karşılaştırma siteleri row 124/134'te düzeltilmişti → **temiz negatif**. **OlayForm düzenleme** ön-doldurması zaten `dateToIsoDate(new Date(baslangic))` (=yerel gün) kullanıyor, `slice` değil. **E2E (Playwright, Europe/Istanbul):** yerel 15 Ağu'daki tüm-gün olayı (`2026-08-14T21:00Z` saklı) düzenleme formunda tarih alanı **"2026-08-15"** ön-dolduruyor (UTC dilim "2026-08-14" DEĞİL), tümGün açık; değişiklik yapmadan yeniden kaydedince `baslangic` **`2026-08-14T21:00:00.000Z`** kalıyor (**kayma yok**), tumGun=true. Böylece tüm-gün olay yaşam döngüsü (saklama, takvim gösterimi, düzenleme gidiş-dönüşü, ICS) uçtan uca saat-dilimi doğru. Pageerror yok. `npm run build` geçti, 349 test yeşil. |
| 134 | 2026-08-12 13:39 | **HATA düzeltildi: tüm-gün olayları ICS'e bir gün ERKEN aktarılıyordu (saat dilimi).** row 123-124'teki yerel-gün sınıfının ICS tüm-gün yolunda kaçan örneği. Tüm-gün olay `baslangic`'i UTC saklanır: yerel 15 Ağu 00:00 → **`2026-08-14T21:00:00Z`** (İstanbul UTC+3). ICS dışa aktarımı `baslangic.slice(0,10)` ile **UTC gününü** (14 Ağu) alıp `DTSTART;VALUE=DATE:20260814` yazıyordu → Google/Apple/Outlook'ta olay uygulamanın gösterdiği günden **bir gün önce** çıkıyordu. **Düzeltme:** tarih `yerelGun()` (takvim/rapor ile aynı yardımcı) ile türetiliyor → `DTSTART` gösterilen günle eşleşiyor. Zamanlı olaylar (UTC an) ve hukuki-süre tüm-gün satırları (saf tarih) zaten doğruydu, dokunulmadı. Önceki denetimler (row 114) UTC konteynerinde koştuğundan (`slice==yerelGun`) bunu yakalayamazdı; yalnızca UTC-dışı saat dilimi ortaya çıkarır. **E2E (Playwright, Europe/Istanbul):** takvimin 15 Ağu'da gösterdiği tüm-gün olayı artık `DTSTART;VALUE=DATE:20260815` (önce 20260814) aktarıyor; takvimde 15'te (14'te değil). Ek: tekrar adedi (MAKS 60) ve depolama kotası göstergesi (sıfıra-bölme korumalı) denetlendi — sağlam. Pageerror yok. `npm run build` geçti, 349 test yeşil. |
| 133 | 2026-08-12 12:30 | **Takvim ay ızgarası + gezinme uçtan uca doğrulandı — sağlam, kod değişikliği yok.** Izgara üretimi tarih-aritmetiği yoğun (Pazartesi-başı hafta, ay sınırlarını aşan 42 hücre, "bugün" işareti, olay yerleşimi) ve birim olarak test edilmemişti. **Kod denetimi:** `kaydirma=(ilk.getDay()+6)%7` Pzt→0…Paz→6 doğru; `new Date(yil, ay, 1-kaydirma)` doğru Pazartesi'ye sarıyor; 42 hücre her ayı kapsar; "bugün" ve olaylar yerel `dateToIsoDate` ile işaretleniyor (saat dilimi düzeltmesiyle tutarlı); `ayKaydir` `new Date(yil, ay+yon, 1)` ile yıl sınırını (JS normalizasyonu) doğru geçiyor. **E2E (Playwright):** başlık "Ağustos 2026"; ızgara **42 hücre**; bugün hücresi **12** (konteyner tarihi 2026-08-12) işaretli; 15 Ağustos hücresinde olay noktaları var (yerel güne doğru yerleşim); **+5 ay → "Ocak 2027"** (Ara 2026→Oca 2027 yıl sınırı doğru, hâlâ 42 hücre); "Bugün" → "Ağustos 2026". Pageerror yok. Ayrıca denormalizasyon-bayatlama kardeş taraması: muvekkilId (row 132'de düzeltildi); `hareketler` metni kasıtlı-denetim; ad/başlık gösterimleri join ile güncel → başka canlı bayatlama yok. `npm run build` geçti, 349 test yeşil. |
| 132 | 2026-08-12 11:35 | **HATA düzeltildi: dosya başka müvekkile taşınınca finansı eski müvekkilde kalıyordu (müvekkil bazlı bekleyen ödeme yanlış).** Finans toplamları denetlenirken bulundu. Büro özeti/liste tutarları iç tutarlı; ama **finans/olay/görev kayıtları oluşturulurken `dosya.muvekkilId` denormalize kopyalanıyor** ve `dosyaGuncelle` dosyanın müvekkilini değiştirirken bu kopyaları güncellemiyordu. Müvekkil bazlı bekleyen ödeme `finans.muvekkilId`'den (indeksli sorgu) okunduğundan, bir dosya başka müvekkile atanınca finansı **eski müvekkilde** görünmeye devam ediyordu (yanlış alacak). **Düzeltme:** `dosyaGuncelle` müvekkil değiştiğinde dosyaya bağlı finans/olay/görev satırlarındaki `muvekkilId`'yi (hepsi `dosyaId` indeksli) bir transaction'da eşitliyor. (`sureler` tip-dışı, indekssiz, hiç sorgulanmayan bir `muvekkilId` taşıyor → dokunulmadı.) **E2E (Playwright):** müvekkil A'nın dosyasında bekleyen ₺10.000 → A'da görünüyor; dosya düzenleyip müvekkil B'ye alınınca `finans.muvekkilId`=B oluyor ve bekleyen **A:0 / B:₺10.000**'e taşınıyor (düzeltmeden önce A'da kalırdı). Pageerror yok. `npm run build` geçti, 349 test yeşil. |
| 131 | 2026-08-12 10:29 | **Güvenlik-kritik oturum oto-kilidi (arka plan zaman aşımı) uçtan uca doğrulandı + belge önizleme blob yaşam döngüsü denetlendi — sağlam, kod değişikliği yok.** **(1) Oto-kilit (F15):** `KilitKapisi` kilidi **arka plan süresine** göre uyguluyor (`visibilitychange`): gizlenince maskele + anı kaydet, dönünce `Date.now()-gizli >= zamanAsimiMs` ise kilitle. PWA için doğru yaklaşım (askıdayken idle sayaç güvenilmez). `0` seçeneği Ayarlar'da **"Hemen"** etiketli → `>=0` her dönüşte kilitler (etikete uygun, hata değil). **E2E (Playwright, visibilityState taklidi):** Senaryo-1 (Hemen/0): soğuk açılış kilitli → oturum bayrağıyla açıldı → arka plan → **ekran maskesi çıktı** → dönüş → **anında kilitlendi** ✓. Senaryo-2 (5 dk, kısa arka plan): açık → kısa gizlen/dön → **kilitlenmedi** ✓ (yalnızca eşik süresi aşılınca kilitler). 0 pageerror. Cold-start kilit + yanlış PIN reddi row 45/111'de zaten görülmüştü; bu tur zamanlama yolu (hiç test edilmemişti) doğrulandı. **(2) BelgeOnizleme:** blob object URL yaşam döngüsü doğru (`createObjectURL` `[belge]`'de, cleanup'ta `revokeObjectURL` → sızıntı/erken-iptal yok); önizleme `mimeTur`'e göre img/pdf/audio, gömülemeyen tür için zarif "önizleme yok" + indirme; her zaman "Cihaza indir". Sağlam. `npm run build` geçti, 349 test yeşil. |
| 130 | 2026-08-12 09:30 | **Elle çizilen SVG grafikleri kenar dağılımlarında denetlendi — sağlam, kod değişikliği yok.** Dinamik SVG (sıfıra bölme → NaN öznitelik, tek nokta, uç değer) hata kaynağıdır; row 106 yalnızca boş veriyi görmüştü. **Kod korumaları teyit edildi:** sütun grafiği `enBuyuk = Math.max(1, …)` ile tabanlı (sıfıra bölme yok), halka tüm dilimleri `toplam > 0` ile kapılıyor + sıfır dilim atlanıyor, yatay çubuklar `Math.max(1, …)`. **E2E (Playwright, kontrollü kenar veri):** finans+süreler temizlenip tek gider (tek kategori, tek ay) + tek kritik süre bırakıldı → Raporlar'da **hiç NaN/Infinity/undefined SVG özniteliği yok**, halka tek %100 segment ("1 açık süre" tam kırmızı çember), sütunlarda tek dolu bar diğerleri 0 yükseklik, yatay çubuk tam genişlik; **0 konsol hatası, 0 pageerror**. Ekran görüntüsü: düzen 390px içinde, kırılma yok, zorunlu süre-uyarısı ve gizlilik altbilgisi yerinde. Sonuç: elle çizilen grafikler boştan uç dağılıma tüm veri yelpazesinde güvenilir. `npm run build` geçti, 349 test yeşil. |
| 129 | 2026-08-12 08:31 | **HATA düzeltildi: "Ara" hızlı-iletişim `tel:` bağlantısı biçimli telefonu ham geçiriyordu (arama açılmayabilirdi).** Müvekkil profili hızlı iletişim (F6) denetlendi. `tel:${muvekkil.telefon}` telefonu olduğu gibi kullanıyordu; Türk kullanıcılar numarayı "+90 555 111 22 33" gibi **boşluklu** girer, ama `tel:` URI'sinde boşluk/ayraç bulunamaz (RFC 3966) — tarayıcı boşlukları %20'ye çevirir ve bazı çeviriciler aramayı açamaz, yani çekirdek "Ara" eylemi biçimli numaralarda güvenilmezdi. **Düzeltme:** href'te yalnızca rakam ve baştaki `+` bırakılıyor (`replace(/[^\d+]/g,'')`) → "+90 555 111 22 33" → `tel:+905551112233`. Görünen numara (künye) değişmedi, yalnızca bağlantı hedefi normalize edildi. **E2E (Playwright):** "+90 555 111 22 33" telefonlu müvekkil artık `tel:+905551112233` üretiyor (boşluk yok); `mailto:` değişmedi. Pageerror yok. `npm run build` geçti, 349 test yeşil. |
| 128 | 2026-08-12 07:33 | **Hazırlık kontrol listesi otomatik-tespit + canlı tepki uçtan uca doğrulandı (imza özellik F10) — sağlam, kod değişikliği yok.** Kontrol listesi verilerden okunuyor (elle işaret yok); tespit kuralları ve reaktiflik test edilmemişti. **Tespit zinciri denetlendi:** belge yüklenince `belgeYukle` `girdi.tur ?? belgeTuruTahmini(dosya)` ile türü ad/MIME'den tahmin ediyor; `belgeTuruTahmini` `toLocaleLowerCase('tr')` + alt-dize `.test` kullanıyor (`\b` yok → Türkçe sınır sorunu yok), `/vekalet|vekâlet/` şapkalı/şapkasız yakalıyor. Hazırlık `belgeler.some(b=>b.tur==='vekaletname')` ile okuyor. **E2E (Playwright, kontrollü dosya):** dosya detayında başlangıç %59, "Vekâletname yüklendi" `data-tamam=false`; **Belgeler sekmesinden "Vekâletname.pdf" yüklendi** → DB'de `tur='vekaletname'` (ad/MIME'den doğru tahmin); sekme değiştirip Genel'e dönünce (**reload YOK**) % **59→77 yükseldi** ve madde `data-tamam=true`'ya döndü — Dexie liveQuery ile **canlı** güncelleme. Pageerror yok. Sonuç: türe-özel kontrol listesi hem doğru tespit ediyor hem veriye canlı tepki veriyor. `npm run build` geçti, 349 test yeşil. |
| 127 | 2026-08-12 06:35 | **HATA düzeltildi: tek-olay ICS indirme dosya adı başlıktaki "/" gibi güvensiz karakterleri taşıyordu + regex kardeş taraması.** Önce row 126'nın `\b`-Türkçe hatasının kardeşlerini aradım: tüm regex'ler (`gunluk.ts` redaksiyon: e-posta/IBAN/GSM/TCKN) denetlendi — rakamlar ASCII olduğundan `\b\d{11}\b` (TCKN) sağlam; başka `\b`-Türkçe hatası yok (temiz negatif). **Bulunan HATA:** `OlayForm` tek-olay ICS'i indirirken dosya adını başlıktan yalnızca `\s+`→`-` ile türetiyordu; "Yılmaz / Arslan" (uygulamanın kendi tohum adlarında "/" var) → `olay-yılmaz-/-arslan.ics`. `download` özniteliğinde "/" (ve `: * ? " < > |`) tarayıcıya göre ya yolu kesiyor (yalnızca "…/arslan.ics") ya da bozuyor → dosya adı öngörülemez. **Düzeltme:** `dosyaAdiTemizle()` `ics.ts`'e eklendi ve **merkezî olarak `icsIndir` içinde** uygulandı (tüm çağıranlar korunur): güvensiz karakterler tireye, çoklu tire tekile, baş/son kırpılır, hepsi güvensizse "takvim" yedeği; Türkçe harfler korunur. `OlayForm` çağrısı sadeleşti. **E2E (Playwright):** "Case A / Case B Hearing" olayı artık `olay-case-a-case-b-hearing.ics` iniyor ("/" yok); tam-takvim dışa aktarım adı değişmedi. Birim testleri: güvensiz karakterler, Türkçe harf koruması, tire tekilleme, tümü-güvensiz yedeği. `npm run build` geçti, test 346→**349 yeşil**. |
| 126 | 2026-08-12 05:34 | **HATA düzeltildi: satır içi müvekkil eklerken tüzel/gerçek tahmini Türkçe şirket eklerini (A.Ş./Şti./İnşaat) tanımıyordu.** Dosya oluştururken satır içi yeni müvekkil ekleme akışını uçtan uca test ederken bulundu. **Akış sağlam:** form ya mevcut ya yeni müvekkil zorunlu kılıyor (yetim dosya yok); `dosyaEkle` yeni müvekkili oluşturup dosyaya bağlıyor, iki hareket (müvekkil-eklendi + dosya-olusturuldu) yazıyor — E2E'de doğrulandı (24→25 müvekkil, `dosya.muvekkilId==mv.id`, detay adı gösteriyor). **Bulunan HATA:** tür tahmini `/\b(a\.?ş\.?|ltd\.?|şti\.?|holding|inş)\b/i` kullanıyordu; ama JS'te `\b` yalnızca ASCII `\w` üzerinde çalışır ve Türkçe "ş" sözcük karakteri değildir → Türkçe harf içeren tüm alternatifler (a.ş, şti, inş) **HİÇ eşleşmiyor**, ayrıca İ küçük harfe katlanmıyordu. Sonuç: en yaygın Türk şirket biçimleri ("X A.Ş.", "Şti.", "İnşaat") **gerçek kişi** işaretleniyor; yalnızca "Ltd"/"Holding" (saf ASCII) tüzel sayılıyordu. **Düzeltme:** `muvekkilTuruTahmin()` yardımcısına çıkarıldı — önce `tr` küçük harf (İ→i), sonra `\b` yerine elle `[\s.]` sınırlarıyla eşleşme. Artık "Demir İnşaat A.Ş." → tüzel; "Aşkın/Aşır" gibi "Aş" ile başlayan kişi adları → gerçek (yanlış pozitif yok). **E2E doğrulama:** "Zzz Test İnşaat A.Ş." satır içi eklenince `tur='tuzel'` (önce 'gercek'). Birim testleri: şirket ekleri, yanlış-pozitif korumaları, İ katlama. Pageerror yok. `npm run build` geçti, test 343→**346 yeşil**. |
| 125 | 2026-08-12 04:30 | **Süre motorunun en kritik zincirleme kenar durumu doğrulandı + regresyon testi eklendi; ayrıca sessiz saatler incelendi (ertelenmiş).** **(1) Süre motoru (imza özellik, en yüksek risk):** Mevcut testler adli-tatil uzamasını (7 Eylül) kapsıyordu ama yalnızca 7 Eylül'ün hafta içi olduğu yıllarda (2026-09-07 Pzt) — uzamanın hafta sonuna denk gelip HMK m.93 ile **zincirleme** kaydığı durum test edilmemişti. **2025-09-07 PAZAR:** İstinaf 2 hafta, 1 Ağu 2025 → ham 15 Ağu (adli tatilde) → 7 Eylül'e uzar (Pazar) → 8 Eylül'e (Pzt) kaydırılır. Motor bunu **doğru sırada** (önce uzama, sonra hafta sonu) yapıyor — regresyon testi eklendi (iki gerekçe de mevcut). Sağlam, motor kodu değişmedi. **(2) Sessiz saatler denetimi:** `sessizSaatBaslangic/Bitis` yalnızca Ayarlar UI'ında okunup yazılıyor, **hiçbir yerde tüketilmiyor**; `cihazBildirimiGoster`'in çalışma-zamanı çağrısı yok ve uygulamada `setInterval`/tetikleme döngüsü yok. Bu, dürüst mimariyle tutarlı: gerçek cihaz/push bildirimi sunucu (SW push) gerektirir ve adaptör arkasında ertelenmiş; varsayılan uygulama-içi bildirim (zil + Bildirimler ekranı) pasif olduğundan sessiz saate gerek duymuyor. Foreground-only bir döngü kurmak düşük değerli + rahatsız edici olurdu → **ertelenmiş kabul edildi, hata sayılmadı** (bkz. BEKLEYEN mantığı). `npm run build` geçti, test 342→**343 yeşil**. |
| 124 | 2026-08-12 03:40 | **HATA sınıfı süpürüldü: "hangi gün" kararları her yerde YEREL güne bağlandı (takvimle tutarlılık).** Row 123'teki rapor hatası daha geniş bir desenin tek örneğiydi: UTC zaman damgasını `slice(0,10)` ile "gün"e indirmek UTC gününü verir, gece yarısı civarında takvimle (ve kullanıcının saatiyle) çelişir. `slice(0,10)` kullanan tüm siteler tarandı. **Ortak yardımcı `yerelGun()` `domain/tarih.ts`'e taşındı** (tek kaynak; raporSorgulari artık onu içe aktarıyor). Uygulanan yerler: **sorgular** (bugünkü görevler `tamamlanmaTarihi` + dosya durum notu "Duruşma bugün"/sonraki duruşma — yerel `bugun` ile UTC slice karşılaştırması hatalıydı); **dosyaSorgulari** (dosya listesindeki "sıradaki iş" günü); **asistan** (yaklaşan duruşma süzgeci + yazdırdığı sonraki-duruşma tarihi — UTC "bugün" kullanıyordu, takvimle çelişiyordu); **Bildirimler** (hatırlatma tarihi UTC dilimlenirken yanındaki saat yereldi → sınır olayında tarih ve saat farklı günden görünüyordu); **depolama** (`acilisTarihi`/`kapanisTarihi`/`odemeTarihi` UTC now()'dan türetiliyordu → `bugunIso` yerel). **Doğrulama (Playwright, Europe/Istanbul):** 22:00Z (yerel ertesi gün) sınır olayı Bildirimler'de artık tutarlı **"21 Ağustos · 01:00"** gösteriyor (öncesi "20 Ağustos · 01:00"); birim testleri `yerelGun`'u kapsıyor (saf-tarih geçişi + datetime→yerel, 22:00Z sınırı). Pageerror yok. `npm run build` geçti, 342 test yeşil (rapor testi tarih testine taşındı). |
| 123 | 2026-08-12 02:30 | **HATA düzeltildi: raporlar olay/görev zaman damgalarını UTC gününe göre kovalıyordu (takvimle çelişki).** Raporlar tarih-aralığı süzmesini denetledim. `aralikta` her kaydın `isoTarih.slice(0,10)` (UTC gün) değerini aralıkla karşılaştırıyordu. `finans.tarih` saf "YYYY-MM-DD" olduğu için sorunsuz; ama `olay.baslangic` ve `gorev.tamamlanmaTarihi` **UTC zaman damgası**. Türkiye UTC+3 olduğundan `2026-08-31T22:00Z` aslında **yerel 1 Eylül 01:00** — **takvim** bunu (yerel gün, `dateToIsoDate(new Date(...))`, bkz. takvimSorgulari) 1 Eylül'e koyarken **rapor** UTC gününe göre Ağustos'a sayıyordu → aynı olay iki görünümde farklı ayda. **Düzeltme:** `raporGunu()` yardımcısı zaman damgasını **yerel güne** indiriyor (UTC datetime'ı takvimin kullandığı aynı `dateToIsoDate` ile çevirir; saf tarihi olduğu gibi geçirir), `aralikta` artık onu kullanıyor. **Doğrulama (Playwright, Europe/Istanbul):** 31 Ağu 22:00Z duruşması (yerel 1 Eylül) artık Ağustos aralığında **0**, Eylül aralığında **1** sayılıyor — takvimle tutarlı. Birim testleri: saf-tarih geçişi + datetime→yerel-gün dönüşümü (sınır 22:00Z dâhil). Pageerror yok. `npm run build` geçti, test 340→**343 yeşil**. |
| 122 | 2026-08-12 01:30 | **Türkçe büyük/küçük harf araması + görev tamamlama/hareket akışı uçtan uca doğrulandı — sağlam, kod değişikliği yok.** İki alan hedeflendi. **(1) Türkçe harf katlama (klasik JS tuzağı):** tüm `toLowerCase/Upper` kullanımları tarandı — arama/süzme/gösterim her yerde `toLocaleLowerCase('tr')`/`toLocaleUpperCase('tr')` kullanıyor (yalnızca bir testte düz `.toLowerCase`). `aramaSorgulari` **hem sorguyu hem alanları** aynı `nrm` (tr) ile katlıyor (tek taraflı katlama hatası yok); ad/başlık sıralamaları `localeCompare(…, 'tr')`, tarih sıralamaları ISO (yerelden bağımsız). **E2E (Playwright):** "istanbul"→"İstanbul Barosu…", "İSTANBUL"→aynı, "ışık"→"IŞIK Danışmanlık…", "IŞIK"→aynı eşleşti — noktalı/noktasız i varyasyonları doğru (düz `.toLowerCase()` "İ"yi birleşik-noktalı "i̇" yapıp eşleşmeyi bozardı; `tr` yereli doğru veriyor). **(2) Görev tamamlama + hareket + tekrar:** tamamla→`durum=tamamlandi`+`tamamlanmaTarihi` kuruldu+`gorev-tamamlandi` hareketi (+1); "Tamamlanan" süzgecinde görünüyor; geri al→`bekliyor`+tarih temizlendi (Dexie undefined-sil)+**yeni hareket yok** (delta 0, geri alma loglanmıyor); **tekrar eden seri:** 2. yineleme tamamlanınca yalnızca o tamamlandı, 1 ve 3 `bekliyor` kaldı (seri örnek-bazlı, `gorev.id`'ye göre). Pageerror yok. Sonuç: Türkçe arama doğruluğu ve görev yaşam döngüsü güvenilir. `npm run build` geçti, 340 test yeşil. |
| 121 | 2026-08-12 00:32 | **Uzun-içerik düzen denetimi tüm dinamik ekranlara genişletildi + "çipte uzun metin" kardeş taraması — yeni hata yok, kod değişikliği yok.** Row 120'de konu-çip taşması bulununca aynı sınıf hataların başka yerde olup olmadığını sistematik aradım. **(1) Kardeş taraması:** tüm `.tag` (nowrap çip) kullanımları tarandı — konu dışında hepsi güvenli: sabit etiket (durum/tür), kısa esas no, kısa kullanıcı etiketi (etiketler), ya da biçimli tutar (`tutarKisa`); cümle uzunluğunda tek kullanıcı alanı **yalnızca** konuydu (düzeltildi). Liste satırları zaten `.truncate` kullanıyor. **(2) 8 ekran, 390px, patolojik uzun veri** (uzun duruşma başlığı bugün + uzun görev + uzun finans kalemi + row 120'nin uzun dosya/mahkeme/unvanı): Genel Bakış, Takvim (ay+ajanda), Görevler, Finans, Asistan, Bildirimler, Ara — **hepsinde sayfa taşması 0px**. Uzun başlıklar ekran görüntüsünde düzgün kırpılıyor (ör. Görevler'de "Vekâletname aslını mah…" + ÖNCELİKLİ rozeti yerinde). **Yanlış-pozitifler:** dashboard/asistan `hero-ring`/`safety-ring` dekoratif halkaları viewport kenarını aşıyor ama kapsayıcı kart `overflow:hidden` ile kırpıyor (sayfa taşması yok) — denetim betiğim yalnızca `overflow-x:auto/scroll` atalarını atladığından işaretlendi, hata değil. Pageerror yok. Sonuç: uzun-içerik güvenliği tüm dinamik ekranlarda doğrulandı; konu tek gerçek örnekti. `npm run build` geçti, 340 test yeşil. |
| 120 | 2026-08-11 23:29 | **TASARIM HATASI düzeltildi: uzun "konu" dosya detayında çip içinde yatay taşma (659px) yapıyordu.** Uzun-içerik düzen denetimi yaptım (hukuki veride çok uzun metinler olur: mahkeme adı, şirket unvanı, dava konusu). 390px mobil görünümde patolojik uzun kayıtlar (uzun boşluklu unvan + boşluksuz tek kelime + uzun mahkeme/konu) oluşturup her ekranda yatay taşma ve öğe taşması ölçtüm. **Bulgu:** Dosya detay hero'sunda `dosya.konu` bir `.tag` çipi olarak basılıyordu; ama `.tag` `white-space: nowrap` (durum/esas no gibi kısa çipler için doğru). Konu bir cümle ("İşçilik alacağı, kıdem ve ihbar tazminatı ile fazla mesai…") olduğundan çip içeriği kadar genişleyip **sayfayı ~659px** viewport dışına itiyordu — ayrıca büyük harf/mono çipte tam cümle görsel dile de aykırıydı. **Düzeltme:** konu çipten kaldırıldı (durum + esas no çipte kalıyor); konu zaten hemen altındaki bilgi ızgarasında "Konu" alanında **satır kaydırarak düzgün** gösteriliyor (yedek yok, gösterim korundu). **Doğrulama (Playwright, 390px + ekran görüntüsü):** dosya detay taşması **659px→0**; konu ızgarada sarılı; sekme şeridi (`overflow-x:auto`) tasarım gereği kayıyor (sayfa taşması yok); diğer detay/liste ekranları (uzun unvan/boşluksuz kelime dâhil) **0 taşma**. Pageerror yok. `npm run build` geçti, 340 test yeşil. |
| 119 | 2026-08-11 22:31 | **Yedek geri yükleme dayanıklılığı denetlendi + sertleştirildi (bozuk dosya artık DB'ye dokunmadan reddediliyor).** Row 107 mutlu yolu + yanlış parolayı görmüştü; bu tur **hatalı/bozuk yedek** dosyalarının veriyi yok edip etmediği (yüksek önem: geri yükleme tüm tabloları `clear()` ediyor) test edildi. **Önce doğrulama:** `yedektenGeriYukle` işlem (transaction) içinde `clear()`+`bulkAdd` yapıyor → Dexie herhangi bir fırlatmada geri alıyor; UI catch'i tüm hata tiplerini yakalıyor. **E2E (Playwright, 7 hatalı girdi):** JSON değil / yanlış biçim / gelecek sürüm / tablolar-nesne-değil / **tablo alanı dizi değil** (`dosyalar:"boom"`) / kayıtta id yok / belgeler-dizi-değil — **hepsinde veri korundu** (27 dosya/24 müvekkil/40 finans değişmedi), çökme yok. **Bulunan boşluk + düzeltme:** `dogrula` yalnızca `tablolar`ın nesne olduğunu kontrol ediyordu, alanların **dizi** olduğunu değil; bu yüzden `dosyalar:"boom"` doğrulamayı geçiyor, geri yükleme tabloları `clear()` edip sonra `bulkAdd`'de patlıyor (veri güvenliği yalnızca işlem geri almasına kalıyor) ve genel "Yedek okunamadı." mesajı çıkıyordu. **Düzeltme:** `dogrula` artık her tablo alanının (varsa) dizi olduğunu doğruluyor → bozuk dosya **DB'ye dokunmadan ÖNCE** net "Yedek içeriği bozuk." mesajıyla reddediliyor (savunma derinliği). `dogrula` dışa aktarıldı, her ret yolu için 6 birim testi eklendi. Doğrulama sonrası: 2 dizi-değil vakası artık "bozuk" mesajı veriyor, veri yine korunuyor. `npm run build` geçti, yedek testi 6→12, **340 test yeşil**. |
| 118 | 2026-08-11 21:34 | **Erteleme (snooze) akışı uçtan uca doğrulandı — sağlam, kod değişikliği yok.** Zaman temelli durum (id→an haritası, süresi geçen kayıt temizliği, geri al) klasik hata kaynağı olduğundan hedeflendi. **Kod okuması:** hatırlatma id'si **kararlı** (`${olay.id}-${ofset}` / `${sure.id}-${ofset}` — DB id + sabit ofset), erteleme haritası bu id'ye anahtarlanıyor; `hatirlatmaErtele` süresi geçmiş girdileri temizleyip yenisini ekliyor (harita şişmez), `hatirlatmaErtelemeyiKaldir` simetrik siliyor. Yaklaşan liste (dâhil, `>= alt`) ile aktif sayaç (hariç, `> alt`) eşiği bilinçli asimetrik. **E2E (Playwright, izole DB — tek test olayı, profil `{durusma:[60,0]}`):** (1) "1 saat önce" hatırlatması "Şimdi"de, erteleme çipleri var, zil rozeti=1; (2) **"1 saat" ertele →** `erteleme[ev-test-1-60]` tam **+60 dk**'ya kuruldu, zil **1→0** (aktif sayaç ertelenmişi dışlıyor), "ertelendi" rozeti + "Ertelemeyi geri al" çıktı; (3) **geri al →** girdi silindi, zil **0→1**, hatırlatma "Şimdi"ye döndü; (4) **süresi geçmiş temizliği:** 2 saat eski öksüz erteleme enjekte edilip gerçek hatırlatma ertelenince öksüz **silindi** (harita yalnızca canlı anahtarı taşıyor). Pageerror yok. Zaman dilimi tutarlı (İstanbul UTC+3). `npm run build` geçti, 334 test yeşil. |
| 117 | 2026-08-11 20:41 | **CİDDİ HATA düzeltildi: düzenleme formu bir önceki kaydın verisini gösteriyordu (veri bozulması riski) + finans tutar biçimi tutarlılaştırıldı.** Finans düzenleme gidiş-dönüşünü (row 112 kırpma yolunu) uçtan uca doğrularken bulundu. **(1) Yönlendirici remount hatası:** `Routes` eşleşen içeriği **anahtarsız** render ediyordu; `/finans/A`→`/finans/B` geçişinde React aynı `FinansForm` örneğini yeniden kullanıyor, `yuklendi` bir-kez korumalı ön-doldurma tekrar çalışmıyor → **B formunda A'nın verisi görünüyor** ("AAA kaydı / 1.111,00"), kaydedilirse `finansGuncelle(B, A'nın değerleri)` ile **B'nin üstüne A yazılıyordu**. Tüm `:id` düzenleme formlarını (dosya/olay/görev/müvekkil/finans) etkileyen genel hata. **Düzeltme:** eşleşen içerik `key={path}` ile anahtarlandı → farklı kayıt taze mount olur; anahtar sorgu dizesini içermez → süzgeç (query) değişimi remount etmez. **Playwright:** düzeltmeden önce B "AAA/1.111,00", sonra doğru "BBB/2.222,00"; 11 rotanın hepsi render, query-nav korunuyor. **(2) Tutar biçimi:** düzenleme formu tutarı **ayraçsız** ("12500,00") ön-dolduruyordu, oysa hemen altındaki ipucu ve uygulamanın her yeri gruplu Türkçe biçim ("₺12.500,00") kullanıyor. Yeni `tutarDuzenlenebilir` yardımcısı (gruplu, simgesiz) eklendi; çıktısı `metindenKurus` ile **kayıpsız gidiş-dönüş** (test) yapıyor. Gerçek mount'ta artık "12.500,00" (ipucuyla tutarlı). **(3) Yan doğrulama:** Dexie 4.4.4 `setByKeyPath` `undefined` değeri **siliyor** (kaynak okundu) → tüm `*Guncelle`'lerin boşaltılan opsiyonel alanı temizleme deseni (müvekkil telefon, dosya esas no, finans odemeTarihi…) doğru. Kırpma değişmezi düzenleme yolunda da tutuyor (fazla ödeme 20000→12500, bekleyen=0). Pageerror yok. `npm run build` geçti, para testi 11→13, **334 test yeşil** (2 commit). |
| 116 | 2026-08-11 19:26 | **Olay düzenleme tarih-saat gidiş-dönüşü GERÇEK saat diliminde (UTC+3) doğrulandı — sağlam, kod değişikliği yok.** Form yerel saatle (`dateToIsoDate`/`saat`) çalışıyor ama olaylar UTC (`…Z`) saklanıyor; Türkiye UTC+3 olduğundan her yükleme/kayıtta dönüşüm var — UTC test konteyneri (offset 0) bunu gizler. Bu yüzden Playwright'ı **`timezoneId: 'Europe/Istanbul'`** ile koşturdum (offset −180 doğrulandı). **Sonuç:** tohum olayı "Haciz işlemi" `2026-08-16T07:30:00Z` saklı → düzenleme formu **10:30** ön-dolduruyor (UTC 07:30 → yerel 10:30 doğru); yerel **15:45**'e değiştirilip kaydedilince `2026-08-16T12:45:00Z` saklanıyor (15:45 İstanbul = 12:45 UTC) — **kayma yok, gün atlaması yok, saat kaybı yok**. `damgaKur` yerel datetime kurup `toISOString()` ile UTC'ye çeviriyor; yükleme yerel getter kullanıyor → tutarlı, kayıpsız. Bu en riskli düzenleme yoluydu (saat dilimi hatası olsa her gerçek UTC+3 kullanıcının olay saatlerini bozardı). Pageerror yok. `npm run build` (önceki) geçti, 332 test yeşil. |
| 115 | 2026-08-11 18:28 | **Müvekkil sil→arşivle veri-bütünlüğü akışı uçtan uca doğrulandı — sağlam, kod değişikliği yok.** `muvekkilSilVeyaArsivle` mantığı okundu + iki durum tarayıcıda test edildi. **Kod sağlam:** bağlı dosya varsa `arsivlendi=true` (kalıcı silme YOK, dosyalar korunur, yetim dosya olmaz); yoksa transaction'da müvekkil + notları siliniyor. Hard-delete'te yalnızca `hareketler` (denormalize metin taşır, canlıya çözülmez) dangling `muvekkilId` bırakıyor — bu `dosyaSil`'in de hareketleri koruması (denetim izi) ile **tutarlı ve kasıtlı**. **E2E (Playwright):** (A) dosyalı müvekkil (Aydın Tekstil) sil → **arşivlendi** (kayıt duruyor, `arsivlendi=true`), profile döndü; (B) UI'dan dosyasız müvekkil oluşturup sil → **kalıcı silindi** (yok), listeye döndü. UI ipucu ("dosyası varsa arşivlenir; yoksa kalıcı") doğru. Pageerror yok. Not: B'nin ilk denemesi ham-IDB kaydının düzenleme formunu render etmemesi (test artefaktı) yüzünden başarısızdı; UI'dan oluşturulan kayıt doğru siliniyor. `npm run build` (önceki) geçti, 332 test yeşil. |
| 114 | 2026-08-11 17:27 | **ICS dışa aktarım içeriği uçtan uca doğrulandı (Google/Apple/Outlook'a aktarım) — sağlam, kod değişikliği yok.** `ics.ts` birim testliydi; bu tur gerçek dışa aktarım dosyası (gerçek veri → .ics) denetlendi. TakvimSenkron'dan indirilen dosya ayrıştırıldı: **CRLF satır sonları** (ICS zorunluluğu), `BEGIN/END:VCALENDAR` sarmalı, `PRODID` + `VERSION:2.0` başlıkları mevcut; **16 VEVENT = 13 olay + 3 süre** (DB ile birebir — hem takvim olayları hem hukuki süreler dahil); her VEVENT'te UID/DTSTART/SUMMARY var (16/16/16), **0 bozuk tarih** (hepsi geçerli `YYYYMMDDTHHMMSSZ`). Örnek VEVENT tam biçimli: kararlı `@juriscalendar` UID'i, DTSTART/DTEND, LOCATION, DESCRIPTION, CATEGORIES. Pageerror yok. Ayrıca OlayForm bitiş<başlangıç doğrulaması (kod okuması) sağlam — string HH:MM karşılaştırması doğru, kaydı bloke ediyor. Sonuç: takvim dışa aktarım özelliği (geçerli, içe-aktarılabilir ICS) güvenilir. `npm run build` (önceki) geçti, 332 test yeşil. |
| 113 | 2026-08-11 16:26 | **Finans para mantığına regresyon testi eklendi (row 112 hatasını kilitler).** Dün düzeltilen negatif-bakiye hatası yüksek riskli (mali) olduğundan kalıcı koruma yazıldı. `durumNormalize` ve `odenenKirp` saf fonksiyonları dışa aktarıldı (içerideydi) ve **`finansIslemleri.test.ts` (8 test)** eklendi: `odenenKirp` toplamı aşan ödemeyi toplama, negatifi 0'a kırpıyor, meşru kısmiyi koruyor, sınır değerleri (0/tam toplam) ve **"bekleyen = tutar − odenen her zaman ≥ 0" değişmezi**; `durumNormalize` eşikleri (ödenen 0→bekliyor, <toplam→kısmi, ≥toplam→ödendi). Artık biri `finansEkle`'yi düzenleyip kırpmayı kaldırırsa test kırmızıya döner. `npm run build` (tsc strict) geçti, testler 324→**332 yeşil** (34 dosya). |
| 112 | 2026-08-11 15:34 | **HATA düzeltildi: toplamı aşan kısmi ödeme negatif "bekleyen" bakiyesi üretiyordu.** Finans kısmi-ödeme akışı test edilirken bulundu. `durumNormalize` durumu doğru türetiyor (ödenen≥tutar→'odendi') ama `finansEkle`/`finansGuncelle` `odenenTutar`'ı **kırpmadan** saklıyordu. **Sonuç (hata):** ₺1.000'lik kayda kısmi ödeme ₺2.000 girilince `odenenTutar=200000 > tutar=100000` saklanıyor, doğrulama uyarısı yok → `bekleyen = tutar − odenenTutar = −₺1.000` (**negatif bakiye**) ve tahsilat toplamı şişiyor. **Düzeltme:** ortak `odenenKirp` yardımcısı `odenenTutar`'ı **[0, tutar]** aralığına kırpıyor (fazla/negatif ödeme); iki fonksiyonda da kırpılmış değer hem `durumNormalize`'a hem kayda gidiyor → `bekleyen` her zaman ≥ 0. **Doğrulama (Playwright):** fazla ödeme (₺2.000/₺1.000) artık odenenTutar=100000'e kırpılıyor, durum 'odendi', **bekleyen=0** (düzeltmeden önce −100000); meşru kısmi (₺400/₺1.000) bozulmadan çalışıyor — odenenTutar=40000, durum 'kismi', bekleyen=₺600. `npm run build` (tsc strict) geçti, 324 test yeşil. |
| 111 | 2026-08-11 14:35 | **PIN kilit güvenlik akışı uçtan uca doğrulandı (hassas müvekkil verisi kapısı) — sağlam, kod değişikliği yok.** Row 45 mutlu yolu görmüştü; bu tur güvenlik-kritik yollar test edildi. **Kripto sağlam:** PIN `pinOzetiUret` ile **PBKDF2-SHA256 tuzlu özet** (`pbkdf2$tuz$hash`) olarak saklanıyor — ham PIN ASLA saklanmıyor; `pinDogrula` yeniden türetip karşılaştırıyor. **UI doğrulaması (Playwright):** (1) Ayarlar'dan PIN 1234 kuruldu → DB'de `kilitEtkin=true`, `pinOzeti` "pbkdf2$" ile başlıyor ve düz "1234" DEĞİL; (2) reload → **kilit ekranı geliyor** ("JurisCalendar kilitli" + PIN pad); (3) **yanlış PIN (9999) → reddedildi, hata gösterildi, kilitli kaldı**; (4) doğru PIN (1234) → açıldı, pano göründü; (5) ekran görüntüsü: kilit ekranı **tamamen opak** koyu zemin — pano içeriği hiç görünmüyor (arkada DOM'da ama görsel+etkileşimsel olarak engelli; kripto notunun dürüst "erişim kapısı, şifreleme değil" kapsamıyla uyumlu). Pageerror yok. Sonuç: güvenlik kapısı (hash'li saklama, yanlış-PIN reddi, opak gizleme) güvenilir. `npm run build` (önceki) geçti, 324 test yeşil. |
| 110 | 2026-08-11 14:00 | **Çevrimdışı / PWA sözü uçtan uca doğrulandı (yerel-öncelikli çekirdek) — sağlam, kod değişikliği yok.** Local-first uygulamanın olmazsa olmazı: bağlantısız çalışmak. Playwright ile: (1) ilk yükleme online → **service worker kuruluyor ve sayfayı kontrol ediyor**; (2) `setOffline(true)` sonrası **reload SW önbelleğinden açılıyor** (hata sayfası yok, "Çevrimdışısınız" bandı görünür); (3) rotalar çevrimdışı geziliyor (`/dosyalar` render); (4) **çevrimdışı kayıt oluşturma çalışıyor** — görev online 21→22 VE offline 22→23 (IndexedDB yerel yazımı ağ gerektirmiyor); (5) `CevrimdisiUyari` bandı `role=status` ile görünüyor; pageerror yok. **Test yanlış-alarmı düzeltildi:** ilk denemede "offline kayıt 21→21" çıktı ama bu benim seçici hatamdı (`/kaydet|ekle/i`+`.first()` yanlış düğmeyi tıklamış); tam düğme adı "Görevi ekle" + `fill()` ile hem online hem offline oluşturma doğrulandı. Sonuç: yerel-öncelikli/çevrimdışı sözü gerçekten tutuyor. `npm run build` (önceki) geçti, 324 test yeşil. |
| 109 | 2026-08-11 12:56 | **İmza özellik — süre hesabı → dosyaya kaydet akışı uçtan uca doğrulandı (sağlam, kod değişikliği yok).** Uygulamanın çekirdek değeri (hukuki son gün hesabı) UI'dan koşuldu. **Hesap doğru + adli tatil uzaması canlı:** İstinaf (HMK 345, 2 hafta) + başlangıç 2026-08-15 → ham son gün 29 Ağustos (adli tatil içinde, 20 Tem–31 Ağu) → **7 Eylül 2026'ya uzatıldı** (HMK m. 104); UI "7 Eylül 2026 · 27 gün kaldı" gösterdi, uyarı mevcut. **Kayıt doğru:** "Dosyaya kaydet" → 1 yeni süre kaydı, `sonTarih=2026-09-07` (ham değil, **uzatılmış yasal tarih** saklandı), doğru `dosyaId`; `takvimeEkle` (varsayılan açık) bağlı takvim olayı üretti (`sureId` ile). **Test yanlış-alarmı düzeltildi:** `kaynak='sure-hesabi'` alanı **Sure'de değil Olay'da** (line 124) — süre motorunun ürettiği takvim olayını işaretler; süre kaydında böyle bir alan yok, benim ilk kontrolüm yanlış varlığa bakmıştı. Kod (satır 60) olaya `kaynak:'sure-hesabi'`+`sureId` doğru yazıyor. Pageerror yok. Sonuç: imza özellik (hesap + adli tatil + dosya/takvim bağı) uçtan uca güvenilir. `npm run build` (önceki) geçti, 324 test yeşil. |
| 108 | 2026-08-11 11:35 | **Tekrar eden seri yaşam döngüsü uçtan uca doğrulandı (oluştur → kapsamlı silme) — sağlam, kod değişikliği yok.** Karmaşık `seriesId` mantığı row 101'de kod olarak denetlenmişti; bu tur gerçek UI akışı koşuldu. **Oluşturma:** `/takvim/yeni`'de haftalık×4 olay oluşturuldu → tam **4 kayıt**, tarihler 2026-08-11 / 08-18 / 08-25 / 09-01, aralar **7,7,7 gün** (kümülatif kayma yok — `tekrar.ts`'in "her yineleme orijinalden" tasarımıyla uyumlu). **Kapsamlı silme:** bir kaydın düzenlemesinde "**Yalnızca bu**" → seri 4'ten 3'e (tam bir kayıt silindi); başka birinde "**Tüm seri**" → 3'ten 0'a (o seriesId'li tüm kalanlar silindi). Pageerror yok. Sonuç: seri üretimi (adet, haftalık aralık), seriesId ataması ve iki silme kapsamı UI üzerinden birebir çalışıyor. Böylece iki karmaşık akış daha (boş-veri geçen tur, yedek/tekrar bu turlarda) canlı doğrulandı. `npm run build` (önceki) geçti, 324 test yeşil. |
| 107 | 2026-08-11 10:27 | **Yedek al→geri yükle turu uçtan uca doğrulandı (verinin tek güvencesi) — her iki yol sağlam, kod değişikliği yok.** Sunucusuz uygulamada yedek verinin TEK kurtarma yolu; row 92 kodu denetlemişti, bu tur tarayıcıda gerçek tur koşuldu. **Şifresiz yol:** 27 dosya/28 belge/24 müvekkil/40 finans/13 olay/3 süre/21 görev kaydedildi → veri silindi (0) → yedekten geri yüklendi → tüm sayımlar birebir orijinaline döndü; kritik olarak **belge Blob'u base64 turundan bayt-birebir sağ çıktı** ("Vekâletname.txt" 138 bayt→138 bayt, çok baytlı Türkçe karakterler dahil, aynı id). **Şifreli yol (AES-GCM `.jcenc`):** parolayla dışa aktarıldı → silindi → **yanlış parola: veri BOZULMADI (sayım 0'da kaldı, nazik hata gösterildi)** — yıkıcı işlem şifre çözmeden ÖNCE olduğundan yanlış parola denemesi veri kaybettirmiyor (row 92'nin "doğrulama-önce-yıkım" özelliği canlı doğrulandı) → doğru parola: 27 dosyaya tam geri yükleme. Konsol/pageerror temiz. Sonuç: uygulamanın veri-güvenliği cankurtaran halatı her iki yolda (özellikle kritik hata modunda) güvenilir. `npm run build` geçti, 324 test yeşil. |
| 106 | 2026-08-11 09:31 | **CİDDİ HATA düzeltildi: son dosyasını silen kullanıcıya demo verisi diriliyor + profili/ayarları eziliyordu.** Boş-veri durumunu test ederken bulundu. `tohumlaGerekiyorsa()` yalnızca `dosyalar.count()===0`'a bakıyordu; bu "ilk kurulum" ile "kullanıcı tüm dosyalarını sildi"yi ayırt edemiyor. **Sonuç (hata):** onaylanmış/kurulmuş bir kullanıcı son dosyasını silince, sonraki her açılışta `main.tsx` tohumlamayı çağırıyor → **24 sahte demo dosyası geri geliyor** VE `db.ayarlar.put(...)` kullanıcının ayarlarını eziyor (adı "Ayşe Kaya" oluyor, `profilKuruldu`/`hukukiOnay` siliniyor → yeniden onboarding+onay, PIN ve LLM ayarı gidiyor). **Düzeltme:** tohumlamadan önce `ayarlar.profilKuruldu || hukukiOnay` varsa `return false` — kullanıcı uygulamayı zaten kurmuşsa örnek veri diriltilmez, ayarlar ezilmez. Tam sıfırlama (`db.delete()`) ayarları da sildiğinden koşul geçer → taze kurulum kasıtlı tohumlanır (bozulmadı). **Doğrulama (Playwright):** kurulu kullanıcı + 0 dosya → reload sonrası `dosyaSayisi=0` (diriltme yok), `kullaniciAdi="Gerçek Kullanıcı"` (ezilmedi); pano "Av. Gerçek" ve zarif boş durumlar gösteriyor ("karşılaştırma yok", NaN yok). **Ek:** tüm boş-veri ekranları (pano/raporlar/finans/dosyalar) görsel + otomatik denetlendi — grafikler boş veride zarif (boş halka/çubuk, ₺0), konsol temiz. `npm run build` geçti, 324 test yeşil. |
| 105 | 2026-08-11 08:26 | **Tatil verisi (hukuki hesabın en yüksek doğruluk riski) denetlendi — doğru + kapsam-uyarılı; kod değişikliği yok.** Yanlış tatil tarihi → sessizce yanlış hukuki son gün. `tatil.ts` tabloları objektif olarak doğrulandı. **Sabit tatiller (7 gün):** yılbaşı, 23 Nisan, 1 Mayıs, 19 Mayıs, 15 Temmuz (2016 sonrası doğru eklenmiş), 30 Ağustos, 29 Ekim — hepsi doğru; arefe/28 Ekim öğleden sonra bilinçle tam-tatil sayılmıyor (ileri-kaydırma güvenli yönü). **Dinî bayramlar (Diyanet, 2024–2030):** Ramazan 2024-04-10 / 2025-03-30 / 2026-03-20 ve Kurban 2024-06-16 / 2025-06-06 / 2026-05-27 bilinen resmî tarihlerle birebir; 2027–2030 projeksiyonları tutarlı ay-takvimi ilerlemesiyle (~yılda 10–12 gün erken, artık yıllar hesaba katılmış) makul. Kodda ±1 gün resmî-ilan uyarısı var. **Kapsam sınırı doğru işleniyor:** `DINI_BAYRAM_KAPSAMI = 2024–2030`; SureHesapla hesaplanan son gün kapsam dışıysa "bayram kaydırması eksik olabilir" uyarısı gösteriyor — dürüst. **Sınır:** süre kataloğu gün-sayıları hukuki yorum (`[!]` avukat alanı) — dokunulmadı; yalnızca objektif takvim tarihleri denetlendi. Sonuç: en kritik hukuki-veri güvenilir. `npm run build` (önceki) geçti, 324 test yeşil. |
| 104 | 2026-08-11 07:27 | **Ağ çıkışı / gizlilik kısıtı uçtan uca denetlendi — mimari örnek düzeyinde sağlam, kod değişikliği yok.** Kısıt: "veri cihazdan dışarı çıkmaz, üçüncü taraf analitik yok, LLM varsayılan kapalı." Tüm ağ çıkışları arandı. **Bulgular (hepsi uyumlu):** (1) uygulamadaki **tek** `fetch` `services/llm.ts:61` — kullanıcının kendi LLM uç noktası; başka hiçbir çıkış yok (analitik/telemetri/beacon/WebSocket/hata-raporu YOK). (2) LLM **üç katmanlı kapı ardında:** `llmDurumu` `llmEtkin` yoksa 'kapali' (varsayılan), uç nokta+anahtar yoksa 'yapilandirilmadi', ikisi de varsa 'hazir'; Asistan yalnızca 'hazir'de LLM yolunu sunar; `llmSor`'un kendisi uç nokta/anahtar yoksa `LlmHatasi` fırlatır. Varsayılan `llmEtkin: false` (ayarlarIslemleri + seed). (3) `index.html` yalnızca yerel kaynak yüklüyor — CDN/Google Fonts/analitik script YOK (fontlar `@font-face` ile cihazda barındırılıyor). (4) PWA/workbox: `navigateFallback: index.html`, yalnızca yerel varlıkları precache ediyor, harici `runtimeCaching` kuralı **yok**. Sonuç: uygulamanın çekirdek gizlilik sözü kod düzeyinde tam tutuyor — dışa giden tek şey, kullanıcının bilinçli açtığı LLM. `npm run build` (önceki) geçti, 324 test yeşil. |
| 103 | 2026-08-11 06:32 | **Mutlak gizlilik iddiası (kendi LLM'iyle çelişen) düzeltildi — banlı-ifade kısıtının ruhu.** Sert kısıt: mutlak gizlilik/güvenlik iddiası yasak ("hiçbir veri paylaşılmıyor" / "hacklenemez" / "%100 güvenli"). UI metinleri tarandı: **tam banlı ifade YOK** (grep temiz). Ama **Asistan** hero metni (kritik yokken) "Her şey veriden; **hiçbir bilgi dışarı çıkmaz**" diyordu — oysa AYNI ekranın altbilgisi "LLM açıkken yalnızca sorunuz ve dosya özeti gönderilir" diyor. Yani mutlak "hiçbir bilgi çıkmaz" iddiası, ekranın kendi opsiyonel LLM özelliğiyle **çelişiyordu** (banlı ifadelerle aynı sınıf aşırı-iddia). **Düzeltme:** hero "Değerlendirme cihazınızda, kendi verinizden yapılır." olarak hassaslaştırıldı — yerel-öncelikli güvenceyi korur, mutlak iddiayı bırakır, altbilgideki LLM istisnasıyla çelişmez. **Dokunulmayanlar (doğru):** GenelBakis/Ayarlar "müvekkil verisi cihazdan dışarı çıkmaz" — CLAUDE.md'nin **kendi onayladığı** ifade; Raporlar/TakvimSenkron "hiçbir bilgi dışarı **gönderilmez**" — uygulamanın sunucuya iletim yapmadığını doğru anlatıyor (dışa aktarım kullanıcı-başlatımlı yerel indirme). `npm run build` geçti, 324 test yeşil. |
| 102 | 2026-08-11 05:31 | **Zorunlu "bilgilendirme amaçlıdır" uyarısı eksik ekranlara eklendi (CLAUDE.md sert kısıtı).** Kısıt: "Hukuki süre hesabı bilgilendirme amaçlıdır. **Her ekranda ve hesap çıktısında** bu uyarı görünür olmalı." Uyarının olduğu/olmadığı yerler süre gösteren tüm yüzeylere karşı denetlendi. **Zaten uyumlu:** SureHesapla, Takvim, DosyaDetay (süreler sekmesi), Asistan (disclaimer'ında "süre teyidi kullanıcının sorumluluğundadır" zaten var). **Eksik bulunanlar (düzeltildi):** (1) **Genel Bakış** panosu "Yaklaşan son tarihler" bölümünde hesaplanmış süreleri (kural adı + son gün + kalan) belirgin gösteriyor ama uyarı yoktu — alttaki kart yalnızca gizlilik/güvenlik kartı; süre listesi varken bölümün hemen altına Takvim/DosyaDetay ile aynı sözcüklerle muted uyarı eklendi. (2) **Bildirimler** süre-türevli hatırlatma sürelerini gösteriyor ama disclaimer yalnızca hatırlatma mekaniğini anlatıyordu — mevcut satıra süre-teyit cümlesi eklendi. (3) **Raporlar** "Açık süreler" halkası süre aciliyetinden türüyor — grafiğin altına aynı uyarı kondu. Görsel dil korundu (muted `t-small`, sakin ton); referans panodaki mockup'ta yok ama sert kısıt onu geçersiz kılıyor. Playwright ile 5 ekranda uyarı doğrulandı + pano ekran görüntüsüyle yerleşim onaylandı. `npm run build` geçti, 324 test yeşil. |
| 101 | 2026-08-11 04:27 | **Silme/veri-bütünlüğü (en yüksek veri-kaybı riski) denetlendi — mimari sağlam, kod değişikliği yok.** En riskli alan (kayıt silme + bağlı kayıt bütünlüğü) eleştirel okundu. **Sağlam bulunanlar:** (1) **Dosya silme** `dosyaSil` tek atomik `rw` transaction'ında dosyayı + 7 bağlı türü (olay/süre/görev/finans/belge/kişi/not) `dosyaId` ile siliyor — yetim kayıt kalmıyor, yarıda hata olursa geri alınır. (2) **Seri silme** (olay/görev) `where('seriesId').equals(...).primaryKeys()` → `bulkDelete` ile doğru; tekil silme `delete(id)`; UI "yalnızca bu"→tekil, "tüm seri"→seri doğru bağlanmış. (3) **Hatırlatmalar** tabloya YAZILMIYOR — olay+süre+ofset ayarlarından **anlık türetiliyor** (`hatirlatmaSorgulari`), dolayısıyla bir olay/görev silinince hatırlatması kendiliğinden kayboluyor: **tasarım gereği yetim hatırlatma imkânsız** (senkron tutulacak ikinci kayıt yok). (4) Erteleme (snooze) durumu ayarlarda; silinen olayın orphan snooze anahtarı türetilen listede hiç sorgulanmadığından zararsız. (5) Nav'da `aria-current="page"` zaten var. **Gözlem (hata değil):** `hatirlatmalar` Dexie tablosu yalnızca yedek al/geri-yükle'de geçiyor, çalışma zamanında hiç yazılmıyor — atıl şema; boş ve zararsız, kaldırmak şema göçü + yedek biçimi değişikliği gerektireceğinden (gerçek kullanıcı verisi riski) bilinçle dokunulmadı. Sonuç: veri-kaybı riski taşıyan çekirdek güvenilir. `npm run build` (önceki) geçti, 324 test yeşil. |
| 100 | 2026-08-11 03:31 | **Arama/süzgeç sonuç sayıları canlı bölge yapıldı (WCAG 4.1.3 — durum mesajları).** Girdi türleri + canlı bölgeler denetlendi. **Sağlam bulunanlar:** form girdileri mobil için doğru (telefon `type=tel`, TCKN `inputMode=numeric` — `type=number` tuzağından kaçınmış, e-posta `type=email`, tutar `inputMode=decimal`, tarih/saat yerel seçici); istemci alanlarında `autoComplete=off` (gizlilikle uyumlu — tarayıcı istemci verisini kaydetmez); form hataları/toast/çevrimdışı zaten `role=alert`/`status`. **Bulunan eksik:** liste ekranlarında (Dosyalar/Müvekkiller/Finans) ve genel aramada (`/ara`) sonuç **sayısı** ve **"sonuç yok"** mesajı canlı bölge değildi — ekran okuyucu kullanıcı arama/süzgeç değiştirince kaç sonuç kaldığını duymuyordu. **Düzeltme:** dört `.list-count` ve dört boş-durum mesajına `role="status"` (kibar) eklendi. Playwright ile doğrulandı: süzgeç değişince sayı düğümü **yerinde kalıp** metni güncelliyor (24 dosya→1 dosya, aynı DOM düğümü) → kibar duyuru güvenilir; her tuşta değil, kullanıcı durunca yerleşen sayı okunur. Görsel değişiklik yok. `npm run build` geçti, 324 test yeşil. |
| 99 | 2026-08-11 02:29 | **Belge önizleme modalına odak yönetimi eklendi (WCAG 2.4.3 — modal odak tuzağı).** Tüm modal/örtü katmanları denetlendi. **Sağlam bulunanlar:** Drawer ve DevirTeslim tam odak yönetimine sahip (aç→ilk öğeye odak, Tab tuzağı, Esc, kapat→odağı geri ver); KilitKapisi/onboarding tam ekran geçitler. **Bulunan eksik:** `BelgeOnizleme` (belge önizleme penceresi) `role="dialog"`+`aria-modal`+Esc+arka-plan-tıkla-kapat'a sahipti ama **odak yönetimi yoktu** — açılınca odak diyaloğa taşınmıyor, Tab arka plana kaçıyor (obscured içeriğe), kapanınca tetikleyiciye dönmüyordu. **Düzeltme:** DevirTeslim/Drawer'daki birebir desen satır içi eklendi (`pencereRef`, aç→Kapat düğmesine odak, Tab tuzağı, Esc, kapat→odağı geri ver); odaklanabilir seçici iframe/audio içeriği de kapsıyor (PDF/ses önizlemesi). **Not:** üç modal artık aynı ~30 satırlık kalıbı taşıyor — DRY için ortak kancaya çıkarılabilir; ama çalışan iki kritik modalı (özellikle her zaman var olan Drawer) riske atmamak için şimdilik satır içi bırakıldı (bilinçli). Playwright ile doğrulandı: açılınca odak "Kapat"ta, Tab tuzağı tutuyor, Esc kapatıyor, odak tetikleyiciye dönüyor. Görsel değişiklik yok. `npm run build` geçti, 324 test yeşil. |
| 98 | 2026-08-11 01:29 | **Rota başına sayfa başlığı eklendi (WCAG 2.4.2 — Sayfa Başlıklı).** Başlık yapısı + belge başlığı tarayıcıda denetlendi. **Sağlam bulunanlar:** her rotada tam **bir h1** ve başlık sırası her yerde 1→2 (atlama yok); yer işaretleri doğru (`<header>` üst çubuk, `<nav>` drawer, `<main id="ana-icerik">`). **Bulunan tek eksik:** SPA olduğu için `document.title` tüm rotalarda sabit "JurisCalendar" kalıyordu — ekran okuyucu sayfa değişimini duyurmaz, tarayıcı geçmişi/sekmeleri ayırt edilemez. **Düzeltme (merkezî, per-component değil):** `RouteDefinition`'a `baslik` alanı eklendi, `Routes` eşleşen rotanın başlığını `document.title = "<baslik> · JurisCalendar"` olarak effect'te yazıyor; 24 rotanın hepsine Türkçe başlık verildi (Genel Bakış, Takvim, Yeni Olay, Dosya Detayı, Dosya Düzenle…), 404 → "Sayfa bulunamadı". Playwright ile doğrulandı: doğrudan yükleme + **uygulama-içi gezinme** (hash değişimi, reload yok) başlığı canlı günceller; detay/düzenle/404 başlıkları doğru. Görsel dile dokunulmadı — yalnızca erişilebilirlik/UX. `npm run build` (tsc strict) geçti, 324 test yeşil. |
| 97 | 2026-08-11 00:27 | **Hareket azaltma (prefers-reduced-motion) kapsamı tamamlandı — kaçan tek sonsuz animasyon düzeltildi.** Tüm sonsuz animasyonlar `prefers-reduced-motion: reduce` altında denetlendi. **Kapsanan (doğru):** dashboard yenileme simgesi (`donen`) + iskelet parıltısı (`kayan`) → dashboard.css bloğu; onboarding döngüleri `ob-parla`/`ob-imlec`/`ob-titre`(.ob-kasa)/`ob-akis`(.ob-hat-metin) → onboarding.css bloğu; tur halkası/balonu → tur.css bloğu; ayrıca tokens.css `--duration-*`'ı 0ms yapıyor (token kullanan tüm geçişler). **Kaçan tek animasyon:** `shell.css`'te üst çubuktaki sürüm-yenile düğmesi simgesi (`.surum-icon[data-donuyor='true']`, `donen 700ms infinite`) — shell.css'te hiç reduced-motion bloğu yoktu, oysa dashboard'daki birebir aynı `donen` spinner'ı neydi. Tutarsızlık: hareket-hassas kullanıcı bu simgeyi durmadan dönerken görürdü. **Düzeltme:** shell.css'e reduced-motion bloğu eklenip `.surum-icon[data-donuyor]` animasyonu durduruldu (uygulamanın per-component desenine uygun; global "nuclear reset" yerine, mevcut mimariyi bozmamak için). Playwright'ın reducedMotion emülasyonuyla doğrulandı: kapalıyken `animation-name: donen`, açıkken `none`. Referans bu detayı belirtmiyor → yalnızca erişilebilirlik. `npm run build` geçti, 324 test yeşil. |
| 96 | 2026-08-10 23:29 | **Klavye odak halkası düzeltildi — form girişlerinde tutarsız/zayıf odak göstergesi (WCAG 2.4.7).** Odak yönetimi tarayıcıda denetlendi: uygulamanın **global `:focus-visible`** kuralı klavye gezintisinde 2px teal halka çiziyor (fareyle `:focus:not(:focus-visible)` ile bastırılıyor) — özenli, doğru desen. **Ama** giriş kuralları (`.input`/`.select`/`.textarea`, `.search-input`, `.belge-etiket-giris`) düz `:focus { outline: none }` kullanıyordu; bu daha spesifik olduğundan global halkayı **klavye için de** eziyor, geriye yalnızca 1px kenarlık-rengi değişimi kalıyordu — odak göstergesi tam da formlarda (klavye kullanıcısının en çok ihtiyaç duyduğu yerde) en zayıf. Playwright ile kanıtlandı: klavyeyle odaklı düğme `outline: 2px teal` alırken, klavyeyle odaklı giriş (`:focus-visible` eşleştiği hâlde) `outline: none` alıyordu. **Düzeltme:** üç kuraldaki `outline: none` kaldırıldı (teal kenarlık korundu) → artık klavye odağında global 2px halka beliriyor, fare odağında (metin alanları hariç, spec gereği) temiz kalıyor. Ekran görüntüsüyle doğrulandı: giriş artık düğmelerle aynı belirgin halkayı alıyor, kırpılma yok. Referans odak durumlarını belirtmiyor → görsel dile dokunulmadı, yalnızca erişilebilirlik güçlendirildi. `npm run build` geçti, 324 test yeşil. |
| 95 | 2026-08-10 22:31 | **Renk kontrastı (WCAG 1.4.3) tarayıcıda ölçüldü — bulgular tasarımın belgeli "soluk ve pastel" dili; palet DEĞİŞTİRİLMEDİ (bilinçli karar).** Playwright ile 16 rotada gerçek render edilen metinlerin ön/arka plan kontrast oranları hesaplandı: 85 metin AA eşiğinin (4.5:1 / büyük 3:1) altında. **Kök-neden analizi:** neredeyse tümü kategori-vurgu renginde (`--acc-fg`) metin — stat-caption, deadline-kalan (N gün kaldı), date-badge, task-badge, tag, money-amount, money-value, rapor-stat-value, avatar baş harfleri. Bunlar DESIGN-REFERENCE.md'de açıkça tanımlı: §"Vurgu renkleri **soluk ve pastel** — dikkat çeken tek şey içerik" ve §3 kategori paleti (#C96A5B/#D9A45B/#8E7CC3/#6E93B8/#5FB49C, "hepsi soluk pastel, zemini %12 opaklıkta") — tam da bu kullanımlar için (aciliyete göre renkli süre, tarih rozeti, finans). **Referans JPG (02) ile doğrulandı:** birincil sayılar (24/8/17/₺184.500) KOYU (yüksek kontrast) — uygulama da öyle (dashboard stat-value); yalnızca ikincil accent metinler (+3 bu ay, 2 gün kaldı) soluk-renkli — kasıtlı. Finans ekranı ekran görüntüsüyle görüldü: gelir yeşil / gider kırmızı / bekleyen turuncu / yaklaşan mavi — tutarlı **anlamsal** renk sistemi. **Karar: palet değiştirilmedi.** `--acc-fg`'yi koyulaştırmak (a) "soluk ve pastel" referans dilini (pazarlık konusu değil) ihlal eder, (b) anlamsal gelir/gider kodlamasını yok eder. WCAG 1.4.1 (yalnız-renk) zaten karşılanıyor: +/− işareti ve metin, rengi olmadan da anlamı taşıyor. Not: gelecekte bu bulgu naif "düzeltme"yle referans sadakatini bozmasın diye kayda geçirildi (minmax(0,1fr) geri-alması gibi). Kod değişikliği yok, 324 test yeşil. |
| 94 | 2026-08-10 21:35 | **Form kontrollerinde etiket ilişkisi düzeltildi — 3 erişilebilirlik kusuru (WCAG 4.1.2/3.3.2).** Playwright ile tüm rotalarda konsol hatası + yapısal a11y taraması yapıldı: **konsol tertemiz** (0 hata/uyarı/pageerror), ama 4 form kontrolünde görünür `field-label` olmasına rağmen **programatik etiket yok** (kontrol `<label>` değil `<div className="field">` içinde). Kaynak-neden: bazı alanlar `<label className="field">` ile sarılıyor (Başlık, Dosya…) ama üçü değildi. **Düzeltmeler:** (1) `TekrarSecici` (olay+görev formları) iki kontrol taşıdığından (sıklık + adet) tek label ile sarılamıyor → `useId` + `<label htmlFor>` ile sıklık seçimi ilişkilendirildi; (2) FinansForm "Kategori" tek kontrollü → kardeşleriyle tutarlı biçimde `<div className="field">`→`<label className="field">`; (3) Ayarlar geri-yükleme dosya girişi (`display:none`, "Geri yükle" düğmesiyle tetikleniyor — zaten a11y ağacı dışı) savunma amaçlı `aria-label` + `tabIndex=-1`. **Doğrulama:** tarayıcıda `.labels` (DOM'un kendi ilişkisi) her select'te doğru etiketi gösteriyor (Tekrar, Kategori); tekrar-taraması 18 rotada temiz; finans kategori onChange çalışıyor (harc→bilirkisi), tekrar adet-girişi hâlâ beliriyor (koşullu render değişmedi). Görsel değişiklik yok (`.field-label`/`.field` sınıfları span/label/div'de aynı). `npm run build` (tsc strict) geçti, 324 test yeşil. |
| 93 | 2026-08-10 20:37 | **Dar ekran (320px) yatay taşma denetimi — tarayıcıda ölçüldü; uygulama temiz, kod değişikliği yok.** Önceki denetimler 360/390px'e bakmıştı; bu tur en küçük yaygın telefon genişliği **320px** (EOL iPhone SE) sistematik ölçüldü. Playwright + gerçek preview ile onboarding IndexedDB'den tohumlanıp (ayarlar `hukukiOnay`/`profilKuruldu`/`turGoruldu`) **tam yeniden yükleme** sonrası (ham-IDB yazımı Dexie liveQuery'yi tetiklemez — bu yüzden ilk deneme yanlışlıkla onay ekranını ölçtü, düzeltildi) 18 statik rota + tüm detay sayfaları (dosya/müvekkil/görev/finans, gerçek tohum id'leri) + dosya detayının 7 sekmesi + onboarding taranarak `documentElement.scrollWidth − clientWidth` ölçüldü: **hepsi 0px** (hem 320 hem 360). Ek stres: **320px × en büyük yazı ölçeği (%125, `cokBuyuk`)** — yalnızca gösterge panelinde gösterge kartı ızgarasında **2px** (algılanamaz, alt-piksel) taşma; kaynak: `.stat-value` iri mono sayısının (`₺184.500`, 1.75rem tabular-nums) bölünemez min-content'i 320'de yarım kabı ~2px aşıyor. **`minmax(0,1fr)` denendi ama GERİ ALINDI:** kart içeriği min-content altına inemediğinden taşmayı karta taşıyıp 2px→31px'e büyüttü (dört ızgarada da). Doğru karar: 2px bırakıldı — tasarım referansının kalın istatistik tipografisini bozmaya değmez, yalnızca EOL-cihaz × maksimum-erişilebilirlik kesişiminde ve tasarımın pratik tabanı olan 360px'in altında. Kalıcı otomatik test eklenmedi (proje testleri saf node birim; tarayıcı doğrulaması elle yapılıyor). Sonuç: uygulama 320px'e kadar tam duyarlı. `npm run build` geçti, 324 test yeşil. |
| 92 | 2026-08-10 19:27 | **Yedekleme/geri yükleme denetlendi + saf yardımcılara regresyon testi (kod hatası yok, yalnızca test).** Local-first uygulamada yedek verinin tek güvencesi olduğundan `data/yedek.ts` eleştirel okundu. **Güvenli bulundu:** `dogrula` (biçim/sürüm/tablolar kontrolü) YIKICI işlemden **önce** çalışıyor — bozuk/yabancı dosya DB'yi silemez; ayrıca `clear()`+`bulkAdd` tek Dexie transaction'ında, `bulkAdd` yarıda hata verirse clear geri alınır (veri korunur); şifreli yedek yalnızca AES-GCM zarfıyla çıkıyor. **Hata yok.** Yine de kullanıcıya dönük iki saf fonksiyonun testi yoktu ve kritik karar sürüyorlar: `sifreliMi` parola ekranının çıkıp çıkmayacağını, `yedekOzeti` geri yüklemeden önce "neyin üzerine yazılacağı" sayısını belirliyor. **Yeni `yedek.test.ts` (6 test):** şifreli zarf tanıma + geçersiz JSON'da çökmeden false; yedi tablo sayımı + boş yedek + eksik tablo dizisinde `?? 0` dayanıklılığı. `npm run build` (tsc strict) geçti, testler 318→**324 yeşil** (33 dosya). |
| 91 | 2026-08-10 18:33 | **Veri katmanı saf yardımcıları denetlendi + regresyon testleri eklendi (kod hatası yok, yalnızca test).** Domain denetimi bittikten sonra `src/data/` altındaki test edilmemiş saf fonksiyonlar tarandı. **Sağlam bulunanlar:** `raporCsv` (kontrollü sözlük etiketleri + `;` ayraç + BOM → serbest metin kaçışı riski yok), `finansOzeti` (gelir/gider/bekleyen doğru toplanıyor), `ics.ts` (kaçış doğru + zaten testli), `boyutMetni`/`haftaSinirlari`/`ofsetEtiketi` (biçim/tarih/etiket yardımcıları). Hata bulunmadı — bu turdaki tek gerçek kusur zaten düzeltilen `para.ts`'ti. Yine de üç yardımcının **testi yoktu**; bunlar temiz sözleşmeli saf fonksiyonlar olduğundan üç ortak-konumlu test dosyası yazıldı: `ofsetEtiketi` (katalog eşleşmesi + dk/saat/gün yedek birimleri), `boyutMetni` (B/KB/MB eşikleri + yuvarlama), `haftaSinirlari` (Pzt–Paz sınırı; pazar hâlâ o haftada, aralık tam 6 gün — komşu haftadan öge sızmasına karşı). Varsayım: node-vitest'te Dexie içe aktaran veri modülleri sorunsuz import ediliyor (mevcut `onayIslemleri.test.ts` bunu zaten kanıtlıyor). `npm run build` geçti, testler 306→**318 yeşil** (32 dosya). |
| 90 | 2026-08-10 03:25 | **Temel tarih yardımcıları (`tarih.ts`) denetlendi — sağlam; domain kritik-denetimi tamamlandı (değişiklik yok).** Her şeyin bağlı olduğu foundational modül eleştirel okundu. **Doğru bulunanlar:** `gunFarki` `Math.round((a-b)/GUN_MS)` ile DST'ye dayanıklı (yerel gece-yarısı arası 23s/25s bile tam güne yuvarlanır — gün sayısı hukuken önemli; ayrıca Türkiye 2016'dan beri DST kullanmıyor, kenar durum zaten oluşmaz); Türkçe büyük harf her yerde `toLocaleUpperCase('tr')` (i→İ, I değil); `kalanSureMetni`/`aciliyet` eşikleri, `goreliZaman` göreli zaman, `selamlama`, `aralik` doğru. **Hata yok.** Böylece **domain kritik-denetimi tamam:** foundational (`tarih`) + iki en karmaşık hesap (`sureHesabi`, `tekrar`) sağlam doğrulandı ve mantık taşıyan her modülün doğrudan testi var; bu denetimdeki tek gerçek kusur zaten düzeltilen `para.ts` para ayrıştırmasıydı. `npm run build` geçti, 306 test yeşil. |
| 89 | 2026-08-10 02:25 | **Tekrar (recurrence) mantığı eleştirel denetlendi — sağlam, hata yok (değişiklik yok).** İkinci en karmaşık hesap modülü `tekrar.ts` satır satır okundu (recurrence klasik hata kaynağıdır). **Doğru bulunanlar:** adet `[1,60]`'a kırpılıyor (0/negatif/kesirli/aşırı girdiye dayanıklı); **kritik olarak her yineleme ORİJİNAL başlangıçtan hesaplanıyor** (`addMonths(ilk, i)`, kümülatif değil) → klasik aylık kayma hatası yok (31 Ocak'tan: 31 Oca→28 Şub→**31 Mar**→30 Nis, kayan 28 Mar değil); ay sonu/artık yıl kırpması `date-fns`'e devredilmiş (sağlam); `hafta-ici` başlangıç hafta sonuysa ilk iş gününe kaydırıp tam `n` iş günü topluyor. **Hata yok.** Böylece iki en karmaşık hesap modülü (süre + tekrar) sağlam doğrulandı; bu domain denetimindeki tek gerçek kusur zaten düzeltilen `para.ts` ayrıştırma hatasıydı. `npm run build` geçti, 306 test yeşil. |
| 88 | 2026-08-10 01:25 | **Çekirdek süre hesabı (`sureHesabi.ts`) eleştirel olarak denetlendi — mantık sağlam, kod hatası yok (değişiklik yok).** para.ts'te ciddi bir hata çıkması üzerine, uygulamanın en yüksek riskli fonksiyonu (hukuki son gün hesabı) satır satır okundu. **Doğru bulunanlar:** gün süresi `başlangıç + N` (HMK m. 92/1, tebliğ günü sayılmaz); hafta `miktar×7` gün (son haftada karşılık gelen gün, m. 92/2); ay ekleme kısa aya **son güne sabitleme** ile (31 Ocak+1 ay→28/29 Şubat, m. 92/2 son cümle); yıl ekleme 29 Şubat→28 Şubat (`setDate(0)`); adli tatil (m. 104) yalnızca `adliTatileTabi` kural + ham son gün 20 Tem–31 Ağu aralığındayken 7 Eylül'e uzatıp sonra tatil kaydırması; katalog icra/ceza kurallarını doğru şekilde `adliTatileTabi:false` işaretlemiş; tatil kaydırması (m. 93) hafta sonu/resmî tatilde ilk iş gününe + insan-okur gerekçe. **Hata yok.** Tek gözlem — **hukukî yorum** meselesi (kod kusuru değil): 20 Temmuz öncesi bir hafta sonuna denk gelen son gün, kaydırma sonrası adli tatile girebilir (kod adli-tatil kontrolünü kaydırmadan ÖNCE ham tarihte yapıyor); bu tartışmalı ve hukuki içerik olduğundan (`[!]` avukat alanı) değiştirilmeyip kayda geçirildi. Sonuç: en kritik hesap güvenilir. `npm run build` geçti, 306 test yeşil. |
| 87 | 2026-08-10 00:25 | **Bildirim adaptörü testleri eklendi — mantık taşıyan tüm modüllerin doğrudan testi tamamlandı (kod değişikliği yok, hata yok).** Kalan test boşlukları denetlendi: `olay.ts` saf veri (TS `Record<OlayTuru,…>` zaten eksiksizliği derlemede zorluyor) — test gereksiz; `bildirim.ts` (bildirim kanalı adaptörü) gerçek mantık taşıyor ve testi yoktu. **Yeni `bildirim.test.ts` (9 test)** iki değişmezi kilitliyor: (1) **DÜRÜSTLÜK** — `kanalDurumlari` SMS ve e-postayı her zaman "yapılandırılmadı" bildiriyor (uygulama-içi "hazır"); kullanıcı olmayan bir teslimat sözüne kanmasın (projenin dürüstlük ilkesiyle uyumlu, regresyon koruması). (2) **ZARİF DÜŞÜŞ** — Notification API yokken (node/eski tarayıcı) `pushIzniIste`→"desteklenmiyor", `cihazBildirimiGoster`→false (çökme yok). Ayrıca izin durumları taklit edilerek (granted/denied/default) `pushDurumu`/`pushIzniIste` eşlemeleri ve `kanalDurumEtiketleri` eksiksizliği doğrulandı. Hata bulunmadı (para.ts'in aksine); bu modül zaten doğruydu. Böylece domain+services'te mantık taşıyan her modülün doğrudan birim testi var. `npm run build` geçti, testler 297→**306 yeşil** (29 dosya). |
| 86 | 2026-08-09 23:25 | **Ciddi mali ayrıştırma hatası düzeltildi — `metindenKurus` Türkçe binlik ayracını yanlış çözüyordu (+ `para.ts` testleri).** Kapsam denetimini sürdürürken (para.ts'in testi yoktu) gerçek ve ciddi bir hata bulundu: `metindenKurus` tek noktayı hep ondalık sayıyordu, oysa Türkçede nokta binlik ayracıdır. **Sonuç:** `"1.250"` → **₺1,25** (olması gereken ₺1.250 — 1000× hata), `"12.345"` → ₺12,35, `"1.250.000"` → **null** (kayıt tümüyle reddediliyor). Türk avukat "1.250 TL" vekâlet ücretini ₺1,25 olarak kaydedebilirdi. **Düzeltme:** ayraç belirsizliği yeniden çözüldü — iki ayraç varsa SONDA olan ondalıktır; tek tür ayraç yalnızca sonrasında 1–2 rakam varsa ondalıktır, aksi hâlde binlik ayracıdır (Türkçe-öncelikli, İngilizce uyumlu; negatif ve para simgesi korunur). Artık `"1.250"→1250`, `"12.345"→12345`, `"1.250.000"→1250000`; önceden doğru çözülen tüm biçimler (`1.250,00`, `1250,50`, `1250.50`, `1,250.50`, `1.234.567,89`) korunuyor. **Yeni `para.test.ts` (11 test):** tutarTam/tutarKisa biçimleri, tüm ayrıştırma senaryoları + regresyon koruması + geçersiz→null + yuzdeDegisim/yuzdeMetni (eksi işareti U+2212). Yalnızca ayrıştırma mantığı değişti, biçimlendirme aynı. `npm run build` geçti, testler 286→**297 yeşil**. |
| 85 | 2026-08-09 22:25 | **Test kapsamı güçlendirildi — en hassas çekirdek `tatil.ts` için doğrudan birim testleri eklendi (13 test).** Kapsam denetiminde tek gerçek boşluk bulundu: hukuken en kritik modül olan tatil/adli-tatil kuralları (`src/domain/tatil.ts`) yalnızca `sureHesabi` üzerinden dolaylı test ediliyordu; **kendi test dosyası yoktu.** Yanlış tatil → yanlış son gün olduğundan bu modülü doğrudan sabitlemek yüksek değerli ve düşük riskli (yalnızca test, uygulama davranışı değişmedi). **Yeni `tatil.test.ts` (13 test, 2026 referans yılı, hafta günleri UTC'de doğrulandı):** `haftaSonuMu`; `resmiTatilMi` (sabit + çok günlü dinî bayram aralığı + **arefe/bayram-sonrası tam tatil değil** + sıradan hafta sonu resmî tatil sayılmaz); `calismaGunuDegilMi`; `ilkCalismaGunu` (iş günü→aynı, hafta sonu→Pzt, resmî tatil→sonraki, **bayram+hafta sonu zincirini tümüyle aşma**: Kurban 27–30 May + 31 May Paz → 1 Haz); `tatilAdi`; `adliTatildeMi` (20 Tem–31 Ağu uçları dahil, 19 Tem/1 Eyl hariç); `adliTatilUzamaGunu` (o yılın 7 Eylül'ü). `npm run build` geçti, testler 273→**286 yeşil** (27 dosya). |
| 84 | 2026-08-09 16:25 | **Kararlı durum / sağlık teyidi — yapılabilir tüm işler tamam, kalan her şey kullanıcıya bağlı (`[!]`); değişiklik yok.** Bu oturumda 8 gerçek düzeltme yapıldı (chevron, kontrol-listesi etiketleri, tur spotlight, Asistan etiketleri, DevirTeslim modal odağı, form hata duyurusu, ARIA sekme yapısı, ARIA sekme klavyesi) + kapsamlı denetimler (tasarım spec'i, kod hijyeni, tüm CRUD, kontrast, çevrimdışı/PWA, klavye, tüm etkileşimli bileşenler). **Sağlık:** ana JS paketi 498 kB (146 kB gzip) — önceki ölçümle (~497 kB) pratikte aynı, oturumun değişiklikleri (çoğu aria niteliği + küçük işleyiciler) ihmal edilebilir ağırlık kattı, şişme yok; CSS 73 kB (12 kB gzip); precache 24 girdi; 273 test yeşil; typecheck temiz; 0 audit açığı (önceki). **Durum:** açık `[ ]` madde yok; `packages/hukuk-ai` hâlâ `package.json` içermiyor (paketi app'e bağlama BEKLEMEDE). Kalan tüm işler **kullanıcı adımı gerektiren `[!]`**: geçici sürüm düğmesini kaldırma (paketlemede), hukuki metin (avukat), Google/Apple OAuth kimlikleri + e-posta doğrulama sunucusu, cihazlar arası senkron altyapısı, App Store yayını, ve paket tüketilebilir olunca app'e bağlama (+ tur maskeleme cümlesini geri ekleme). Otonom döngünün yapabileceği iş kalmadı; uygulama kararlı ve tamamlanmış durumda bu maddeleri bekliyor. `npm run build` geçti. |
| 83 | 2026-08-09 15:25 | **Takvim ızgarası erişilebilirliği denetlendi — zaten sağlam, kusur yok (değişiklik yok).** A11y dizisinin son karmaşık bileşeni takvim (Takvim.tsx). Gün hücreleri (hem ay ızgarası `cal-day` hem hafta şeridi) **gerçek `<button>`** — klavyeyle işletilebilir; **`aria-label` tam tarih + kayıt sayısı** taşıyor ("8 Ağustos 2026, 3 kayıt"); **`aria-pressed`** seçili durumu duyuruyor; dekoratif olay noktaları (`cal-dots`) `aria-hidden` (bilgi etikette sayı olarak, 1.4.1 sağlanıyor). Tam `role="grid"` 2B ok-tuşu deseni yok; ama bu bir tarih-seçici değil, ay **görünümü** (her gün basit bir gezinme düğmesi) — buton temelli yaklaşım meşru ve erişilebilir bir seçim; sekmelerin aksine ızgara olarak başlamadığından griddesenine çevirmek "yarım işi bitirmek" değil, gereksiz büyük bir değişiklik olurdu. Sonuç: takvim klavye-erişilebilir ve iyi etiketli; kusur yok. Böylece a11y denetimi tüm bileşenlerde tamamlandı (etiketler, modal odak, hata duyurusu, sekme deseni+klavye, kontrast, yer imleri, klavye sırası, yan menü, takvim). `npm run build` geçti, 273 test yeşil. |
| 82 | 2026-08-09 14:25 | **Sekme klavye deseni tamamlandı — ARIA tabs artık tam (roller + kablolama + klavye).** Geçen tur sekme yapısını (tablist/tab/tabpanel + id/aria-controls/aria-labelledby) kurmuştum ama WAI-ARIA tabs deseninin **klavye etkileşimi** eksikti — desen roller + klavyedir, yani iş yarımdı. **Tamamlandı (DosyaDetay):** roving tabindex (yalnızca seçili sekme `tabIndex=0`, diğerleri `-1`) + tablist'te `onKeyDown` — **ok tuşları** sekmeler arası geziyor (otomatik etkinleştirme, odak seçimle birlikte taşınıyor), **Home/End** ilk/son sekmeye gidiyor, uçlarda sarma var. **Canlı doğrulandı:** ArrowRight Genel→Duruşmalar→Süreler, End→Notlar, Home→Genel, ArrowLeft baştaan sona sarıyor; her adımda seçim+odak birlikte, roving tabindex tutarlı (tek `tabIndex=0`, o da seçili sekme), konsol hatası yok. Böylece WAI-ARIA Tabs deseni baştan sona uyumlu (2.1.1 klavye + tam desen). `npm run build` geçti, 273 test yeşil. |
| 81 | 2026-08-09 13:25 | **Klavye-yalnız gezinme doğrulandı (bütünsel a11y) — kusursuz, değişiklik yok.** Bu oturumun a11y dizisini tamamlamak için gösterge panelinde Tab sırası uçtan uca denetlendi. **Sonuç örnek düzeyde:** 1) **atlama bağlantısı ilk** ("İçeriğe geç"), 2-5) üst çubuk (Menüyü aç / sürüm / Ara / "Bildirimler, 15 aktif hatırlatma"), 6) hero "Güncelle", 7+) bölümler görsel sırayla (Hepsini gör → süre satırları → duruşma satırları). **Her durakta** erişilebilir ad var ve **görünür odak halkası** çıkıyor (focus-visible çalışıyor); mantıklı sıra; klavye tuzağı yok; tüm etkileşimli öğeler Tab ile erişilebilir. WCAG 2.1.1 (Klavye), 2.4.1 (Blok atlama/skip-link), 2.4.3 (Odak sırası), 2.4.7 (Odak görünürlüğü) karşılanıyor. Böylece bu oturumun a11y denetimi kapsamlı biçimde tamamlandı: etiketler (Asistan düz.), modal odak (DevirTeslim düz.), hata duyurusu (düz.), sekme deseni (tamamlandı), kontrast (ölçüldü), yer imleri/başlık/atlama/odak (doğrulandı), yan menü odak tuzağı (örnek), klavye işletilebilirliği (bu tur). Ayıklanacak a11y hatası kalmadı. `npm run build` geçti, 273 test yeşil. |
| 80 | 2026-08-09 12:25 | **Regresyon + çevrimdışı çekirdek vaadi doğrulandı — bu oturumun 7 değişikliğinden sonra hiçbir gerileme yok (kod değişikliği yok).** Bu oturumda 7 gerçek düzeltme yapıldı (chevron, kontrol-listesi etiketleri, tur spotlight, Asistan etiketleri, DevirTeslim odak, form hata duyurusu, sekme ARIA) — hepsi UI/a11y katmanında, SW/veri katmanına dokunmadı. Yine de uygulamanın **çekirdek vaadi (çevrimdışı çalışır)** yeniden doğrulandı: service worker kayıtlı ve kontrol ediyor (1 cache); **ağ kapatılıp reload edildiğinde uygulama tam yükleniyor** (`navigator.onLine=false`, uygulama kabuğu + "Çevrimdışısınız. Uygulama cihazınızda çalışır" şeridi), **çevrimdışı rota gezinmesi çalışıyor** (/takvim → h1 "Ağustos 2026"), atlama bağlantısı ("İçeriğe geç") mevcut, konsol hatası yok. Yani a11y/UI düzeltmeleri çevrimdışı yeteneği bozmadı. `npm run build` geçti, 273 test yeşil. |
| 79 | 2026-08-09 11:25 | **Renk kontrastı ölçüldü (WCAG) — birincil içerik ve kontroller AA geçiyor; soluk vurgu etiketleri referansın kasıtlı pastelleri (değişiklik yapılmadı, yapılırsa "görsel dilinden şaşma" ihlali olurdu).** Somut kontrast denetimi: **Geçenler (AA≥4.5):** birincil metin 14.66, ikincil 6.10, soluk 4.97, slate 4.81 (ikincil/soluk zaten önceki oturumda AA için koyulaştırılmış); koyu buton beyaz metin 15.55, adaçayı hero'da mürekkep metin 8.37; koyu yüzeylerde her şey geçiyor. **AA altı (beyaz üstünde):** kategori vurgu renkleri metin olarak — kırmızı 3.69, mor 3.61, mavi 3.22, kehribar 2.23, teal/yeşil 2.47; bunlar "gün kaldı", tahsilat +/−, "İyi ilerliyor", teal bağlantılar gibi **ikincil/dekoratif-destekleyici** metinlerde kullanılıyor. **Değerlendirme:** bu renkler DESIGN-REFERENCE §3'ün açıkça belirttiği **pazarlık konusu olmayan** pastel paleti (teal = bağlantı rengi) birebir uyguluyor; softluk kasıtlı ("Vurgu renkleri soluk ve pastel; dikkat çeken tek şey içerik"). Ayrıca **WCAG 1.4.1 sağlanıyor** — bilgi renkle tek başına verilmiyor (öncelik noktalarında `aria-label`, finansta +/− işareti, durumlarda metin). Yani ayıklanacak **tasarım hatası yok**: renkleri koyulaştırmak referanstan sapmak olurdu (asıl hata bu olurdu). AA-vs-marka estetiği, soluk vurgular için kullanıcının değer tercihidir; ölçüm kayda geçirildi. Birincil okunur içerik ve tüm etkileşimli kontroller AA uyumlu. `npm run build` geçti, 273 test yeşil. |
| 78 | 2026-08-09 10:25 | **Erişilebilirlik — dosya detayı sekme deseni (ARIA tabs) tamamlandı; ayrıca toggle'lar denetlendi (kusursuz).** **Toggle denetimi:** Bildirim kanalları/Tüm gün/LLM anahtarları `<button aria-pressed>` ile erişilebilir (geçerli toggle deseni); 592/629/647'deki `<div className="switch-row">` ise etkileşimli değil, sadece etiket+gerçek `<button>`/rozet taşıyan yerleşim satırları — kusur yok. **Sekme deseni eksikti:** dosya detayı sekmeleri `role="tablist"`/`role="tab"`/`aria-selected` taşıyordu ama **panel tarafı yoktu** — sekmelerde `id`/`aria-controls` ve içerikte `role="tabpanel"` (dolayısıyla `aria-labelledby`, klavyeyle odaklanabilirlik) eksikti; yani ARIA tabs deseni yarımdı. **Düzeltme:** her sekmeye `id={sekme-…}` + `aria-controls={panel-…}`; içerik `role="tabpanel" id={panel-…} aria-labelledby={sekme-…} tabIndex=0` sarmalayıcısına alındı. Böylece ekran okuyucu "sekme paneli, [ad]" duyuruyor ve klavye kullanıcısı paneli odaklayıp kaydırabiliyor. **Canlı doğrulandı:** Genel ve Belgeler sekmelerinde tab↔panel bağlantısı karşılıklı doğru (id/aria-controls/aria-labelledby tutarlı, tabindex=0), aktif sekme değişince panel kimliği/etiketi takip ediyor, konsol hatası yok. `npm run build` geçti, 273 test yeşil. |
| 77 | 2026-08-09 09:25 | **Erişilebilirlik hatası düzeltildi — form doğrulama hataları ekran okuyucuya duyurulmuyordu.** A11y denetimi durum/hata mesajlarına genişletildi. **Zaten iyi:** durum mesajları duyuruluyor (SurumYenile/Ayarlar `role="status"`, çevrimdışı şeridi `role="status" aria-live`, hata sınırı `role="alert"`). **Kusur:** form doğrulama hataları (`<p className="field-error">{hata}</p>`) **sessizce** DOM'a ekleniyordu — `role`/`aria-live` yoktu; gönderim başarısız olunca ekran okuyucu kullanıcısı "Dosya başlığı girilmeli." gibi hatayı **duymuyordu** (WCAG 3.3.1 Hata Tanımlama / 4.1.3 Durum Mesajları). Aynı sorun kilit ekranı "PIN yanlış" mesajında da vardı. **Düzeltme:** 8 hata mesajına `role="alert"` eklendi — 5 form (Dosya/Finans/Görev/Müvekkil/Olay), KayıtEkranı, Ayarlar PIN hatası ve KilitKapısı. Uygulamanın mevcut "durum duyur" kuralıyla tutarlı (hatalar durumdan daha da önemli). **Canlı doğrulandı:** yeni dosya formunda boş gönderim → hata artık `role="alert"` ile render oluyor ("Dosya başlığı girilmeli."). `npm run build` geçti, 273 test yeşil. |
| 76 | 2026-08-09 08:25 | **Erişilebilirlik hatası düzeltildi — DevirTeslim modalı `aria-modal="true"` diyordu ama odak yönetimi yoktu.** A11y denetimi etkileşimli katmanlara genişletildi (onay, kayıt, tur, yan menü, DevirTeslim, kilit). **Temiz olanlar:** onay/kayıt formları etiketli; tur `role=dialog`+`aria-label`+`aria-live=polite`; **yan menü örnek düzeyde** (`<nav aria-label="Ana gezinme">`, Esc ile kapanır, açılınca odak kapat düğmesine gider, Tab tuzağı, kapanınca odak geri döner, gövde kaydırma kilidi, kapalıyken `aria-hidden`, aktif linkte `aria-current`); kilit ekranı `role=dialog`+etiketli tuşlar (`aria-label="Sil"`). **Kusur (DevirTeslim):** `role=dialog aria-modal="true"` bildiriyordu ama odağı içeri almıyor, Esc ile kapanmıyor, Tab'ı tuzaklamıyordu — beyan edilen "dışarısı hareketsiz" sözü davranışla çelişiyordu (klavye/ekran-okuyucu odağı arka planda kalabiliyordu). **Düzeltme:** Drawer'ın kanıtlanmış desenini uyguladım — açılınca odak ilk butona, Esc kapatır, Tab pencere içinde döner, kapanınca odak geri verilir. **Canlı doğrulandı** (masaüstü bayrağı + LLM yapılandırması kurulup Asistan'dan tetiklenerek): modal açılışta odak "Telefonda yap"ta, **Esc kapatıyor**, konsol hatası yok. `npm run build` geçti, 273 test yeşil. |
| 75 | 2026-08-09 07:25 | **Erişilebilirlik hatası düzeltildi — Asistan soru-cevap alanındaki select ve input erişilebilir etikete sahip değildi.** Bu tur odaklı bir a11y denetimi yapıldı (13 sayfa: başlık hiyerarşisi, landmark, form etiket eşleşmesi, erişilebilir ad, görsel alt). **Sonuç neredeyse temiz:** her sayfada tek `h1`, atlanmış başlık seviyesi yok, tam olarak bir `main`, adsız buton/bağlantı yok, alt'sız görsel yok. **Tek gerçek kusur (Asistan):** "Dosyaya danışın" formundaki dosya seçme `<select>`'i ve serbest soru `<input>`'u **erişilebilir etiket taşımıyordu** — ekran okuyucu adsız "combo box"/"edit text" okur (WCAG 4.1.2 / 3.3.2); diğer formlar sarmalayan `<label>` kullanırken bu ikisi yalnızca görünür bağlam + placeholder'a dayanıyordu. **Düzeltme:** `aria-label="Danışılacak dosya"` (select) ve `aria-label="Sorunuz"` (input) eklendi. Playwright ile doğrulandı: Asistan artık UNLABELED uyarısı vermiyor, diğer sayfalarda uyarı yok. `npm run build` geçti, 273 test yeşil. |
| 74 | 2026-08-09 06:25 | **Kod hijyeni denetlendi (ekran QA'dan farklı mercek) — kod düzeyi de bitmiş kalitede, yeni hata yok (kod değişikliği yok).** Görsel taramalar tükendiği için bu tur kod sağlığına bakıldı. **Tip denetimi:** `npm run typecheck` (`tsc -b --noEmit`) **temiz** (hata yok). **Katılık:** `tsconfig.app.json` `strict: true`, `noUnusedLocals: true`, `noUnusedParameters: true`, `noFallthroughCasesInSwitch: true` — yani geçen derleme kullanılmayan yerel/parametre/import ve switch fallthrough olmadığını garanti ediyor. **Yarım iş işaretleri:** src'de TODO/FIXME/HACK/XXX **yok**; tek `eslint-disable-next-line no-console` `services/gunluk.ts`'de — redakte eden kaydedicinin bilinçli/tek meşru console sınırı (console-guard testiyle tutarlı). **Bastırma yok:** `@ts-ignore`/`@ts-expect-error` yok. Eslint yapılandırması yok ama TS katılığı + testler kapsıyor. Sonuç: yalnızca arayüz değil, kod tabanı da temiz ve bitmiş kalitede; ayıklanacak (tasarım ya da kod) hata bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 73 | 2026-08-09 05:25 | **Silme akışı uçtan uca doğrulandı; CRUD kapsamı tamamlandı — yeni hata yok (kod değişikliği yok).** Son test edilmemiş CRUD yolu olan **silme** canlı denendi (dosya sayısı IndexedDB'den ölçülerek): önce 27 → geçici dosya oluşturuldu → 28. Düzenleme formunda **"Sil" iki adımlı onay** taşıyor: ilk tık düğmeyi "Emin misiniz?"e çeviriyor + "…kayıtları kalıcı olarak silinecek" uyarısı + iptal; ikinci tık siliyor. İki adım tamamlanınca sayı 28 → **27'ye döndü** (kademeli silme, dosyaSil ile) ve **liste sayfasına yönlendirildi** (`/dosyalar`). Konsol hatası yok. Yıkıcı eylemin yerleşik `confirm()` yerine uygulama içi iki adımlı onayla korunması doğru UX (geri döndürülemez eylemden önce teyit). Böylece CRUD tümü doğrulandı: Oluştur (71: doğrulama+kalıcılık+gezinme), Oku (çok sayıda tur), Güncelle (70: düzenleme formları önden dolu), **Sil (bu tur: iki adımlı onay + kademe + gezinme)**. Ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 72 | 2026-08-09 04:25 | **Açık kalan soru kapatıldı — Bildirimler "Yaklaşan" satırındaki "1 gün önce" etiketi kusur değil (kod değişikliği yok).** Daha önce (özellik-sayfası taramasında) bu etiketin gelecekteki hatırlatmalar için kafa karıştırıcı olabileceğini not edip ertelemiştim; bu tur kaynağı incelendi. `ofsetMetni` (domain/hatirlatma.ts) "aynı gün / N dk önce / N saat önce / N gün önce" üretiyor — bunlar hatırlatmanın **önden-haber ofseti** (olaydan ne kadar önce hatırlatacağı). Bildirimler satırında **olayın gerçek tarih+saati zaten alt satırda** görünüyor (`altBaşlık · kısaTarih saat`); sağdaki `ofsetEtiketi` ise bir olayın **birden çok hatırlatmasından hangisi** olduğunu ayırt ediyor (ör. 30/7/1 gün önce). Ayrıca bu "N gün önce" sözlüğü **Ayarlar → Hatırlatma zamanları** çipleriyle birebir aynı ("30 gün önce", "7 gün önce", "1 gün önce", "Aynı gün") — yani kullanıcının seçtiği ofsetle tutarlı. Türkçede "duruşmadan 1 gün önce hatırlat" deyimsel ve doğru. Sonuç: etiket bilinçli, tutarlı ve ayırt edici; kusur değil. Böylece daha önce ertelenen tek açık gözlem de kapandı. `npm run build` geçti, 273 test yeşil. |
| 71 | 2026-08-09 03:25 | **Kayıt oluşturma akışı uçtan uca doğrulandı (doğrulama + kalıcılık + geri bildirim); yeni hata yok (kod değişikliği yok).** Bu tur eylem-geri-bildirim döngüsü canlı test edildi. **Doğrulama:** yeni dosya formunda boş gönderim → "Dosya başlığı girilmeli."; yalnızca başlık → "Müvekkil seçilmeli." (net Türkçe hata mesajları). **Kalıcılık + gezinme:** başlık + müvekkil seçilip gönderilince yeni dosya oluşuyor ve **yeni dosyanın detayına yönlendiriliyor** (geri-bildirim = sonuca gitme; toast'a gerek yok) — detay doğru render: girilen başlık ("Test Davası / Deneme"), seçilen müvekkil (Anadolu Makine Ltd. Şti.), AÇILIŞ bugüne (9 Ağustos 2026) otomatik ayarlı, hesaplanan hazırlık (%45 "Dikkat gerekiyor"), sekme sayaçları 0. **Yan doğrulama:** yeni dosyanın "Hazırlık kontrolü"nde olumsuz maddeler ("Bekleyen ödeme yok / Kaçırılmış süre yok / Geciken görev yok") ✓ ile **olumlu etiketi** gösteriyor — 62. satırdaki etiket düzeltmesinin gerçek yeni-dosya senaryosunda çalıştığını teyit ediyor. Konsol hatası yok. Sonuç: CRUD oluşturma yolu sağlam; ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 70 | 2026-08-09 02:25 | **Düzenleme (edit) modundaki formlar denetlendi — mevcut veri doğru dolduruluyor, yeni hata yok (kod değişikliği yok).** Bu tur daha önce bakılmayan **düzenleme formları** hedeflendi (yeni-kayıt formlarından farklı olarak `:id` ile mevcut veriyi yüklerler): dosya, müvekkil, olay, görev, finans düzenleme rotaları gerçek tohum ID'leriyle açıldı. **Sonuç:** hepsi doğru başlık ("… DÜZENLE" üst-etiketi + kaydın adı) ve dolu alanlarla geliyor; dosya 8/9, olay 5/7, görev 4/6, finans 5/7 alan dolu (boşlar o kayıtta gerçekten boş olanlar, ör. karar no); taşma/konsol hatası yok. **Müvekkil düzenleme** yakından bakıldı: AD gerçek değerle dolu, TÜR doğru "Tüzel kişi" seçili ve kimlik etiketi türe göre **"Vergi no"** (form→düzenleme tutarlı); telefon/e-posta/etiket alanları **placeholder** gösteriyor çünkü bu tohum müvekkilinin o verileri yok (detay sayfasında da "—" görünüyordu) — yani "1/6 dolu" gerçek veriyi yansıtıyor, doldurma hatası değil. Düzenleme modu ayrıca uygun biçimde bir **"Sil"** (kırmızı, ölçülü) eylemi ekliyor. Sonuç: düzenleme formları da cilalı ve doğru; ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 69 | 2026-08-09 01:25 | **Tasarım, yazılı spec'e (DESIGN-REFERENCE.md) karşı denetlendi — ekran görüntüsü değil, dokümante kurallar merceğiyle; uyum tam, yeni hata yok (kod değişikliği yok).** Önceki turlar JPG'lerle karşılaştırmıştı; bu tur spec'in **yazılı kuralları** madde madde koda karşı denetlendi. **Tipografi (§2):** üç font rolü uygulanıyor — serif başlık (Newsreader), sans gövde (Inter), mono etiket (IBM Plex Mono); `--tracking-label: 0.16em` spec'teki ~0.16em ile birebir; fontlar **cihazda barındırılıyor** (`@font-face` → `assets/fonts/*.woff2`, `latin-ext` alt kümesiyle Türkçe İ/ı/ş/ğ/ü/ö/ç desteği), Google Fonts/CDN yok — local-first/çevrimdışı/gizlilik kurallarıyla uyumlu. **Renk (§3):** tüm paleti spec'le birebir (zemin #f7f6f3, kart #fff, kenarlık #ece9e4, birincil #1c2b2d, marka adaçayı #a8c5b4/#e4efe7, teal #5fb49c, lacivert #16262e; kategori kırmızı/turuncu/mor/mavi/yeşil hep aynı). Tek sapma: `--text-secondary/-muted` WCAG AA için bilinçli koyulaştırıldı (spec'in "soluk" niyeti korunuyor) + gölge saf siyah yerine mürekkep-tonlu rgba (ihmal edilebilir). **Şekil (§4):** kart 16px (`--radius-lg`), iç öğe 8–12px, hap 999px, gölge en fazla `0 1px 2px .04`, kart içi 20px (`--space-5`), bölüm arası 16px — hepsi spec'le uyumlu. **Ton (§6):** UI metinlerinde **ünlem yok** (grep: yalnızca kod yorumundaki `[!]` plan işareti), sayılar Türkçe biçimde. Sonuç: uygulama, yazılı tasarım dilinin sadık ve dikkatli bir gerçeklemesi; sapmalar yalnızca bilinçli erişilebilirlik iyileştirmeleri. `npm run build` geçti, 273 test yeşil. |
| 68 | 2026-08-09 00:25 | **Kenar durumlar denetlendi — 404 sayfası, boş-durum davranışı ve dosya silme bütünlüğü; yeni hata yok (kod değişikliği yok).** Bu tur daha önce bakılmayan kenar durumlar hedeflendi. **404:** geçersiz rotaya gidildi (`#/bogus…`) — temiz, görsel dile uygun sayfa ("HATA 404 / Böyle bir sayfa yok." + sakin açıklama + "Genel bakışa git" teal bağlantısı + üst çubuk hâlâ erişilebilir), taşma/konsol hatası yok. **Boş durum:** veri depoları temizlenip yeniden yüklenince uygulama `tohumlaGerekiyorsa` ile örnek veriyi **yeniden tohumluyor** (`db.dosyalar.count()===0` ise) — yani tümüyle boş uygulama yalnızca anlıktır, gerçek kullanıcı senaryosu değil (kasıtlı demo davranışı; bölüm bazlı boş durumlar — Notlar/görüşme/arama — zaten doğrulanmıştı). **Bütünlük:** dosya silme (`dosyaSil`, dosyaIslemleri.ts:131) tek işlemde bağlı tüm kayıtları (olaylar/süreler/görevler/finans/belgeler/kişiler/notlar) **kademeli siliyor** — normal kullanımda yetim (orphan) süre/olay kalmıyor; panelde gördüğüm "Dosya yok" yalnızca harici temizlemeyle oluşan test artefaktıydı, kodda ise savunmacı bir yedek (`dosya?.baslik ?? 'Dosya yok'`). Sonuç: kenar durumlar da düzgün; ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 67 | 2026-08-08 23:25 | **Küçük ekran (360×640) taşma taraması + genel arama sayfası denetlendi — yeni hata yok (kod değişikliği yok).** Bu oturumda çoğu kontrol 390px'te yapılmıştı; bu tur dar ekran (360×640) ve daha önce bakılmayan **/ara** hedeflendi. **Taşma taraması:** 14 rota (dashboard, dosyalar, müvekkiller, takvim, süre, finans, görevler, raporlar, asistan, bildirimler, ayarlar, dosya formu, finans formu, dosya detayı) 360×640'ta gezildi — **hiçbirinde yatay taşma yok, konsol hatası yok.** **Genel arama (/ara):** üç durum denetlendi: (boş) placeholder + **cihaz depolaması göstergesi** (kullanım/kota + ilerleme çubuğu + "belgeler cihazda saklanır; yedek almayı unutmayın" — local-first için isabetli) + arama kapsamı ipucu; (sonuçlu) "Demir" → "2 SONUÇ", türe göre gruplu (DOSYALAR·1, MÜVEKKİLLER·1) tutarlı ikon+chevron; (sonuçsuz) sorguyu yansıtan boş durum ("…için sonuç yok."). Hepsinde taşma/konsol hatası yok. Sonuç: uygulama dar ekranda da sağlam, arama cilalı; ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 66 | 2026-08-08 22:25 | **Tur, farklı ekran boyutlarında doğrulandı — geçen turki düzeltme her boyutta sağlam, yeni hata yok (kod değişikliği yok).** Geçen tur 4. adımın spotlight'ını düzeltmiştim (ekran dışı panel); bu tur turun **küçük (360×640), temel (390×844) ve büyük (430×932)** ekranlarda sağlamlığı denetlendi. Yöntem: onboarding güvenilir yükseklikte (390×844) tamamlanıp sayfa hedef boyuta `setViewportSize` ile küçültülüyor (tur resize dinleyicisiyle yeniden ölçüyor), sonra 8 adım tek tek gezilip her adımda **balon tümüyle ekran içinde mi**, **halka ekran içinde mi**, **balon halkayı ne kadar örtüyor** ölçülüyor. Sonuç: **360×640** — 8 adımın hepsinde balon tam ekran içinde, halka ekran içinde; halka görünürlüğü çoğu adımda %100, 4. adımda %59 (uzun Q&A paneli; balon altta alt kısmı örtüyor ama başlık+dosya seçici+öneri çipleri görünür — kabul edilebilir), 7. adımda %79. **430×932** — hepsi %100 (fazladan yükseklikte balon ve halka hiç çakışmıyor). Hiçbir boyutta BALLOON-OFF/RING-OFF/RING-COVERED uyarısı ve konsol hatası yok. Küçük ekran 4. adımı görsel de doğrulandı: teal spotlight gerçek "Dosyaya danışın" panelini vurguluyor. Yani geç-yüklenen içerik + küçük ekran birleşiminde bile spotlight artık hep görünür hedefi işaret ediyor. `npm run build` geçti, 273 test yeşil. |
| 65 | 2026-08-08 21:30 | **Tur hatası düzeltildi — 4. adımda spotlight ekran dışı paneli işaret ediyordu (kullanıcının "hata dolu" şikâyeti alanı).** Turun 8 adımı baştan sona Playwright ile gezildi ve gerçek bir kusur bulundu: 4. adım (`asistan` — "Gerçek panel: Dosyaya danışın") balonu görünürken, işaret ettiği spotlight halkası ekranın **altında, görünüm dışındaydı** (halka y≈987–1473, viewport 844) — kullanıcı "işte gerçek panel" metnini görüp ekranda Asistan **Gündem** listesini görüyordu, asıl "Dosyaya danışın" (soru-cevap) paneli ise foldun altındaydı. **Kök neden:** Tur motoru hedefi bir kez (erken) `scrollIntoView` ediyor; ancak Asistan "Gündem" listesi IndexedDB'den **eşzamansız** yüklenince sayfa uzuyor ve hedef aşağı kayıyor — sonraki yeniden-ölçümler (0/120/260/420ms) kutuyu güncelliyor ama yeniden kaydırmıyordu, hedef ekran dışında kalıyordu. **Düzeltme (Tur.tsx):** her yeniden-ölçüm tikinde hedef görünür bandın dışına düştüyse (`r.top > vh−80 || r.bottom < 80`) yeniden ortalanıyor; böylece geç yüklenen içerik sayfayı uzatsa bile spotlight hep görünür öğeyi işaret ediyor. Playwright ile doğrulandı: 4. adım halkası artık y≈196–682 (viewport içi), spotlight gerçek soru-cevap panelini (Dosya seçin + öneri çipleri + "Sor") vurguluyor, balon metniyle eşleşiyor; **8 adımın hepsinde halka artık ekran içinde**, konsol hatası yok. (jsdom olmadığından birim test uygun değil; doğrulama Playwright ile — önceki tur çalışmalarıyla aynı yöntem.) `npm run build` geçti, 273 test yeşil. |
| 64 | 2026-08-08 20:25 | **Tasarım taraması detay sekmeleri ve müvekkil detayına genişletildi — yeni hata yok (kod değişikliği yok).** Geçen tur Genel sekmesindeki etiket hatası düzeltilmişti; bu tur dosya detayının **kalan 6 sekmesi** (bu kez sekmeler `[role=tab]` ile gerçekten değiştirilerek: Duruşmalar/Süreler/Görevler/Belgeler/Finans/Notlar) ve **müvekkil detay** sayfası denetlendi (390×844). **Otomatik kontrol:** hepsinde `aria-selected` doğru geçiyor, yatay taşma yok, konsol hatası yok. **Görsel denetim:** (Süreler) "Süre hesapla" + onay-kutulu/kaldır-X'li süre satırı + uyarı notu — kontroller işlevsel, kırpma zarif. (Belgeler) "Belge yükle" + tür/boyut/zaman satırları + satır içi "+ etiket". (Finans) özet kartları (tahsil/gider/bakiye/bekleyen, renk kodlu) + işlem satırları, global Finans ile tutarlı; bekleyen ₺12.500 checklist düzeltmesiyle uyumlu. (Notlar) "İlgili kişiler" + "Notlar" — iyi boş durumları ("Henüz ilgili kişi eklenmemiş.", "Bu dosyada not yok."), boşken "Not ekle" pasif. **Müvekkil detay:** tüzel kişi için kimlik alanı doğru şekilde **"VERGİ NO"** gösteriyor (form→detay uçtan uca tutarlı), boş değerler "—"/₺0,00, bağlı dosya satırı, "Görüşme geçmişi" boşken pasif buton + boş durum, local-first altbilgi. Sonuç: detay yüzeyleri de cilalı; ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 63 | 2026-08-08 19:25 | **Tasarım taraması formlara genişletildi — 5 kayıt formu denetlendi, yeni hata yok (kod değişikliği yok).** Bu tur giriş formları hedeflendi (390×844, Playwright): Dosya, Müvekkil, Olay (Takvime ekle), Görev, Finans. **Otomatik kontrol:** hepsinde yatay taşma yok, konsol hatası yok. **Görsel denetim:** tümü tutarlı görsel dile sahip (küçük-kapital etiketler, 2-kolon satırlar, birincil buton), yardımcı placeholder/ipuçları var. Öne çıkanlar: Olay formunda tür seçici ikon ızgarası + "Tüm gün" anahtarı + tekrar; Görev formunda "Hazır şablonlar" çipleri + tekrar ipucu; Finans formunda TL biçimi (`1.250,00`) + ödeme durumu çipleri; Müvekkil formunda **kimlik alanı türe göre doğru uyarlanıyor** (`gercek`→"TC kimlik", `tuzel`→"Vergi no" — Türk hukukuna uygun, MuvekkilForm.tsx:178). **Yerel-ayar incelemesi:** yerleşik `<input type=date/time>` başsız Chromium'da en-US biçiminde (12s AM/PM, mm/dd/yyyy) render oluyor; `locale:'tr-TR'` altında bile aynı — yani bu, tarayıcı UI yerel ayarına bağlı **başsız test artefaktı**, gerçek Türk cihazda 24s/gg.aa render olur (değerler zaten ISO saklanıyor: `09:30`, `2026-08-08`); ayrıca uygulama tarih alanlarının altına insan-okur yankı koyuyor ("8 Ağustos 2026", "İşlem tarihi: …") — belirsizlik giderilmiş. Yerleşik denetim biçimi HTML/CSS ile zorlanamayacağından ve mobil UX için doğru seçim olduğundan değişiklik gerekmedi. Sonuç: formlar da cilalı; ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 273 test yeşil. |
| 62 | 2026-08-08 18:30 | **Tasarım/anlam hatası düzeltildi — dosya "Hazırlık kontrolü" listesinde olumsuz etiketler çarpı işaretiyle çelişiyordu.** Dosya detayında (Genel sekmesi) hazırlık kontrol listesi denetlendi ve gerçek bir kusur bulundu: olumsuz ("… yok") ifadeli maddeler eksik (✗) olduğunda etiket olumlu hâlini koruyup **çift olumsuz** üretiyordu — ör. bekleyen bir gider varken satır "**Bekleyen ödeme yok** ✗ / Bilirkişi ücreti ödenecek" diyordu (etiket "ödeme yok" derken çarpı ve eylem "ödeme var"ı gösteriyor). Aynı sorun "Kaçırılmış süre yok" ve "Geciken görev yok" maddelerinde de vardı. **Varsayım:** durum işareti gerçeğe uymalı; olumsuz maddelere `etiketEksik` alanı eklendi (odemeler→"Bekleyen ödeme var", sureler→"Kaçırılmış süre var", gorevler→"Geciken görev var") ve DosyaDetay eksik durumda bu etiketi gösteriyor (`tamam ? etiket : etiketEksik ?? etiket`). Olumlu maddeler ("Vekâletname yüklendi" ✗ vb.) zaten çarpıyla doğru okunduğundan dokunulmadı. `HazirlikMaddesi` tipine opsiyonel `etiketEksik` eklendi; 2 yeni birim test (eksikken doğru etiket, tamamken olumlu etiket). Canlı arayüzde doğrulandı: satır artık "Bekleyen ödeme var" gösteriyor. `npm run build` geçti, testler 271→**273 yeşil.** |
| 61 | 2026-08-08 17:25 | **Tasarım taraması özellik sayfalarına genişletildi — 8 sayfa denetlendi, yeni hata yok (kod değişikliği yok).** Önceki iki tur gösterge panelini taramıştı; bu tur özellik sayfalarına odaklanıldı (390×844, Playwright). **Otomatik kontrol:** 8 rotanın (Dosyalar/Müvekkiller/Takvim/Süre/Finans/Raporlar/Bildirimler/Ayarlar) hepsinde **yatay taşma yok, konsol hatası yok.** **Görsel denetim:** (a) **Raporlar** — dönem çip şeridi, istatistik satırı, elle-SVG gelir/gider çubuk grafiği + açık-süre halka grafiği + kategori çubukları + dosya bazlı liste + CSV dışa aktar + "hiçbir bilgi dışarı gönderilmez" notu; temiz. (b) **Finans** — özet kartları, filtre çipleri, gelir/gider tutarları renk kodlu (+yeşil / −kırmızı), 40 kayıt tutarlı satırlar. (c) **Müvekkiller** — şirketlere klasör-avatar, bireylere baş harf rozeti, bakiye çipleri; düzenli. (d) **Dosyalar** — hazırlık %, müvekkil·mahkeme, renk kodlu sonraki-olay, DERDEST/tür/esas-no etiketleri. (e) **Takvim** — ay ızgarası (bugün vurgulu + tür-renkli olay noktaları), gün ajandası, dışa aktar, uyarı notu; **çift yüzen düğme ("Süre hesapla" + "Ekle") gerçek viewport'ta ölçümle doğrulandı: birbiriyle/dışa-aktar/uyarı ile çakışma yok** (Süre hesapla 706–760, Ekle 768–820, 8px boşluk). (f) **Ayarlar** — profil, yazı boyutu, hatırlatma ofsetleri, bildirim kanalları (YAPILANDIRILMADI durumları), sessiz saatler, dürüst LLM açıklaması, yedekleme, PIN, tur yeniden başlat, verileri sıfırla, sürüm altbilgisi. Tümü görsel dile ve token sistemine uygun. Sonuç: özellik sayfaları da baştan sona cilalı; ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 271 test yeşil. |
| 60 | 2026-08-08 16:24 | **Tasarım-uyum taraması sürdürüldü — gösterge panelinin kalan bölümleri (dosya sağlığı, son hareketler + altbilgi, yan menü) referansla doğrulandı; yeni hata yok (kod değişikliği yok).** Bu turda daha önce yakından bakılmayan referanslar canlı arayüzle karşılaştırıldı: (05) **Dosya sağlığı "Hazırlık durumu"** — başlık+yüzde, taraf·mahkeme alt satırı, ilerleme çubuğu (accent seviyeye göre) ve durum satırı ("… bekleniyor" + "İyi ilerliyor/Dikkat gerekiyor") referansla birebir; **yüzde `%73` biçimi** Türkçe yazımına uygun ve tahsilat kartındaki `+%12` ile tutarlı (referansın `82%` yazımı kendi içinde tutarsız — canlı uygulama doğru); çubuk rengi statüye göre eşleniyor (aynı statü→aynı renk), referansın aynı statüde farklı renkleri dekoratifti — canlı uygulama daha tutarlı. (06) **Son hareketler + güvenlik altbilgisi** — hareket satırları (tür ikonu + başlık + alt + göreli zaman) ve koyu altbilgi kartı ("Dosyalarınız güvende, odağınız sizde." + doğru local-first cümle, yasak ifade yok) referansa uygun; canlı uygulamanın tür-bazlı ikonları (klasör/onay/zil/kıvılcım) referansın tek-tip kıvılcımından daha bilgilendirici. (07) **Yan menü** — teal "J" logosu + "Juris/CALENDAR", aktif durum (teal kenar+nokta), durum kartı, Ayarlar, kullanıcı satırı referansla uyumlu; navigasyon "ÇALIŞMA ALANI" + "YÜRÜTME" olarak mantıklı biçimde genişletilmiş (erken maketten fazla özellik için). (08/09) Raporlar/Takvim "yakında" maketleriydi; artık gerçek tam özellikler onların yerini aldı (planlanan evrim). Sonuç: gösterge paneli baştan sona referansa sadık; geçen turdaki chevron düzeltmesi dışında ayıklanacak tasarım hatası bulunmadı. `npm run build` geçti, 271 test yeşil. |
| 59 | 2026-08-08 15:42 | **Tasarım hatası düzeltildi — "Yaklaşan son tarihler" satırlarındaki yersiz chevron kaldırıldı (referansa uyum).** Referans görsellerle (docs/design-reference/03) canlı arayüz yan yana denetlendi: referansta gösterge paneli liste satırlarının **hiçbirinde** sonda chevron yok (süre satırı "X gün kaldı", duruşma "saat", görev "rozet" ile bitiyor) ve süre başlıkları tam görünüyor. Canlı uygulamada ise yalnızca süre satırına bir `chevron-right` (`.row-aside` içinde) eklenmişti — bu hem referans yapısından sapıyor hem de başlık sütununu ~24px daraltıp "Bilirkişi ücreti yatırılacak" ve "Cevap dilekçesi son günü" başlıklarını erken kırpıyordu (kardeş duruşma/görev satırlarıyla da tutarsızdı). **Düzeltme:** chevron + tek çocuklu `.row-aside` sarmalayıcısı kaldırıldı; `deadline-kalan` doğrudan satıra yerleştirildi (hareket satırlarındaki mevcut desenle aynı); ölü `.row-aside` CSS kuralı temizlendi. Ölçümle doğrulandı (390px): başlık sütunu 153→177px; "Bilirkişi…" artık tam sığıyor; en uzun örnek başlık ("Cevap dilekçesi son günü", 182px) referansın çekildiği geniş viewport'ta (≥414px) tam sığıyor, 390/393px'te yalnızca 2–5px kırpılıyor (alt satırların zaten tasarım gereği yaptığı zarif kırpma) — referansın rahat 12px boşluklarını bir tohum-örneği için daraltmak yeni bir sapma olacağından boşluklara dokunulmadı. `.row-chevron` başka sayfalarda (Takvim, Müvekkiller, Asistan) kullanıldığından korundu. `npm run build` geçti, 271 test yeşil. |
| 58 | 2026-08-08 14:26 | **Merge sonrası derleme hijyeni doğrulandı — paket izole, dağıtım etkilenmedi (değişiklik yok).** `packages/hukuk-ai` merge'inden sonra kontrol: kök `tsconfig.json` yalnızca `tsconfig.app.json`/`tsconfig.node.json`'a referans veriyor — **paketi build etmiyor**; paket bağımsız (kendi tsconfig'i; testleri vitest globbing ile ayrı koşuyor). `npm run build` sonrası: git temiz (stray artifact yok), `packages/hukuk-ai` altında emitlenmiş `*.js`/`*.d.ts` yok, `dist/` yalnızca app içeriyor (hukuk-ai sızıntısı yok). Yani monorepo merge'i temiz izole; app'in Pages dağıtımı eskisi gibi. Kod değişikliği gerekmedi. 271 test yeşil. |
| 57 | 2026-08-08 13:27 | **Gizlilik/iddia metni denetimi — yasak ifade yok, kalan iddialar doğru (değişiklik yok).** Geçen turki tur maskeleme iddiasını düzelttikten sonra tüm arayüz metni tarandı. Sonuç: **yasak ifadeler ("hiçbir veri paylaşılmıyor"/"hacklenemez"/"%100 güvenli") YOK.** "Müvekkil verisi cihazdan dışarı çıkmaz" (dashboard güvenlik kartı, Ayarlar KVKK notu) ve Asistan hero'daki "hiçbir bilgi dışarı çıkmaz" iddiaları — tur'daki gibi **sahte bir mekanizma** iddiası değil; local-first mimarinin doğru ifadesi: app'in sunucusu/analitiği yok, kendiliğinden veri göndermiyor; veri yalnızca kullanıcının açık eylemiyle çıkıyor (şifreli yedek ya da opt-in AI → kullanıcının kendi uç noktası), her biri ayrıca doğru şekilde açıklanıyor (Ayarlar: "yalnızca şifreli çıkar", "ham müvekkil kaydı gitmez"). Kural motoru gerçekten ağ çağrısı yapmıyor → "hiçbir bilgi dışarı çıkmaz" o bağlamda doğru. Sahte iddia kalmadığından ve kullanıcının bilinçli değer-önerisi metnini gereksiz yere değiştirmek aşırıya kaçacağından **kod değişikliği yapılmadı**. `npm run build` geçti, 271 test yeşil. |
| 56 | 2026-08-08 12:29 | **Tasarım/gizlilik hatası düzeltildi — turdaki maskeleme iddiası gerçek davranışla uyuşmuyordu.** Tespit: tur "AI Asistan" durağı "Gönderdiğinizde kimlik bilgileri maskelenir; sağlayıcıya yalnızca maskeli metin gider" diyordu; ancak `llmSor` dosya özetini (`dosyaOzeti` müvekkil adını ve dava başlığını içeriyor) **maskesiz** gönderiyor — `hukukAi.maskele` app'te hiç çağrılmıyor (maskeleme motoru `packages/hukuk-ai` merge edildi ama henüz app'e bağlanmadı; paketin `package.json`'ı yok = tüketime hazır değil, ayrı session'ın alanı). Hukuk uygulamasında yanlış gizlilik iddiası CLAUDE.md kuralına aykırı. **Varsayım:** paketi prematüre bağlamak yerine tur metnini gerçek davranışa göre dürüstleştirdim (Ayarlar/altbilgi ile tutarlı: "cihaz-öncelikli; yapay zekâ açıkken yalnızca soru+kısa özet kendi uç noktana gider, ham müvekkil kaydı çıkmaz"). Kod yorumu + test güncellendi (`turAdimlari.test.ts` artık gerçek gizlilik davranışını doğruluyor). **hukuk-ai app'e bağlandığında maskeleme cümlesi geri eklenecek** (o zaman gerçek olacak). `npm run build` geçti, 271 test yeşil. |
| 55 | 2026-08-08 11:28 | **`packages/hukuk-ai` paketi dala merge edildi (PR #1, başka session) — entegre derleme/testler doğrulandı.** Maskeleme/NER/araştırma motoru (TCKN/IBAN/e-posta/telefon/plaka/esas-no/tarih kuralları, NER sözlüğü, Türkçe ek çözümleme, research client/guard + testleri) `8f3c51c` ile geldi. Bu klasöre dokunulmadı (F21'de kurulan sözleşmenin gerçek uygulaması, ayrı session). Kendi doküman commit'im üstüne rebase edildi; **entegre `npm run build` geçiyor, testler yeşil: 271/271 (26 dosya)** — eskiden 141. App tarafı hâlâ `src/services/hukukAi.ts` sözleşmesini kullanıyor (paketi app'e bağlama işi ayrı bir adım). |
| 54 | 2026-08-08 11:26 | **QA — işletim sistemi karanlık modu (yeni kontrol), hata yok.** Açık-tema-only uygulamalarda sık görülen bir kusur: sayfa `color-scheme` bildirmezse tarayıcı, OS karanlık modunda yerel kontrolleri (tarih seçici, select, input) koyu render eder → okunmaz olur. Kontrol: `index.html`'de `<meta name="color-scheme" content="light">` **var**. Playwright ile `colorScheme:'dark'` emülasyonunda finans formu doğrulandı: gövde/inputlar açık zeminde koyu okunur metin (bodyBg #f7f6f3, text #1c2b2d), yerel tarih seçicileri açık stille, chip/select/textarea temiz — karanlık-mod kırılması yok. Kod değişikliği gerekmedi. |
| 53 | 2026-08-08 10:26 | **Sağlık taraması — çevrimdışı (PWA) sözü uçtan uca doğrulandı; hata yok.** `npm audit`: **0 açık**. Derleme sağlıklı: 497 kB JS / 145 kB gzip, 24 precache girişi. **Çevrimdışı testi** (uygulamanın çekirdek vaadi "internetsiz çalışır"): Playwright ile service worker kayıtlı + kontrol ediyor (1 cache); ağ kapatılıp **reload edildiğinde uygulama tam yükleniyor** (Takvim + olaylar + çevrimdışı şeridi), çevrimdışı rota gezinmesi (/takvim) çalışıyor — tümü SW cache + IndexedDB'den, ağ yok. Kod değişikliği gerekmedi. |
| 52 | 2026-08-08 09:28 | **QA — DevirTeslim (son kalan akış) doğrulandı; tüm akışlar kapsandı (kod değişikliği yok).** DevirTeslim penceresi E2E tetiklemesi LLM yapılandırması (etkin+uç nokta+anahtar) + `juris-masaustu` bayrağı gerektiriyor; otomasyonda onBlur kaydı oynak olduğu için tam tetiklenemedi, ancak bileşen kod incelemesiyle doğrulandı: düzgün modal (`role=dialog`, aria-modal, dışına tıkla-kapat), her zaman "Telefonda yap" (varsayılan vurgulu — telefon-öncelikli, şartnameye uygun), yalnızca masaüstü erişilebilirse "Bilgisayara aktar", dürüst not; token tabanlı sınıflar, taşma riski yok. Tetik mantığı zaten birim testli (`devir.test.ts`). Gerçek transfer altyapısı zaten `[!]`. Böylece uygulamanın **tüm akışları** QA'landı (E2E ya da kod+birim-test). Spekülatif değişiklik yapılmadı. |
| 51 | 2026-08-08 08:27 | **QA — kalan akışlar toplu doğrulandı, hata yok (kod değişikliği yok).** (1) AI Asistan: "Gündem" öncelikli bulgu listesi + "Dosyaya danışın" (dosya seç → hızlı soru → kural motoru cevabı "…öne çıkan bir eksik işlem görünmüyor") + gizlilik/uyarı altbilgisi ("LLM isteğe bağlı, varsayılan kapalı; açıkken yalnızca soru+dosya özeti gider"). (2) Bildirim erteleme: "1 saat" → "ertelendi" rozeti, "geri al" geri alıyor. (3) Olay formunda tekrar (Tekrar) kontrolü var, taşma yok. (4) ICS dışa aktarma: `juriscalendar-takvim-2026-08-08.ics` iniyor. Skip link `/asistan`'da gerçek görünümde gizli (y=-62) doğrulandı — fullPage ekran görüntülerindeki görünürlük sabit-öğe kırpma yanılsaması. Konsol hatası yok. Böylece tüm çekirdek akışlar QA'landı; tek istisna DevirTeslim (masaüstü bayrağı + LLM uzun-görev gerektirir; gerçek transfer zaten [!]). Spekülatif değişiklik yapılmadı. |
| 50 | 2026-08-08 07:26 | **QA — Süre hesapla (çekirdek özellik) uçtan uca doğrulandı, hata yok (kod değişikliği yok).** İstinaf başvuru süresi (HMK m. 345, 2 hafta) + tebliğ 20 Temmuz 2026 senaryosu: ham son gün 3 Ağustos, adli tatile denk geldiği için **7 Eylül 2026'ya uzatıldı (HMK m. 104)** — hesap hukuken doğru. Ekran: hesaplanan son gün + kalan gün + süre/kanun + ham son gün + kaydırma gerekçesi ("Süre adli tatile denk geldi…") + dosyaya kaydetme + **zorunlu uyarı** ("Bu hesap bilgilendirme amaçlıdır ve hukuki tavsiye niteliği taşımaz…"). CLAUDE.md kuralı (her hesap çıktısında uyarı görünür) karşılanıyor. Taşma/konsol hatası yok, düzen temiz. Spekülatif değişiklik yapılmadı. |
| 49 | 2026-08-08 06:29 | **QA — daha önce görülmeyen akışlar denetlendi, hata bulunmadı (kod değişikliği yok).** (1) Belge önizleme penceresi (`BelgeOnizleme`): gerçek görsel (320×200 PNG) gömülü açılıyor, önizlenemeyen tür (.txt) klasör ikonu + "Bu tür için gömülü önizleme yok" + boyut + "Cihaza indir" gösteriyor — her iki durum da temiz, scrim + Esc/kapat çalışıyor, taşma/konsol hatası yok. (2) Form doğrulaması: boş dosya formunda kayıt engelleniyor ve net kırmızı hata ("Dosya başlığı girilmeli."); boş müvekkil formu da kaydetmiyor — sessiz hatalı kayıt yok. (3) Skip link regresyon kontrolü: normal gezinmede gizli (y=-62, odaksız), yalnızca klavye odağında beliriyor — doğru. Sonuç: bu alanlar sağlam; spekülatif değişiklik yapılmadı. |
| 48 | 2026-08-08 05:34 | **Tasarım hatası düzeltildi — sayı rakam ortasından bölünmesin (finans doğruluğu).** Büyük tutar testinde bir önceki turdaki `overflow-wrap:anywhere` (body) yan etkisi ortaya çıktı: finans özet kutusundaki büyük tutar (`.money-value`, nowrap değil) rakam ortasından sarıyordu ("₺100.000.02 / 3.899,99") — finans uygulamasında yanlış okunmaya yol açan ciddi bir kusur. Düzeltme (`base.css`): sayı gösterimlerine (`.stat-value`, `.money-value`, `.money-amount`, `.t-mono`) `overflow-wrap: normal; word-break: keep-all` — rakamlar hep bütün kalır; e-posta/adres gibi düz metin yine body `anywhere` ile sarar. Doğrulandı: ₺100.000.023.899,99 tek satırda, hücreyi/sayfayı taşırmıyor; gerçekçi tutarlar (₺184.500 vb.) aynen tek satır; sayfa taşması yok. `npm run build` geçti, 141 test yeşil. |
| 47 | 2026-08-08 04:28 | **Tasarım hatası düzeltildi — uzun/boşluksuz metin taşması.** Uzun boşluksuz token (uzun e-posta/adres/URL) testinde müvekkil detay sayfası ciddi taşıyordu (967>390): ad alt satırı, E-POSTA ve ADRES değerleri sarmadan kabın dışına çıkıyordu. Kök neden: hiçbir yerde `overflow-wrap` güvenlik ağı yoktu. Düzeltme (`base.css` body): `overflow-wrap: anywhere` — uzun metin gerektiğinde kelime içinde sarar, kabı taşırmaz; `.truncate` (nowrap) alanları etkilenmez, normal metinde tetiklenmez. Doğrulandı: uzun token'lı ad/e-posta/adres/not artık kart içinde sarıyor, taşma yok (390px). Gerçek müvekkil verisi (uzun kurumsal e-posta/adres) için önemli sağlamlık. `npm run build` geçti, 141 test yeşil. |
| 46 | 2026-08-08 03:31 | **Tasarım hatası düzeltildi — dar ekran (320px) taşması.** Uç viewport denetimi (320px ve 768px) yapıldı; tek gerçek hata: finans kaydı formunda "İşlem tarihi / Vade" ikili `.field-row`'u 320px'te yatay taşıyordu (369>320) — yerel `<input type=date>` içsel asgari genişliği ızgara hücresinden geniş. Düzeltme (`form.css`): (1) `.input/.select/.textarea`'ya `min-width:0` (ızgara/esnek hücrede küçülebilsinler), (2) `@media (max-width:360px)` altında `.field-row` tek sütuna iner. Normal telefonlarda (390px+) düzen değişmiyor; yalnızca çok dar ekranlarda ikili alanlar alt alta geçiyor. Doğrulandı: 320 ve 360px'te tüm rotalar taşmasız; 768px zaten temizdi. `npm run build` geçti, 141 test yeşil. |
| 45 | 2026-08-08 01:40 | **QA taraması — tasarım hatası bulunamadı (kod değişikliği yok).** Plan'da açık `[ ]` madde kalmadığından (yalnızca `[!]` BEKLEYEN), daha önce yakından bakılmayan alanlar denetlendi: (1) dosya detayı sekme şeridi — `overflow-x:auto` kayan şerit, 7 sekmenin hepsi (Genel/Duruşmalar/Süreler/Görevler/Belgeler/Finans/Notlar) tıklanabilir, kırpma hatası değil; (2) uzun metin baskısı — çok uzun görev başlığı eklendi, yatay taşma yok, truncation çalışıyor; (3) kilit ekranı (ilk kez görsel denetim) — koyu inverse zemin, teal "J", serif başlık, 4 PIN noktası, numerik tuş takımı; PIN kur→yeniden başlat→kilit→1234 ile açılış uçtan uca çalışıyor. Konsol hatası yok, görsel dile uygun. Sonuç: uygulama tamam ve cilalı; spekülatif değişiklik yapılmadı (regresyon riskine karşı). Kalan tek iş `[!]` BEKLEYEN (OAuth, hukuki metin, cihaz senkronu, sürüm-yenile düğmesi kaldırma, App Store) — kullanıcı adımları gerektirir. |
| 44 | 2026-08-08 01:33 | **F23 son madde — 44pt dokunma hedefleri (görsel dil korunarak).** Önce Playwright ile tüm ekranlarda 44px altı kontroller ölçüldü; 4'ü WCAG 2.5.8 AA asgarisinin (24px) bile altındaydı (görev onay kutusu 21px, snooze/section/safety bağlantıları 20-23px). Çözüm: `styles/dokunmatik.css` — görünür boyutu DEĞİŞTİRMEDEN görünmez `::after` dokunma alanıyla hedefi 44px'e çıkarma. Çakışmayı önleyen kural: geniş/satır kontrolleri yalnızca dikey (44px), yalıtık kare ikon düğmeleri iki eksen (44×44), görev onay kutusu sola+dikey (sağdaki `row-main` bağlantısına taşımadan). Liste satırları/başlık bağlantıları sınır-çakışması riskiyle kapsam dışı (zaten AA). Empirik doğrulama: görünür boyutlar aynı (38×38/40×40 vb.), uzatılmış alandan tıklama tetikliyor, onay kutusu↔satır bağlantısı ve takvim ileri/geri **doğru** yönleniyor (Ağustos→Eylül→Ağustos), ekran görüntülerinde regresyon yok. Bununla F23'ün tüm maddeleri tamam. `npm run build` geçti, 141 test yeşil. |
| 43 | 2026-08-08 00:29 | **F16 kapanışı — sürüm etiketi + son elden geçirme; F16 erişilebilirlik [x].** `package.json` sürümü `vite.config.ts`'te `__APP_VERSION__` define'ıyla gömüldü (env.d.ts bildirimi eklendi); Ayarlar altbilgisi artık "JurisCalendar · sürüm 0.1.0 (84257d4) · veriler cihazınızda saklanır." gösteriyor. Son elden geçirme taraması: 20+ ekran (arama sonuçları, müvekkil detayı, raporlar Son-12-ay dahil) tarayıcıda kontrol edildi — **yatay taşma yok, konsol hatası yok**, kontrast AA ve aria/odak tam. F16'nın erişilebilirlik maddesi F23'e dayanarak [x] işaretlendi. Kalan ürün-öncesi temizlik (geçici sürüm-yenile düğmesi, OAuth, hukuki metin) `[!]` BEKLEYEN'de kalıyor. `npm run build` geçti, 141 test yeşil. |
| 42 | 2026-08-07 23:29 | **F23 erişilebilirlik — aria/odak/ekran okuyucu geçişi tamam.** Playwright ile 15 ekran + drawer'da tüm etkileşimli öğeler (button/a/input/select/textarea) erişilebilir ad açısından denetlendi: **adsız öğe yok** — zaten güçlü kapsama (Icon `title`, form label'ları, `aria-current`, `aria-pressed`, dekoratif ikonlarda `aria-hidden`, drawer/tur/kilit `role="dialog"`, odak halkası `:focus-visible`). Kalan boşluklar kapatıldı: (1) **"İçeriğe geç" skip link** — klavye/switch kullanıcıları üst çubuğu atlayıp içeriğe geçer; hash router'ı bozmamak için `preventDefault` + `<main id="ana-icerik" tabindex=-1>`e elle odak; odaklanınca marka-ink pill olarak beliriyor; (2) Ayarlar durum mesajı `role="status" aria-live="polite"` (işlem sonucu ekran okuyucuya duyurulur); (3) zil düğmesine sayı içeren ad ("Bildirimler, N aktif hatırlatma") — görsel noktanın metin karşılığı. Tarayıcıda doğrulandı: ilk Tab→skip link, Enter→içerik odağı, hash değişmiyor; zil adı "…12 aktif hatırlatma". 44pt dokunma hedefi maddesi bilinçli açık bırakıldı (WCAG AA/24px zaten karşılanıyor; 44px yoğunluğu değiştirir). `npm run build` geçti, 141 test yeşil. |
| 41 | 2026-08-07 22:28 | **F23 kontrast (WCAG AA) — tasarım hatası düzeltildi.** Python ile kontrast denetimi yapıldı: `--text-muted` #9aa0a0 kırık beyaz üzerinde yalnızca **2.46:1** (AA'nın çok altında), `--text-secondary` #6b7472 **4.45:1** (sınırda). İkisi de aynı gri-yeşil aileyi koruyacak şekilde koyulaştırıldı: secondary→**#5b6462** (5.64), muted→**#6b7170** (4.60) — ikisi de sayfa/kart/sunken üzerinde AA. Tarayıcıda görsel doğrulama: eyebrow'lar ve alt metinler artık rahat okunuyor ama hâlâ sade, serif başlıklarla yarışmıyor; editoryal görsel dil korundu. Marka (teal) ve kategori rozet renkleri kimliği bozmamak için **bilinçli** bırakıldı (günlüğe yazıldı; kalan F23 maddesi: 44pt dokunma hedefleri + kapsamlı aria). `tokens.css` tek dosya değişti (bileşenler token kullandığı için tüm ekranlara yayıldı). `npm run build` geçti, 141 test yeşil. |
| 40 | 2026-08-07 21:41 | **Tur tepki düzeltmesi (kullanıcı: "laglı hareket ediyor, eski hâli daha güzeldi").** Bir önceki turda eklediğim `hazir` durumu (geçişte balonu gizleyip hedef ölçülünce göstermek) sıçramayı çözmüş ama **gecikme/lag hissi** yaratmıştı — "İleri"ye basınca kısa bir boşlukta hiçbir şey görünmüyordu. `hazir` kaldırıldı; eski sürümün tepkiselliği geri getirildi ama iki gerçek hata (ortada-balon sıçraması + çentik örtüşmesi) korunarak düzeltildi: balon HER ZAMAN görünür ve "İleri"de metin ANINDA değişir; `kutu` null'lanmadığı için balon önceki konumda kalıp yeni hedefe **kısa/snappy 140 ms** kayışla oturur (ortaya kaçış yok); güvenli-alan kırpması duruyor. `Tur.tsx` sadeleştirildi, `tur.css` geçişleri `--duration-fast`e çekildi. Tarayıcıda ölçüldü: 8 durakta `instantTextChange=true` (metin 25 ms içinde değişiyor), balon hiç gizlenmiyor, hepsi ekran içinde, gerçek paneller spotlight; bitişte kapanıyor. `npm run build` geçti, 141 test yeşil. |
| 39 | 2026-08-07 21:33 | **Tasarım denetimi + F23 yazı boyutu erişilebilirliği.** Önce tüm ekranların (20 rota + dosya detayı, formlar, 404) tarayıcı denetimi yapıldı (Playwright, 390×844): yatay taşma yok, konsol hatası yok. Fark edilen "Takvim FAB üst üste biniyor" görüntüsü **fullPage ekran görüntüsü yanılsamasıymış** (gerçek viewport'ta `fab-spacer` ile temiz duruyor) — kod değişikliği gerekmedi. **F23 kazanımı:** İleri yaş kitle için Ayarlar → **Yazı boyutu** (Normal/Büyük/Çok büyük) eklendi; kök `font-size` %100/112.5/125, tüm ölçüler `rem` olduğundan arayüz orantılı büyüyor, `%` taban sistem büyütmesini koruyor, ayar kalıcı (`domain/yaziOlcegi.ts` saf eşleme + `App.tsx`'te köke uygulama + `types.ts` `yaziOlcegi` alanı). Varsayılan görsel dil değişmedi (kural gereği). **4 yeni test** (`yaziOlcegi.test.ts`, toplam 141). Tarayıcıda doğrulandı: 16→20px, "Çok büyük"te düzen bozulmadan büyüyor, reload'da kalıcı, Normal'e dönüyor. Ayrıca F23'te reduced-motion [x] (token'lar + tur animasyonları saygılı) ve ses/titreşim maddesi konu dışı işaretlendi (sesler slaytla kaldırıldı). Kontrast/44pt dokunma/aria tam denetimi paleti değiştireceği için kullanıcı onayına bırakıldı (günlüğe yazıldı). `npm run build` geçti. |
| 38 | 2026-08-07 20:59 | **Tur — cihaz videosu geri bildirimi düzeltildi ("çok uzak ve hata dolu").** Kullanıcı iki ekran kaydı gönderdi; ffmpeg ile kareler incelendi. Üç somut hata + bir içerik boşluğu bulundu ve giderildi: (1) **Sıçrama:** rota/menü değişiminde balon önce ORTADA beliriyor sonra hedefe zıplıyordu → `hazir` durumu eklendi; geçiş sürerken yalnızca karartma görünür, balon+halka hedef ölçülünce BİRLİKTE belirir. Tarayıcıda 8 geçişin hepsinde sıçrama yok (mid-transition örneklemesi: centered=0). (2) **Çentik/durum çubuğu örtüşmesi:** aşağıdaki hedeflerde (ör. Asistan "Dosyaya danışın") balon üste taşınıp saatin arkasına kaçıyordu → `env(safe-area-inset-top/bottom)` piksel olarak ölçülüp balon güvenli banda kırpılıyor, ayrıca balon yüksekliği `useLayoutEffect` ile ölçülüp ekran içinde tutuluyor. (3) **Akıcılık:** yumuşak beliriş (`tur-belir`) + halka/balon konum geçiş animasyonu; `prefers-reduced-motion`'da kapalı; ortalanmış balon `translate(-50%,-50%)`'i bozmayan ayrı keyframe. (4) **İçerik "çok uzak":** genel özellik anlatımı yerine **senaryo temelli** metin — "yeni iş → menüden Asistan'a → gerçek panelde dilekçe/maskeleme → dosya → takvim → süre". `Tur.tsx` yeniden yazıldı, `turAdimlari.ts` metinleri + `tur.css` güncellendi. `npm run build` geçti, 137 test yeşil. |
| 37 | 2026-08-07 18:30 | **F16 — birim testleri (hatırlatma ofsetleri) tamam.** Süre motoru ve hazırlık skoru zaten test ediliyordu (`sureHesabi.test.ts`, `hazirlik.test.ts`); eksik olan hatırlatma ofset türetmesiydi. O mantık `data/hatirlatmaSorgulari.ts` içinde Dexie `useLiveQuery` kapanışlarına gömülü olduğu için DB/DOM'suz test edilemiyordu. Saf çekirdek `domain/hatirlatma.ts`'e çıkarıldı: `ofsetMetni`, `tetikAni`, `pencere`, `pencereDe`, `gectiMi`, `etkinTetik` (erteleme çözümü), `ertelemeAktif`, `aktifMi`. İki kanca (yaklaşan liste + aktif sayaç) artık bu saf yardımcıları çağırıyor — **davranış birebir korundu**, iki asimetrik eşik (yaklaşan liste alt sınırı DÂHİL / aktif sayaç HARİÇ) yorumla sabitlendi. **17 yeni test** (`hatirlatma.test.ts`; toplam 137): ofset etiketleri, tetik aritmetiği, pencere/aktif eşik farkı, erteleme (ileri taşır / süresi geçen yok sayılır), erteleme aktifliği. Tarayıcıda regresyon kontrolü: Bildirimler sayfası "Şimdi"/"Yaklaşan" grupları + erteleme düğmeleri + ofset etiketleriyle önceki gibi doluyor, üst çubuk zili noktası çıkıyor. `npm run build` geçti. |
| 36 | 2026-08-07 18:25 | **F16 — çevrimdışı davranış + hata hâlleri tamam.** İki gerçek boşluk kapatıldı (boş/yükleniyor hâlleri zaten 18 sayfada `section-empty`/`SatirIskeleti` ile vardı). (1) **Çevrimdışı göstergesi:** `data/cevrimici.ts` (`useCevrimici` — `navigator.onLine` + online/offline olayları) ve `components/CevrimdisiUyari.tsx` — içerik akışının başında (AppShell), yalnızca çevrimdışıyken görünen sakin amber bilgi şeridi (`cloud-off` ikonu). Local-first olduğu için ton alarm değil bilgi: "Çevrimdışısınız. Uygulama cihazınızda çalışmaya devam ediyor; verileriniz kaydedilir." (yasak ifade yok). (2) **Hata sınırı:** `components/HataSiniri.tsx` (sınıf bileşeni, `getDerivedStateFromError`+`componentDidCatch`) — bir bileşen çökerse beyaz ekran yerine kurtarma kartı ("Bir şeyler ters gitti" + "Ana sayfaya dön" hash'i köke alıp sıfırlar / "Yeniden dene" yerinde tekrar); hata `gunlukHata` ile **redakteli** günlüklenir. `main.tsx`'te `<App/>` sarıldı (yakala-hepsini). Yeni ikon `cloud-off`, yeni stil `styles/durum.css`. Tarayıcıda doğrulandı: Playwright `setOffline(true)`→şerit çıkıyor + doğru metin, tekrar çevrimiçi→kayboluyor; hata kartı gerçek CSS ile görsel doğrulandı (tasarım diline uygun). `npm run build` geçti, 120 test yeşil. Varsayım: çevrimdışı UI davranışı testi için DOM/testing-library kurulu olmadığından (node ortamı) birim testi yerine tarayıcı doğrulaması yapıldı — bu F16'daki ayrı "birim testleri" maddesiyle karışmaz. |
| 35 | 2026-08-07 17:42 | **F17 yeniden yazıldı — animasyonlu slayt yerine gerçek uygulama içi rehberli tur (kullanıcı isteği).** Kullanıcı sahte ekranlı tanıtım slaytını istemedi; "gerçekten uygulamanın içinde tutorial… dilekçe kısmını menüden AI asistana tıklayıp sen götürüp gerçek panelden göstereceksin." Slayt gösterisi silindi (`onboarding/Onboarding.tsx`, `script.ts`, `script.test.ts`, `efektler.ts`) — `HukukiOnay`/`KayitEkrani`/`hukukiMetin.ts` ve `onboarding.css` kaldı. Yeni `src/tur/`: `Tur.tsx` (SVG maske spotlight + halka + konumlanan balon; RouterProvider altında, çalışan uygulamanın üzerinde katman; `useNavigate` ile gerçek rotalara kendi gidiyor; `juris-drawer` CustomEvent'iyle yan menüyü açıp Asistan'a geçiyor; ~320ms geçiş boyunca yeniden ölçüp spotlight'ı kayarken takip ediyor) ve `turAdimlari.ts` (8 durak veri). Duraklar (kullanıcı seçimi): Genel bakış+menü → menüden Asistan'a geçiş (drawer açık, "Asistan" öğesi spotlight) → gerçek `/asistan` panelinde "Dosyaya danışın" + maskeleme açıklaması ("kimlik bilgileri maskelenir; sağlayıcıya yalnızca maskeli metin gider") → `/dosyalar` FAB → `/takvim` → `/sure` → kapanış. Gate sırası artık onay → kayıt → app+tur; `ayarlar.turGoruldu` kalıcı bayrak, Ayarlar → Yardım → "Uygulama turunu yeniden başlat". Yakalanan hata: router `Link` yalnızca belirli prop'ları geçirdiği için FAB'daki `data-tur` düşüyordu → hedef `.fab` sınıfına çevrildi. **8 yeni test** (`turAdimlari.test.ts`, toplam 120). Playwright ile uçtan uca doğrulandı: onay→kayıt→tur 8 durakta gerçek panellerde spotlight (fab dâhil), Bitir'de kapanıyor, reload'da açılmıyor; konsol temiz. `npm run build` geçti. |
| 0 | 2026-08-03 22:45 | Video çözümlendi, görsel referans + yol haritası repoya işlendi. 3 saatlik bekleme kuruldu. |
| 1 | 2026-08-04 02:30 | **F0 tamam.** Vite+React+TS iskeleti, token'lar, gömülü fontlar, ikon seti, kabuk (üst çubuk + drawer), 13 yol, PWA (manifest/SW/ikonlar), Pages iş akışı. Varsayım: react-router yerine kendi hash router'ımız — kalan iki yüksek zafiyet yalnızca RSC modunda ve düzeltmesi yok, bizim kullanmadığımız kod yolu. `npm audit` sıfır. Derleme 157 kB JS (50 kB gzip). |
| 34 | 2026-08-05 08:20 | **F17 düzeltme (kullanıcı bildirimi).** S6 maskeleme onay ekranında iki hata giderildi: (1) maskeler siyah blok yerine artık **etiket** gösteriyor (MÜVEKKİL/TCKN/ADRES/KARŞI TARAF/İŞYERİ/IBAN/DOSYA NO) — `Parca` ham blok yerine `parca.etiket`, `maskeliMetin` `[ETİKET]` biçimi, `.ob-maske` koyu etiket çipi; (2) "Orijinali Göster" artık çalışıyor — basılı-tut yerine spec'teki gibi **dokun → 1 sn gerçek metni göster → geri dön** (`orijinaliGoster` + timer, `pm` adım 'b'de `!orijinal`). Tarayıcıda doğrulandı: 7 etiket görünüyor; "Orijinali Göster"→"Kemal Arslan" 1 sn açılıp maskeye dönüyor; konsol temiz. 117 test yeşil. |
| 33 | 2026-08-05 08:05 | **F22 — Gizli günlükleme (redacting logger) tamam.** `services/gunluk.ts`: `redakteEt` TCKN(11)/IBAN(TR+24)/e-posta/GSM desenlerini maskeler; `gunlukle`/`gunlukHata` konsola **redaksiyondan sonra** yazar (tek konsol kullanan yer burası). `main.tsx`'teki tek `console.error` logger'a taşındı. Proje ESLint kullanmadığından "doğrudan console yasak" kuralı, `import.meta.glob` ile tüm kaynağı tarayıp `gunluk.ts` dışında `console.` arayan bir **test kapısıyla** uygulandı (varsayım: lint yerine test — günlüğe yazıldı). **4 yeni test** (toplam 117): desen redaksiyonu, müvekkil verili hatanın maskeli çıkışı, ek bağlam redaksiyonu, console-kapısı. Dürüst kapsam: serbest metin isim/adres pattern'le yakalanamaz; tasarım gereği logger'a ham müvekkil kaydı/isim geçirilmez (mesaj geliştirici metnidir), yapısal kimlikler (TCKN/IBAN/e-posta/GSM) maskelenir. Derleme geçti. |
| 32 | 2026-08-05 07:50 | **F21 — hukuk-ai arayüz sözleşmesi tamam.** `src/services/hukukAi.ts` (yalnızca TİP): `HukukAi` arayüzü — `maskele(metin)→{maskeliMetin, eslesme}`, `demaskele(maskeliMetin, eslesme)`, `sor(maskeliSoru, secenekler?)`, `hazirMi()`; `MaskeEslesmesi`/`MaskeSonucu`/`MaskeParcasi`/`MaskeTuru`/`AiSecenekleri` tipleri; gizlilik sözleşmesi (eşleme cihazda kalır, AI'a yalnızca maskeli metin gider). `bagliDegilHukukAi` varsayılanı paket yokken derlenmeyi/çalışmayı sağlıyor (maskele passthrough, hazirMi false, sor açık hata). `packages/hukuk-ai/` klasörüne **dokunulmadı** (başka session). **3 yeni test** (toplam 113): sahte uygulama sözleşmeye uyuyor, maskele/demaskele tutarlı, bağlı-değil davranışı. Derleme geçti. |
| 31 | 2026-08-05 07:35 | **F20 — Cihazlar arası iş bölümü (devir teslim) tamam.** `services/devir.ts` adaptörü: `masaustuErisilebilir()` varsayılan **false** (gerçek algılama/eşleme sunucu ister → [!]; test için `localStorage['juris-masaustu']='1'`), oturum içi "telefonda yap" tercihi (bellekte), gerçek süre ölçümü (`sureOlc`/`sureTahminiMs` — `performance.now`, ölçüm yoksa null = uydurma yok), `devirSorulmali`. `components/DevirTeslim.tsx`: "Telefonda yap" her zaman + varsayılan vurgulu; "Bilgisayara aktar" yalnızca masaüstü erişilebilirse; ölçülü süre etiketi. Asistan'ın "Yapay zekâya sor" (uzun-dilekçe işi) akışına bağlandı: masaüstü yoksa devir sorulmadan çalışır (sessizce telefon), varsa pencere çıkar; llmSor süresi ölçülüyor. **7 yeni test** (toplam 110): erişilebilirlik varsayılanı, oturum dedup, ölçümden tahmin/uydurma-yok. Tarayıcıda uçtan uca doğrulandı: masaüstü yok→pencere yok; bayrak açık→pencere (Telefonda yap + Bilgisayara aktar); "Telefonda yap"→kapanır+AI çalışır; ikinci soruda tercih hatırlandı→pencere yok. Karar: local-first + sunucusuz olduğu için gerçek masaüstü algılama/transfer [!]; doğru varsayılan davranış (sessizce telefon) kondu. |
| 30 | 2026-08-05 07:05 | **F19 — Kayıt/giriş ve profil tamam.** `onboarding/KayitEkrani.tsx`: onay sonrası profil adımı — E-posta ile devam (yerel profil: ad, soyad, ünvan, opsiyonel e-posta; `kullaniciAdi`=ad+soyad); Google/Apple butonları görünür ama tıklanınca "henüz yapılandırılmadı" der (OAuth [!]). App gating sırası artık: onboarding → onay → **kayıt** → app; `profilKuruldu` bayrağı. Yan menü (drawer) alt profil satırı artık **dinamik** — `useAyarlar`'dan ad/ünvan + baş harflerinden avatar (eskiden "Ayşe Kaya" hardcoded'dı). Türe uygun `basHarfleri` (`toLocaleUpperCase('tr')`). Tarayıcıda uçtan uca doğrulandı: onay→kayıt çıkıyor, Google→"yapılandırılmadı", e-posta formu Mehmet Demir/Avukat kaydediyor→app, drawer "Mehmet Demir / Avukat / MD" gösteriyor, reload'da kayıt çıkmıyor; konsol temiz. 103 test yeşil. Not: OAuth ve e-posta doğrulama sunucusu BEKLEYEN'de [!]. |
| 29 | 2026-08-05 06:40 | **F18 — Hukuki onay akışı tamam.** `onboarding/hukukiMetin.ts` (sürüm + yer tutucu ToS/Aydınlatma metni — içerik [!] avukatta — + 5 onay maddesi), `data/onayIslemleri.ts` (`metinHash` SHA-256, `onayGerekli`, `onayKaydet`), `onboarding/HukukiOnay.tsx` (kaydırılabilir metin → sona gelmeden kutular pasif; 5 kutu işaretsiz; "Kabul et ve başla" yalnızca hepsi işaretliyken aktif). App.tsx gating sırası: onboarding → onay → app; onay atlanamaz. Onay kaydı zaman+sürüm+hash olarak `ayarlar.hukukiOnay`'a yazılır; sürüm değişince yeniden istenir. **Varsayım:** tek birleşik uzun metin (ToS+Aydınlatma) sona kaydırılınca 5 kutu birden açılır (her kutuya ayrı kaydırma alanı yerine) — 4 kısa madde zaten kısa taahhüt. **4 yeni test** (toplam 103): hash determinizmi/biçimi, onayGerekli mantığı. Tarayıcıda uçtan uca doğrulandı: Atla→onay çıkıyor, buton başta pasif, kutular kaydırmadan pasif, kaydırınca aktif, 5 işaret→buton aktif→kabul→app; kayıt (sürüm=…taslak-1, hash, zaman) yazıldı; reload'da onay çıkmıyor; konsol temiz. |
| 28 | 2026-08-05 06:10 | **F17 — İlk açılış öğretici modu (onboarding) tamam.** `src/onboarding/`: `script.ts` (sahne metin/zaman + demo dava + 7 maskeli parça + korunan İzmir/14 Eylül + iş bölümü verisi — hepsi veri, bileşene gömülü değil), `efektler.ts` (WebAudio "tık" + titreşim, `prefers-reduced-motion`'a saygılı), `Onboarding.tsx` (oynatıcı + tüm sahneler). Sahneler: S0 bulut→telefon, S1 kendi dolan form, S2 geri sayım halkası, S3 telefona dokun→arama→dosyaya not, S4 turuncu bildirim→CTA, S5 mesaj yazılır+dosya eklenir→Gönder, S6 maskeleme (a: parçalar tık sesiyle tek tek maskelenir + İzmir/tarih yeşil korunur; b: "7 bilgi maskelendi"+basılı-tut Orijinali Göster; c: Cihazınız|Yapay Zekâ bölünmüş ekran, hattan maskeli metin, kasa yerinde titrer; d: "Gerçek bilgilerle değiştir"→maskeler sırayla açılır→"Kimliği hiç dışarı çıkmadı"), "Neyi nerede" (maddeler telefon sütununa düşer), S7 özet. Her sahnede Atla; App.tsx `onboardingTamam` bayrağıyla gating. **Karar/varsayım:** Atla/bitiş şu an doğrudan app'e giriyor; hukuki onay (F18) sonraki turda araya eklenecek — o yüzden "onaya yönlendirme" kutusu bu koşulla işaretlendi. Demo verisi DB'ye yazılmıyor (27→27 doğrulandı). **5 yeni test** (toplam 99): yasak dil denetimi, maskeleme verisi. Tarayıcıda uçtan uca doğrulandı: onboarding açılıyor, Atla→app, kalıcılık, auto-ilerleme S0→S3, telefon dokunuşu→S4; konsol temiz. Erişilebilirlik temeli (≥44pt, reduced-motion, ses/titreşim kapatılabilir) kondu; tam F23'te. |
| 27 | 2026-08-05 05:20 | **F12 — Opsiyonel LLM katmanı tamam.** `services/llm.ts` adaptörü (bildirim.ts deseninde): `llmDurumu` kapali/yapilandirilmadi/hazir; `llmSor` OpenAI-uyumlu `/chat/completions` gövdesiyle kullanıcının kendi uç noktasına POST atıyor (OpenAI/Azure/OpenRouter/Ollama/kendi vekili). Gizlilik kararı: varsayılan **kapalı**, kural motoru varsayılan asistan; açıkken cihazdan çıkan tek şey soru + `dosyaOzeti` (ham müvekkil kaydı/TCKN/tam finans değil). Ayarlar'da açınca uç nokta + model + anahtar alanları çıkıyor, ne gönderildiği net yazıyor. Asistan'da yapılandırılmışsa "Yapay zekâya sor" düğmesi; yanıtın altında "soru ve özet {host} adresine gönderildi" notu; ağ/HTTP hatalarında nazik mesaj, kural motoruna düşüş. **5 yeni test** (toplam 94): durum kapısı, sağlayıcı adı. Tarayıcıda uçtan uca doğrulandı: kapalıyken düğme yok + kural motoru çalışıyor; yapılandırınca düğme çıkıyor; bozuk uç nokta nazik hata veriyor (uygulama çökmedi). CORS gerçeği ve tarayıcıdan doğrudan çağrı sınırı arayüzde belirtildi. |
| 26 | 2026-08-05 04:55 | **F13 — Rapor serbest tarih aralığı tamam.** `useRaporVerisi` artık `(baslangic, bitis)` alıyor; sütun grafiği aralığın dokunduğu her ay için kova üretiyor (en çok 24), aktivite sayıları (duruşma/görüşme/tamamlanan görev), gider dağılımı, dosya bakiyesi ve toplamlar aralığa göre süzülüyor. Açık süre halkası bilinçli olarak **anlık durum** (o anki açık süreler) — tarihe bağlı değil, alt yazıda belirtildi. Raporlar ekranına hazır aralık çipleri (Bu ay / Son 3 / Son 6 / Son 12 / Bu yıl / Serbest) + serbest modda başlangıç-bitiş tarih girişleri (min/max ile tutarlı) eklendi; grafik alt yazısı seçili aralığı gösteriyor. `buAy*` alanları `donem*` olarak yeniden adlandırıldı. Tarayıcıda doğrulandı: son 6 ay → 6 çubuk, son 12 ay → 12 çubuk, bu ay → 1 çubuk, serbest → 2 tarih girişi; konsol temiz. 89 test yeşil. |
| 25 | 2026-08-05 04:30 | **F11 — Hatırlatma snooze tamam.** Hatırlatmalar tabloda tutulmadığı (olay+süre × ofsetten anlık türetildiği) için erteleme durumu Ayarlar'da `hatirlatmaErtelemeleri` haritasında saklanıyor: türetilmiş hatırlatma kimliği (`olayId-ofset` / `sureId-ofset`) → yeniden gösterileceği ISO an. `useYaklasanHatirlatmalar` ve aktif sayaç, erteleme aktifse tetiklemeyi ileri ana taşıyor; süresi geçen erteleme yok sayılıp hatırlatma özgün zamanında yeniden beliriyor (harita böylece sızmıyor, `hatirlatmaErtele` de eski kayıtları temizliyor). Bildirimler ekranında aktif ("Şimdi") hatırlatmalara 1 saat / 3 saat / yarın (09:00) / 1 hafta erteleme düğmeleri; ertelenmiş olanlarda "ertelendi" rozeti + "geri al". Karar: reddedilen değil ertelenen kayıt yeniden görünür — kaçırma riskini artırmamak için erteleme kalıcı gizleme değil. Tarayıcıda uçtan uca doğrulandı: 16 aktif → biri ertelendi (harita 1, aktif 15, rozet çıktı) → geri al (harita 0, aktif 16); konsol temiz. 89 test yeşil. |
| 24 | 2026-08-05 04:05 | **F2 hızlı görev + F5 süre kataloğu genişletme tamam.** Genel bakışta "Yeni görev ekle" artık forma yönlendirmek yerine **satır içi** açılıyor: başlık yaz, Enter'la kaydet (panelden ayrılmadan), "Detaylı" ile tam forma geçiş; Esc ile kapanır. Süre kataloğu 16 → **31 kural**: replik/düplik (HMK 136), istinaf dilekçesine cevap (347), yargılamanın iadesi (377), hakem kararına iptal (439); icra şikayet (İİK 16), gecikmiş itiraz (65), kambiyo ödeme emrine itiraz (168/5 gün), ihalenin feshi (134), istirdat (72); ceza eski hâle getirme (CMK 42), koruma tedbiri tazminatı (142); idari üst makama başvuru (İYUK 11), YD kararına itiraz (27/7 gün), idari eylemden tam yargı (13). Karar düzeltme HMK ve İYUK'ta kaldırıldığından bilinçli eklenmedi (hukuki doğruluk). adliTatileTabi belirsiz/kısa usul sürelerinde güvenli tarafta (false) tutuldu. **8 yeni test** (toplam 89): yeni kuralların ham son günü elle sabitlendi, katalog sayısı ve id benzersizliği, tüm katalog×başlangıç taraması hâlâ çalışma günü veriyor. Tarayıcıda doğrulandı: panelden görev eklendi (21→22), yeni kurallar süre seçicisinde çıkıyor; konsol temiz. |
| 23 | 2026-08-05 03:35 | **F3 + F7 — Tekrar eden olay ve görevler tamam.** Yeni `domain/tekrar.ts`: saf üreteç `tekrarGunleri` (günlük / hafta-içi / haftalık / iki-haftalık / aylık / yıllık; ay-sonu taşması date-fns ile kırpılıyor, hafta-içi hafta sonlarını atlıyor, azami 60). Karar: kural saklamak yerine seri **maddeleştiriliyor** — oluşturmada her yineleme ayrı kayıt, ortak `seriesId` ile bağlı; böylece takvim/gösterge/ICS/hatırlatma okuma yolları hiç değişmiyor, tek yinelemeyi düzenlemek doğal kalıyor. DB v2: `olaylar`/`gorevler`'e `seriesId` indeksi (toplu silme için; eski tablolar devrediyor). Paylaşılan `TekrarSecici` bileşeni her iki forma eklendi (yalnızca yeni kayıtta); silmede seri üyesi için "yalnızca bu / tüm seri (N)" seçeneği. Görevde tekrar vade tarihi gerektiriyor (yineleme vadeyi taşır), vade yoksa uyarı. **8 yeni test** (toplam 87): bilinen tarihlerle günlük/haftalık/aylık/yıllık/hafta-içi ve sınır. Tarayıcıda uçtan uca doğrulandı: haftalık×4 olay tek seriesId ile oluştu, "tüm seri" hepsini sildi; aylık×3 görev 15 Eylül/Ekim/Kasım vadeleriyle oluştu, vadesiz tekrar reddedildi; konsol temiz. |
| 22 | 2026-08-05 03:05 | **F9 — Belge yönetimi tamam + son placeholder kapandı.** `/ara` artık gerçek **genel arama** ekranı (tanıtım ekranı yerine): dosya (başlık/esas no/mahkeme/karşı taraf/konu), müvekkil (ad/kimlik/telefon/e-posta/etiket), görev (başlık/açıklama) ve belge (ad/etiket/not) tek kutudan taranıyor (`aramaSorgulari.ts`, Türkçe `toLocaleLowerCase('tr')`, ≥2 harf, canlı Dexie sorgusu, gruplu sonuçlar). **Belge önizleme** (`BelgeOnizleme`): görsel/PDF/ses gömülü açılıyor, önizlenemeyen türde indirmeye yönlendiriyor; object URL kapanışta serbest bırakılıyor, Esc ile kapanıyor — hem aramada hem dosya detayı Belgeler sekmesinde. **Etiketleme:** belge satırında etiket ekle/sil (Enter ile), tekilleştirme; arama etiketlerle eşleşiyor. **Depolama göstergesi:** `navigator.storage.estimate` ile kullanılan/kota çubuğu + belgelerin toplam boyutu, %85 üstünde kırmızı uyarı. Tarayıcıda uçtan uca doğrulandı: "Demir" → dosya+müvekkil grupları; belgeye "acil" etiketi eklenip yeniden yüklemede korundu; önizleme açıl/Esc kapan; konsol temiz. Placeholder bileşeni artık hiçbir route'ta kullanılmıyor. |
| 21 | 2026-08-05 02:45 | **F16 (kısmi) — README genişletildi.** Kurulum/geliştirme komutları (dev/build/preview/test), yerel base-siz derleme vs Pages `BASE_PATH=/MYLOVE/` notu, GitHub Pages yayın adımları (Source → GitHub Actions, `main`'e push, yayın adresi), iPhone Safari ile ana ekrana ekleme adımları + simge sabitleme uyarısı, Capacitor ile App Store paketleme yolu (init/add ios/sync/Xcode Archive) ve dürüst güvenlik/gizlilik bölümü (PIN PBKDF2 özeti, Face ID/Touch ID, şifreli `.jcenc` yedek, OS disk şifrelemesi). Deploy iş akışı doğrulandı: `main` push tetikliyor, `BASE_PATH=/MYLOVE/` ayarlı. F16'da kalanlar: erişilebilirlik geçişi, boş/yükleniyor/hata hâlleri, sürüm etiketi ve geçici "sürüm yenile" düğmesinin kaldırılması — düğme kullanıcı hâlâ test ettiği için şimdilik duruyor. |
| 20 | 2026-08-05 02:40 | **F10 — Hazırlık durumu tamam.** Hazırlık motoru (`domain/hazirlik.ts`) artık **dosya türüne duyarlı**: her tür için ayrı kontrol listesi şablonu (`TUR_MADDELERI`). İcra takibinde duruşma ve gider avansı beklenmez, "mahkeme" → "İcra dairesi", "esas no" → "takip no"; ceza (kamu davası) harçsız ve gider avanssız; arabuluculukta mahkeme/esas no/harç/duruşma/gider avansı yok; tüketici davası harçtan muaf (6502 s.K. m. 73/2); idari yargı dosya üzerinden karar verdiği için duruşma beklenmez. Uygulanmayan madde yüzdeye hiç katılmaz — icra dosyası "duruşma yok" diye haksız düşmüyor. Maddeler yine tamamen veriden okunuyor (elle işaretleme yok), ağırlıklı yüzde ve en ağır eksikten "sıradaki adım" korunuyor. **8 yeni test** (toplam 79): tür bazlı madde seçimi, icra etiket düzeltmeleri, tam dosyada %100, kaçmış sürenin etkisi, icra'nın duruşmasızlıktan ceza almaması. Tarayıcıda 4 dosya detayında kontrol listesi + yüzde doğrulandı (Demir İnşaat %82, Özkan %100), konsol temiz. |
| 19 | 2026-08-05 02:35 | **F15 — Güvenlik tamam.** `services/kripto.ts`: PIN'i PBKDF2 (210k tur) ile özetleyip yalnızca özeti saklıyor (ham PIN hiçbir yerde durmaz), şifreli yedek AES-GCM+PBKDF2 ile parola türetip `.jcenc` üretiyor. `services/biyometri.ts`: WebAuthn platform doğrulayıcısıyla Face ID/Touch ID açışı — kayıt kimliği (credential id) saklanıyor, biyometrik veri cihazın güvenli bölgesinde kalıyor. `KilitKapisi`: açılışta + arka plandan seçilen süre (0/1/5/15 dk) sonra PIN/biyometri sorar, arka plandayken içerik maskelenir; PIN pad + "Face ID/Touch ID ile aç" düğmesi. Ayarlar → Güvenlik: PIN kur/değiştir/kaldır, kilitlenme süresi, biyometri etkinleştir/kapat, şifreli yedek parolası, KVKK notu. **4 yeni test** (toplam 71): PIN eşleşme/ham PIN sızmıyor/tuz benzersiz, şifrele-çöz turu, yanlış parola fırlatıyor, Türkçe karakter korunuyor, biyometri WebAuthn yoksa çökmeden false. Dürüst karar: IndexedDB düz kalıyor — her sorguda çözmek anahtarı zaten bellekte tutar, gerçek at-rest korumayı OS disk şifrelemesi (iOS'ta açık) verir; şifreleme cihaz dışına çıkan tek veriye (yedek) uygulandı. Kilit akışı tarayıcıda uçtan uca doğrulandı (PIN kur → kilitle → aç), konsol temiz. TS 5.9 `Uint8Array<ArrayBufferLike>`/`BufferSource` uyumsuzluğu `b64Coz`'da ArrayBuffer'ı doğrudan ayırarak çözüldü. |
| 18 | 2026-08-05 01:20 | **F14 — Takvim senkronu tamam.** `services/ics.ts`: RFC 5545 uyumlu iCalendar üretimi — metin kaçışı, 75 oktet satır katlama, zamanlı olaylar UTC damgayla, tüm-gün olaylar ve hukuki süreler VALUE=DATE ile. Takvimi dışa aktar ekranı: tüm takvimi .ics indir + Apple/Google/Outlook adım adım rehber + canlı abonelik (webcal) için dürüst "sunucu gerekir" notu. Olay formunda tek-olay "telefon takvimine ekle (.ics)". **8 yeni test** (toplam 61): biçim, UTC/tüm-gün ayrımı, UID benzersizliği, karakter kaçışı, iptal atlama. Tarayıcıda gerçek indirme doğrulandı (16 VEVENT). Karar: webcal canlı akış sunucu istediği için dosya dışa aktarma tercih edildi; değişiklikte yeniden indir. |
| 17 | 2026-08-04 21:20 | **F11 tamam — son placeholder kapandı.** Ayarlar ekranı: profil, olay türüne göre hatırlatma ofset profilleri (düzenlenebilir hap ızgarası), bildirim kanalları, sessiz saatler, LLM anahtarı (kapalı), JSON yedek indir/geri yükle, veri sıfırlama. `services/bildirim.ts` kanal adaptörü — SMS/e-posta sunucu gerektirdiği için açıkça "yapılandırılmadı", push için Notification API izni. Bildirimler ekranı: hatırlatmalar tabloda tutulmuyor, olay+süre×ofset profilinden **anlık türetiliyor** (senkron tutulacak ikinci kayıt yok); "Şimdi/Yaklaşan" grupları. Üst çubuk zili artık gerçek aktif hatırlatma sayısını gösteriyor. **Artık her menü hedefi gerçek — hiç tanıtım ekranı kalmadı.** Tarayıcıda doğrulandı (55 hatırlatma, ofset seçimi kaydediliyor). |
| 16 | 2026-08-04 17:25 | **F12 — Asistan tamam (kural motoru).** `domain/asistan.ts`: dosya bağlamından deterministik bulgular (açık süre aciliyeti, ödenmemiş gider, yaklaşan duruşma, geciken görev, eksik vekâletname, düşük hazırlık) + dosya özeti + niyet-çıkarımlı soru-cevap. Ekran: büro geneli öncelik sıralı "Gündem" + dosya seçip danışma. **8 yeni test** (toplam 53): şartname örnekleri birebir doğrulandı — "Bu dosyada eksik bir işlem var mı?" → "Bilirkişi ücreti henüz yatırılmadı (vade 9 Ağustos)."; istinaf 2 gün kritik bulgu. Karar: LLM değil kural motoru — örnekler veriden çıkıyor, çevrimdışı çalışır, yanlış üretmez, müvekkil verisi dışarı gitmez; LLM ileride opsiyonel/kapalı gelecek (arayüzde yazılı). Yakalanan hata: sabit "nın" iyelik eki "süresinın" gibi ünlü uyumu bozuyordu — ek kaldırılıp yeniden ifade edildi. Tarayıcıda doğrulandı. |
| 15 | 2026-08-04 13:45 | **F13 — Raporlar tamam.** Tümü elle çizilmiş SVG (tasarım kısıtı, harici grafik kütüphanesi yok): bu ay aktivite (duruşma/görüşme/tamamlanan görev), son 6 ay gelir-gider sütun grafiği, açık süreler aciliyet halkası (donut), kategori bazlı gider yatay çubukları, dosya bazlı gelir-gider listesi. CSV dışa aktarma UTF-8 BOM + noktalı virgül ayraçla (Excel Türkçe yerel ayarında düzgün açılsın). Yakalanan hata: yatay çubuk dolgusu `span` olduğu için `display:block` olmadan genişlik uygulanmıyordu — düzeltildi, oranlar doğrulandı (316/27/19/9 px). Grafikler token renklerini kullanıyor. **/loop 1h** geldi ama jenerik otonom vekil moduna geçmek yerine mevcut saatlik geliştirme cron'u (`e79b4057`) korundu — kullanıcının net isteği özellik geliştirme. Tarayıcıda doğrulandı. |
| 14 | 2026-08-04 13:30 | **F9 çekirdeği tamam.** Dosya detayı Belgeler sekmesi artık salt-okunur değil: PDF/Word/Excel/görsel/ses yükleniyor (Blob, IndexedDB, 25 MB sınır), indiriliyor, siliniyor; tür ad/MIME'den otomatik tahmin ediliyor. Ortak `belgeIslemleri.ts` (F8 dekont arşiviyle paylaşılan) kullanıldı. Tarayıcıda sahte PDF yükleme doğrulandı. Kalan F9: etiketleme + global belge arama, gömülü önizleme, depolama göstergesi — sonraki turlara. |
| 13 | 2026-08-04 13:20 | **F8 — Finans tamam.** Genel finans ekranı: büro özeti (bu ay tahsilat/gider, toplam bekleyen, 7 gün içi yaklaşan ödeme sayısı) + süzgeçli liste; tahsilat yeşil +, gider −. Form: 11 kategori (kategori seçince gelir/gider yönü otomatik), Türkçe tutar girişi (`metindenKurus`: "7.500,50"→750050 kuruş, canlı önizleme), ödeme durumu (bekliyor/kısmi/ödendi — kısmide ödenen tutar ayrı), vade. **Dekont/makbuz arşivi:** ortak `belgeIslemleri.ts` katmanı (F9'da da kullanılacak) ile PDF/görsel Blob olarak IndexedDB'ye yükleniyor, kalemle ilişkilendiriliyor, indirilebiliyor (25 MB sınır). Dosya detayı Finans sekmesi de tıklanabilir + kayıt ekleme bağlandı. Özet bu ay ₺184.500 tahsilat gösteriyor (referansla tutarlı). Tarayıcıda uçtan uca doğrulandı. |
| 12 | 2026-08-04 11:20 | **F7 — Görevler tamam.** Genel görev ekranı: görevler zamana göre gruplanıyor (gecikmiş/bugün/yarın/yaklaşan/vadesiz/tamamlanan) — avukatın ilk sorusu "neyi kaçırdım, bugün ne var", öncelik ikincil. Süzgeçler (açık/bana atanan/öncelikli/tamamlanan), 8 hazır şablon, kullanıcıya atama, oluştur/düzenle/sil. Not: "Bana atanan" varsayılan büro kullanıcısını (Ayşe Kaya) esas alıyor; tohumdaki görevler Mert/Elif'e atalı olduğu için bu süzgeç şu an boş — doğru davranış. **Döngü durumu:** kalıcı saatlik tetikleyici (`create_trigger`) onay kapısına takılıyor, kullanıcı onaylasa da geçmiyor (harness onay akışı sorunu). İlk oturumdaki 4 saatlik kalıcı Routine hâlâ çalışıyor (09:13 TR oturumunu o başlattı) — güvenilir taban bu; saatlik session-cron `e79b4057` container uyanıkken ek uyanış veriyor. Tarayıcıda uçtan uca doğrulandı. |
| 11 | 2026-08-04 10:20 | **F6 — Müvekkiller tamam.** Liste (isim/telefon/e-posta/etiket araması, açık dosya sayısı, bekleyen ödeme rozeti), profil (künye, hızlı iletişim tel:/mailto:, dosyalar, görüşme geçmişi satır içi ekleme), oluştur/düzenle formu. Kararlar: (1) "ödeme durumu" müvekkilin **tüm dosyalarındaki** bekleyen kalemlerin toplamı — avukat tek bakışta görsün; (2) bağlı dosyası olan müvekkil **silinmez, arşivlenir** (veri kaybını önlemek için), yalnızca hiç dosyası yoksa tam silinir. İkon setine telefon ve zarf eklendi. Cron `e79b4057` her saat :07'de (= TR :07, UTC+3 tam saat kaymalı) uyanıyor; sıradaki 11:07 TR. Tarayıcıda uçtan uca doğrulandı. |
| 10 | 2026-08-04 09:40 | **F5 — süre motoru tamam.** `domain/tatil.ts`: sabit resmî tatiller + 2024–2030 dinî bayram tablosu (Diyanet) + adli tatil. `domain/sureHesabi.ts`: 17 kurallı katalog ve hesaplama (tebliğ günü sayılmaz, hafta/ay/yıl, ay sonu sabitleme, tatil→ilk iş günü, adli tatil→7 Eylül). Arayüz: süre türü + tarih seç → son gün, ham son gün, gerekçeler, uyarı; dosyaya + takvime kaydediyor. **14 yeni test** (toplam 45): bilinen tarihler elle doğrulanıp sabitlendi, ayrıca tüm katalog×başlangıç kombinasyonlarında son günün her zaman çalışma günü olduğu taranıyor. Varsayımlar: (1) arefe ve 28 Ekim öğleden sonrası tam tatil sayılmadı — o günlerde süre "tatil saatinde" hâlâ dolabilir, kaydırma yönü hep ileri olduğu için kullanıcıya fazladan zaman göstermemek güvenli taraf; (2) dinî bayram tarihleri ±1 gün kayabilir, kapsam dışı yıllarda kullanıcı uyarılıyor. Tarayıcıda uçtan uca doğrulandı (istinaf 20 Tem → 7 Eylül; ödeme emri hafta sonu kaydırması). |
| 9 | 2026-08-04 09:30 | **F4 tamamlandı.** Dosya oluştur/düzenle/sil formu: dosya açarken müvekkil kaydı yoksa aynı ekranda "Yeni müvekkil ekle" ile oluşturuluyor (şirket eklerinden — A.Ş./Ltd./Şti. — tüzel kişi sezgisi). Dosya silmede bağlı tüm kayıtlar (olay, süre, görev, finans, belge, kişi, not) tek işlemde temizleniyor, yetim kayıt kalmıyor. Not ve ilgili kişi Notlar sekmesinde satır içi formla eklenip siliniyor. Tarayıcıda uçtan uca doğrulandı. Not: yerel önizleme base'siz derleme ister (`BASE_PATH` yalnızca Pages dağıtımında). |
| 8 | 2026-08-04 06:30 | **iOS ana ekran ikonu düzeltildi** (kullanıcı bildirdi: logo yerine "J" harfi çıkıyor). Sebep: `index.html`'de ikon yolları göreli (`./apple-touch-icon.png`) yazılmıştı; sayfa sondaki eğik çizgi olmadan (`…/MYLOVE`) açıldığında bu, alan adı köküne çözülüyor, dosya 404 dönüyor ve iOS uygulama adının ilk harfinden yedek ikon üretiyordu. Kök-mutlak yola (`/apple-touch-icon.png`) çevrildi; Vite bunu base ile `/MYLOVE/…` olarak yeniden yazıyor (derlemede doğrulandı). Ayrıca apple-touch-icon alfa kanalından arındırılıp düz RGB yapıldı (eski iOS saydamlık uyumu) ve `sizes="180x180"` eklendi. **Not: ana ekran ikonu ekleme anında sabitlenir; kullanıcının kısayolu silip yeniden eklemesi gerekiyor — sürüm yenileme düğmesi içeriği tazeler ama ikonu değiştirmez.** |
| 7 | 2026-08-04 06:25 | **Geçici sürüm yenileme düğmesi** eklendi (kullanıcı isteği). Üst çubukta derleme kimliğini gösteriyor; dokununca service worker'ı güncelletiyor, Cache Storage'ı boşaltıyor ve sorgu parametresiyle yeniden yüklüyor. Ana ekrana eklenmiş PWA'nın eski sürümde takılıp kalması silip yeniden ekleme gerektiriyordu, artık gerektirmiyor. Ayrıca workbox'a açıkça `skipWaiting` + `clientsClaim` eklendi — takılmanın asıl sebebi buydu. Cache Storage temizliği IndexedDB'ye dokunmuyor; tarayıcıda doğrulandı, 27 dosya yenileme sonrası yerinde. F16'ya silme maddesi eklendi. |
| 6 | 2026-08-04 06:15 | **F4 (okuma tarafı) tamam.** Dosya listesi: arama (başlık, müvekkil, esas no, mahkeme, karşı taraf), dört süzgeç, hazırlık yüzdesi, "sırada ne var" satırı; en yakın işi olan dosya üstte. Dosya detayı yedi sekme: Genel (künye + hazırlık kontrol listesi), Duruşmalar, Süreler, Görevler (tıklanabilir), Belgeler (indirilebilir), Finans (gelir/gider/bakiye/bekleyen), Notlar+İlgililer. Arama `toLocaleLowerCase('tr')` kullanıyor — "YILMAZ" ile "Yılmaz" eşleşiyor, varsayılan `toLowerCase` bunu kaçırırdı. Not: dolgu dosyalarına taban veri eklendiği için birçok dosya %100 çıkıyor; kontrol listesi "hazırlık", "risk" değil — yaklaşan süre ayrı gösteriliyor. |
| 5 | 2026-08-04 06:05 | **Pages yayında** (`https://emreakilli-cyber.github.io/MYLOVE/`) — depo sahibi ayarı açtıktan sonra dağıtım zinciri baştan sona geçti. Takvime olay ekleme/düzenleme/silme formu bitti: tür seçici, dosya bağlama, yerel tarih/saat seçicileri, tüm gün anahtarı, silme onayı. Varsayım: tarih ve saat için tarayıcının kendi seçicileri kullanıldı — iOS'ta yerel tekerlek arayüzü bizim yazacağımız her şeyden iyi; ABD biçimli tarih gösterimini dengelemek için altına Türkçe tarih ipucu kondu. Olay satırları artık düzenleme formuna gidiyor; süreler motorun ürettiği kayıt olduğu için elle düzenlenmiyor, dosyasına götürüyor. Uçtan uca tarayıcıda doğrulandı. |
| 4 | 2026-08-04 06:00 | **Dağıtım engeli bulundu ve F3 büyük ölçüde bitti.** Üç Pages çalışması da `configure-pages` adımında düşmüş; derleme ve testler her seferinde geçmiş, yalnızca yayın engellenmiş. Sebep: depoda Pages hiç açılmamış. `enablement: true` denendi, Actions token'ı Pages sitesi *oluşturma* yetkisine sahip değil ("Resource not accessible by integration") — bu ayarı yalnızca depo sahibi açabiliyor. Takvim ekranı: ay ızgarası, hafta şeridi (telefonda saat ızgarası okunmadığı için 7 günlük şerit tercih edildi), ajanda; olaylar ve hukuki süreler tek listede. |
| 3 | 2026-08-04 05:50 | **F2 tamam.** Genel bakış artık gerçek: hero, 2×2 istatistik, yaklaşan süreler, günün programı, görev listesi (tıklayınca tamamlanıyor ve hareket günlüğüne yazıyor), hazırlık durumu, son hareketler, güvenlik kartı. Videodaki dört rakam da veriden hesaplanıyor ve tutuyor. Ek olarak: Türkçe tarih/para yardımcıları (`toLocaleUpperCase('tr')` — "NISAN" değil "NİSAN"), ağırlıklı hazırlık motoru, canlı Dexie sorgu katmanı. Varsayım: hazırlık listesi en düşük yüzdeyi değil, **yakında işi olan** dosyalar arasında en düşüğü öne alıyor; duruşması olmayan bir dosyanın eksiği acil değil. Dolgu dosyalarına vekâletname + ödenmiş harç eklendi, yoksa sıralamayı yapay olarak dolduruyorlardı. 31 test yeşil. |
| 2 | 2026-08-04 02:40 | **F1 (repository hariç) tamam.** 13 tablo, alan modeli, Dexie şeması, tohum verisi ve JSON yedekleme. Varsayım: tohum tarihleri sabit değil **bugüne göre** kuruluyor — sabit tarihler bir hafta sonra ölü demoya döner. Tohum, videodaki rakamları gerçekten üretiyor (24 aktif dosya, bu hafta 8 duruşma / 2'si bugün, 17 bekleyen görev / 4 öncelikli, bu ay 184.500 ₺ / +%12); bu sayılar teste bağlandı. Para birimi kuruş tam sayısı, süreler gün (`IsoDate`) olarak saklanıyor. Tarayıcıda tohumlama doğrulandı, tekrar açılışta veri ezilmiyor. |
